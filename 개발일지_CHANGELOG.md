# 마사마사 개발일지 (CHANGELOG)

- **목적**: 박사 논문 '교육 모형 개발' 연구의 개발 과정 기록. 수업 적용(테스트)에서 얻은 요구를 어떤 버전에서 무엇으로 보완했는지 추적한다.
- **유지 규칙**: 코드를 수정할 때마다 ① `main.py`·`templates/index.html`의 `APP_VERSION`(날짜 병기)을 올리고 ② 이 문서에 항목을 추가한다. 화면 좌측 상태줄에서 현재 배포 버전을 확인할 수 있다.
- **설계 근거**: 논문 제3장의 설계 결정(특히 〈표 33〉·〈표 34〉), 챗봇 코드, 이 개발일지 — 이 셋뿐이다.

---

## v148 — 2026-08-23 · 첫 화면이 없는 단추를 그리고 있었다

### ⓐ 온보딩 ③장의 「말투」가 진짜 화면과 달랐다

첫 진입 안내 셋째 장은 **주제 대화 설정창의 축소판**이다. 그런데 「말투」 칸에
**슬라이더 하나**를 그려 놓고 아래에 「존댓말」이라고 적어 두었다.

실제로는 다르다. 그 칸에는 **페이더가 둘** 들어간다 —

```
   말투
     낯선 사이  ──────●──  절친        ← 친밀도
     하급자    ────●────  상급자       ← 지위
     → 존댓말                          ← 이 둘에서 저절로 정해진다
```

말투는 **고르는 것이 아니라 정해지는 것**이다. 그런데 첫 화면이
「말투를 고르는 슬라이더가 하나 있다」고 가르치고 있었으니,
학습자는 들어가서 **없는 단추를 찾게** 된다.

첫 화면이 거짓말을 하면 그 뒤 화면이 아무리 맞아도 헤맨다.

양 끝 이름(「낯선 사이」·「절친」·「하급자」·「상급자」)은 실제 화면에서
**그대로 가져다 쓴다**. 두 벌로 적어 두면 또 어긋난다.

### ⓑ 「말투(말투)가 자동으로 정해져요」

설정창 아래 안내에 겹말이 있었다. 「화계」를 「말투」로 바꾸면서
괄호 안까지 함께 바뀐 자국이다.

```
전  친밀도·지위 페이더로 말투(말투)가 자동으로 정해져요
후  친밀도와 지위를 정하면 말투가 저절로 정해져요
```

「페이더」도 뺐다 — 학습자가 아는 말이 아니다.

---
## v147 — 2026-08-23 · 원문이 번역체면 열여덟 언어가 다 번역체가 된다

저자 지적 셋. 「호아랑은 왼쪽 정렬, 사용자는 오른쪽 정렬 / **번역이 상해!! 번역체야.
그 틈에 대화가 식어? 식긴 뭘 식어. 두 글자? 표현이겠지.** / 혹시나 해서 에스파뇰로
하고 봤더니 웬 영어랑 같이 나오는데?」

### ⓐ ★★ 학습 화면 문구가 **두 벌뿐**이었다

`IDC_TXT` 에 `ko` 와 `en` 만 있었다. `idcT()` 가 없는 언어를 영어로 떨어뜨리므로
**스페인어·몽골어로 봐도 학습 화면 전체가 영어였다.**

```
Now you take the turn 2/2   Hold to talk   All done!   Listen again   ← Back
```

v146에서 요소 이름에 모국어를 병기해 놓고도 정작 **틀 자체가 영어**였던 것이다.
열여섯 언어를 채웠다(34개 열쇠 × 16). `VP_TXT`(목소리 고르기)도 일곱 벌뿐이라
열한 언어를 마저 채웠다.

**표 하나만 빠져도 화면 전체가 영어가 된다.** 그래서 검사는 개수가 아니라
**빠진 언어가 있는가**를 잰다 — 여섯 표(`IDC_TXT`·`VP_TXT`·`STT_MSG`·`NZ_NOW`·
`HOME_IDC`·`MK_MSG`)를 `data-lang` 단추 목록과 대조하고, 열쇠 수도 한국어와 견준다.

### ⓑ 대화문 줄을 좌우로

호아랑은 왼쪽, 학습자는 오른쪽. 둘 다 가운데면 누가 말하는지 한 박자 늦게 안다.
너비를 88%로 잡아 **남는 쪽이 눈에 보이게** 했다 — 그 빈자리가 곧 「누가」다.

### ⓒ ★★ 번역이 상한 게 아니라 **한국어 원문이 상해 있었다**

번역이 이상하다는 지적이었지만, 원문을 보니 원문이 이상했다.
**원문이 번역체면 열여덟 언어가 다 번역체가 된다.**

| 있던 말 | 무엇이 틀렸나 | 고친 말 |
|---|---|---|
| 그 틈에 대화가 **식어** | 사람이 안 쓰는 비유 | 그러다 **둘 다 아무 말도 안 하게 돼** |
| **두 글자**로 차례가 넘어가 | 「너는?」은 세 글자, 「어떻게 생각해?」는 일곱 글자 | **짧은 말 하나로** 차례가 넘어가 |
| 지금 이 자리에서 **서로 무엇인가를** 봐요 | 무엇을 본다는 건지 말이 안 됨 | 서로 **어떤 사이인지를** 봐요 |

다른 편들은 「말하던 친구는 「그만하라는 건가」 싶어서 이야기를 접어」처럼
**무슨 일이 벌어지는지**를 그대로 적고 있었다. turn-3 만 비유로 도망갔다.

### ⓓ ★★★ 파 보니 더 큰 것 — **마흔 편 중 열다섯이 화계를 섞고 있었다**

한 항목 안에서 「~해요」로 시작해 「~합니다」로 끝난다.

```
move-1 (해요체 편)   … 부탁하는 꼴로 말해요. 「~ 좀 주실 수 있어요?」 그러면 한 번에 통합니다.
turn-2 (합쇼체 편)   … 조용히 있으면 차례는 그냥 넘어갑니다. … 기다리면 늦어요.
```

**화계를 가르치는 화면이 정작 자기 화계를 안 지키고 있었다.**

설명 글의 화계는 그 편의 `tier` 를 따른다 — 학습자가 **곧 쓸 말투로 듣게** 하려는
설계다. 그러니 한 항목 안에서는 끝까지 한 화계여야 한다. 열다섯 편을 고르게 폈다.

| tier | 편 수 | 설명 글의 화계 |
|---|---|---|
| formal | 9편 | 합쇼체 |
| polite | 6편 | 해요체 |

`check.py` 에 ⑮ 를 넣었다 — ㄱ) 한 항목 안에서 화계가 하나인가
ㄴ) 그것이 그 편의 `tier` 와 같은가. **두 번째가 더 중요하다** — 고르기만 하고
엉뚱한 화계로 고르면 학습자가 딴 말투를 배운다.

※ `turn-3` 을 고치다 「한마디」가 1~3급 밖이라 `lvcheck` 에 걸렸다 → 「짧은 말 하나」로.
  고치는 김에 새 낱말을 들이면 안 된다.

### 검사

`nativetest.js` 에 ⑥⑦ 을 더해 31가지. 대화문 검사에 ⑮ 를 더했다.
검사 **40 (JS) · 4 (Python) · 8 (대화문)** 모두 통과.

### 고친 파일

```
app.html            IDC_TXT 16개 언어 · VP_TXT 11개 언어 · .sc-line 좌우 정렬
idc_corpus/         설명 글 17편 (화계 15 + 어색한 말 2)
IDC대화문/           같은 17편 (작업본도 함께 — 한쪽만 고치면 다음 판에 되돌아온다)
IDC대화문/check.py   ⑮ 설명 글의 화계
nativetest.js       ⑥ 문구 표 · ⑦ 좌우 정렬
```

---

## v146 — 2026-08-23 · 모국어가 끝까지 따라가게 · 대화문이 느렸던 진짜 까닭

몽골어로 바꾸고 「한국어 대화를 잘 하는 방법」에 들어가 보니, 제목만 번역되고
**요소 이름·대화문·단계 이름은 한국어로 남아 있었다.** 무엇을 배우는 자리인지
골라서 들어가 보기 전에는 알 수 없다.

### ⓐ 요소 이름에 모국어 병기

한국어 이름을 지우는 것이 아니다 — **그것도 배울 말이다.** 아래에 얇은 글씨로 붙인다.

```
말 주고받기
Steering the give-and-take        ← 11.5px · weight 400
```

자리는 이미 있었다. v143 에서 논문 용어(「대화이동 관리」)를 걷어낸 그 자리
(`.idc-c2`)다. 번역도 이미 있었다 — `idc_move` … `idc_nonverbal` 아홉 열쇠가
18개 언어로 다 들어 있었는데 **결과 화면에서만 쓰고 학습 화면에서는 안 썼다.**
한국어로 보는 학습자에게는 안 붙인다 — 같은 말이 두 줄이 된다.

### ⓑ 학습 대화문을 주제 대화와 **같은 물건**으로

같은 대화문인데 화면마다 다르게 생겼다. 무엇보다 학습 화면에는
**모국어 번역이 아예 없었다** — 주제 대화에서는 줄을 누르면 번역이 펼쳐지는데
학습 화면에서는 소리만 다시 났다.

| | 가져온 것 | 안 가져온 것 |
|---|---|---|
| 꼴 | `.sc-line`·`.sc-who`·`.sc-text`·`.sc-native` | 기능단계 구분선 |
| 짓 | 눌러서 번역 펼치기 · 그 줄만 다시 듣기 | 장소·목적 머리줄 |

학습 화면에는 과업이 없으므로 단계·장소는 안 가져온다.
호아랑 얼굴(v138)과 정답 줄 강조(`lit`)는 그대로 둔다.
「② 추측」 걸음의 한 줄도 같은 꼴로 바꿨다 — 거기만 다르면 「방금 그 줄」로 안 읽힌다.

서버는 줄마다 `native` 를 **이미 실어 보내고 있었다.** 화면이 안 쓰고 있었을 뿐이다.
기능 단계 이름(`stages[].native`)도 마찬가지였다.

**두 판짜기가 싸우지 않게** 옛 `.idl-line` 규칙을 `.idl-line:not(.sc-line)` 으로 좁혔다.

### ⓒ ★★ 대화문이 느렸던 진짜 까닭 — **두 번 기다리고 있었다**

「발화 사이가 4초쯤 걸린다, 한국인 선생님들이 너무 느리다고 한다」

절반으로 줄이려고 값을 찾다가 원인을 알았다.

```js
await ttsPlay(line.text, voice);                    // ← 소리가 다 끝날 때까지 기다린다
return Math.min(7000, 900 + line.text.length * 165); // ← 그 소리의 길이만큼 또 기다린다
```

`playPcm24` 가 `onended` 로 끝을 알려 주므로 `ttsPlay` 는 **이미 재생이 끝난 뒤에**
돌아온다. 그런데 그 뒤에 글자 수로 어림잡은 값을 한 번 더 기다렸다.
**한 줄을 두 번 재생할 만큼 쉰 셈이다.**

글자 수 어림잡기는 원래 내장 음성(끝을 안 알려 준다)용 장치였는데,
클라우드 음성으로 옮긴 뒤에도 남아 있었다.

```
줄 사이 틈   900 + 글자수 × 165 (≈ 2~4초)  →  650ms 고정
```

사람의 말차례 사이 틈은 보통 0.2~0.5초다. 배우는 자리라 조금 여유를 두어 0.65초.
**「절반으로」보다 더 줄었다** — 절반도 여전히 두 번 기다리는 값이기 때문이다.
학습 화면과 주제 대화가 **같은 값**(`SC_GAP`)을 쓴다. 한쪽만 고치면 또 어긋난다.

### ⓓ ★★ 추천 발화가 배역을 헷갈렸다 — 맥락·정체성이 프롬프트에 없었다

호아랑: 「나모니님은 더운 날씨를 좋아하십니까?」
추천 발화: 「…그러니까 **나모니님은** 더운 날씨를 좋아하냐는 말이죠?」

**학습자가 자기 이름을 3인칭으로 되받았다.** 방향이 뒤집혔다.

까닭을 찾아보니 자유 대화 쪽 도움말 프롬프트에 **누가 누구인지가 한 줄도 없었다.**

```python
head = ("한국어 학습자가 챗봇과 자유 대화 중이다. 잠시 막혀서 도움을 요청했다.\n"
        f"- 지금 이 학습자에게 필요한 상호작용: {focus}")
```

주제 대화 쪽에는 「학습자 역할 / 상대 역할」이 있는데 자유 대화에는 없다.
그러니 대화록에 「나모니님」이 나오면 모델이 그것을 **제3의 인물**로 읽는다.

**맥락·정체성 인식은 〈표 33〉의 기반 층위다. 그것을 재는 챗봇이
정작 자기 프롬프트에서 그걸 빠뜨리고 있었다.**

고친 것 둘.

**ㄱ) 「누가 말하는가」 칸을 새로 박았다** (두 모드 모두)

```
[★★ 누가 말하는가 — 어기면 말이 뒤집힌다]
- 지금 말할 사람은 학습자다. 아래 제안은 전부 학습자의 입에서 나올 말이다.
- 학습자의 이름은 「나모니」다. 대화록에 「나모니님」이 나오면 그것은
  상대가 학습자를 부른 것이지 제3의 인물이 아니다.
  ★ 학습자가 자기 이름을 부르면 안 된다.
- 상대(호아랑)를 가리킬 때만 2인칭·호칭을 쓴다. 자신은 「저」·「제가」로.
- 상대가 학습자에게 물은 것을 학습자가 도로 학습자에게 묻는 꼴이 되면 안 된다.
  되물으려면 방향을 뒤집어라 — 「저는 ~인데, 호아랑은 어떠세요?」처럼.
```

**ㄴ) 대화록 이름표에 누구인지를 함께 적는다**

```
학습자:  →  학습자(나모니):
상대:    →  상대(호아랑):        (주제 대화면 상대(카페 알바생))
```

이름표가 「학습자」뿐이면 본문에 나오는 이름과 이어 붙일 근거가 없다.

### 검사

`nativetest.js` 를 새로 만들었다 — 언어를 바꾸면 한국어만 남는 자리가 없는지
(요소 카드 · 대화문 · 단계 이름), 줄 사이 틈이 한 곳에서 정해지는지,
**글자 수로 또 기다리는 자리가 남아 있지 않은지** 21가지.

검사 **40 (JS) · 4 (Python) · 8 (대화문)** 모두 통과.

### 고친 파일

```
main.py    도움말 프롬프트에 「누가 말하는가」 칸 · 대화록 이름표에 이름
app.html   요소 카드 모국어 병기 · idlScript 를 .sc-line 꼴로 · 단계 이름 병기
           · SC_GAP(650ms) 로 줄 사이 틈 일원화 · .idl-line 규칙 좁힘
nativetest.js  새 검사 21가지
```

---

## v145 — 2026-08-23 · 「안 들림」을 우리말로, 그리고 애초에 덜 나오게

교실 화면 캡처에 **`<noise>`** 가 크게 찍혀 있었다. 논문 4장 그림 8로 쓸 화면이다.
표식을 우리말로 바꾸는 것은 십 분이면 되지만, **왜 그렇게 자주 나오는가**를
같이 보지 않으면 화면만 예뻐지고 학습자의 경험은 그대로다.

### ⓐ 표식을 「(안 들림)」으로 — 한 자리에서만 거른다

Live API 는 소리를 말로 못 알아들으면 `<noise>` 같은 표식을 전사에 그대로 섞어 보낸다.

**어디서 거를 것인가**가 이 손질의 전부였다. 말풍선·전사 파일·교실 화면·총평이
저마다 그 글을 쓴다. 화면마다 따로 고치면 **또 어긋난다** — v143→v144 에서
홈 라벨이 표 하나만 고쳐져 안 바뀌었던 것과 같은 종류다.
그래서 **웹소켓에서 전사를 받는 그 한 줄**에서 거른다. 그 아래는 저절로 따라온다.

```python
shown, stt_hold = _stt_feed(stt_hold, sc.input_transcription.text)
if shown:
    add_frag("user", shown)                      # 전사·교실·총평이 여기서 나온다
    await websocket.send_text(... "text": shown) # 말풍선도 같은 글
```

**★ 까다로운 점 — 전사는 조각으로 흘러온다.**
`<noise>` 가 `<no` + `ise>` 로 쪼개져 도착하면 조각마다 정규식을 걸어서는 영영 못 잡는다.
표식이 될 수 있는 꼬리(`<`, `<no`, `<nois`…)는 **붙들어 두었다가** 다음 조각과
이어 붙여 다시 본다. 끝내 안 닫히면(진짜 부등호였다면) 차례가 끝날 때 흘려보낸다.

`stttest.py` 를 새로 만들어 **일부러 한 글자씩 쪼개** 넣어 본다 — 16가지.

### ⓑ ★ 정작 중요한 것 — 안 들린 소리를 **애초에 안 올린다**

「STT 는 어쩔 수 없는가」라는 물음에 대한 답. **절반은 어쩔 수 있다.**

`<noise>` 는 「인식을 못 했다」가 아니라 **「말이 없는 소리를 받았다」**는 뜻이다.
지금까지는 버튼을 누른 순간부터 뗄 때까지 **전부** 올려 보냈다. 그래서

| | 무슨 일 | 결과 |
|---|---|---|
| ㄱ | 버튼을 잘못 눌렀다 뗀다 | 방 소리만 올라간다 |
| ㄴ | 누르고 나서 한참 뒤에 말을 시작한다 | 앞의 침묵이 통째로 올라간다 |
| ㄷ | 소리가 너무 작다 | 올려도 알아들을 수가 없다 |

**고친 것** — `activity_start`(차례 시작 신호)를 **버튼을 누른 순간이 아니라
말이 실제로 들어온 순간**에 보낸다.

```
누름 → [소리를 재며 들고만 있음] → 문턱을 두 조각 넘김 → activity_start
                                                    → 들고 있던 것부터 흘려보냄
누름 → [끝까지 문턱 못 넘김]    → 뗌 → 아무것도 안 보냄 (차례가 없던 일이 된다)
```

Gemini 가 그 누름을 아예 모르므로 `<noise>` 도 **안 온다.**
앞머리(ㄴ)도 같은 장치로 잘린다 — 말 시작 직전 소리만 남기고 그 앞은 버린다.

ㄷ은 막을 수 없다. 대신 **그 자리에서 일러 준다**. 「안 들림」을 되받는 것보다
학습자가 무엇을 해야 하는지 아는 편이 낫다. 18개 언어로 두 가지를 갈라 말한다.

```
소리가 아예 없음   「소리가 안 들어왔어요. 꾹 누른 채로 말해 보세요.」
소리가 너무 작음   「소리가 너무 작아요. 조금 더 크게 말해 주세요.」
```

**문턱은 낮게 잡았다** (RMS 0.030 · 잇달아 두 조각 ≈ 256ms).
조용히 말하는 학습자를 잘라먹는 것이 「안 들림」이 한 번 더 뜨는 것보다 훨씬 나쁘다.

### ⓒ 조용히 지우지 않는다 — `/version` 에 `stt` 칸

```json
"stt": { "junk": 3, "blank": 1, "last": "안녕하세요 (안 들림) 반갑" }
```

`junk` 가 계속 오르면 마이크·환경 쪽을 봐야 한다는 신호다.
거른 것을 세지 않으면 「요즘은 잘 되네」라고 착각하게 된다.

### ⓓ 넛지 — 「조건 걸고 협상해 보세요!」 를 바꿨다

「협상」이 〈2017 국제 통용 한국어 표준 교육과정〉 1~3급 목록에 **없다.**
학습자에게 무엇을 하라고 이르는 말이 정작 학습자가 모르는 낱말이었다.

```
조건 걸고 협상해 보세요!  →  조건을 걸어 보세요!
```

「조건」은 3급이라 남긴다 — 이 넛지가 가리키는 개념 자체이기도 하다.
18개 언어를 다 고쳤다. **한 표만 고치면 또 어긋난다**(v144 에서 겪은 그것).

### ⓔ 넛지마다 호아랑의 얼굴이 달라진다

빼꼼 나와 「거절해 보세요!」라고 하면서 활짝 웃고 있으면 **말과 얼굴이 어긋난다.**
넛지가 부르는 감정대로 표정을 고른다. 학습자는 글자를 다 읽기 전에 얼굴에서 먼저 안다.

| 표정 | 언제 | 넛지 |
|---|---|---|
| 곤란 | 난처한 자리 | 거절·조건 걸기·다시 부탁·다른 방법 |
| 당황 | 못 알아들은 자리 | 다시·천천히·쉽게·스스로 고치기·돌려 말하기·모국어·뜸 들이기 |
| 신남 | 판을 여는 자리 | 새 화제·이야기 얹기·화제 옮기기 |
| 큭큭 | 마음을 받는 자리 | 공감·되묻기·따라 말하기·바꿔 확인하기 |
| 인사 | 말을 트는 자리 | 먼저 말 걸기 |
| 황당 | 되받는 자리 | 되받아 묻기 |

「호아랑 캐릭터」 폴더의 여섯 장을 `static/` 에 넣었다
(5MB 원본 → 900×900 · 50~120KB).

**「황당」은 색이 안 입혀진 펜 선그림이다. 그대로 쓴다.**
처음에는 화풍이 어긋난다고 뺐는데, 한지색(`#F4F0E6`) 바탕 위에 실제 크기
(118px)로 얹어 보니 선이 또렷하게 읽힌다. 되받아 묻는 자리에는 오히려
이 헐렁한 맛이 맞는다. **선 하나 안 건드리고** 자리만 잡아 넣었다
(이미 누끼가 된 그림이라 알파를 그대로 쓰고, 선그림이라 팔레트를
64색으로 줄여 301KB → 47KB).

### ⓕ ★★ 점수가 두 벌이었다 — 한 벌로 합쳤다

대화가 끝나면 숫자가 **두 개** 떴다. 위 카드에 하나, 아래 요소 프로파일 머리줄에 하나.

**까닭은 역사다.** v59 이전에는 IDC 가 챗봇에 들어오기 전이라
**기능 단계 진행률로만** 점수를 매겼다. 그 뒤 IDC 여덟 요소가 들어오면서
점수가 하나 더 생겼는데, **옛 점수를 걷어내지 않았다.**

그런데 `IDC_ELEMENTS` 의 **첫 칸이 `stage`**(기능 단계의 조직과 흐름)다.
기능 단계는 이미 IDC 여덟 요소 안에 있다. **같은 것을 두 번 세어
두 자리에 보이고 있었던 셈이다.**

눈금도 서로 달랐다.

| | 무엇 | 범위 |
|---|---|---|
| 기능단계 진행률 | 과업 달성도 | **100 을 넘을 수 있다** (퀘스트가 150·200을 묻는다) |
| IDC 총점 | 능력 프로파일 | 100점 만점 |

둘 다 「점」이라 부르니 학습자는 서로 다른 두 점수로 읽었다.

**고친 것 — 점수는 IDC 총점 하나다.**

```
결과 카드 큰 숫자   기능단계 진행률  →  IDC 총점 (100점 만점)
프로파일 머리줄     총점을 또 찍음   →  관찰된 요소 수 「5 / 8」
기능 단계          점수            →  단계 목록(✓/—) · 진행률 막대
퀘스트 q150·q200   「150점 만들기」  →  「진행률 150% 넘기기」 (18개 언어)
```

「점」이라는 말은 이제 **IDC 총점에만** 쓴다.
기록·전사·서버에 남기는 `score` 도 IDC 총점으로 바꿨다 — 화면과 자료가
어긋나면 논문에서 어느 쪽을 써야 할지 알 수 없게 된다.
과업 달성도는 `stageScore` 로 따로 남긴다(눈금이 다르니 버릴 이유는 없다).

**자유 대화는 결과 카드가 없다.** 거기서는 머리줄이 총점을 보이는 유일한 자리라
`renderIdcProfile(..., showTotal)` 로 갈랐다.

`scoretest.js` 를 새로 만들어 셋을 지킨다 — ① 큰 숫자가 IDC 총점인가
② 한 화면에 총점이 두 번 뜨지 않는가 ③ 「점」을 진행률에 쓰지 않는가.
**서버 쪽 근거도 함께 잰다** — `stage` 가 IDC 요소에서 빠지면 이 손질의 전제가
무너지므로, 그때는 검사가 깨져서 알려 준다.

### ⓖ 검사가 또 거짓말할 뻔한 자리 둘

두 검사가 **옳은 변경인데도 깨졌다.** 둘 다 「개수·글자」를 재고 있었다.

| 검사 | 무엇을 재고 있었나 | 무엇으로 바꿨나 |
|---|---|---|
| `questtest.js` | 파일 전체에서 `qRefuse:"` 를 세어 18인가 | **문구 표 안에서만** 센다 |
| `qa_all.py` | `hamPeekHtml(title, items, ms, cls)` 글자 그대로 | **네 번째 자리가 cls 인가** |

표정 표가 `qRefuse:"trouble"` 이라는 같은 열쇠를 쓰자 19가 되어 깨진 것이다.
**어디서 세는지가 무엇을 세는지만큼 중요하다.**

새 검사도 넷 달았다 — 쓰는 표정마다 그림 자리가 있는가 · 표정을 단 넛지가
서버 목록에 있는가 · 그림 파일이 실제로 있는가.

### 고친 파일

```
main.py        _stt_clean·_stt_feed·_stt_blank 신설 · 웹소켓 전사 · /class-log · /version
app.html       침묵 게이트(sendPcmChunk·startTalking·stopTalking) · STT_MSG 18언어
               · NZ_FACE 표정 표 · hamPeekHtml 다섯째 인자 · qCond 18언어
               · 점수 한 벌로(rcScoreVal·renderIdcProfile·저장·전사·q150·q200)
static/        ham_trouble·ham_fluster·ham_excited·ham_greet·ham_giggle·ham_absurd (새 그림 6장)
stttest.py     새 검사 — 쪼개져 오는 표식을 잡는가 (16가지)
scoretest.js   새 검사 — 점수가 하나인가 (13가지)
questtest.js   문구 세는 자리를 좁힘 + 표정 검사 4가지
qa_all.py      서명 대조를 성질 검사로
```

검사 **39 (JS) · 4 (Python) · 8 (대화문)** 모두 통과.

---

## v144 — 2026-08-20 · 홈 라벨을 마저 고치고, 교실 화면에 호아랑을

### ⓐ ★★ v143 에서 홈 카드를 **못 고쳤다** — 표가 하나 더 있었다

배포하고 화면을 보니 홈에 여전히 「한국어 대화 상호작용 능력」이 떠 있었다.
`IDC_TXT.home` 은 고쳤는데, 화면에 뜨는 것은 **다른 표**였다.

```js
/* 홈 카드 두 줄 — 이 함수가 파일 앞쪽에서 돌므로 표도 여기 둔다(v131).
   뒤쪽 IDC_TXT 를 부르면 아직 만들어지지 않아 ReferenceError 가 난다. */
const HOME_IDC = { ko: ["한국어 대화 상호작용 능력", …], … };
```

같은 말이 **두 표에 나뉘어** 있었고, 심지어 글자도 달랐다
(`한국어 상호작용 대화 능력` vs `한국어 대화 상호작용 능력`).
그래서 하나를 고쳐도 화면은 안 바뀌었다.

18개 언어를 다 갈아 끼웠다. 그리고 `classtest.js` 가 이제 **표 셋을 다 훑는다**
(`I18N` · `IDC_TXT` · `HOME_IDC`). 하나만 보면 또 놓친다.

### ⓑ 온보딩을 두 번까지만

온보딩(6장)은 v131 부터 있었다. 그런데 `introShown` 이 그냥 변수여서
**화면을 새로 고칠 때마다 처음부터 다시** 떴다.

```js
const OB_KEY = "obSeen";        // 기기에 세어 둔다
if (introShown || obSeenCount() >= 2) { showEarphonePopup(); return; }
```

처음 쓸 때는 안내가 있어야 하지만 세 번째부터는 **길을 막는 벽**이 된다.
두 번인 까닭 — 첫날은 정신없이 넘기고 둘째 날에야 읽는 학습자가 있다.

※ 이 화면은 **교사 안내와 함께** 도는 것을 전제로 한다(3.4 / 4.3.3).
  혼자 쓰는 도구로 세운 것이 아니다.

### ⓒ 교실 화면에 호아랑

```
   전 :  호아랑  ▌어서 오세요.
   후 :  (얼굴)  ▌어서 오세요.
```

「호아랑」 석 자가 줄마다 붙으면 읽을 것이 늘고, 누가 말하는지는 그림이 훨씬 빨리 알려 준다
— 학습 화면에서 v138 에 이미 한 일이다. 교실 화면에도 같게 맞췄다.

- 호아랑 말줄 왼쪽에 얼굴(`ham_idle`)
- 머리에 앱 아이콘(`hamster`)
- 아직 아무도 안 고른 화면에 큰 호아랑(`qz_hello`)
- 목록이 비었을 때 수줍은 호아랑(`ham_shy`)

네 그림 모두 이미 앱에서 쓰던 것이라 따로 올릴 것이 없다.

---

## v143 — 2026-08-20 · 교실 화면을 목록형으로 · 학술 용어를 화면에서 걷다

### ⓐ ★★ v142 의 「교실에 띄우기」 단추는 **처음부터 안 보이는 물건**이었다

「코드도 안 뜨더만」 — 맞는 말이었다. 단추 자체가 안 보였다.

```css
.export-btn { display: none !important; }                            ← 전역
#histExportBtn:not(.hidden) { display: inline-block !important; }    ← 이것만 예외
```

새 단추도 `.export-btn` 을 달았으니 태어날 때부터 죽어 있었다.
코드는 멀쩡했고 검사도 초록불이었다 — `classList.contains("hidden")` 만 봤기 때문이다.
**「숨김 클래스가 없다」와 「눈에 보인다」는 다른 말이다.** jsdom 은 CSS 를 계산하지 않으므로
그 차이를 영영 못 본다.

파 보니 `.export-btn` 을 단 단추 **열한 개 중 열 개가 안 보이고** 있었다.
오래된 상태라 지금 살리면 검증 안 된 단추가 열 개 튀어나온다. 그래서 **뜻만 코드에 못 박았다.**

```css
#exportBtn, #inappCopyBtn, #consentCancelBtn, … #appDlCloseBtn { display: none !important; }
```

숨는 단추는 그대로다. 달라진 것은 **클래스가 아니라 이름으로** 죽인다는 것뿐이다.
이제 `.export-btn` 은 그냥 생김새 클래스이므로, **새 단추는 달아도 보인다.**

`visibletest.js` 를 새로 만들었다. CSS 를 읽어
「전역으로 죽은 클래스를 달았는데 · JS 는 보이려고 `.hidden` 을 떼는데 · 되살리는 규칙이 없는」
자리를 찾는다. v142 의 사고가 정확히 그 꼴이었다.

### ⓑ 교실 화면 — 코드 하나하나가 아니라 그날 목록에서 고른다

```
   v142        학습자가 대목 고름 → 네 글자 코드 → 교사가 하나하나 입력
   v143        대화가 끝나면 저절로 올라감 → 교사 화면 왼쪽에 그날 반 전체
```

교실에서는 코드를 받아 적고 넣고 있을 틈이 없다. 그리고 학습자가 눌러 줘야만 볼 수 있었다.
교사가 필요한 것은 **그날 반 전체가 한 화면에 뜨는 것**이다. 거기서 골라 보면 된다.

- 왼쪽에 이름·시각·줄수, 누르면 오른쪽에 대화문이 크게
- 20초마다 저절로 새로 고침 — 수업 중 새로 끝난 대화가 알아서 올라온다
- 글자 크기 `+`/`−`, 고른 크기는 그 기기에 남는다
- 하루치만 · 메모리에만 · 파일로 안 남김(대화 기록은 이미 드라이브에 따로 쌓인다)

**여전히 대화문만 띄운다.** 점수도 자가 점검도 총평도 안 띄운다 —
판단을 먼저 보이면 반 전체가 그 판단을 따라간다.

### ⓒ 학술 용어를 학습자 화면에서 걷었다 (지도교수 지적)

> 「'상호작용'이라는 건 굉장히 학술적 용어야. 이걸 그대로 노출시켜도 되냐?」

맞는 말이다. 학습자는 **상호작용을 배우러 온 것이 아니라 말을 배우러** 왔다.
작게 붙여 두었어도 **읽힌다.** 읽히면 물어보게 되고, 물어보면 그때부터 그 말을 설명해야 한다 —
학습자가 배울 것이 하나 늘어난다.

| 자리 | 전 | 후 |
|---|---|---|
| 홈 카드 | 한국어 **상호작용 대화 능력** | 한국어 대화를 잘 하는 방법 |
| 결과 ④ 제목 | **상호작용 대화 능력** | 대화를 잘했나 보기 |
| 도장판 | 🏅 **상호작용** 도장판 | 🏅 대화 도장판 |
| 도장판 칸 | 기능 단계 · 화제 관리 · 대화이동 · 차례 관리 | 대화 단계 · 이야깃거리 · 주고받기 · 말 차례 |
| 요소 카드 | 말 주고받기 <sub>**대화이동 관리**</sub> | 말 주고받기 |
| 학습 창 머리 | 막혔을 때 풀기 <sub>**의사소통 단절 수정**</sub> | 막혔을 때 풀기 |
| 자가 점검 줄 | 말 주고받기 <sub>**대화이동 관리**</sub> | 말 주고받기 |
| 비언어 안내 | **비언어적 행위**(시선·표정·고개 끄덕임) | 몸짓·표정·눈 맞추기 |

논문 용어는 **교사 화면과 전사 파일**에만 남는다. 전사에는 그대로 논문 용어로 찍힌다 —
거기가 연구 자료다.

※ 이 설계는 **교사 안내를 전제로 한다**. 순수 자가학습 도구로서의 검증은 이 논문의 범위 밖이다
  (지도교수 확인). 4.3.3 과 6.3 에 그 취지를 적을 것.

### ⓓ ★★ 요소 목록 화면의 제목이 **한 번도 채워진 적이 없었다**

```html
<span class="idc-h-title" id="idcTitleEl">   ← 결과 화면 ④ (1963줄)
<h2 id="idcTitleEl">                          ← 요소 목록  (2212줄)
```

**아이디가 겹쳐 있었다.** `getElementById` 는 앞엣것을 집으므로, 요소 목록 화면의
제목과 부제는 영영 빈칸이었다 — ✕ 아래로 카드만 덩그러니 떠 있었다.
스크린샷에 그대로 찍혀 있었는데 못 알아봤다.

이름을 갈랐다(`idcListTitleEl` · `idcListSubEl`). `classtest.js` 가 문서 전체의
아이디 겹침을 본다.

### 새 검사

| | 무엇을 보나 |
|---|---|
| `visibletest.js` | CSS 를 읽어 「보이려고 하는데 안 보이는 단추」를 찾는다 · 일부러 숨긴 열 개 목록이 그대로인가 |
| `classtest.js` | 대화가 끝나면 저절로 올라가는가 · 학술 용어가 화면 말 사전에 남았는가 · 아이디가 겹치는가 · 교사 화면을 `main.py` 에서 꺼내 눌러 본다 |
| `sharesrv.py` | 하루 지난 것을 치우는가 · 45명이 한꺼번에 끝내도 되는가 · 교사 화면에 판단이 안 뜨는가 |

`sharesrv.py` 와 `idcfulltest.js` 가 각각 한 번씩 **거짓 빨간불**을 냈다.
하나는 설명글(docstring)의 「점수도 … 안 띄운다」를 잡았고,
하나는 CSS 한 줄을 글자 그대로 찾다가 그 줄이 없어지자 터졌다.
둘 다 **글자가 아니라 뜻을 보게** 고쳤다.

---

## v142 — 2026-08-20 · 교실에 띄우기 — 파일이 아니라 화면으로

### ⓐ txt 를 카톡으로 보내는 것은 물건과 쓰임이 안 맞았다

지금까지 대화 기록을 교실로 가져가는 길은 **txt 내보내기**뿐이었다.
그런데 그 파일은 **연구자용**이다 — 머리말·친밀도·지위·점수·퀘스트·IDC 판정·총평이 다 붙는다.
학습자가 그것을 카톡으로 주고받는 그림은 어색하다.

교실에서 필요한 것은 파일이 아니라 **화면**이다.
한 대목 서너 줄을 크게 띄워 놓고 다 같이 보는 자리.

```
   학습자 폰                          교사 노트북 → 빔프로젝터
   대화 기록 → 교실에 띄우기      →    /class 에 코드 넣기
   처음 줄·끝 줄을 눌러 대목 잡기
   「K7QM」                              그 대목이 크게 뜬다
```

### ⓑ 기록이 **글 덩어리 하나**여서 대목을 잘라 낼 수 없었다

`saveConversationToHistory()` 는 `buildTranscriptText()` 의 결과를 통째로 넣고 있었다.
읽기엔 되지만 **어디부터 어디까지가 한 차례인지** 글에서 되찾아야 한다.

턴을 따로 남기게 했다. 글 덩어리는 그대로 둔다 — 내보내기와 지난 기록 보기가 그걸 쓴다.

```js
turns: [{ r:"ham", t:"어서 오세요." }, { r:"me", t:"코트를 보려고요." }, …]
```

※ v141 이전에 저장된 기록에는 턴이 없다. 그 기록에서는 「교실에 띄우기」 단추가 아예 안 뜬다 —
   눌렀다가 빈 화면을 보는 것보다 없는 편이 낫다.

### ⓒ 대목은 **처음 줄과 끝 줄**을 눌러 잡는다

줄마다 켜고 끄게 하면 **띄엄띄엄 고르게** 되는데, 대화는 이어져야 뜻이 산다.
거꾸로(끝→처음) 눌러도 잡히고, 세 번째로 누르면 처음부터 다시 잡는다.
두 줄부터 만들 수 있고 열두 줄까지 간다(그 이상은 대형 화면이 넘친다).

### ⓓ 대화문만 보낸다 — 이것이 이 화면의 핵심 설계다

교사 화면에는 **점수도, 호아랑이 권한 것도, 자가 점검도, 이름도** 안 띄운다.

판단을 먼저 보이면 반 전체가 그 판단을 따라간다.
「이 자리에서는 이렇게 말했으면 더 자연스러웠겠다」는 말이
**학습자들 입에서 먼저** 나와야 한다. 그것이 이 활동의 목적이다.

이름을 안 담는 까닭도 같다 — 누구 것인지 띄우면 발표가 부담이 된다.
서버로 나가는 몸통은 `{turns, title}` 둘뿐이고, 검사가 그것을 지킨다.

### ⓔ 왜 메모리에만 두나

수업 한 시간 안에 쓰고 버리는 것이다. 남길 까닭이 없고,
남기면 학습자 발화가 서버 여기저기에 흩어진다.

```
두 시간 뒤 저절로 사라짐 · 한 서버에 300건까지 · 넘치면 오래된 것부터
```

서버가 다시 뜨면 없어진다. 그때는 학습자가 코드를 다시 만들면 된다 —
없어져서 곤란한 물건이 아니다.

코드는 **네 글자**, `0·O·1·I·L` 을 뺀 31자에서 뽑는다.
대형 화면에서 0 과 O 는 서로 안 갈린다.

### ⓕ 교사 화면(`/class`)

- 코드 칸 하나, 큰 글씨 대화문, 글자 크기 `+`/`−`
- 고른 크기는 그 기기에 남는다 — 교실마다 화면 거리가 다르다
- 주소에 `?c=코드` 를 붙여 두면 새로 고침만으로 열린다
- **학생 화면에는 이 주소로 가는 길이 없다.** 검사가 링크·주소 바꾸기·창 열기 셋을 다 본다

### 새 검사

| | 무엇을 보나 |
|---|---|
| `sharetest.js` | 대목 고르기를 **실제로 눌러 본다** · 서버로 나가는 몸통에 이름·점수가 안 섞였는가 · 교사 화면을 `main.py` 에서 **꺼내 돌려** 코드를 넣어 본다 |
| `sharesrv.py` | 코드 3000개가 안 겹치는가 · 두 시간 뒤 치우는가 · 넘치면 오래된 것부터인가 · 한 반 45명이 한꺼번에 써도 되는가 |

`sharetest.js` 의 「학생 화면에 /class 로 가는 길이 없다」는 처음에 **글자만 찾다가 거짓 빨간불**을 냈다.
주석에 적힌 「/class」를 잡은 것이다. 갈 수 있느냐를 보게 고쳤다 — 글자가 아니라 길을 본다.

---

## v141 — 2026-08-20 · 디지털 자가 점검 — 종이 성찰지가 안 도는 자리를 화면이 맡는다

### ⓐ 왜 넣나 — 논문에 이미 자리가 있었다

3.4.3에 이렇게 써 두었다.

> 점검의 상당 부분은 챗봇의 자동 점검과 대화 기록으로 대체될 수 있다.

그리고 〈표 40〉의 변형 운용에서 개별 연습을 교실 밖 과제로 돌리면
**종이 성찰지는 아예 작동하지 않는다.** 걷을 사람도, 걷을 자리도 없다.

그 자리를 화면이 맡는다.

### ⓑ 자동 점검(AI)과 자가 점검(학습자)은 다른 물건이다

결과 화면에는 이미 **AI가 매기는** 요소별 상/중/하가 있었다(〈표 42〉).
없던 것은 **학습자가 스스로 매기는 쪽**이다.

둘은 나란히 있을 때 값이 생긴다 — **어긋남 자체가 자료**다.
사실 코드 주석에 v88부터 그렇게 적혀 있었다.

> 자기 평가와 실제 수행의 차이 자체가 연구 자료가 된다(5장)

그런데 지금까지 자기 평가는 **별점 하나(총체적)**뿐이었다.
요소별로는 그 대조를 할 수가 없었다.

### ⓒ 차례가 곧 설계다

```
   ⓪ 별점  →  ① 요소 자가 점검  →  ② 후기  →  ③ 점수·단계  →  ④ AI 판정  →  ⑤ 총평
   └────────── 학습자가 매긴다 ──────────┘   └────────── AI 가 매긴다 ──────────┘
```

**자가 점검이 AI 판정보다 앞이어야 한다.** 뒤에 두면 등급을 보고 나서 답하게 되어
자기 평가가 닻을 내린다(anchoring). 둘은 서로를 못 보게 놓아야 한다.

주제 대화(6장)와 자유 대화(5장) 둘 다 같은 차례다 — 자유 대화만 빼면
〈표 40〉 변형 운용에서 그쪽 성찰이 통째로 빈다.

### ⓓ 여덟 개가 아니라 서넛만 묻는다

3.4.3의 취지가 **점검의 간소화**인데 대화를 마친 학습자에게 여덟 개는 피로다.
5단 척도도 과하다. **「했어요 / 안 했어요」 두 갈래**로 묻는다.

무엇을 물을지는 이렇게 고른다.

```
ㄱ) 이 대화에서 호아랑이 실제로 권한 요소   ← 방금 겪은 것이라 답이 정확하다
ㄴ) 모자라면 오늘의 퀘스트가 걸린 요소       ← 대화 전에 이미 보여 준 것
ㄷ) 그래도 모자라면 listen·turn·topic·repair
```

ㄱ이 되는 까닭 — 서버는 개입할 때마다 `{"type":"intervene", "qid":…, "el":…}` 로
**어떤 요소인지 이미 실어 보내고 있었다.** 화면이 그걸 버리고 있었을 뿐이다.

요소 이름은 학습 화면과 **같은 학습자 말**을 쓴다(「대화이동 관리」가 아니라 「말 주고받기」).
논문 용어는 그 아래 아주 작게 곁들인다 — 교실에서 이어 붙일 때 필요하다.

### ⓔ 후기 — 모국어로, 안 써도 넘어간다

3급 초입 학습자에게 한국어 서술은 벽이다. 벽을 세우면 빈칸만 쌓인다.
**편한 말로 써도 좋다**고 화면이 먼저 말한다. 건너뛰어도 다음으로 간다.

### ⓕ 남는 곳 — 세 군데에 함께 적는다

| | 무엇을 위해 |
|---|---|
| 기기 기록(`localStorage`) | 회차마다 쌓이면 그대로 **포트폴리오**가 된다 |
| 서버(웹소켓 `self_check`) | 연구 자료 |
| 전사 txt | AI 판정과 **한 파일에 나란히** — 어긋남을 바로 읽을 수 있다 |

전사 파일에는 논문 용어로 적는다. 화면은 학습자 말, 파일은 연구 말.

```
 스스로 점검: ✓대화이동 관리, ✗의사소통 전략, ✓상호작용적 듣기
 학습자 후기: 값을 깎는 말이 잘 안 나왔다
 상호작용 대화 능력: 62점
```

### ⓖ 또 TDZ — 세 번째다

`idcTouched` 를 결과 화면 쪽(7000줄대)에 `let` 으로 선언했더니
**앱이 통째로 죽었다.**

```
Uncaught [ReferenceError: Cannot access 'idcTouched' before initialization]
```

바로 위 `questReset()` 이 파일을 읽는 즉시 한 번 도는데, `let` 은 끌어올려지지 않는다.
v110(`bgmGain`) · v113(`GONG_VOL`) 에 이어 세 번째다.
**쓰는 자리가 아니라 처음 닿는 자리 앞**에 두어야 한다. 주석에 못 박아 두었다.

### 그 밖에

- 누를 때 판을 다시 그리지 않는다. 다시 그리면 방금 누른 단추가 **다른 물건으로 바뀌어**
  눌린 티가 안 나고 움직임도 끊긴다. 두 화면의 같은 줄만 찾아 불을 맞춘다.
- 새 문구 7개를 **18개 언어 전부**에 넣었다(`I18N_SELF`). `i18ntest.js` 통과.
- `btntest` · `pctest` · `restest` · `uxtest` 넷이 옛 장 번호를 박아 두고 있었다.
  장이 늘었으니 검사도 함께 옮겼다 — 화면이 바뀌면 검사도 바뀐다.

### 새 검사

`selftest.js` — 화면을 실제로 돌려 넘겨 보고, 눌러 보고, 기록·서버·전사까지 따라간다.

```
① 장 차례 (자가 점검이 AI 판정보다 앞인가)   ⑤ 서버로 가는가
② 호아랑이 권한 요소를 묻는가                 ⑥ 전사 파일에 남는가
③ 눌러 보면 담기는가                          ⑦ 새 대화를 열면 비워지는가
④ 후기가 담기는가                             ⑧ 그리다 터진 곳
```

---

## v140 — 2026-08-20 · 다섯 편이 안 돌던 까닭, 그리고 탭을 삼키던 것

### ⓐ ★★ 대화문 다섯 편을 만들어 놓고 한 편만 보여 주고 있었다

도장은 쌓이는데 대화문은 늘 같았다. **단위가 어긋나 있었다.**

```js
idlSeen.push(d.place || "");        // 화면: 「옷 가게에서 …」 (자리 설명)
```
```python
fresh = [x for x in pool if x.get("id") not in seen_set]   # 서버: 「topic-3」 (편 id)
```

화면은 **자리 설명**을 보내고 서버는 **편 id**로 견줬다. **한 번도 안 맞았다.**
그래서 늘 다섯 편 전부가 「안 본 것」이 됐고, 거기에 화계까지 걸리니
그 화계에 해당하는 한두 편만 계속 뽑혔다. `strategy` 는 존댓말 편이 하나뿐이라
페이더를 안 건드린 학습자에게는 **문자 그대로 같은 편만** 나왔다.

거기에 두 가지가 더 있었다.

- 여덟 요소가 **목록 하나를 같이 썼다** — 요소를 옮기면 남의 기록에 밀려났다
- 화면을 새로 고치면 **사라졌다** — 늘 처음부터

셋을 다 고쳤다. 이제 요소마다 따로, id 로, `localStorage` 에 적는다.
그리고 **편 수보다 하나 적게만** 쥔다 — 늘 「안 본 것」이 하나는 남으므로
다섯 편이 본 차례대로 깔끔하게 한 바퀴 돈다. 서버가 `pool` 로 편 수를 알려 준다.

고르는 규칙도 차례를 못 박았다.

```
ㄱ) 안 본 것이 먼저다          ← 첫째 규칙
ㄴ) 그중에서 화계가 맞는 것     ← 화계는 기준이 아니라 같은 값일 때의 저울
ㄷ) 다 봤으면 처음부터
```

예전에는 ㄱ과 ㄴ이 뒤바뀌어 있었다.

### ⓑ ★★ 시작 영상이 학습자의 진짜 탭을 삼키고 있었다

```js
function endSplash() { ... swallowNextClick(); }
```

삼키기는 **손가락으로 건너뛸 때**를 막으려고 넣은 것이다. `pointerdown` 으로
영상을 건너뛰면 그 손가락에서 `click` 이 한 번 더 나와 뒷화면 단추를 저절로 누른다.
그 하나만 삼키려던 것이었다.

그런데 영상이 **시간 초과로** 걷힐 때도 삼켰다.
회선이 느리면 4초 뒤 저절로 끝나는데(`startGuard`), 그때는 삼킬 합성 클릭이 애초에 없다.
그래서 **학습자가 진짜로 누른 것**을 대신 삼켰다.

교실에서 마흔다섯 명이 한꺼번에 들어오면 영상이 늦게 뜬다. 바로 그 뒤 0.8초 안에
누른 것이 통째로 사라진다 — 「단추가 가끔 안 눌린다」는 말이 여기서 나왔다.

`endSplash(byTap)` 을 두고 **손가락으로 건너뛴 것일 때만** 삼킨다.
(`ended`·`error` 는 Event 를 넘기므로 `=== true` 로 못 박아야 한다.)

이건 검사를 만들다 걸렸다. 「되돌아온 화면에서 맞는 것을 눌렀는데 아무 일도 없다」는
빨간불이 계속 떠서, 처음엔 검사가 거짓말하는 줄 알았다. `dispatchEvent` 가 `false` 를
돌려주는 것을 보고 `preventDefault` 에 덫을 놓았더니 `Document.eat` 이 나왔다.
**검사가 맞고 앱이 틀렸다.**

### ⓒ 틀리면 1초 뒤 저절로 다시 고를 수 있다

예전에는 「🔊 다시 듣기」 단추를 눌러야 다시 고를 수 있었다.
**틀린 사람에게 단추를 하나 더 누르게 하면 거기서 그만둔다.**
이건 시험이 아니라 배우는 자리다. 몇 번이든 고를 수 있어야 한다.

🤔 를 1초 보이고 첫 화면으로 돌아간다. **짚어 준 말은 지우지 않는다** —
1초는 읽기엔 너무 짧으므로, 되돌아간 뒤에도 위에 남아 그것을 보며 다시 고를 수 있다.

그러려면 짚어 주는 말이 있어야 하는데 **40편 중 13편이 비어 있었다.**
빈 편에서는 다시 고르는 것이 찍기가 된다. 열세 개를 채웠다 —
정답을 말하지 않고 **어디를 다시 보라**고만 하는 말이다.

누를 때마다 되돌리기 예약을 먼저 지운다. 안 그러면 틀린 뒤 1초를 안 기다리고
바로 맞는 것을 골랐을 때, 앞서 걸어 둔 예약이 깨어나 **맞혀 놓은 것을 지운다.**

### ⓓ 줄바꿈을 어절 단위로

```
   전 :  아르바이트
         가 있거든
   후 :  아르바이트가
         있거든
```

`word-break` 는 물려받으므로 `body` 한 곳으로 앱 전체가 덮인다.
낱말을 자르라고 따로 걸어 두었던 네 군데(`.bubble` `.rev-body` `.sg-hint` `.hist-item`)도
되돌렸다. `overflow-wrap: break-word` 는 남겨서, 한 어절이 줄보다 길 때만 마지막 수단으로 자른다.

### ⓔ lvcheck 가 두 번 더 거짓말하고 있었다

|잡은 것|실제|
|---|---|
|`볼게요` 4급|`보다`(1급) + `-(으)ㄹ게요`. 목록의 「볼」은 **뺨**이다|
|`-는 한` 4급|「물건을 살 **때는 한** 번」의 관형사 「한」|
|`-고 해서` 4급|「넘어가려**고 해서**」 = `-려고 하다` + `-아서`|

받침 ㄹ 을 되돌리는 것으로 첫째를 고쳤는데, 처음에 아무 데나 떼었더니
**4급 22개를 새로 놓쳤다**(`볼`·`출산`·`솔직히`·`뜰`…). 오탐 하나 잡으려다 스물둘을 흘린 셈이다.
`-(으)ㄹ게/-(으)ㄹ까/-(으)ㄹ래…` 가 실제로 붙은 자리에만 쓰도록 좁혔다 —
목록 5,444개 가운데 **판정이 달라진 낱말 0개**, 잡을 것만 잡는다.

짧은 문형은 딴 말 속에 그대로 들어 있으므로 앞뒤를 함께 본다.

```
이제  3급 넘는 어휘 0개 · 3급 넘는 문법 0개
```

### 새 검사

| | 무엇을 보나 |
|---|---|
| `rotatetest.py` | 다섯 편이 도는가 — `main.py` 의 고르는 코드를 떼어 열두 번 뽑아 본다 |
| `seentest.js` | 화면이 편 id 를 보내는가 · 편 수보다 하나 적게 쥐는가 · 새로 고쳐도 남는가 |
| `quiztest.js ①-2` | 틀린 뒤 1초에 저절로 돌아오는가 · 짚어 준 말이 남는가 · 몇 번이든 되는가 |
| `quiztest.js ③` | 시작 영상이 진짜 눌림을 삼키지 않는가 |
| `hints.py` | 힌트가 빈 편을 채운다 |

`rotatetest.py` 는 규칙을 베끼지 않는다 — `main.py` 에서 `_idc_pick` 을 떼어 **그대로 돌린다.**
「한 바퀴 안에서 안 겹친다」 「같은 것이 잇달아 안 나온다」를 여덟 요소 × 열두 번으로 본다.

---

## v139 — 2026-08-20 · 화면이 멎던 것을 잡고, 말하기 단추를 하나로

### ⓐ ★★ 고르고 나면 화면이 멎었다 — 한 글자짜리 사고

아이폰에서 ②추측의 답을 고르면 **선택지가 통째로 사라지고 물음만 남았다.**
맞았는지 틀렸는지도, v138에서 넣은 🎉·🤔 도 한 번도 안 보였다.

```js
o.classList.add(k === q.ans ? "ok" : (k === idl.picked ? "no" : ""));
                                                          ~~~~
```

`classList.add("")` 는 빈 글자를 못 받고 **SyntaxError 를 던진다.**
고른 것도 정답도 아닌 자리에서 빈 글자가 들어가면서 `forEach` 가 거기서 끊겼고,
**남은 선택지가 아예 안 그려졌다.** 정답을 맨 앞에서 골랐으면 첫 바퀴에 터져
선택지가 하나도 안 남는다. 아래에 있던 결과 줄도 같은 이유로 영영 못 그렸다.

한 글자를 안 넣어서 화면 하나가 통째로 죽어 있었다.

```js
const mark = (k === q.ans) ? "ok" : (k === idl.picked ? "no" : "");
if (mark) o.classList.add(mark);
```

틀리면 **판 전체가 한 번 흔들린다.** 선택지 한 줄만 흔들리면 눈이 못 따라간다.
`quiztest.js` 를 새로 만들어 **실제로 눌러 본다** — 맞는 것도 틀린 것도.

### ⓑ ⑤걸음에 해설이 그대로 있었다 — 주석만 고치고 코드는 안 고침

v138 에 이렇게 적어 두었다.

> ★ v138 — 여기는 **입을 떼는** 자리다. 대화문도 해설도 뺀다.

정작 `(d.meaning||[]).forEach(...)` 줄에 조건을 안 걸었다.
③④⑤가 같은 코드를 지나는데 ③④에서만 멈추게 해 놓고 ⑤는 안 막은 것이다.
**주석은 고쳤는데 코드는 안 고쳤다.** 화면을 안 돌려 봤기 때문이다.

### ⓒ 말하기 단추를 세 자리에서 하나로

이 앱에는 꾹 눌러 말하는 자리가 셋이다.

| | 자리 | v138 까지 |
|---|---|---|
| ㄱ | 주제·자유 대화 | 초록 알약 · 창살 무늬 · 파동 |
| ㄴ | 발화 연습 | 같음 + 아래 안내 「🔴 듣는 중… 다 말하면 손을 떼세요」 |
| ㄷ | 학습 ⑤걸음 | **남색 네모** · 아래 안내 없음 · 글씨도 달랐다 |

셋이 저마다 굴어서 학습자가 자리를 옮길 때마다 다시 익혔다.
v112 에서 「학습자들이 혼란스러워하더라」는 말을 듣고 한 번 맞췄는데, ㄷ이 빠져 있었다.

ㄷ에 ㄴ의 옷(`.pr-mic`)과 아래 안내를 그대로 입혔고, 글씨도 한 글자까지 맞췄다
(「떼면 보냅니다」 → 「떼면 전송」, 「듣고 있어요…」 → 「알아듣는 중...」).

그리고 셋 다 **진동**을 붙였다. `pttBuzz()` 한 곳에서 만든다.

```
누를 때 26ms   ·   뗄 때 13ms   ·   틀렸을 때 16-55-16
```

교실에서는 소리를 못 켠다. 그럴 때 진동이 「지금 듣고 있다」는 **유일한 신호**가 된다.
vibrate 를 안 받는 기기(아이폰 사파리)에서는 아무 일도 안 일어난다 — 화면 신호는 그대로다.

### ⓓ ⑤걸음을 발화 연습과 같은 뼈대로

같은 「말해 보는 자리」인데 두 화면의 생김새가 달랐다.

```
   왼쪽 안내 한 줄  →  상대 말풍선  →  내가 할 말(큰 카드)  →  🔊 다시 듣기
                    →  말하기 단추  →  아래 안내
```

해설은 **왼쪽 정렬**로 바꿨다. 가운데로 두면 줄마다 시작점이 달라서
눈이 매번 줄 앞을 찾아야 한다 — 여러 줄짜리 글에는 맞지 않는다.

### ⓔ 해설을 짧게 — 22줄

③④에 세 줄이 나란히 뜬다. 한 줄이 70자면 세 줄이 200자다.
3급 초입 학습자에게 200자는 화면이 아니라 **벽**이다. 읽다 지치면 그 아래 문형을 아예 안 본다.

걷어낸 것은 겹치는 말뿐이다.

- 「한국어에서는」 — 세 마디 중 셋째는 늘 한국어 이야기다. 말 안 해도 안다
- 예를 셋 늘어놓은 것 → 둘로. 셋째 예는 새 정보를 안 준다
- 「~게 아니라 ~예요」 — 앞에서 말한 것을 뒤집어 또 말한 것

```
평균 44자 → 40자   ·   가장 긴 것 83자 → 59자   ·   모두 473자 덜 읽는다
```

대화문·문형·연습은 손대지 않았다. 그건 배울 거리 자체다.

### ⓕ 기능 단계 — 「계획 짜기」를 「선물 사기」로

기획 대화(Hundsnurscher 1986)의 세 단계를 학습자 말로 옮기면 이렇게 된다.

```
뭘 정해야 하는지 알기  /  어떻게 할까 이야기하기  /  누가 뭘 할지 나누기
```

**셋 다 머릿속 일이라 대화문에서 눈에 안 보인다.** 조각을 읽어도 어느 줄인지 못 짚는다.
구매 대화는 다르다 — 들어가고, 보고, 고르고, 계산한다. **몸으로 하는 일**이라 줄과 붙는다.

`stage-3` 을 **백화점에서 어머니 선물 사기**로 다시 썼다(구매 대화 5단계).
`stage-1`(옷 가게)과 같은 원형이 되지만, 선생님이 논문에서 확정한 원형을
**다른 장면에서 한 번 더** 보는 것이라 오히려 낫다.

화계를 지키려고 `stage-4`(중고 자전거)를 **또래 직거래 반말**로 바꿨다.
`stage-3` 이 반말 자리였는데 구매 대화는 존댓말이라, 그대로 두면
다섯 편에 반말이 하나도 안 남는다(`check.py ⑥`).
파는 사람도 사는 사람도 학생인 자리라 반말이 오히려 자연스럽다.

### ⓖ 글자가 끌려서 단추가 안 눌리던 것

폰에서 단추 위 글자를 조금 길게 누르면 **글자가 선택되면서**
안드로이드는 구글 검색 팝업이, 아이폰은 복사·검색 막대가 떠 버린다.
그 순간 눌림이 취소되어 「단추가 안 먹는」 것처럼 보인다.

말하기 앱이라 글자를 긁어 갈 일이 거의 없다. 통째로 막고
입력칸·대화 기록·해설만 다시 열었다.

### 새 검사

| | 무엇을 보나 |
|---|---|
| `quiztest.js` | 고른 뒤 맞았는지 틀렸는지가 **화면에 보이는가** (맞는 것·틀린 것 둘 다 눌러 본다) |
| `ptttest.js` | 세 자리 말하기 단추가 같은 옷·같은 글씨·같은 진동인가 · 글자 선택이 막혔는가 |
| `shorten.py` | 해설 줄이기 (다시 돌려도 탈이 없다) |
| `stage3.py` | stage-3·4 다시 쓰기 |

두 검사 모두 **페이지를 실제로 돌려 눌러 본다.** 이번 두 사고는
문자열 검사로는 하나도 못 잡았을 것이다 — 코드는 다 있었고, 돌 때만 터졌다.

---

## v138 — 2026-08-20 · 학습 화면을 아이폰에서 보고 일곱 군데

40편이 앱에 들어간 뒤 실제 기기에서 여섯 걸음(⓪~⑤)을 다 눌러 보았다.
내용은 맞는데 **읽히지가 않았다.** 고친 일곱 가지는 전부 「무엇을 보여 주느냐」가 아니라
**「무엇을 안 보여 주느냐」**에 가깝다.

### ⓐ 이름표를 얼굴로 (①대화문)

줄마다 「호아랑」 석 자가 붙어 있었다. 대화문이 여덟 줄이면 스물넷 글자가
**읽을 것도 아니면서 자리를 차지한다.** 누가 말하는지는 그림이 훨씬 빨리 알려 준다.
학습자 쪽은 「나」로 두었다 — 얼굴이 둘이면 누가 나인지가 흐려진다.

### ⓑ 물음을 호아랑이 묻는다 (②추측)

물음이 굵은 글씨로 선택지 바로 위에 붙어 있어서, 물음인지 선택지인지 구분이 안 됐다.

```
바뀐 것 :  얼굴 + 얇은 글씨(weight 500)  ·  물음과 ⓐⓑⓒ 사이 18px
           선택지 사이 간격 8px → 10px
```

굵게 하면 강조가 되지만, **선택지도 굵으면 강조가 사라진다.**
물음을 얇게 하니 오히려 선택지가 눈에 들어왔다.

### ⓒ 들어가기에 이모지를 크게 (⓪)

무엇을 배우는 자리인지가 글로만 있었다. 요소 이모지를 64px로 앞세웠다.

### ⓓ 맞았다·틀렸다를 몸으로 (②추측)

색만 바뀌고 아무 일도 안 일어났다.
맞으면 🎉 와 함께 살짝 커졌다 돌아오고(`idlPop`), 틀리면 🤔 와 함께 좌우로 흔들린다(`idlShake`).
차례 맞히기의 조각도 같다. `prefers-reduced-motion` 이면 움직이지 않는다.

### ⓔ 해설과 문형을 갈라 놓았다 (③④)

③뜻풀이와 ④문형이 한 덩어리로 흘러서 어디까지가 설명인지 몰랐다. 사이에 줄을 그었다.

### ⓕ 문형을 **채워서** 보인다 ★ 이번 판의 핵심

```
   전 :  나도 ~한 적 있어
   후 :  나도 등산한 적 있어
              ~~~~
```

`~` 만 있으면 3급 초입 학습자는 **무엇을 넣어야 하는지 자체를 모른다.**
빈칸이 연습이 아니라 벽이 된다. 한 번 채워진 것을 보고 나면 갈아 끼울 수 있다.

색으로 둘을 가른다 — **붉은색 = 그대로 외울 뼈대 · 검은색 = 갈아 끼우는 자리.**

40편 157개 문형 가운데 `~` 가 있던 **63개를 그 편의 대화문 맥락으로 채웠다.**
채운 말은 그 대화문 안에서 골랐다(`topic-2` 의 「등산」은 호아랑이 방금 한 말이다).

담는 꼴은 **조각 배열**이다. 문자열에 표시를 박으면 표시가 화면에 새어 나온다.

```json
"forms_filled": [ [["나도 ",1], ["등산",0], ["한 적 있어",1]] ]
                     글자        1=뼈대(붉음) / 0=갈아 끼우는 자리(검음)
```

조각을 이어 붙이면 학습자가 보는 한 줄이 된다. **음절 가운데를 자르지 않는다** —
「기다리」+「ㄹ게」로 쪼개면 화면에 「기다리ㄹ게」로 깨져 보인다. `check.py ⑭` 가 이것을 본다.

읽어 주는 소리도 채운 것을 읽는다. 전에는 `~` 만 지우고 읽어서 말이 안 되는 토막이 나갔다.

### ⓖ ⑤연습에서 대화문과 해설을 뺐다

④문형에서 이미 다 보여 주었는데 ⑤에서 또 늘어놓고 있었다.
**말할 것 앞에 글이 많으면 입이 안 떨어진다.** 남긴 것은 둘뿐 —
상대가 방금 한 말과 내가 할 말. ④에서도 대화문은 뺐다(해설은 남겼다).

### 검사

| | 무엇을 보나 |
|---|---|
| `check.py ⑭` | 채운 꼴의 개수·`~` 잔여·검은 칸 유무·음절 깨짐 |
| `corpusui.js ③` | 채운 문형이 **화면에서** 붉고 검게 갈리는가 (jsdom 으로 실제 그려 봄) |
| `fillforms.py` | 63개를 채우는 곳 (표를 고치고 다시 돌리면 된다) |
| `mklesson.py` | 화면 검사가 먹을 응답을 **main.py 코드를 떼어 돌려서** 만든다 |
| `tocorpus.py` | `IDC대화문/` → `idc_corpus/` 옮기기 (`--check` 로 확인만) |

`mklesson.py` 를 새로 만든 까닭 — `corpusui.js` 와 `ordertest.js` 가 먹던 응답이
**손으로 적어 둔 파일**이었다. 원본이 바뀌어도 검사는 옛말을 하게 된다(다섯 번 그랬다).
이제 `main.py` 의 코퍼스 코드를 정규식으로 떼어 그대로 실행해서 만든다.

### 그 밖에

- `syncdocx.py` 가 **돌릴 때마다 「24군데 되돌림」**이라고 했다. 고친 것이 없는데도 그랬다.
  검토용 docx 는 ⓐⓑⓒ 를 섞어 찍는데(정답이 늘 ⓐ면 안 읽고도 안다),
  읽어 올 때 그 차례를 그대로 `wrong1`·`wrong2` 에 넣고 있었다.
  내용이 같으면 자리도 그대로 둔다. 이제 두 번 돌리면 0이다.
- `csscheck.js` 가 아직도 옛 이름 `i.html` 을 보고 있었다. 루트 검사 8개를 고칠 때 빠졌다.
- 검토용 docx 의 ④문형에 채운 꼴을 나란히 찍는다 — 검토도 화면에서 보이는 대로 해야 한다.

---

## v137 — 2026-08-19 · 40편 완성 · 기능 단계를 박용익으로 다시 세움

### ⓐ 기능 단계를 잘못 알고 있었다

v136의 「기능 단계」 5편은 이런 꼴이었다.

```
말 걸기 → 용건 꺼내기 → 자세히 묻기 → 확인하기 → 인사하고 마치기
```

박용익 《대화분석론》 용어해설(304쪽)이 정확히 이것을 갈라 놓았다.

> **회화분석론**은 대화를 **시간의 진행 과정에 따라 순차적으로** 나눈다 —
> 시작 단계·중간 단계·종료 단계.
> **기능 단계**는 대화의 목적이 실현되는 **기능의 성취도에 따라** 복합 대화를 나눈 것.
> 회화분석론이 **순차적·형태적으로** 나눈 것에 대해서,
> 대화문법론이 **기능적 관점을 바탕으로 보완한 용어.**

제가 만든 것은 **전자**였다. 이름은 「기능 단계」인데 내용은 「시작–중간–종료」였다.

**둘째 잘못** — 단순 대화에 단계를 붙였다.
`stage-1`(터미널에서 표 사는 곳 묻기)은 143쪽이 예로 든 **길 묻기와 같은 단순 대화**다.
「하나의 대화이동 연속체로 완결」되므로 **기능 단계가 애초에 없다.**

### ⓑ 다시 만든 다섯 편 — 전부 복합 대화

| | 대화 유형 | 근거 |
|---|---|---|
| 1 | 구매 (옷 가게) | **이남호·이찬규(2025)** 구매 대화 원형 |
| 2 | 전화 상담 (인터넷 고장) | Schank 1981; Brinker·Sager 1987 |
| 3 | 계획 짜기 (친구와 여행) | Hundsnurscher 1986 |
| 4 | 값 흥정 (중고 자전거) | Franke 1985 |
| 5 | 수업 (한국어 수업) | 박용익 1994 |

1번은 **선생님이 논문에서 확정한 것을 챗봇이 그대로 가르치는 것**이 된다.

### ⓒ 연구 용어를 학습자에게 보이지 않는다

「문제규정 단계」를 화면에 띄우면 학습자가 막힌다. 그런데 이 프로젝트에 이미 답이 있었다 —
다른 요소는 전부 `easy`(학습자)와 `acad`(논문) 두 이름을 두고 있다. 단계도 같게 했다.

```
name : 뭐가 있나 보기            ← 화면
acad : 탐색 단계                ← 검토용 docx 에만
```

### ⓓ 차례 맞히기 — 이 묶음만 물음이 다르다

한 발화가 아니라 **대화 한 판의 흐름**을 보는 것이라 세 갈래 고르기로는 배울 것이 안 남는다.
「ⓐ 인사-용건-마무리 ⓑ …」는 손으로 쌓는 것이 아니라 **글을 읽는 문제**가 된다.
조각을 섞어 놓고 차례대로 눌러 쌓게 했다. 잘못 놓으면 눌러서 도로 뺀다.

### ⓔ 어휘를 3급 초입으로 — 표준 교육과정으로 전수 대조

대상은 **2급을 수료하고 3급에 들어온 학습자**다. 「북돋우다」「비꼬다」「얹다」는
모어 화자에게는 쉬워도 그들에게는 처음 보는 말이다.

〈2017 국제 통용 한국어 표준 교육과정(4단계)〉 어휘 5,444 · 문법 514를 `lv13.json` 으로
뽑아 두고, **학습자가 보는 글만** 대조했다(대화문·문형·연습은 안 본다 — 그건 배울 거리다).

- 4급 어휘 **21개, 34군데** → 1~3급으로 (위아래·끼어들다·생김새·침묵·청하다·공손하다…)
- 4급 문법 **1개** → `-에 따라` → `~가 다르면`
- 하숙집 3편 → 원룸·옆집 (요즘 유학생이 겪는 자리로)
- 별표(`**`) 24군데 → 화면에는 그냥 별표로 뜬다

검사 셋을 새로 넣었다 — ⑫-2(별표), ⑬-0(어려운 말 32개), 그리고 `lvcheck.py`.

### ⓕ 내가 헛짚은 것 셋

**ㄱ) 선생님이 docx 에서 고치신 것이 json 에 안 들어가고 있었다.**
검토는 docx 로 하고 앱은 json 을 쓴다. **검토가 헛일이 되고 있었다.**
`syncdocx.py` 를 만들어 33군데를 되돌렸다.

**ㄴ) 「낱말 → 단어」 치환이 조사를 안 봤다.** 「낱말이」가 「단어이」가 됐다(8군데).
문자열만 바꾸면 조사가 어긋난다.

**ㄷ) `lvcheck.py` 가 두 번 거짓말했다.**
처음엔 「해서」「합니다」 같은 **어미를 낱말로 세서** 43개를 뱉었고,
그다음엔 불규칙 활용(`몰라서`→모르다 1급, `골라`→고르다 1급)을 **4급으로 오판**했다.
둘 다 고쳐 지금은 오탐 3건만 남는다.

### ⓖ 검사가 앱을 모함했다

`ordertest.js` 가 「조각이 안 쌓인다」고 계속 빨간불을 냈다. 파고들어 보니
**앱은 멀쩡했고 검사가 틀렸다.** 두 가지가 겹쳐 있었다.

- `new MouseEvent(...)` 를 `dispatchEvent` 로 쏘면 jsdom 이 자리에 따라 안 받는다 → `el.click()`
- `idl.ord = []` 로 **새 배열을 끼워** 넣었는데, 앞 걸음에서 맞힌 뒤 남은 1.4초 예약이
  나중에 다시 그리면서 내가 쥔 단추와 화면의 단추가 어긋났다
  → 비우기만 하고(`length = 0`) 예약이 지나가길 기다린다

> 배운 것: 검사가 빨간불을 낼 때 **앱을 먼저 의심하는 것이 늘 맞지는 않다.**
> 특히 화면을 흉내 내는 검사는 흉내가 어긋나서 우는 때가 있다.
> 「누른 클릭이 단추까지 닿았나」를 한 겹씩 짚어 보고서야 알았다.

### ⓗ 전체

40편(8요소 × 5편) 완성. 검사 31개 통과 — JS 28 · Python 3.

---

## v136 — 2026-08-19 · 40편 완성 · 차례 맞히기

### ⓐ 마지막 묶음 — 기능 단계의 조직 5편

여덟 요소가 다 찼다. **40편(8 × 5).**

| | 목적 | 자리 | 화계 |
|---|---|---|---|
| 1 | 묻기 | 버스 터미널에서 표 사는 곳 | 합쇼체 |
| 2 | **부탁하기** | 하숙집 방값 미루기 | 해요체 |
| 3 | **항의하기** | 배달이 잘못 왔다 | 합쇼체 |
| 4 | 약속 잡기 | 친구와 시험공부 | 해체 |
| 5 | 처음 만나 소개하기 | 한국어 교실 첫날 | 해요체 |

**목적을 다르게 잡은 것이 이 묶음의 요점이다.** 다섯 편이 다 「인사→용건→마무리」면
두 번째부터는 안 읽고 눌러도 맞는다. 목적이 다르면 **차례가 달라진다.**

- **부탁**은 사정이 부탁보다 **먼저** 온다 (「아르바이트비가 늦게 나와서요」 → 「다음 주에 내도 될까요?」)
- **항의**는 문제가 요청보다 **먼저** 온다 (「다른 음식이 왔습니다」 → 「바꿔 주실 수 있습니까?」)
- **약속**은 확인이 **맨 마지막**에 온다 (「금요일 두 시, 도서관 앞이지?」)

이 대비가 곧 학습이다.

### ⓑ 차례 맞히기 — 이 묶음만 물음이 다르다

한 발화가 아니라 **대화 한 판의 흐름**을 보는 것이라, 세 갈래 고르기로는 배울 것이
남지 않는다. 「ⓐ 인사-용건-마무리 ⓑ …」는 손으로 쌓는 것이 아니라 **글을 읽는 문제**가 된다.

조각을 섞어 늘어놓고 **차례대로 눌러 쌓게** 했다.
잘못 놓은 것은 눌러서 도로 뺀다. 다 쌓으면 채점하고, 틀리면 다시 할 수 있다.

`ordertest.js` 로 **실제로 눌러 봤다** — 틀린 차례로 넣어 보고, 다시 하기를 누르고,
맞는 차례로 넣고, 쌓은 것을 도로 빼는 것까지. 화면 검사는 눌러 보지 않으면 뜻이 없다.

### ⓒ 검사가 **또** 코드를 베껴 두고 있었다 — 다섯 번째

`bgmtest.js` 가 이러고 있었다.

```js
ok("반말은 가까울 때만", /if \(d >= SPEECH_CLOSE\) return p >= 45 \? 2 : 1;/.test(html));
```

**코드 한 줄을 글자 그대로 찾고 있다.** v134에서 눈금을 고치자(45→35, 70→65) 그대로 깨졌다.
v134에서 셋을 만났는데 여기까지 다섯 번째다.

고칠 때 **무엇을 지키려던 검사였나**를 다시 물었다. 답은 논문 〈표 4-x〉의 원칙이었다 —
「반말은 유대가 확보된 관계에서만」, 「낯선 사이·대등은 합쇼체」. 이 둘은 **눈금이 바뀌어도
변하지 않는다.** 그래서 글자를 찾는 대신 함수를 떼어 와 **6666칸을 돌려** 확인한다.

> 규칙으로 삼는다: **검사는 원본을 베끼지 않는다.**
> 코드 한 줄을 정규식으로 박아 두면, 그건 검사가 아니라 **복사본**이다.
> 원본이 바뀔 때 같이 안 바뀌고, 그때부터 검사는 거짓말을 한다.
> 지키려는 것이 무엇인지 적고, 그것을 **돌려서** 본다.

### ⓓ 목록에는 있는데 파일이 없었다

`HOME_BGM` 에 `bgm_spring.mp3` 가 있는데 `static/` 에 파일이 없었다.
**네 곡 중 하나라 25% 확률로 배경음이 안 났다.**
(다른 창에서 `bgm_rain` 을 지우며 함께 지워진 듯하다)

`sndtest.js` 의 「파일 있음」 검사가 이것을 잡았다 — 검사가 제 일을 했다.
원본에서 다시 인코딩해 되살리고(-12.5 LUFS, 다른 곡과 같은 자리),
**반대 방향**도 보게 했다: 파일은 있는데 목록에 없으면 아무도 안 트는 파일이
깃헙에 얹혀 다닌다. **목록과 파일은 한 벌이어야 한다.**

### ⓔ 루트 폴더의 검사 여덟이 안 돌고 있었다

`i.html` 이라는 옛 파일 이름과 죽은 절대 경로를 보고 있었다.
배포 폴더 것만 고치고 루트를 안 고쳤던 탓이다. **같은 파일이 두 벌 있으면
한쪽만 고치게 된다.** 여덟을 고쳐 이제 루트에서 **28개가 모두 돈다.**

---

## v135 — 2026-08-19 · 만들어 둔 대화문 35편을 앱이 쓰게 했다

### ⓐ 만들어 놓고 안 쓰고 있었다

7요소 × 5편, 모두 검수를 거친 대화문이 `IDC대화문/` 에 있었다.
그런데 **앱은 그것을 몰랐다.** `main.py` 어디에도 그 폴더를 읽는 코드가 없었다.
학습 화면은 여전히 열 때마다 모델에게 새로 지어 달라고 하고 있었다.

그래서 이렇게 됐다.

| | 지금까지 | v135 |
|---|---|---|
| 기다림 | 20~30초 (자주 실패) | **없음** (파일에서 꺼냄) |
| 무엇이 나오나 | 그때그때 다름 | 검수한 35편 중 하나 |
| 처음 온 학습자 | 기록이 없어 못 씀 | 바로 됨 |
| 한국어로 볼 때 LLM 호출 | 3~4회 | **0회** |
| 논문에서 | 「LLM이 지은 대화문」 | 연구자가 설계·검수한 자료 |

### ⓑ 어떻게 물렸나

- 서버가 켜질 때 `idc_corpus/*.json` 을 한 번 읽어 요소별로 담는다.
- `/idc-lesson` 이 **검수본을 먼저** 본다. 화계가 맞는 것을 우선하고,
  최근에 본 편은 뒤로 미룬다. 다 봤으면 다시 처음부터.
- 정답 자리는 낼 때마다 섞는다 — 늘 ⓐ면 학습자가 눌러 보고 안다.
- `④문형`·`⑤연습`도 그 편에 붙은 것을 쓴다. 화면이 편 `id` 를 같이 보내
  서버가 찾아 준다. **대화문과 연습이 어긋날 일이 없어졌다.**
  (실제로 어긋난 적이 있다 — `topic-5` 의 문형이 표시 안 한 줄의 말이었다)
- 모국어 번역은 **편마다 한 번만** 만들고 재워 둔다. 한국어로 보면 아예 안 부른다.

**`static/` 밑에 두지 않았다.** 거기 두면 웹으로 열리므로 학습자가 주소만 치면
**정답이 다 보인다.** 서버 루트에 두어 FastAPI 가 내보내지 않게 했다.

검수본이 없는 요소(`stage`)는 예전처럼 모델이 짓는다. 길을 지우지 않았다.

### ⓒ /version 에서 보인다

```
"idc": { "corpus": 12, "gen": 1, "have": {"context":5, "listen":5, …}, … }
          검수본으로 낸 판    모델이 지은 판
```

`gen` 이 자꾸 오르면 검수본을 못 찾고 있는 것이다.

### ⓓ 검사 둘을 새로 만들었다

**`corpustest.py`** — 코퍼스가 실려 있는가, 요소 이름이 서버 목록과 맞는가,
화계에 맞는 편만 고르는가, 본 것을 미루는가, 내보내는 모양이 예전과 같은가,
`static/` 으로 새지 않는가.

**`corpusui.js`** — **화면까지 닿는가.** 서버가 잘 골라도 화면이 못 그리면 소용없다.
`cardtest.js` 와 같이 페이지를 실제로 돌리고, `/idc-lesson` 응답을 검수본으로
가로채 먹인 뒤 ⓪~⑤ 걸음을 하나씩 넘겨 본다.

> 이 검사를 만들면서 **제 검사가 거짓말하는 것**을 먼저 겪었다.
> ⓪ 들어가기 화면만 보고 「대화문이 안 보인다」고 했는데,
> 대화문은 ① 걸음에서 나온다. **걸음마다 보이는 것이 다르다는 것**을
> 검사가 몰랐다. 화면을 검사할 때는 **어느 화면인지**부터 맞춰야 한다.

---

## v134 — 2026-08-19 · 「아는 사이」에서 지위 페이더가 아무 일도 안 하고 있었다

### ⓐ 눈금이 두 벌이었다

화면에 뜨는 한 줄을 25칸 전부 뽑아 보니 이랬다.

```
아는 사이 · 하급자    ~아요/어요로 말해 보세요
아는 사이 · 약간 아래  ~아요/어요로 말해 보세요
아는 사이 · 대등      ~아요/어요로 말해 보세요
아는 사이 · 약간 위    ~아요/어요로 말해 보세요
아는 사이 · 상급자    ~아요/어요로 말해 보세요
```

**지위를 아무리 움직여도 아무 일이 없다.** 페이더가 거짓말을 하고 있었다.

까닭은 눈금이 두 벌이었기 때문이다.

| | 경계 |
|---|---|
| 화면 라벨 | 0~15 낯선 · **16~35 아는** · 36~65 적당 · 66~85 친한 · 86~ 절친 |
| 화계 판정 | `SPEECH_FAR=15` · `SPEECH_CLOSE=60` · 지위는 45/70/55/30 |

「아는 사이」(16~35)가 화계 판정에서는 **가운데 구간**으로 떨어져 지위와 무관하게
해요체가 됐다. 같은 「대등」 칸 안에서도 p=45를 넘는 순간 말투가 바뀌어,
학습자가 무엇을 움직였는지 알 수 없었다.

**학습자가 보는 경계와 코드가 쓰는 경계는 같아야 한다.** 둘 다 35·65를 쓰게 했다.
논문 〈표 4-x〉와 **10201칸이 한 칸도 안 틀리게** 맞는다.
(표에서 한 칸만 고쳤다 — 「아는 사이 · 대등」. 표대로면 둘 다 합쇼체인데
아는 사이 동급끼리 「알겠습니다」는 어색하다. 합쇼체는 친밀도가 아니라 **격식성**에
붙는 말이라, 창구·접수 같은 공적 자리에서는 맞지만 아는 사이의 사적 대화에서는 과하다.)

이제 이렇게 나온다.

```
아는 사이 · 하급자    나는 ~습니다 · 호아랑은 ~아요/어요
아는 사이 · 대등      ~아요/어요로 말해 보세요
아는 사이 · 상급자    나는 ~아요/어요 · 호아랑은 ~습니다
```

비대칭이 드러나는 칸이 8칸 → **12칸**으로 늘었다. 비상호적 경어법은 학습자가
가장 어려워하는 자리이고, 그것을 보여 줄 자리가 그만큼 늘었다는 뜻이다.

### ⓑ 고쳐 놓고 배포에 안 실릴 뻔했다

`app.html` 을 고쳤는데 **`app.html.gz` 를 다시 굽지 않았다.**
서버는 gz 를 먼저 읽으므로, 그대로 올렸으면 **고치기 전 코드가 돌았을 것**이다.
고친 것과 올라가는 것이 다른 상태 — 가장 찾기 어려운 종류다.

> 배운 것: 화면 파일을 고치면 **gz 도 같이 굽는다.** 이건 손으로 기억할 일이 아니라
> 검사가 봐야 한다. `qa_all.py` ⑫가 이미 「app.html.gz 최신」을 보고 있었는데
> 크기만 보고 **내용은 안 보고 있었다.**

### ⓒ 검사 셋이 v134에서 한꺼번에 깨졌다 — 셋 다 같은 병이었다

**ㄱ) 화면 JS 를 파이썬으로 다시 구현해 두었다.**
`_jsrun()` 이 정규식으로 JS 를 뜯어 조건식을 흉내 내고 있었다. 두 줄일 때는 됐지만
함수가 조금 자라자 그대로 깨졌다. **흉내 내지 말고 노드로 돌린다.**
덤으로 25칸 표본이 아니라 **10201칸 전부**를 대조하게 됐다 —
어긋나기 쉬운 곳은 경계값 언저리인데, 25칸 표본은 그걸 비켜 간다.

**ㄴ) 상수를 검사에 손으로 베껴 두었다.** `_sv = {"SPEECH_CLOSE": 60, ...}`.
원본이 바뀌어도 검사는 옛 숫자를 들고 있었다. 이제 `main.py` 에서 읽는다.

**ㄷ) 검사가 옛 결정을 정답으로 박아 두고 있었다.**
⑲가 「합쇼체는 낯선 사이 칸에서만 (눈금 15)」를 통과 조건으로 걸어 두고 있었다.
방금 되돌린 바로 그 결정이다. **네 번째다.**

> 배운 것: 셋 다 「검사가 원본을 **베끼거나 흉내 낸 것**」이다.
> 베낀 것은 원본이 바뀔 때 같이 안 바뀌고, 그러면 **검사가 거짓말을 한다.**
> 검사는 원본을 **읽거나 돌려야** 한다. 옮겨 적으면 안 된다.
> 그리고 결정이 바뀌면 검사도 바꾸되, **왜 바뀌었는지를 검사에 적어** 두어야
> 다음 사람이 「옛날엔 이랬구나」로 오해하지 않는다.

### ⓓ 검사

`qa_all.py` ⑯에 「아는 사이에서 지위가 화계를 가르는가」를 항목으로 넣었다.
이번 문제를 **직접** 겨누는 항목이다 — 되돌아가면 바로 걸린다.

---

## v133 — 2026-08-19 · 학습 화면 손보기 여덟 · 「흐름 짜기」도 여기서 배운다

### ⓐ ★★ 이전 단추가 안 보이던 **진짜** 까닭
v131에서 「감추지 말고 흐리게」로 고쳤는데도 그대로였다. 원인이 딴 데 있었다.

```css
.export-btn { display: none !important; }   /* 전역 */
```

v84에서 **대화 중에만** 내보내기 단추를 감추려던 규칙이 블록 밖으로 새어 나와 **전역**이 되어 있었다. 그래서 `#histExportBtn` 에만 예외를 뚫어 두었던 것이다(v92 주석이 그 흔적이다).

내 학습 화면 단추가 그 이름표를 쓰고 있었으니 **이전·다시 듣기가 통째로 안 보였다.** 그 규칙을 지금 손대면 무엇이 딸려 나올지 모르므로, 학습 화면은 **제 이름표(`.idl-btn`)** 를 쓰게 했다.

### ⓑ 퀘스트 단추와 배경음이 창을 안 따라오던 까닭
`syncOverlayFlag()` 는 `.lang-overlay:not(.hidden)` 을 보므로 이미 맞았는데, 그것을 **부르는** 감시 목록에 새 창 둘(`idcOverlay`·`idlOverlay`)이 빠져 있었다. 넣었다.

배경음도 같은 자리에서 갈린다 — **여기서는 홈 배경음을 끄고 대화 배경음(`chat_bgm`, 가야금)** 을 깐다. 여기도 대화를 듣는 자리다.

### ⓒ 다음으로 넘어갔는데 이전 대화가 계속 들렸다
재생 반복문이 **자기 걸음을 안 보고** 끝까지 돌았다. 걸음을 옮길 때 `idlHalt()` 를 먼저 부르고, 반복문 안에서도 `idl.step !== atStep` 이면 멈춘다. 두 겹으로 막았다.

### ⓓ 맞히면 호아랑이 옆에서 빼꼼
글자로만 「맞아요!」가 뜨던 것을 `hamPeekHtml` 로 바꿨다. 사람이 알려 주는 편이 낫다. 다음 걸음으로 넘어가는 틈도 0.7 → 1.4초로 늘려 볼 겨를을 주었다.

### ⓔ ★ 마이크가 켜졌다 꺼졌다 하던 까닭
`pointerdown`/`pointerup` 만 달아 두었다. 셋이 새고 있었다.

- 손가락이 단추 밖으로 나가면 **뗀 신호를 못 받아** 계속 녹음
- 손을 떼자마자 끊어 **끝음절이 잘림**
- 신호를 놓치면 **영영 녹음 상태**

**주제·자유 대화의 말하기 단추가 이미 셋을 다 막고 있었다** — `setPointerCapture` · 꼬리 250ms · 안전장치(30초) · `window` 의 `pointerup` 받이. 그대로 가져왔다. 또 만들어 둔 장치를 새 화면이 안 쓰고 있었다(v129·v130에 이어 세 번째다).

### ⓕ ⑤ 사용 — 보여 줄 것을 줄였다
④에서 이미 문형을 보여 주었는데 ⑤에서 또 늘어놓고 있었다(cue·목표·번역·뒤반응·안내·다시듣기 단추). **말할 것 앞에 글이 많으면 입이 안 떨어진다.**

남긴 것은 둘뿐 — **상대가 방금 한 말**과 **내가 할 말**. 번역·뒤따르는 반응·다시듣기 단추를 뺐다(말풍선을 누르면 들린다).

### ⓖ ★ 「대화의 흐름 짜기」도 여기서 배운다
지금까지 기능 단계는 「대화 전체의 흐름이라 발화 하나로 못 가르친다」며 교실로 넘겼다. 저자가 바로잡았다.

> **발화 하나로는 못 가르치지만 대화 한 판으로는 가르칠 수 있다.**
> 지난 주제 대화에는 기능단계가 처음부터 끝까지 들어 있다.

- 표시할 줄은 **한 단계에서 다음 단계로 넘어가는 그 줄**을 잡는다(인사→용건, 용건→마무리). 그 자리에 놓이는 말이 곧 형태다.
- 그때는 대화문을 8~10줄로 조금 길게 잡아 **처음과 끝이 다 보이게** 한다.
- ④ 형태는 퀘스트 문형이 없으므로 **단계 표지 표현**을 화계 3단으로 따로 두었다(「그럼 이제 ~요」「마지막으로 하나만 더요」).

**여기서 배우는 것 여덟, 교실은 「몸으로 말하기」 하나뿐이다.**

### 검증
검사 30종 + 통합 점검 + `namecheck` + `fittest` 전부 통과. `idctest2` 에 ⑩ 「창 안의 살림」 10종을 더했다 — 단추 이름표·창 따라가기·재생 멈춤·마이크 손놀림·⑤ 군더더기까지 코드 자리로 잰다.

## v132 — 2026-08-19 · 학습 대화문은 **주제 대화 기록에서만**, 그리고 **반드시**

### ⓐ 자유 대화 기록은 쓰지 않는다
v130에서 주제 대화와 자유 대화를 다 끌어왔다. 주제 대화만 쓰기로 좁혔다.

**자유 대화는 자리도 상대도 그때그때 다르다.** 「어디서 누구와 있었던 일인가」가 흐리니 ⓪ 들어가기 한 줄(「카페에서 친구를 만났어요」)이 서지 않는다. 학습 자료가 되려면 **자리가 있어야 한다.**

주제 대화는 장소·역할·목적이 정해져 있어, 그 대목을 떼어 와도 자리가 그대로 따라온다.

```js
loadHistory().filter(h => h && h.mode === "rp")
```

### ⓑ ★ 기록이 있으면 **반드시** 거기서
v130의 프롬프트에 「쓸 만한 대목이 정말 없으면 새로 지어도 된다」는 빠져나갈 구멍을 열어 두었다. 모델은 그런 구멍을 잘 찾는다 — 「딱 맞는 대목이 없다」는 이유로 새로 지으면 이 화면의 요점이 사라진다.

> 위 대화가 주어졌으면 **반드시 거기에서 골라라.** 새로 짓는 것은 기록이 **아예 없을 때뿐**이다.
> 「딱 맞는 대목이 없다」는 이유로 새로 짓지 마라 — 맞는 대목이 없으면 **가장 가까운 대목을 골라 그 자리에 넣어** 만들면 된다.
> 그것이 이 화면의 요점이다. 학습자가 **자기가 한 말**에서 배워야 한다.

### ⓒ 그래도 새로 지으면 밖에서 보인다
프롬프트를 못 박아도 모델이 안 따를 수 있다. **말로만 못 박고 확인하지 않으면 v122~v123 꼴이 난다.**

`/version` 의 `idc` 칸에 두 값을 더했다.

```
"idc": { "ok": 12, "mine": 11, "miss": 1, ... }
         만든 판   내 대화   기록을 줬는데도 새로 지은 판
```

`miss` 가 쌓이면 프롬프트가 안 먹은 것이다. 서버 로그에도 `⚠` 로 남는다.

### 검증
검사 30종 + 통합 점검 + `namecheck` + `fittest` 전부 통과.

## v131 — 2026-08-19 · 학습 화면 겉모습 · 아홉 요소를 모두 보인다

### ⓐ ★ 홈 카드 이름이 비어 있던 진짜 까닭 — 또 삼킨 예외
`IDC_TXT`(문구 표)는 파일 **뒤쪽**에 `const` 로 있는데, 화면 문구를 칠하는 함수는 **앞쪽**에서 돈다. 아직 만들어지지 않은 것을 부르니 `ReferenceError` 가 났고, **내가 감싸 둔 `try/catch` 가 그것을 삼켜** 빈 칸이 됐다.

**v124의 `native` 와 똑같은 종류다.** 삼키는 자리를 또 만들었다. 게다가 배경까지 옅은 베이지여서 흰 글씨(`.home-card { color:#fff }`)가 묻혔다 — 두 겹으로 안 보였던 것이다.

→ 홈 카드가 쓸 두 줄만 담은 표(`HOME_IDC`, 18개 언어)를 **쓰는 곳보다 앞에** 두었다. 이름은 **「한국어 대화 상호작용 능력」**.

**그리고 `cardtest.js` 를 새로 만들었다.** jsdom 에 `fetch` 를 가짜로 물려 페이지를 **실제로 돌린 뒤** 홈 카드 세 칸이 다 채워졌는지 본다. 문자열 검사로는 이 종류를 영영 못 잡는다 — v124에서 배운 것을 화면 쪽에도 세웠다.

### ⓑ 겉모습을 주제·자유 대화와 같은 꼴로
| | 바탕 |
|---|---|
| 주제 대화 | 청록 `#2A4A55 → #4A7C74` |
| 자유 대화 | 갈색 `#7E5238 → #9C6A45` |
| **상호작용 능력** | **오방색의 청(靑) `#22356B → #3A5AA0`** |

청은 동방·봄·시작이라 **배움의 첫 자리**에 맞고, 앞의 둘과도 확실히 갈린다.

빗장 무늬는 그대로 두고 **아자무늬(亞字, 한옥 창살)** 를 한 겹 더 얹었다. 완자무늬(卍)는 외국인 학습자에게 오해를 살 수 있어 피했다.

### ⓒ ★ 이모지를 걷어냈다 — 그림으로
저자가 여러 번 말한 것을 새 화면을 만들며 또 어겼다. **SVG 열 개를 새로 그렸다.**

사다리(홈 카드) 하나와 요소 아홉. 아홉은 **모두 말풍선 변주**로 통일했다 — 마주 본 말풍선 둘(말 주고받기), 돌아오는 화살표(차례), 끊어진 선을 잇는 모양(단절 수정), 막힌 것을 돌아가는 곡선(전략), 크기 다른 말풍선 둘(맥락·존대), 계단 넷(기능 단계), 점선 말풍선(비언어 — 소리가 아니다).

기존 아이콘과 같은 스트로크 규격(`fill:none`, `stroke:currentColor`)이라 화면 어디에 놓아도 어울린다.

### ⓓ ★★ 아홉 요소를 **모두** 보인다
지금까지 챗봇에서 배울 수 있는 일곱만 보였다. 저자가 바로잡았다 — **못 배우는 것도 보여야 한다.**

> 아예 안 보이면 「없는 것」이 되어 버린다. 학습자가 상호작용 대화 능력의 **전체 모습**을 알 수 없다.

- **여기서 배운다** (일곱) — 누를 수 있고 도장이 찬다
- **교실에서** (둘) — 점선 테두리, 흐린 빛깔, 눌리지 않음
  - 기능 단계의 조직과 흐름 — 대화 **전체의 흐름**이라 발화 하나로 가르칠 수 없다(주제 대화의 진행률이 맡는다)
  - 비언어적 행위 — 시선·표정·몸짓이라 음성 대화에서 관찰할 수 없다

**화면에 〈표 34〉의 매체 배분이 그대로 드러난다.** 논문의 표가 학습자의 화면이 되는 셈이다.

### 잔 실수 하나
파이썬 f-문자열로 CSS 를 만들며 `0%%` 를 그대로 흘렸다. `%` 는 f-문자열에서 특별하지 않은데 `%`-서식으로 착각했다. `csscheck` 가 「`)` 가 있어야 한다」로 잡았다.

### 검증
검사 **30종**(`cardtest.js` 새로) + 통합 점검 + `namecheck` + `fittest` 전부 통과.

## v130 — 2026-08-19 · ★ 학습 대화문을 학습자 자신의 대화에서 끌어온다

### ⓐ ★★ 대화문의 재료가 바뀌었다 — LLM이 짓지 않고, **내가 한 말**에서
지금까지 학습 대화문은 LLM이 매번 새로 지었다. 이제 **학습자가 실제로 나눈 대화**(주제 대화·자유 대화 기록)에서 끌어온다.

값이 셋이다.

1. **자기가 한 말이라 「저게 나였구나」가 된다.** 남의 이야기가 아니다.
2. **그 학습자의 어휘·말투·자주 가는 자리**가 그대로 재료가 된다.
3. **자료의 진정성** — 지어낸 교재 대화문이 아니라 실제로 오간 말이다.

지난 대화는 이미 기기에 남아 있었다(`masamasaHistory`, 최근 30판). 그 기록문에서 `[시각] 나/호아랑:` 다음 줄만 뽑아 최근 60줄을 서버에 함께 보낸다. 서버는

- 그 대화에서 **가르칠 것이 드러나는, 또는 드러날 뻔한 대목**을 하나 고르고
- 앞뒤로 늘려 6~8줄로 다듬되 **학습자가 쓴 말은 되도록 그대로 살린다**(맞춤법·어색한 곳만)
- 학습자가 그 자리에서 **하지 못한** 것을 가르칠 것이라면 그 자리에 넣어 **「이렇게 했으면 좋았을 대화」**로 만든다

쓸 만한 대목이 없거나 기록이 없으면(처음 온 학습자) 새로 짓는다. 어디서 왔는지는 `from` 으로 돌려주고, 화면 ⓪에 **「지난번 내 대화에서 가져왔어요」** 딱지가 붙는다.

**논문에서** — v129에서 「LLM이 짓는다」를 DA 계보로 방어했는데, 이제 그 방어가 필요 없는 자리가 생겼다. 원형(〈표 33〉)은 연구자가 정하고 **사례는 학습자 자신의 발화**다.

### ⓑ 대화문 재생이 부자연스러웠던 이유
`ttsPlay(text)`만 이어 붙이고 있었다. 그래서

- 두 사람이 **같은 목소리**로 말해 누가 말하는지 알 수 없었고
- 줄 사이 간격이 **220ms 고정**이라 긴 줄에서 겹치고 짧은 줄에서 늘어졌다

**주제 대화의 「들어보기」(`scPlayAll`)가 이미 셋을 다 하고 있었다** — 역할별 목소리(`scMyVoice()`), 글자 수로 어림잡은 기다림(`900 + len × 165`, 최대 7초), 다음 줄 미리 받기(`ttsPrefetch`). 그대로 가져왔다. 재생 중인 줄에 테두리가 생기고, 창을 닫으면 소리도 멈춘다.

**만들어 둔 장치를 새 화면이 또 안 쓰고 있었다.** v129의 구어체 규칙과 같은 종류의 빠뜨림이다.

### ⓒ 「이전 단추가 없다」
첫 걸음에서 `visibility: hidden` 으로 감췄다. **감추면 있는지 없는지 알 수가 없다.** 자리에 두되 흐리게(`opacity .35`) + `disabled` 로 바꿨다.

단추 줄도 `position: sticky` 로 바닥에 붙였다. 몸통만 구르고 단추는 늘 손 닿는 자리에 남는다 — 휴대폰에서 아래로 밀려 안 보이던 것도 함께 막힌다.

### 검증
검사 29종 + 통합 점검 + `namecheck` + `fittest` 전부 통과. `idctest2`에 재생·이전 단추·내 대화 14종을 더했다. 기록문에서 발화를 뽑는 정규식은 **「(말 끊김)」 표시가 붙은 줄도 뽑히는지**까지 따로 돌려 확인했다.

### 남은 것
- 홈 카드 이름·오방색·아자무늬·이모지 걷어내기 (VM이 끊겨 미뤘다)
- IDC **아홉 요소 전부** 보이기 (교실에서 배우는 둘 포함)
- 동기부여 들머리 화면 — 프롬프트는 `동기부여화면_프롬프트.md` 에 있다

## v129 — 2026-08-19 · 학습 화면 (2/3) — 형태와 사용 · 선행 연구를 대고 다시 본 것

### ⓐ 이론 틀을 바로잡았다 — **OHP(관찰–가설–실험)**
v128에서 나는 ⓪의 근거로 스키마 이론(하향식 듣기 모형)을 댔다. **겨냥이 어긋난 근거였다.** 하향식 듣기 모형은 **이해(comprehension)** 이론인데, 이 화면이 겨누는 것은 이해가 아니라 알아차림이다.

저자가 바로잡았다 — **OHP(Observe–Hypothesize–Experiment)** 다. 화면의 걸음이 그대로 대응한다.

| 걸음 | OHP |
|---|---|
| ① 듣기 | **관찰(Observe)** |
| ② 추측 | **가설(Hypothesize)** |
| ⑤ 발화 연습 | **실험(Experiment)** |

③ 의미와 ④ 형태는 가설을 확인해 주는 자리다. 스키마 이론보다 **네 걸음이 훨씬 정확히 맞물린다.**

### ⓑ 내가 틀린 지적 하나 — 「하향식」 용어 충돌
화용 교수 문헌 한 곳이 "top down"을 **연역**의 뜻으로 느슨하게 쓴 것을 보고, 「논문에서 이 말을 쓰면 위험하다」고 과하게 경고했다. **틀린 지적이었다.**

한국어교육의 표준 정의는 이렇다 — **형태(작은 단위) 먼저 = 상향식**, **대화문 전체(맥락)에서 세부 형태로 = 하향식**. 우리 설계는 정확히 하향식이다. **「하향식」을 그대로 쓴다.**

### ⓒ 계보는 CA가 아니라 DA — 「LLM이 지은 대화문」 방어
CA-SLA는 **자연 발화 자료**를 요구하므로, 내가 「LLM이 지은 대화문이라 진정성이 없다」고 지적했다. 그런데 저자 연구의 계보는 **DA(담화분석)** 다. CA는 완전히 귀납적이지만 **DA는 연역적이고, 원형에서 사례로 내려온다.**

> **LLM이 만드는 것은 원형이 아니라 원형의 실현 사례다.** 원형은 연구자가 정했다 — 〈표 33〉 IDC 구성 요소, 〈표 34〉 매체 배분, 이남호·이찬규(2025) 기능단계 분석. 코드에서도 요소별 `gist`(무엇을 하는 것인가)는 **고정된 서술**이고, LLM은 그 서술을 만족하는 대화를 생성할 뿐이다.

구멍이 아니라 계보다.

### ⓓ ★ 내가 실제로 빠뜨린 것 — 구어체 규칙을 안 물려줬다
학습 대화문 프롬프트가 챗봇의 `SPOKEN_RULES`(문어체 금지, 담화표지, 조각문 …)를 **한 줄도 물려받지 않고 있었다.** 이미 만들어 둔 규칙을 새 화면만 못 쓰고 있었다. 프롬프트 맨 앞에 얹었다.

교재 대화문이 구어성을 못 담는다는 오랜 논의를 피하려면 **이미 있는 장치부터 쓰는 것**이 먼저였다.

### ⓔ ★ 선택지를 셋으로 — 둘이면 찍어도 절반이다
「첫 시도 정답률 = 배우기 전에 이미 알고 있었는가의 지표」라고 해 놓고 **두 갈래**로 물었다. 아무렇게나 눌러도 **절반이 맞는다.** 지표가 오염되고, 학습자는 알아차리지 않고 눌러 본다.

→ **셋으로.** 기저율이 50% → 33%로 내려간다. 오답 둘은 서로 달라야 하고, 둘 다 「이 힘을 모르는 사람이 실제로 하는 오해」여야 한다. 자리는 서버에서 Fisher–Yates로 섞는다.

### ⓕ ④ 형태 — 그제야 문형
`QUEST_BY_EL`(요소 → 문형 뒤집기 표)로 그 요소의 문형을 모아, **지금 화계에 맞춰** 낸다. 눌러서 들을 수 있다. v120에 `QUEST_FORM`을 지우지 않고 남겨 둔 것이 여기서 쓰인다.

### ⓖ ⑤ 사용 — 방금 관찰한 자리에서 이어서
`POST /idc-drill` 이 **방금 들은 대화문과 자리를 그대로 물려받아** 말차례 연쇄 둘을 만든다. 새 상황을 또 만들면 **관찰–가설–실험의 흐름이 끊긴다** — 프롬프트에 「새 상황을 만들지 마라」를 못 박았다.

- 마이크는 **발화 연습과 같은 길**(`/stt`, 목표 문장을 힌트로)을 쓰고 **같은 잣대**(`simScore ≥ 0.76`)로 잰다. 두 화면의 판정이 달라서는 안 된다.
- 연쇄 둘은 서로 다른 자리(cue 있음 / 없음 · follow 있음)로 만든다.

### ⓗ 하지 않기로 한 것
**A/B 배정(귀납 vs 연역)** 을 제안했으나 저자가 「귀납적 학습 이론의 장점을 근거로 택한다」고 정했다. 넣지 않는다. 명시적>암시적은 robust하나 연역vs귀납은 미결이므로, 설계 근거로 서는 데 문제가 없다.

### 검증
- 검사 **29종** + 통합 점검 ㉓ + `namecheck` + `fittest` 전부 통과.
- `idctest2`에 형태·사용 8종을 더했다. 특히 **「④가 ③ 뒤에 오는가」·「⑤가 맨 뒤인가」** 를 `idlPaint` 안의 **자리로** 잰다 — 순서가 뒤집히면 이 화면은 뜻이 없다.
- `domcheck`가 `idlFb`를 「HTML에 없는 id」로 잡았다. **코드가 만들어 붙이는 자리**는 HTML에 없는 것이 정상이다 — `.id = "..."` 로 지어 넣는 것을 알아보게 고쳤다. 예외 목록에 이름을 적는 대신 **성질로** 가렸다.

### 남은 것 (v130)
결과 화면에 「배운 것 / 해낸 것」 두 줄 · 학습 전후 실현 대조표 · 실제 대화 로그를 대화문의 씨앗으로

## v128 — 2026-08-19 · ★★ 「한국어 상호작용 대화 능력」 학습 화면 (1/3)

### ⓐ 이 화면이 논문에서 서는 자리
v127까지 챗봇은 〈표 34〉의 ◎(AI 전담)를 **상황 적용 단계에서만** 다뤘다. 학습자는 요소를 *쓰면서* 배웠지, 요소가 **무엇인지** 배운 적이 없다. 넛지가 「시간을 끌어 보세요!」라고 알려도 왜 필요한지는 교실에서만 다뤄졌다.

이 화면은 **제시–연습–적용 가운데 「제시」와 「연습」을 챗봇으로 끌어온다.** 이남호·이찬규(2024) 모형에서 챗봇이 상황 적용만 맡던 구도가 달라진다.

### ⓑ ★ 하향식 모형(Top-down Processing)을 채택했다
첫 설계안은 **설명이 먼저**였다(상향식). 저자가 하향식으로 잡으면서 순서가 뒤집혔다.

```
   1차안(상향식)          2차안 = 채택 (하향식)
   ─────────────          ────────────────────
   설명을 듣는다     →     ⓪ 상황 한 줄 (배경지식 활성화)
   대화문에서 찾는다        ① 대화를 통째로 듣는다  ← 표시 없이
   연습한다                ② 왜 그렇게 말했을까 (ⓐ/ⓑ)
                          ③ 아, 그거였구나 (의미)  ← 여기서 불이 켜진다
                          ④⑤ 형태·사용 (v129)
```

**근거.** 학습자는 이미 대화를 할 줄 안다 — 모국어로 수없이 해 왔다. 없는 것은 **한국어로 그것을 하는 방법**이다. 설명부터 주면 아는 것을 다시 가르치는 셈이다.

**⓪이 왜 필수인가.** 스키마 이론(Rumelhart 1980)에서 하향식 처리는 배경지식이 **활성화되어 있어야** 작동한다. 활성화 없이 듣기를 시작하면 낱말부터 듣는 상향식으로 떨어진다. 듣기 지도의 '듣기 전–듣기–듣기 후' 3단계(Underwood 1989; Anderson & Lynch 1988)도 같은 자리다. ⓪을 빼면 「하향식을 채택했다」가 성립하지 않는다.
※ **판본·쪽수는 저자가 확인해야 한다.** 여기에 적은 것은 통상적 기술이지 검증된 서지사항이 아니다.

### ⓒ ★ 표시(강조)는 추측이 **끝난 뒤에만** 켠다
①에서 배울 줄에 불을 켜면 **답을 주는 것**이고, 그러면 추측할 거리가 없어진다. 표시는 상(賞)이다 — 맞히고 나서 「아, 여기였구나」가 되어야 한다.

검사 `idctest2.js`가 이것을 지킨다. `idlScript(false)`가 ①에, `idlScript(true)`가 ③에 있는지, 그리고 ①에서 `true`가 새어 들어가지 않았는지를 본다. **이 화면의 핵심이라 코드보다 검사를 먼저 못 박았다.**

### ⓓ 두 갈래 물음의 설계
오답이 **그럴듯해야** 한다. 「배가 고파서」 같은 것은 고르는 재미도 배울 것도 없다. 프롬프트에 나쁜 예·좋은 예를 나란히 넣었다.

> 나쁜 예) "졸려서" / 좋은 예) "대답하고 싶지 않아서"

오답은 **이 힘을 모르는 사람이 실제로 하는 오해**여야 한다. 그래야 틀렸을 때 배운다. 틀려도 나무라지 않고 「그렇게 볼 수도 있어요. 그런데 ~」로 잇고, 다시 고를 수 있다. **맞혀야 다음으로 넘어간다.**

정답 자리는 서버에서 섞는다(`os.urandom`) — 늘 ⓐ면 눌러 보고 안다.

### ⓔ 요소 이름 — 쉬운 이름 크게, 학술어 아주 작게
`stage`(기능 단계 — 대화 전체의 흐름이라 발화 하나로 못 가르친다)와 `nonverbal`(음성 대화에서 관찰 불가)을 빼고 **일곱**이다.

| 크게 (15.5px) | 아주 작게 (9.5px) |
|---|---|
| 말 주고받기 | 대화이동 관리 |
| 이야깃거리 다루기 | 화제 관리 |
| 말할 차례 주고받기 | 차례 관리 |
| 막혔을 때 풀기 | 의사소통 단절 수정 |
| 모르는 말 넘어가기 | 의사소통 전략 |
| 듣고 있다고 알려 주기 | 상호작용적 듣기 |
| 상대에 맞춰 말하기 | 맥락·정체성 인식 |

전부 **동사구**로 잡았다 — 「관리」 같은 명사는 중급이 못 읽는다. 학습자와 연구자가 **같은 카드**를 본다.

### ⓕ 정의는 고정, 사례는 변화
| | 매번 새로 | 왜 |
|---|---|---|
| 뜻풀이 | ✗ 캐시 | 설명이 매번 다르면 배우는 사람이 헷갈린다 |
| 상황·대화문·물음 | **○** | 같은 걸 두 번 보면 외운다 (`temperature=0.9`, 최근 여섯 자리를 피함) |
| 화계 | ○ | 홈 페이더를 따름 — 셋을 다 만나야 한다 |

뜻풀이와 대화문을 `asyncio.gather`로 나란히 만든다. 뜻풀이가 캐시에 있으면 대화문만 기다린다.

**뜻풀이에 학술어를 못 쓰게 막았다** — 화행·레지스터·담화·화계·상호작용 등. 총평 프롬프트의 금지 목록과 같은 자리다.

### ⓖ ★ 학습 기록 — 논문 자료가 되는 곳
기기마다 **익명 딱지 하나**(`devId`)뿐. 이름도 연락처도 받지 않는다.

```
POST /idc-learn  { dev, lang, el, step, correct, tries, ms, d, p, tier }
GET  /idc-stats  → 요소별 배움 횟수 · 첫 시도 정답률 · 사람 수
GET  /idc-stats?csv=1  → 내려받기
```

쓸 수 있는 것이 셋이다.

1. **첫 시도 정답률** — 그 요소를 **배우기 전에 이미 알고 있었는지**의 지표. 요소별로 갈리면 그 자체가 발견이다.
2. **★ 배운 뒤 대화에서 실현이 느는가** — 서버는 이미 대화마다 `idcCounts`를 센다. 학습 시각을 남기면 **같은 학습자의 학습 전/후를 가를 수 있다.** 「챗봇이 제시·연습까지 맡을 수 있는가」에 대한 직접 증거다.
3. **어디서 그만두는가** — 네 걸음 중 몇에서 나가는지. 설계가 긴지 짧은지를 말해 준다.

**Render의 디스크는 판을 올릴 때마다 지워진다.** 논문 자료를 거기 두면 안 된다 → 스무 줄마다 **구글 드라이브**로 올린다(기존 녹음 업로더를 그대로 씀). 올리기가 실패하면 **도로 넣어 두고** 다음에 다시 시도한다.

`/version`에 `idc` 칸을 두었다 — v124의 교훈대로, 예외를 삼키는 자리를 만들면 밖에서 셀 수 있어야 한다.

### 남은 것 (v129·v130)
- **v129** — ④ 형태(`QUEST_FORM`을 화계에 맞춰) · ⑤ 발화 연습 연결 · 결과 화면에 「배운 것 / 해낸 것」 두 줄
- **v130** — 학습 전후 실현 대조표 · 연구자용 내려받기 다듬기

### 검증
검사 **29종**(`idctest2.js` 새로) + 통합 점검 ㉓ 17종 + `namecheck` + `fittest` 전부 통과.

## v127 — 2026-08-18 · 마무리 영상에서 제미나이 표시를 지웠다

`static/outro.mp4` 오른쪽 아래에 Veo(제미나이) 워터마크 ✦ 가 6.2초 내내 박혀 있었다.

- **잘라 내지 않고 `delogo` 로 지웠다.** 그 자리가 평평한 다다미 바닥이라 둘레 화소로 메우면 티가 안 난다. 잘라 내면 화면비가 바뀌어 결과 카드 안에서 다시 맞춰야 한다.
- 1.0초·3.0초·5.9초 세 곳을 원본과 나란히 놓고 확인했다. 전부 사라졌다.
- 다시 인코딩하며 오히려 가벼워졌다 — **422KB → 312KB**(CRF 29, veryslow, 소리 없음).
- 캐시 무효화: `outro.mp4?v=103 → ?v=127`, `outro.jpg` 도 같이. 기기에 남은 옛 영상이 계속 나오면 안 된다.

## v126 — 2026-08-18 · 막을 것과 막지 말 것을 갈라 적었다

### ⓐ v125의 문장이 도리어 함정이 될 뻔했다
v125에서 「실제로 법에 걸리는 일, **미성년자가 얽힌 일**, 상대를 해치는 일은 하지 않는다」고 적었다. 저자가 바로 짚었다 — **데이트 신청까지 막히면 안 된다**는 것이 애초의 요지였는데, 저 문장이 그것을 도로 막을 수 있다. 호아랑의 본디 배역이 「갓 쓴 아기 호랑이」라, 모델이 상대역까지 어린아이로 읽으면 「미성년자가 얽힌 일」에 스스로 걸린다.

**막지 말 것을 이름으로 적었다.**

> 데이트·약속 신청, 연락처 묻기, 고백, 값 깎기, 항의·따지기, 환불 요구, 부탁·조르기, 거절 뒤 다시 청하기, 사과 받아내기, 이별 통보, 험담 들어 주기
> — 한국어로 이런 말을 해 보려고 온 사람에게 「그건 좀…」이라고 하면 연습이 안 된다.

그리고 나이를 못 박았다.

- **배역은 학습자가 달리 정하지 않았으면 성인이다.** 호아랑이 본디 아기 호랑이라는 것과 지금 맡은 배역의 나이는 별개다. 아르바이트생·점원·손님·이웃은 모두 어른이다.
- 막을 것은 **실제로 사람이 다치는 일**뿐 — 진짜 범죄를 꾸미거나, 학습자가 상대를 미성년자로 **명시했는데** 성적인 이야기로 가거나, 실제 인물을 해치려는 때.

### ⓑ `/voicepick` 을 세 걸음으로 적었다
「어떻게 하는 거야」라는 물음이 당연했다. 페이지에 **고른 이름 칸 + 복사 단추**를 두고, Render에 넣는 법을 순서대로 적었다.

1. 들어 보기를 누르면 그 이름이 칸에 들어간다 → **복사**
2. Render → 이 서비스 → **Environment**
3. **Add Environment Variable** → `TTS_VOICE` = 복사한 이름 → **Save Changes** (1~2분 뒤 반영)

되돌리려면 그 변수를 지우면 된다. 파일을 다시 올릴 필요는 없다.

### ⓒ `sndtest` 가 조용히 건너뛰고 있었다
곡 목록에 `bgm_spring.mp3` 가 있는데 **프로젝트 static/ 에 그 파일이 없다.** 그런데도 검사가 통과했다 — `static/` 이 없는 자리에서 돌리면 파일 검사가 **말없이 지나갔기** 때문이다.

건너뛰는 것 자체는 옳다(프로젝트 폴더는 바뀐 것만 담는 staging이다). 다만 **건너뛴다고 말은 해야 한다.** 이제 「⚠ static/ 에 mp3 가 없어 파일 검사는 건너뜁니다」라고 찍는다. 삼킨 예외와 같은 종류의 문제다(v124).

확인해 보니 `bgm_spring.mp3`(954KB)는 **「깃헙에 올릴 파일」 폴더의 static/ 에 들어 있다.** 프로젝트 폴더의 static/ 이 바뀐 것만 담는 staging이라 거기에만 없었을 뿐이다. 네 곡 모두 배포 폴더에 있으니 그대로 올리면 된다.

### 검증
검사 28종 + 통합 점검 + `namecheck` + `fittest` 전부 통과.

## v125 — 2026-08-18 · 점수·배역의 태도·목소리

### ⓐ ★ 단계를 건너뛰었는데 100점이 나왔다
다섯 단계 중 셋만 밟았는데(가운데 둘을 건너뛰었는데) **100점**이었다.

```python
task_complete = rp_progress["total"] and (
    len(rp_progress["done"]) == rp_progress["total"]
    or last_idx in rp_progress["done"]        # ← 이 줄
)
```

**마지막 단계에 닿았다는 것만으로 과업 달성으로 쳤다.** 기능단계는 목적을 이루려면 **거쳐야 하는 자리**다. 건너뛴 것은 못 한 것이다. 뒷줄을 지웠다 — 이제 전부 밟아야 100점이고, 그 전에는 밟은 만큼만 준다.

**결과 화면의 글씨도 자리를 바로잡았다.** 예전에 「점수를 키워 달라」고 하신 것을 내가 점수 칸에 적용했는데, 실제로 말씀하신 곳은 **상호작용 대화 능력** 부분이었다. 요소 이름 12 → 15.5px, 까닭 11 → 13px, 등급 딱지도 키웠다. 결과에서 가장 오래 읽는 자리다.

### ⓑ ★ 배역이 학습자의 목적을 막고 있었다
카페 아르바이트생에게 데이트를 신청하는 과업에서, **배역이 끝내 거절해서 목적이 무산됐다.** 연습하러 온 사람이 목적을 이루지 못하면 그 연습은 무엇을 위한 것인가.

상황극 프롬프트 맨 앞에 못 박았다.

- **달성 목적은 학습자의 것이다.** 학습자가 그 목적을 향해 제대로 말하면 **결국 이루어진다.** 한 번 망설이거나 되물을 수는 있어도 끝내 무산시키지 않는다.
- **훈계하지 마라.** 「그건 좀 곤란한 부탁이에요」처럼 배역 밖에서 가르치려 들지 않는다. 서툴게 말했으면 배역 안에서 되물어 다시 말할 기회를 주면 된다.
- 다만 실제로 법에 걸리는 일·미성년자가 얽힌 일·상대를 해치는 일은 배역 안에서도 하지 않는다. 그때만 배역에 맞게 물러선다.

「배역의 감정을 담아라」(갈등 상황에서 처음부터 부드러우면 안 된다)는 규칙은 그대로 두었다. 감정을 드러내는 것과 **목적을 막는 것**은 다르다.

### ⓒ 목소리 — 성별 지정이 반영되지 않던 이유
계획을 만들며 모델이 **「여알바」를 「아르바이트생」으로 다듬으면서 성별을 지웠다.** `pick_voice`는 다듬어진 이름만 보고 있었으므로 알 길이 없었다.

- 계획에 **학습자가 적은 말 그대로**를 `ai_role_raw`로 보관하고, 목소리를 고를 때 함께 본다.
- 계획 프롬프트에 「ai_role 에서 성별과 나이를 지우지 마라」를 넣었다(「여알바」 → 「여자 아르바이트생」).
- `_ROLE_FEMALE`·`_ROLE_MALE`에 **「여-·남-」으로 줄여 적는 말** 24개를 더했다(여알바·여직원·여친·남알바 …). 학습자는 실제로 이렇게 쓴다.

### ⓓ 목소리 고르기를 두 걸음으로
다섯 개가 한 줄로 늘어서 있어 무엇을 고르는 것인지 알기 어려웠다.

1. **누구의 목소리로?** — 배역에 맞춰 자동(기본) · 남성 · 여성
2. **나이는?** — 아이 · 성인 · 어르신 (성별을 고른 뒤에만 보인다)

자동이면 두 번째 걸음이 아예 안 나온다 — 배역이 정하므로 고를 것이 없다. 여섯 짝이 서버의 열쇠(`boy`·`man`·`elder_m`·`girl`·`woman`·`elder_f`)와 그대로 맞물린다.

### ⓔ `/voicepick` — 귀로 듣고 고른다
연구자들이 「어린 호랑이가 아니라 소년 목소리」라고 했다. 표를 보고 고를 수 있는 것이 아니므로, **후보 13개에 같은 문장을 들려주는 페이지**를 만들었다.

```
https://korean-dic.onrender.com/voicepick
```

고른 이름을 Render 환경변수 `TTS_VOICE`에 넣으면 자유 대화의 호아랑 목소리가 바뀐다. 주제 대화는 배역에 맞춰 따로 고르므로 영향이 없다.

### 검증
검사 28종 + 통합 점검(㉒ 20종 추가) 전부 통과. `namecheck` 통과.

## v124 — 2026-08-18 · ★ 없는 이름 하나가 두 판을 먹었다

### ⓐ 진짜 원인 — `native` 가 그 스코프에 없었다
v122에서 `FOCUS_EG`(요소별 구체 보기)를 넣으며 이렇게 썼다.

```python
_egline = "   " + _eg.replace("{n}", native or "학습자의 모국어") + "\n"
```

**웹소켓 핸들러 스코프에는 `native` 라는 이름이 없다.** `ui_lang`은 있지만 `native = LANG_NAMES.get(ui_lang, "")`는 `build_system_prompt`와 `roleplay_setup` 안에만 있었다. 실행하는 순간 `NameError`다.

그리고 그 자리는 예외를 삼키는 `try` 안이었다.

```python
except Exception as e:
    print(f"[도움말] 생성 실패: {e}")     # ← 로그에만 남고 아무 일도 안 일어난다
```

그래서 **v122·v123 두 판 내내 도움말이 한 번도 만들어지지 않았다.** 화면에는 「이 단계의 추천 표현이 없어요」와, 내가 폴백으로 넣어 둔 미리 쓴 문형(「…난 그래. 너는?」)만 남았다. 저자가 「표현의 성능이 너무 안 좋아졌어」라고 한 것이 이것이다 — 성능이 나빠진 것이 아니라 **아무것도 안 돌고 있었다.**

한 줄로 고쳤다.

### ⓑ 이 종류를 다시는 놓치지 않도록
**`ast.parse`도 `node --check`도 이걸 못 잡는다.** 문법은 멀쩡하기 때문이다. 실행해 봐야만 알 수 있고, 그 실행 경로는 예외에 먹혔다.

`namecheck.py` — 파이썬의 `symtable`로 **스코프를 실제로 계산**한다. 함수 안에서 쓰였는데

- 그 함수의 지역도 아니고
- 감싸는 함수의 지역도 아니고(그러면 free로 잡힌다)
- 모듈 전역도 아니고
- 내장도 아니면

→ **실행하는 순간 NameError**다. `qa_all.py` ㉑번에 넣었다. 되돌려 확인했다 — 그 줄을 빼면 `_handle_session → send_hints(4005줄) → native`를 정확히 짚는다.

### ⓒ 삼키는 자리는 밖에서 보이게
예외를 삼키는 곳이 있으면 **반드시 밖에서 셀 수 있어야 한다.** 총평이 그래서 `/version`에 `review` 칸을 두고 있었는데, 도움말에는 없었다. 이제 `hint` 칸이 있다.

```
"hint": { "ok": 0, "fail": 12, "empty": 0, "last": "NameError: name 'native' is not defined (4005줄)" }
```

- `ok`가 0인데 `fail`이 쌓이면 → **코드가 잘못된 것**이다(v122~v123이 그랬다)
- `ok`는 도는데 `empty`만 늘면 → 모델이 빈손으로 돌아오는 것
- 실패 이유에 **몇 줄인지**까지 남긴다

### 되짚어 — 무엇을 놓쳤나
v122에서 「이제 즉시 나온다」고 보고했다. **실제로 돌려 보지 않고 검사만 보고 그렇게 말했다.** 검사는 전부 「그 문자열이 코드에 있는가」를 보고 있었지, 「그 코드가 실제로 도는가」를 보지 않았다. 문자열 검사는 이런 것을 영영 못 잡는다. ㉑번을 넣은 이유다.

## v123 — 2026-08-18 · ★ 도움말은 누르기 전에 이미 만들어져 있다

### ⓐ v122의 「미리 만들기」가 반쪽이었다
> 「넛지는 한참 전에 사라졌는데 이렇게 말해보세요의 표현이 늦게 생성되면 뭔 의미임.」

맞다. v122는 **넛지가 뜰 때만** 미리 만들었다. 그런데 넛지는 상한·차례 간격·페이딩에 걸려 대부분의 차례에 안 나온다. 넛지 없는 차례에는 v121과 똑같이 **누른 뒤부터** 만들기 시작했다. 그리고 미리 만든 경우에도 서버에만 재워 두어 누르는 순간의 왕복이 남아 있었다.

**고친 곳은 두 군데다.**

1. **호아랑의 차례가 끝날 때마다** 도움말을 만든다. 그때가 곧 학습자의 차례다. 막힐지 안 막힐지는 그때 가서야 알지만, **막힌 뒤에 만들기 시작하면 언제나 늦다.**
2. 만든 것을 서버에 재워 두지 않고 **곧바로 화면에 밀어 보낸다**(`ready: true`). 화면은 들고만 있다가 「도와줘」를 누르는 순간 **왕복도 로딩도 없이** 편다.

내가 말을 시작하면(`startTalking`) 들고 있던 것은 버린다 — 지난 자리의 것이다.

부수 효과 하나. 미리 만드는 길은 **아무도 화면 앞에서 기다리지 않으므로** 넉넉히 준다(9초 → 22초). 누르고 나서 만드는 길만 화면 폴백(13초)보다 먼저 끝나면 된다. 「추천 만드는 중…」에서 시간이 다 되어 빈손으로 돌아오던 일이 이래서 생겼다.

### ⓑ 「추천 표현이 없어요」로 끝내지 않는다
빈손일 때 메울 밑천(`scfLastQid`)이 **요소 없는 도움말에 덮여 지워지고 있었다.** 빈 값으로는 덮지 않게 하고, 넛지가 뜰 때의 qid도 밑천으로 삼는다. 그러면 자유 대화에서도 마지막 넛지의 문형을 화계에 맞춰 내놓을 수 있다.

### 값
한 차례에 도움말 생성 한 번이 늘었다(분석과 나란히). 이미 돌고 있으면 건너뛰므로 겹쳐 돌지는 않는다. **학습자가 누르고 기다리는 시간을 0으로 만드는 값**이라 치를 만하다고 봤다.

### 검증
검사 28종 + 통합 점검 전부 통과. 「미리」 10종을 더했다. v122의 「재워 둔 것은 한 번만 쓴다」는 규칙이 바뀌었으므로(화면이 들고 있으니 서버 재움은 같은 차례 안에서만 유효하면 된다) 검사도 그 뜻으로 고쳤다.

## v122 — 2026-08-18 · ★ 넛지와 도움말이 한 순간에 만들어진다

### ⓐ ★ 저자의 정리 — 설계가 한 줄로 정리됐다
> 「애초에 상호작용 대화 요소가 실현될 수 있는 차례가 오면, 그것이 실현된 표현 두 개가 '🪜 이렇게 말해 보세요'에 생성됨과 **동시에** 넛지가 나오는 거지. 도와주기 페이더가 많이 있으면 그 실현이 자주 있는 것이고.」

v121은 이것을 **둘로 쪼개서** 하고 있었다. 넛지를 띄우고, 학습자가 「도와줘」를 누르면 **그때부터** 도움말을 만들었다. 그래서 셋이 어긋났다.

- 누르고 나서 기다려야 했다 — 저자가 「무거워졌다」고 한 것
- 9초 안에 못 만들면 「이 단계의 추천 표현이 없어요」가 떴다
- 넛지가 알린 것과 도움말이 따로 놀 때가 있었다

이제 **넛지를 띄우는 그 순간 도움말 두 개도 함께 만들어 재워 둔다**(`hint_cache`). 누르면 **곧바로** 나온다. 차례가 지나갔거나 2분이 넘으면 재워 둔 것을 버리고 새로 만든다.

### ⓑ 쉽게·공감·모국어가 도움말에 반영되지 않던 이유
`QUEST_LLM`의 `desc`는 「~한 적이 있다」 꼴이다. **판정용 문장이지 생성 지시가 아니다.** 모델이 흘려버렸다.

`FOCUS_EG` — 요소마다 **무엇을 만들라는 것인지** 한 줄로 못 박은 보기를 24개 두었다.

| 항목 | 보기 |
|---|---|
| 쉽게 말해 달라기 | 「그게 무슨 뜻이야? 좀 쉬운 말로 해 줄래?」처럼 **쉬운 말로 다시 해 달라고 청하는** 말 |
| 공감 | 「진짜? 힘들었겠다.」처럼 **상대의 감정을 알아주는** 말 |
| 모국어로 묻기 | 「이거 **{모국어}**로는 ~인데, 한국어로는 뭐라고 해?」 — 실제 그 언어 낱말을 하나 넣게 했다 |

`{n}` 자리에 학습자의 모국어 이름이 실제로 들어간다(베트남어·네팔어 …). 저자가 「모국어(학습자 국기)로 물어보세요가 나와야 할 듯」이라 한 것이 이것이다.

그리고 **요소 지시를 프롬프트 맨 앞으로 올렸다.** 뒤에 있으면 앞의 규칙들에 묻힌다.

### ⓒ 도움말은 발화 셋까지
v121은 「두 문장 45자」로 죄었다가 요소를 담을 자리가 없었다. **「발화 세 개를 넘기지 마라. 요소는 그 세 발화 안에 있어야 한다」**로 바꾸고 자르는 상한도 112 → 150으로 올렸다. 문장 끝에서 자르는 규칙은 그대로다.

### ⓓ 「추천 표현이 없어요」를 없앴다
서버가 빈손으로 돌아오면 화면이 그 문구를 띄웠다. 이제 서버가 **어떤 요소를 실으려 했는지(`qid`)를 함께 보내고**, 화면은 비었을 때 그 요소의 미리 쓴 문형을 **화계에 맞춰** 내놓는다. 맥락에 딱 맞지는 않아도 아무것도 없는 것보다는 낫다. `questForm()`을 v120에 남겨 둔 것이 여기서 쓰인다.

### ⓔ 그 밖에
- **빗소리 곡을 뺐다.** `bgm_rain.mp3` — 목록과 파일 모두. ★ 깃헙에서는 드래그 업로드로 지워지지 않으므로 저자가 직접 지워야 한다.
- 자유 대화 초기값에 **도와주기 「많이」**를 더했다(친밀도 75 · 지위 50 · 도와주기 3).
- 넛지 호아랑이 드나드는 시간을 **0.5초씩 늘렸다**(0.5s → 1s). 읽어야 하는 말인데 휙 지나갔다.
- 검사에 ⑳ 14종 + 소리·초기값 4종을 더했다.
- **`bgmtest`·`sndtest`가 「다섯 곡」을 박아 두어 곡 하나를 빼자 깨졌다.** 재야 할 것은 곡의 **개수**가 아니라 「여러 곡 가운데 하나를 꽂는가」다 — 성질을 재도록 고쳤다. 이번 판에서만 **네 번째** 겪는 같은 종류의 문제다(v119 두 건, v121 한 건).
- 캐시 무효화: `?v=122`, 서비스워커 `hoarang-v122`.

### 남은 것 — 「호아랑이 듣고 있어요」가 느리다
두 번째 보고인데 코드에서 원인을 못 찾았다. 말하기 경로는 v119 이후 손댄 적이 없다. 시스템 프롬프트가 v119~v121에서 약 500자 늘었으나(전체 9KB) 이 정도로 체감될 양이 아니다. **도움말이 무거웠던 것은 이번에 고쳤다**(미리 만들어 재워 두므로 누르면 즉시). 남은 것은 Live API의 응답 지연이라 서버에서 손댈 데가 없다. 다음에 다시 나오면 `?net=1`의 업로드 적체 숫자를 받아 좁힌다.

## v121 — 2026-08-18 · ★ 도와주기 페이더가 도움말에도 걸린다

### ⓐ v120이 화면에 태그를 그대로 뱉었다
넛지 말풍선에 `<span class="nz-now">지금이에요.</span>`가 **글자 그대로** 떴다. `hamPeekHtml`은 이름과 달리 안에서 `textContent`를 쓴다. 이름을 믿고 태그를 넘긴 것이 잘못이다. 꾸밈은 네 번째 인자(`cls`)로 주고, 두 줄은 `title`·`items` 두 인자로 넘긴다.

### ⓑ ★ 페이더의 뜻을 다시 잡았다 — 넛지가 아니라 **도움말**을 조절한다
v120에서 「넛지는 이름만, 형식은 도움말이」를 넣었는데 저자가 「작동하는 경우가 별로 없다」고 했다. 맞다. **넛지가 떠 있는 동안 도움말을 눌러야만** 이어졌기 때문이다. 넛지는 페이더 상한·차례 간격·자율 페이딩에 걸려 자주 나오지 않는다. 이어질 자리가 애초에 드물었다.

저자의 정리대로 **페이더의 대상을 바꿨다.**

| 도와주기 | 지금까지 | v121 |
|---|---|---|
| 뜻 | 넛지를 얼마나 던질까 | **도움말 두 제안에 상호작용 요소를 얼마나 자주 실을까** |
| 0 끔 | 넛지 없음 | 기능 단계 + 맥락만 |
| 1 적게 | 넛지 조금 | 넛지가 방금 알린 것이 있을 때만 얹는다 |
| 2 보통 | 넛지 보통 | **넛지가 없어도** 맥락에 맞는 요소를 골라 얹는다 |
| 3 많이 | 넛지 많이 | 자율에 이른 요소까지 골라 늘 얹는다 |

요소를 고르는 일은 `_fit_intervention()`이 그대로 한다 — 자리 표지·기능단계 금지·페이딩 눈금이 이미 거기 있다. **2와 3의 차이도 그 함수가 이미 가른다**(자율 요소는 「많이」에서만). 도움말이 그 결과를 받아 쓰는 것이 전부다.

- **첫 제안만이 아니라 두 제안 모두**가 그 요소를 실현한다. 다만 서로 다른 방식으로 — 두 갈래를 보여 주자는 원칙(v104)은 그대로다.
- **「억지로 끼워 맞추지 마라」를 프롬프트에 넣었다.** 맥락에서 도저히 안 되면 지시를 접고 흐름에 맞는 말을 주라고 했다. 요소를 우겨 넣은 말은 배울 거리가 아니다.
- 넛지의 자리 고르기는 그대로다. 넛지는 계속 「지금이에요. / ~해 보세요!」로 뜬다.

### ⓒ 도움말이 중간에서 잘렸다
요소를 실으면서 문장이 길어졌고, `_clean_str(h, 80)`이 **말 한가운데를 잘랐다.** 「막 어려운 상황 속에서도」에서 끊긴 말은 따라 읽을 수가 없다.

- `_hint_trim()` — 넘치면 **문장 끝**에서 자른다. 끝을 못 찾으면 말줄임표를 붙여 「여기가 끝이 아님」을 보인다. 상한 80 → 112.
- 프롬프트에 **나쁜 예와 좋은 예를 나란히** 넣었다. 「두 문장을 넘기지 말고 45자 안팎」.

### ⓓ 개입에서 두 항목을 더 뺐다
`INTV_EXCLUDE`를 한자리에 모았다(전에는 `INTERVENABLE`의 뺄셈으로만 있었고, `_fit_intervention`은 그것을 보지 않아 **문서와 코드가 어긋나 있었다** — `qSelfFix`가 대응표에는 「개입 안 함」인데 실제로는 나왔다).

| 항목 | 왜 빼는가 |
|---|---|
| **끼어들기** | 상대가 말하는 **도중에** 마이크를 눌러야 성립한다. 글로 「끼어들어 보세요」를 띄우는 시점에는 이미 말이 끝나 있다 |
| **모르는 말을 다른 말로** | 학습자가 **무엇을 모르는지**를 알아야 권할 수 있는데, 서버는 학습자가 한 말만 본다 |
| 자기 수정 | 스스로 알아채는 일이다. 시켜서 하면 자기 수정이 아니다 |
| 빨리 | 말이 느린지는 글로 잴 수 없다(v118) |

**퀘스트 자체는 살아 있다.** 학습자가 스스로 하면 실현으로 세고 점수에도 들어간다. 챗봇이 먼저 권하지 않을 뿐이다.

### 검증
- 검사 28종 + 통합 점검 전부 통과. ⑱에 페이더↔도움말 3종·자르기 4종·제외 7종·말풍선 3종을 더했다.
- `fittest`의 「닿아야 할 것」 기준을 **전체 28개 → 권할 수 있는 24개**로 바꾸고, 뺀 넷은 **나오면 안 된다**는 검사를 새로 넣었다. 기준을 낮춘 것이 아니라 **재는 대상을 바로잡은 것**이다.
- 검사가 또 주석을 훑다 걸렸다(`nz-now`를 설명한 주석). 주석을 떼고 본다.
- 캐시 무효화: `?v=121`, 서비스워커 `hoarang-v121`.

## v120 — 2026-08-18 · ★ 넛지는 이름만, 형식은 도움말이 준다

### ⓐ ★ 미리 쓰인 문형은 지금 대화를 모른다 — 설계를 바꿨다
저자가 화면 일곱을 보내며 「다섯 중 셋은 이상하다」고 했다. 실제로 그랬다.

- 「시간을 끌어 보세요」 자리에 「…저는 그렇습니다. ○○ 씨는 어떠십니까?」(의견 교환)
- 접수처 마무리에 「이건 다른 이야기입니다만」
- 「왜냐하면」에서 막힌 자리에 「저도 그런 적이 있습니다」

v119에서 **발동 자리**는 고쳤지만 **문형 자체**는 고칠 수 없었다. `QUEST_FORM`은 대화가 시작되기 전에 쓰인 것이라 지금 무엇을 다루는지 모른다. 자리를 아무리 잘 골라도 그 자리에서 할 말이 무엇인지는 알 수 없다. **잘못은 고르기가 아니라 미리 쓴 문형에 있었다.**

저자의 해법을 그대로 구현했다. **개입은 기능만 알리고, 형식은 🪜 도움말이 준다.**

| | v119까지 | v120 |
|---|---|---|
| 넛지 말풍선 | 「「…저는 그렇습니다. ○○ 씨는 어떠십니까?」처럼 말해 보세요」 | 「지금이에요.」 / 「**시간을 끌어 보세요!**」 |
| 형식 | 말풍선이 미리 쓴 문형을 보여 줌 | 학습자가 도움말을 누르면 **첫 번째 제안**이 그 요소를 실현한 발화 |

도움말은 지금 맥락과 화계를 보고 만들므로 **어긋날 자리가 없다.** 이미 화계는 v119에서 맞춰 두었다.

구현은 짧다. `send_teach_intervention`이 방금 알린 것을 `idc_state["hint_focus"]`에 남기고, `send_hints`가 그것을 읽어 프롬프트에 「**첫 번째 제안은 「〈desc〉」가 실현된 발화여야 한다**」를 얹는다. 두 번째 제안은 그것과 무관하게 자연스러운 말로 둔다 — 두 갈래를 보여 주자는 원칙(v104)은 그대로다.

- **두 차례가 지나면 흘려보낸다.** 이미 지난 자리에 뒤늦게 형식을 주면 또 어긋난다.
- **「도와줘」 단추가 같이 움직인다.** 넛지가 기능만 알리므로, 형식이 필요한 학습자가 어디를 눌러야 하는지 바로 보여야 한다. 20초면 가라앉는다.
- `QUEST_FORM`은 **지우지 않았다.** 「한국어 상호작용 대화 능력」 학습 화면과 논문 대응표가 이것을 쓴다. 대화 중에 보여 주지 않을 뿐이다.
- 이것이 **P4(비계는 요청 시 제공)의 온전한 구현**이기도 하다. v104부터 「기능만 알린다」고 적어 두었으나, 말풍선이 문형을 함께 보여 주고 있어 실은 절반만 그랬다.

### ⓑ 화계 눈금 — 합쇼체를 「낯선 사이」 칸에만
`SPEECH_FAR`를 **35 → 15**로 내렸다. 화면의 친밀도 눈금은 `0~15 낯선 사이 · 16~35 아는 사이 · 36~65 적당한 사이 · …`인데 35로 잡아 두어 **「아는 사이」인데도 합쇼체**가 나왔다. 과했다. 이제 합쇼체는 「낯선 사이」에서만 나오고 아는 사이부터는 해요체다. 서버·화면 두 곳을 함께 옮겼고 50칸 대조가 통과한다.

### ⓒ 계획 만들기 — 얼마나 남았는지 보인다
20~40초가 걸리는데 「계획을 만드는 중... ⏳」 한 줄뿐이라 멈춘 것처럼 보였다.

- 시간으로 그리는 진행 막대. `1 - e^(-t/13)` 곡선이라 앞은 빠르고 뒤는 느리다. **95%에서 멈춰 서서 기다린다 — 거짓으로 100%를 채우지 않는다.**
- 문구 넷이 서버가 실제로 하는 일의 차례를 따른다(상황 읽기 → 단계 짜기 → 표현·대화문 → 마무리). 18개 언어.
- **v119에서 늘어난 지연도 함께 줄였다.** `_fill_cues`가 `_fix_style` **뒤에** 붙어 있어 계획 만들기가 최대 15초 더 걸렸다. 서로 다른 곳을 고치므로(대화문 / 표현의 앞말) `asyncio.gather`로 나란히 돌린다.

### ⓓ 그 밖에
- 자유 대화 대화 스타일 초기값 — 친밀도 **75** · 지위 50.
- `넛지_IDC_대응표.docx`에 「이 문형은 대화 중에 제시하지 않는다」는 설명을 넣었다. 표의 뜻이 달라졌기 때문이다.
- 검사에 ⑱ 넛지·도움말 잇기 10종, ⑲ 화계 눈금·계획 만들기 8종을 더했다. 28종 + 통합 점검 전부 통과.
- 검사 두 건이 또 **자기가 고정한 글자**를 찾다가 걸렸다. `restest`는 `await _fix_style(...)`를, `uxtest`는 파일 전체에서 「화계」를 찾고 있었다(주석까지 훑었다). **뜻을 재도록** 고쳤다 — 불리는지, 그리고 학습자가 보는 문구에 있는지.

## v119 — 2026-08-18 · 화계를 세 단계로 · 넛지가 대화의 자리를 본다

### ⓐ ★ 화계(청자경어법)가 화면에만 있고 서버에는 없었다
화면은 세 단계(합쇼체·해요체·해체)로 계산해 「존댓말로 아주 공손하게 말해 보세요」라고 일러 주는데, **서버는 「존댓말/반말」 둘로만 갈랐다.** `_LV_KO = {"polite": "존댓말(해요체)"}` — 프롬프트에 **해요체라고 적혀 있었다.** 그래서 낯선 사이·아주 공손을 골라도

- 호아랑: 「진료 보러 오셨어요?」 (해요체)
- 🪜 도움말: 「네, 30분 정도는 괜찮아요」 (해요체)
- 넛지 문형: 「그럼 ~는 어떻겠습니까?」 (합쇼체 — 이것만 맞았다)

가 한 화면에 같이 나왔다. 저자가 「몇 번은 맞고 몇 번은 틀리다」고 한 것이 이것이다.

서버에 `_speech_of` · `_partner_speech_of`를 두어 **화면과 같은 눈금**(`SPEECH_CLOSE=60`, `SPEECH_FAR=35`)으로 계산하고, 세 곳에 못 박았다.

1. **자유 대화** — `build_system_prompt`의 페이더 좌표 바로 아래
2. **주제 대화** — `build_roleplay_prompt`의 `rp_speech_line`. 배역이 화계를 덮어쓰지 못한다
3. **🪜 도움말** — `send_hints` 프롬프트. **여기에는 화계가 한 글자도 없었다**

`_speech_level`도 세 단계로 갈랐다(「-습니다/-습니까」 먼저 보고, 그 다음 「-요」). 검사에서 **50칸(D·P 5×5 × 두 화자)을 전부 대조**한다 — 한 칸이라도 다르면 학습자는 보는 말과 듣는 말이 어긋난 본보기를 받는다.

- 대화문 손질(`_style_offenders`)은 두 축을 다르게 다룬다. **존대↔반말이 어긋난 줄은 그 자리에서 잘못**이지만, **합쇼체↔해요체는 실제 입말에서 늘 섞인다.** 한 줄씩 잡으면 손질이 끝나지 않고 대화문이 낭독체가 된다 → 그 화자의 판이 **절반 아래로 기울었을 때만** 잡는다.
- `_deference_offenders`도 고쳤다. 화계가 셋이 되자 「다르다」만으로는 위아래를 못 가른다 — 합쇼체:해요체는 격식의 차이지 위아래가 아니다. **한쪽만 반말일 때만** 위아래를 가린다.

### ⓑ ★ 넛지가 맥락에 어긋나던 두 가지 원인
저자가 「맥락에 맞지 않는 넛지가 튀어나온다」고 한 것을 화면 넷으로 되짚어 원인을 둘 찾았다.

**ㄱ) 「청하는 말」의 자리가 너무 넓었다.**
「예약은 **하셨을까요?**」는 지난 일을 **묻는 말**인데, 「~까요?」 하나로 청하는 말이 되어 협상(대안·조건·거절)이 열렸다. 인사만 주고받은 20% 지점에서 「그럼 ~는 어떻겠습니까?」가 나온 이유다.
→ **청하는 말은 상대가 앞으로 할 일을 두고 하는 말이다.** 지난 일을 묻는 말(-았/-었/-였/-셨)은 청하는 말일 수 없다. 확실한 꼴(주세요·부탁드립니다)과 애매한 꼴(~까요?·~실래요?)을 갈라, 애매한 쪽은 과거 표지가 없을 때만 인정한다. 검사 9종 통과.

**ㄴ) ★ 넛지가 대화가 어디쯤 왔는지를 몰랐다.**
서버는 기능단계 충족을 이미 세고 있었다(`rp_progress["done"]`). **넛지가 그것을 보지 않았을 뿐이다.** 그래서 160% 마무리 인사 중에 「이건 다른 이야기입니다만」(화제 전환)이 나왔다.
→ `_stage_phase()`로 **시작 / 전개 / 마무리**를 가르고 `PHASE_BAN`으로 막는다.

| 자리 | 막는 것 | 왜 |
|---|---|---|
| **시작** (한 단계도 못 밟음) | 협상 다섯 · 화제 접기/전환/되돌리기/새 화제 | 협상은 아직 열리지 않았고, 접을 화제도 아직 없다 |
| **전개** | 없음 | |
| **마무리** (마지막 단계 or 100%) | 새 화제·화제 전환·되돌리기·판 벌이기·확장·협상 다섯·모국어 | 새로 벌이면 대화가 되돌아간다. 여기서 할 만한 것은 접기·차례 넘기기·되받기·공감·단절 수정뿐 |

**자유 대화는 늘 「전개」다.** 처음엔 첫 두 차례를 「시작」으로 잡았는데, 상대가 대뜸 무언가를 청해 온 자리에서 협상을 막아 버렸다 — 자유 대화에는 과업이 없으니 **막을 자리도 없다.** `fittest`가 8건으로 잡아냈다.

### ⓒ 발화 연습 ②에서 대화이동 연속체가 사라졌다
「네, 여기 신분증이요.」가 **cue 없이** 놓여 「② 먼저 말 걸어 보기」로 떴다. 그 말로 대화를 열 수는 없다 — 누가 달라고 해야 나오는 말이다. 앞말이 없으니 연속체가 통째로 사라지고, 연속체를 익히자는 연습이 **한 문장 읽기**가 됐다.
→ `_cue_missing`이 「네」「예」「알겠습니다」로 시작하거나 「여기 있~」이 든 발화를 대답으로 보고, `_fill_cues`가 한 번의 호출로 앞말을 채운다. 계획 프롬프트에도 나쁜 예를 박았다.

### ⓓ 그 밖에
- 화계 안내를 **어미 형태**로 바꿨다 — 「존댓말로 아주 공손하게」 → 「**~습니다/습니까?**로 아주 공손하게」. 「존댓말」은 범주 이름이라 무엇을 말하라는 것인지 알 수 없다. 18개 언어 162곳.
- 넛지 말풍선에서 앞머리 「지금이에요!」를 뺐다. 이름이 이미 「~해 보세요!」 꼴이라 두 번 말하는 셈이었고, 좁은 말풍선에서 정작 읽어야 할 이름이 밀려났다.
- `넛지_IDC_대응표.docx`를 다시 만들었다(2쪽). **발동 조건을 코드에서 그대로 뽑아** 세 층(① 자리 한정 · ② 모양으로 짐작 · ③ 폴백)과 단계 금지(✕)까지 적었다. 교육적 개입 항목은 가는 지마켓 산스, 제시 문형은 볼드.
- 검사에 ⑯ 화계 50칸 대조 · ⑰ 앞말 없는 대답 · 청하는 말 9종 · 대화의 자리 4종을 더했다.
- **`qa_all.py`·`fittest.py`에 박혀 있던 절대경로를 걷어냈다.** 세션마다 바뀌는 경로라 다음 판에서 반드시 깨진다(실제로 `fittest`가 이번에 `PermissionError`로 죽어 있었다). 인자로 받고 없으면 제 폴더를 본다. **배포 폴더가 없어도 검사가 죽지 않게** 막았다 — 중간에 죽으면 그 뒤가 통째로 미검증이 된다.
- 캐시 무효화: `?v=118 → ?v=119`, 서비스워커 `hoarang-v119`.

## v118 — 2026-08-18 · 저자 수정본 반영 · 발동 조건 재정리

### ⓐ 개입 항목의 이름과 문형을 저자 수정본으로 교체
저자가 `넛지_IDC_대응표.docx`에서 직접 손본 것을 앱에 그대로 되돌렸다. 이름 28개, 해요체 문형 6개. 이름은 모두 「~해 보세요!」 꼴로 통일되어 **무엇을 하라는 것인지가 앞에 온다**. 문형도 더 짧고 쉬운 말로 바뀌었다.
- 오탈자 하나를 바로잡았다 — 「상대을 말 다시 확인해 보세요!」 → 「상대 말」.
- 문서에서 옮겨 오며 말줄임표가 `...`(마침표 셋)으로 바뀐 것을 `…`로 되돌렸다.

### ⓑ ★ 발동 조건 — 저자가 미뤄 둔 판단
저자가 「자리 한정 / 아무 때나」를 고치다 말고 판단을 요청했다. 코드에서 각 항목이 **실제로 어느 자리 표지에 걸려 있는지** 세어 보고 다음과 같이 정했다.

| 항목 | 저자 안 | 판단 | 근거 |
|---|---|---|---|
| 새로운 이야기 (`qNewTopic`) | 자리 한정 | **자리 한정으로 바꿈** | 협상이 한창인데 「새 이야기 꺼내세요」는 어긋난다. 자리 표지가 셋 있어(여덟 차례 경과·상대가 짧게만 답함·상대가 말을 맺음) 폴백에서 빼도 나온다 |
| 내 이야기 얹기 (`qExpand`) | 자리 한정 | **자리 한정으로 바꿈** | 「상대가 꺼낸 화제」가 있어야 성립한다. 자리 표지가 넷 |
| 천천히·다시·쉽게 | 아무 때나 | **자리 한정 유지** | 짧고 쉬운 말 뒤에 「천천히 말해 달라」고 시키면 **잘못된 짝**을 가르친다. v114에서 고친 바로 그 문제다. 각각 상대가 길게 말함·되물음·어려운 말이라는 자리가 있다 |
| 모국어로 묻기 (`qNative`) | 아무 때나 | **자리 한정 유지** | 막혔을 때의 전략이다. 아무 때나 권하면 모국어 사용을 부추긴다 |
| **빨리 (`qAskFast`)** | 아무 때나 | **★ 개입에서 제외** | 아래 참조 |

**「빨리 말해 달라기」를 개입 대상에서 뺐다.** 두 가지 이유다.
- ㄱ) **어느 자리에도 없어 한 번도 나온 적이 없다.** 「상대가 너무 느리다」는 발화의 모양으로 잴 수 없어 자리를 만들 수가 없었다. v114에서 여섯 개만 나오던 문제를 고쳤는데, 이 하나가 남아 있었다.
- ㄴ) 더 중요한 이유 — 나머지 셋은 **알아듣지 못한 것을 고치는** 말이지만 「빨리」는 고치는 것이 없다. 편의 조절이지 의사소통 단절 수정이 아니다. 학습자는 빠른 단추로 언제든 쓸 수 있고, 챗봇이 먼저 권할 일이 아니다.

**폴백(아무 때나)은 셋으로 줄었다** — `qEndTurn`·`qKeepTurn`·`qEcho`. 폴백은 자리를 못 찾았을 때 그래도 하나 내놓는 자리이므로 **맥락을 타지 않는 것만** 있어야 한다.
개입 대상은 28개 → **26개**(자기 수정·빨리 제외). 확인 결과 **영영 안 나오는 항목은 0개**다.

### 검증
- `nudgetest`가 「문형마다 보기를 둘 이상」 규칙 위반을 두 번 잡아냈다(`qShiftTopic`·`qNative`). v104의 원칙을 문서에서 옮겨 오다 놓친 것이다. **문서와 코드를 오갈 때 규칙이 새는 지점**이라 기록해 둔다.
- 검사 27종 전부 통과. **캐시 무효화**: `?v=117 → ?v=118`, 서비스워커 `hoarang-v117 → v118`.
- `넛지_IDC_대응표.docx`를 새 상태로 다시 만들었다(1쪽). 저자가 고친 `페이더_경어법_대응표.docx`도 프로젝트에 반영했다.

## v117 — 2026-08-18 · 이름만으로는 안 읽히던 개입 여덟 개 · 논문용 표 둘

### ⓐ ★ 「말 끝에 상대에게 물어보세요」가 무슨 말인지 몰랐다
저자가 직접 겪은 문제다 — **연구자도 한 번에 못 읽는 것을 학습자가 읽을 리 없다.**

- 원인은 **문형이 없는 항목이 여덟 개** 있었다는 것이다. 문형이 붙은 20개는 이름 아래에 예시가 뜨지만, 나머지 여덟은 이름만 뜬다. `qEndTurn`은 실제로는 **차례 관리**(자기 말을 맺으며 「○○ 씨는요?」로 공을 넘기기)인데, 이름만 보면 「질문을 하라」로 읽힌다. `qAsk`(질문 3번 하기)와도 겹쳐 보인다.
- **문형 여덟 개를 채웠다**(20 → 28). 화계 세 벌씩 모두.
- **이름도 기능 중심으로 고쳤다.** 「말 끝에 상대에게 물어보세요」 → 「내 말을 맺고 차례 넘기기」. 그 밖 `qEcho`·`qExpand`·`qShiftTopic`·`qNative`도 같은 기준으로.
- **문형은 반드시 둘 이상** — v104의 원칙(하나만 주면 학습자가 그대로 읽어 맥락이 깨진다)을 처음엔 어겼다가 `nudgetest`가 잡아냈다. 여덟 개 모두 「A · B」 두 보기로 다시 썼다. **검사가 설계 원칙을 지켜 준 사례다.**

### ⓑ 논문용 표 두 장
4장에 그대로 넣을 수 있도록 `.docx`로 만들었다. 값은 손으로 옮기지 않고 **코드에서 직접 뽑았다** — 표와 구현이 어긋날 수 없다.

- **`페이더_경어법_대응표.docx`**(1쪽) — 25개 좌표별 화자별 화계(학습자/상대), 같은 좌표에서 제시되는 요청 표현. 비대칭 관계는 음영으로 표시.
- **`넛지_IDC_대응표.docx`**(1쪽) — 개입 28개 × IDC 여섯 요소 × 제시 문형(해요체) × 발동 조건(자리 한정/아무 때나).

### 검증
- 낡은 단정 둘을 고쳤다 — `sndtest`의 「`?v=113`이 다섯 개」(판올림마다 깨진다 → 숫자를 안 박는다), 「말투는 `speechTier()` 한 곳」(v116에서 `speechOf()`로 옮겼다).
- **검사 27종 전부 통과.** `qa_all`의 맥락 판정 2건은 기대 목록이 좁은 것으로 보아 보류.
- **캐시 무효화**: `?v=116 → ?v=117`, 서비스워커 `hoarang-v116 → v117`.

## v116 — 2026-08-18 · ★ 배경음이 PC에서 죽던 진짜 원인 · 경어법 두 축 분리

### ⓐ ★ 배경음 — v110의 아이폰 수정이 PC·안드로이드를 죽이고 있었다
v113→v115에서 두 번 고쳤는데도 안 났다. 매번 다른 곳을 짚었기 때문이다.

- **진짜 원인** — v110에서 배경음을 `createMediaElementSource`로 오디오 그래프에 물리고, 음량을 **GainNode**로 주게 바꿨다. 아이폰이 `audio.volume`을 무시하기 때문이다. 그런데 **크롬·안드로이드는 `el.volume`을 그대로 듣는다.** `fadeIn()`이 `el.volume = 0`으로 내려 두고 GainNode만 올리니, **그래프에 들어가는 소리 자체가 0**이라 아무리 올려도 무음이었다.
  → 아이폰에서만 들리고 PC에서 안 들리던 이유가 이것이다.
- **고침** — 그래프에 물리는 순간 `el.volume = 1`로 되돌리고(감쇠는 GainNode가 전담), `fadeIn`은 **물리지 않은 요소에만** `volume = 0`을 쓴다.
- 배경음 캐시버스터가 `?v=113`에 멈춰 있던 것도 `?v=116`으로 맞췄다.

> **교훈.** 「아이폰에서 안 된다」를 고치면서 「다른 데서는 되던 길」을 함께 끊었다. 한 플랫폼의 예외를 다룰 때는 **다른 플랫폼에서 그 값이 여전히 쓰이는지**를 봐야 한다.

### ⓑ ★ 경어법 — 두 축을 평균 내고 있었다
화면 쪽 말투 계산이 `(친밀도 + 지위) ÷ 2`였다. 한국어 청자경어법에서 **힘(지위)과 유대(친밀도)는 독립된 두 축**인데(Brown & Gilman의 power/solidarity를 한국어에 적용한 논의들), 평균을 내면 전혀 다른 관계가 같은 값으로 뭉개진다.

| 관계 | 평균 | 나오던 말 |
|---|---|---|
| 낯선 사이 · 내가 **상급자** | 51.5 | 「좀 천천히 말해 줄래요?」 |
| 적당한 사이 · **대등** | 50 | 같은 말 |
| 절친 · 내가 **하급자** | 51.5 | 같은 말 |

- **서버와도 어긋나 있었다.** 대화문 생성은 조건식(`d≥60 그리고 p≥45 → 반말`)을 쓰는데 화면은 평균을 썼다. 25칸 중 **5칸에서 「대화문은 반말인데 빠른 단추는 존댓말」**이었다. 학습자는 듣는 것과 배우는 것이 달랐다.
- **고침** — `speechOf(d, p)` 하나로 모았다. 규칙 셋:
  1. **반말은 유대가 있어야 나온다**(D≥60). 지위가 높아도 낯선 사람에게는 쓰지 않는다.
  2. **낯선 사이(D≤35)는 지위와 무관하게 격식체.** 내가 아주 윗사람일 때만 해요체.
  3. 가까운 사이에서 **지위가 위아래를 갈라 비대칭**을 만든다.
  → 25칸 모두 서버의 `want_user`/`want_ai`와 일치함을 확인했다.
- **비대칭을 화면에 드러냈다.** 예전에는 「나」의 말투만 한 줄에 떴다. 이제 갈릴 때는 둘 다 보인다 — 「친한 사이 · 하급자 · **나는 존댓말로 · 호아랑은 반말로**」. 상호적/비상호적 구분은 외국인 학습자가 가장 어려워하는 지점인데 배울 자리가 없었다. 문구 6개를 18개 언어로 넣었다.

### 검증
- **`bgmtest.js` 신설**(14항목) — 그래프에 물린 뒤 `el.volume`이 1인지, `fadeIn`이 그것을 도로 0으로 내리지 않는지, 캐시버스터가 판올림을 따라가는지. 경어법 두 축 분리도 함께 본다.
- 낡은 단정 넷을 고쳤다 — `nudgetest`·`qa_all`의 「문형 14개」(v115에서 20개가 됨), `langtest`의 `<br>`(빈 화면 문구가 바뀜), `restest`의 「탭·꾹 둘 다」(v115에서 꾹 누르기로 통일). **숫자를 박아 둔 단정은 늘릴 때마다 깨진다** — 「14개 이상」으로 바꿨다.
- `qa_all.py`의 맥락 시험대가 `re`·`INTV_ANYTIME`을 못 받아 매번 크래시하던 것도 고쳤다. **검사가 중간에 죽으면 그 뒤 항목은 통과도 실패도 아니다.**
- JS 24종 + qa_all 전부 통과(맥락 판정 2건은 기대 목록이 좁은 것으로 판단 보류).
- **캐시 무효화**: `?v=115 → ?v=116`, 서비스워커 `hoarang-v115 → v116`.

## v115 — 2026-08-18 · 재검토에서 나온 것들

> v111~v114를 손으로 다시 훑었다. **검사가 다 통과한 뒤에** 찾은 것들이라,
> 검사가 무엇을 못 보는지에 대한 기록이기도 하다.

### ⓐ 자리 맞음과 균형이 **같은 층에서 다투고 있었다**

v114에서 「맥락에 맞으면서 쏠리지 않게」를 한꺼번에 잡으려고,
후보를 한 줄로 모아 놓고 「가장 덜 해 본 요소」로 골랐다.
옷 가게 상황극을 통째로 흘려 보니 이런 자리가 나왔다.

```
호아랑: 죄송하지만 더는 어렵습니다. 정찰제라서요.
   나: 음...
        🪜 그 얘기는 그만하고요        ← 엥?
```

**거절당한 자리인데 화제를 접으라고 한다.** 「그래도 한 번만 부탁드려요」가
있어야 할 자리를, 그 요소를 이미 두 번 써서 균형이 밀어낸 것이다.
사용자가 「엥? 싶음」이라고 한 자리가 정확히 이런 곳이었다.

**자리 맞음과 균형은 같은 층에서 다툴 것이 아니다.** 두 층으로 나눴다.

- ㉠ **이 자리에서만 말이 되는 것**이 하나라도 있으면 → 그중에서만 고른다
- ㉡ 그런 것이 없을 때에만 → 아무 때나 되는 것에서 고른다
- 균형은 **각 층 안에서만** 따진다

같은 자리가 이렇게 바뀌었다.

```
호아랑: 죄송하지만 더는 어렵습니다. 정찰제라서요.
   나: 음...
        🪜 「그래도 한 번만 부탁드려요 · 정말 안 될까요?」
```

> 배운 것: 두 가지를 한꺼번에 만족시키려고 **하나의 점수로 합치면**,
> 어느 쪽이 이길지 아무도 예측할 수 없게 된다. 어느 것이 먼저인지가 분명하면
> **층을 나누는 편**이 낫다. 그러면 「맞는 것이 있으면 그것, 없으면 균형」으로
> 사람이 읽을 수 있는 규칙이 된다.

### ⓑ 앞뒤가 안 맞던 두 자리

- **「네」라고 답한 뒤에 거절을 시켰다.** 「입어 보시겠어요?」에 「네」라고 한
  학습자에게 「그건 좀 곤란한데요」를 알려 준다. 이미 좋다고 한 사람에게
  거절하는 법을 가르치는 셈이다. → 학습자가 이미 받아들였으면 이 갈래를 뺀다.
- **거절이 협상의 맨 앞에 있었다.** 후보 차례가 `거절 → 대안 → 조건`이었다.
  실제 협상에서 먼저 나오는 것은 대안과 조건이고, **거절은 가장 센 수다.**
  → `대안 → 조건 → 거절`로 뒤집었다.
- **`qHold`(물러서지 않고 다시 청하기)가 엉뚱한 자리에 있었다.** v114 초안에서
  '내가 질문한 자리'에 붙여 놨는데, 재요청은 **상대가 거절한 자리**에서만 말이 된다.

### ⓒ `shutil` 이 import 없이 쓰이고 있었다

`main.py` 55행. `app.html.gz`가 없을 때 루트의 `app.html`을 대신 쓰는 **비상용 길**이다.
gz가 늘 있었으므로 **한 번도 안 돌았고**, 게다가 통째로 `try/except`로 감싸 있어
터져도 「복사 실패」 한 줄만 찍고 넘어갔다. **비상용 길이 정작 비상 때 안 도는 상태**였다.

사람 눈으로는 못 찾는다 — 안 도는 코드니까. 그래서 `qa_all.py`에 **정적 검사**를
붙였다(`pyflakes`). 「없는 이름을 부르는 곳」만 오류로 보고, 「안 쓰는 지역 변수」처럼
읽기 좋으라고 남긴 것은 넘긴다.

> 배운 것: 예외 처리가 오류를 **숨기는 자리**가 된다. `except`로 감싼 길은
> 실패해도 조용하므로, 그 안이 애초에 실행 가능한지는 **기계가 봐 줘야** 한다.

### ⓓ 죽은 코드 하나 — 확인하고 치웠다

`pick_anytime_intervention`의 「그래도 못 골랐으면 INTV_ANYTIME 에서 하나」라는 뒷길.
v114에서 `_fit_intervention`이 마지막 후보로 `INTV_ANYTIME`을 통째로 담게 되면서,
같은 것을 같은 잣대로 다시 훑는 일이 됐다. **무작위 4000번을 돌려 확인했다** —
`_fit`이 빈손인 908번 가운데 뒷길이 무언가 찾아낸 적은 **0번**. 치웠다.
(남겨 두면 다음 사람이 「여기서도 고르는구나」로 잘못 읽는다)

### ⓔ 자잘한 것

- **홈으로 돌아갈 때 「접힌 페이더 한 줄」이 안 거둬졌다.** 홈에서는 안 보이는
  자리라 티가 안 났지만, 다른 길로 대화 화면에 들어가면 지난 값이 그대로 보인다.
- **빈 화면 문구의 HTML 기본값이 옛말이었다.** JS가 덮어쓰기 전 잠깐 비친다.
- `bgm.mp3`(5.3MB)는 이제 아무도 안 부른다. 지워도 된다.

### ⓕ 어떻게 찾았나 — 브라우저 없이 화면을 돌려 본 것

이번 재검토의 도구는 **jsdom 위에서 app.html 을 실제로 실행해 본 것**이다.
`AudioContext`·`MediaRecorder`·`fetch` 같은 것을 흉내 내 주면 화면 전체가 돈다.
그래야 「대화 시작 → 홈 → 다시 시작」 같은 **길**을 밟아 볼 수 있다.
정규식으로 코드를 훑는 검사로는 여기까지 못 본다.

---

## v114 — 2026-08-18 · 넛지가 여섯 개뿐이었다

### ⓐ 배경음이 안 들린 이유 — 내가 하루 전에 만든 것

v113에서 「다섯 곡을 다 받으면 무겁다」는 생각에 `preload="none"`으로 뒀다.
그랬더니 배경음이 **아예 안 났다.**

배경음은 v110부터 `createMediaElementSource`로 그래프에 물려 있다(아이폰 때문에).
그런데 **아직 아무것도 안 받아 온 요소**를 그래프에 물리면 사파리는 그 뒤로
소리를 내보내지 않는다. 예전에는 `preload="auto"`라 그래프를 만들 시점에 이미
데이터가 있어서 이 문제가 **드러날 일이 없었던 것**이다.

무거움 걱정은 애초에 `preload`로 풀 일이 아니었다. **목록에 다섯을 두되 src는
하나만 꽂는 것**으로 이미 풀려 있었다. 한 사람이 받는 것은 0.7~2.1MB로,
예전 `bgm.mp3`(5.3MB)보다 오히려 가볍다. 되돌리고 `load()`를 명시적으로 부른다.

> 배운 것: **이미 해결된 문제를 한 번 더 해결하려다 새 문제를 만들었다.**
> 최적화를 넣기 전에 「이 걱정이 아직 남아 있나」를 먼저 봐야 했다.

### ⓑ 넛지 28개 가운데 **여섯 개만** 나오고 있었다

「다시 말해 달라기·천천히 말해 달라기 같은 의사소통 전략이 한 번도 안 나온다」는
지적을 받고 세어 보니, 서버가 고를 수 있는 것이 정말 **여섯 개뿐**이었다.

| | v113 | v114 |
|---|---|---|
| 서버가 고를 수 있는 넛지 | **6** / 28 | **27** / 28 |
| 갈래 | 대화이동·화제뿐 | 여섯 갈래 모두 |

왜 이렇게 됐나. v104에서 「많이 힘드셨겠습니다」가 맥락 없이 따라 읽혀 대화가
어그러졌다. 그래서 맥락을 타는 것들을 후보에서 **빼 버렸다.**
**빼는 것은 고치는 것이 아니었다.** 안 나오면 어그러지지도 않지만 배우지도 못한다.

제대로 된 답은 **각각에게 자기 자리를 주는 것**이다. 「천천히 말해 주세요」는
아무 때나가 아니라 **상대가 한꺼번에 길게 말했을 때** 하는 말이다.
그 자리를 말뜻까지 읽지 않고 **발화의 모양만으로** 상당 부분 가려낼 수 있다.

| 상황 표지 | 그 자리의 넛지 |
|---|---|
| 아직 아무 말도 없다 | 먼저 말 걸기 |
| 감정이 실렸다 (힘들·속상·다행…) | 공감 · 되묻기 |
| 지난 일 이야기를 하고 멈췄다 | 「그래서요?」 · 공감 |
| 되물었다 (「네?」「무슨 뜻이에요?」) | **명료화 응답** · 돌려 말하기 |
| 무언가를 청해 왔다 (「~까요?」「~주세요」) | **거절 · 대안 · 조건** |
| 거절·난색을 보였다 | **다시 청하기** · 대안 |
| 내가 길게 말했다 | 알아들었는지 확인 |
| 한꺼번에 아주 길게 왔다 (80자↑) | **천천히** · 요약 확인 · **다시** |
| 어려운 말이 나왔다 | **쉽게** · 다시 |
| 여덟 차례를 넘겼다 | 화제 정리 · 전환 · 복귀 |
| 물었는데 짧게만 답한다 | 길게 잇기 |

고르는 마지막 단계에서 **가장 덜 해 본 요소**의 것을 집는다.
맥락에 맞으면서 한쪽으로 쏠리지 않는다 — 이 둘을 같이 잡는 것이 요점이다.

**한 번 크게 헛짚었다.** 「어려운 말」을 「네 글자 이상」으로 잡았더니
「힘들어요」「그렇군요」「먹었어요」가 죄다 걸렸다. 한국어는 **용언이 활용하면서
길어진다** — 길이만으로는 못 가른다. 조사를 떼고 남은 것이 **용언 어미로 끝나지
않을 때**만 낱말로 본다. 그리고 **덜 잡는 쪽으로 기울였다.** 잘못 잡으면 엉뚱한
넛지가 나가지만, 못 잡으면 다른 표지가 대신 받아 준다.

**`qHold`를 엉뚱한 자리에 넣었다가 바로잡았다.** 「물러서지 않고 다시 청하기」인데
처음에 '내가 질문한 자리'에 붙였다. 재요청은 **상대가 거절한 자리**에서만 말이 된다.

**`qAskFast`(「좀 더 빨리 말해 주세요」)만 자리를 주지 않았다.** 말이 **느린지**는
글로 알 수 없다 — 서버가 보는 것은 글자뿐이고 말 속도는 거기 안 남는다.
억지로 자리를 만들면 엉뚱한 데서 나간다. 빠른 단추(🐇)로 언제든 할 수 있으므로
학습자가 못 하게 되는 것은 아니다. **모르는 것은 모른다고 두는 편이 낫다.**

### ⓒ 새로 나오게 된 것들에 **문형**을 붙였다

예전에는 서버가 못 골라서 문형이 없어도 티가 안 났다. 이제 나오므로 무엇을
말해야 하는지 같이 줘야 한다 — **이름만 보여 주면 넛지가 아니라 수수께끼다.**
거절·대안·재요청·돌려 말하기·명료화 응답·화제 복귀 여섯에 문형을 넣어 14 → **20개**.
말투 3단계 × 2~3가지는 그대로다.

### ⓓ 이름을 빠른 단추와 같은 낱말로

「천천히 말해 달라고 하기」 → **「천천히」**. 다시·쉽게·빨리도 같다(18개 언어, 120건).
빠른 단추가 🐢천천히인데 넛지 이름이 「천천히 말해 달라고 하기」면 **같은 것을
두 이름으로 부르는 것**이다. 뜻은 함께 나오는 문형이 지고 간다.

### ⓔ 검사

`fittest.py`를 새로 만들었다 — `_fit_intervention`을 실제로 떼어 와 14가지 대화
모양에 대고 돌리고, 27개가 닿는지·요소가 쏠리지 않는지까지 본다.
`qa_all.py`의 기대값 둘은 **결정이 바뀌어서** 고쳤다(이야기에는 공감이 먼저,
빈 대화에는 먼저 말 걸기). 왜 바뀌었는지를 주석으로 남겼다.

---

## v113 — 2026-08-18 · 소리를 고르게, 고른 값을 한 줄로

### ⓐ 배경음 15% — 그리고 **파일끼리 27dB이 벌어져 있었다**

배경음을 기기 음량의 20% → **15%**로 낮췄다(징도 같은 비율로 0.5 → 0.375).
그런데 홈 배경음을 무작위로 돌리려고 새 곡 넷을 받아 재어 보니, 문제가 배율이 아니었다.

| 음원 | 라우드니스 |
|---|---|
| 기존 bgm.mp3 | **-9.5** LUFS (게다가 +0.8 dBTP — 클리핑) |
| 단화 | -14.7 |
| 휘파람새 | -21.9 |
| 덕수궁 빗소리 | -32.0 |
| 싱그러운 봄 아침 | **-36.8** |

**27dB이 벌어져 있다.** 같은 배율을 곱하면 어떤 날은 아무 소리도 안 들리고
어떤 날은 시끄럽다. 무작위로 고르는 순간 **음량이 매번 도박**이 된다.

- 다섯 곡을 모두 **-11 LUFS / TP -1.5 dBTP**로 맞췄다(이제 1.5dB 안쪽).
  덤으로 기존 곡의 클리핑(+0.8 dBTP)도 없어졌다.
- 320kbps 스테레오 → **112kbps**로 다시 인코딩. 14MB → 6.7MB.
- `<audio src>`를 **비워 두고** 고른 한 곡만 받는다(`preload="none"`).
  다섯 곡을 다 미리 받으면 20명 동시 접속에 그것만으로 134MB다.
  한 사람이 받는 것은 0.7~2.1MB.

> 배운 것: 「소리를 줄여 달라」는 요청에서 손댈 곳은 **배율과 음원 두 곳**이다.
> 배율만 만지면 파일마다 다른 크기는 그대로 남는다. 여러 음원을 섞어 쓰기로 한
> 순간부터 **음원을 한 기준으로 맞추는 일**이 배율보다 먼저다.

`GONG_VOL`을 상수로 빼면서 **`const` TDZ 함정을 또 만들었다** — 선언보다 위에서
쓰고 있었다. v110의 `bgmGain`과 똑같다. 선언을 위로 옮기고, 검사에
「선언이 쓰는 곳보다 위인가」를 항목으로 넣었다. 세 번째는 없게.

### ⓑ 접힌 페이더 자리에 한 줄

v111에서 대화가 시작되면 페이더를 접게 했다. 화면은 넓어졌으나 **학습자가
자기가 뭘 골랐는지 잊는다.**

숫자를 다시 보여 주는 것은 뜻이 없다 — 「70 · 친한 사이」의 70이 무슨 뜻인지는
학습자가 알 바가 아니다. 학습자가 알아야 할 것은 딱 하나, **어떻게 말하나**다.
그래서 값이 아니라 **할 일**을 한 줄로 적는다.

```
친한 사이 · 🪜 보통 · 존댓말로 말해 보세요
절친 (BFF) · 상급자 (VIP) · 🪜 많이 · 반말로 말해 보세요
```

- 말투는 친밀도·지위를 **함께** 봐야 나온다. 그 계산은 이미 `speechTier()` 한 곳에
  있으므로 다시 만들지 않았다(같은 식을 두 벌 두면 한쪽만 고쳐져 어긋난다).
- **지위가 '대등'일 때는 말하지 않는다.** 기본값이라 정보가 없고, 한 줄이 길어지면
  휴대폰에서 두 줄로 넘어가 도리어 안 읽힌다. 도움을 껐을 때도 마찬가지.
- 페이더를 다시 펼치는 자리에서는 이 줄을 거둔다 — 같은 말이 두 번 보이면 안 된다.

### ⓒ 한 줄이 오래된 구멍을 찾아냈다

이 줄은 관계·지위 이름(`distLv`·`powerLv`)을 **맨 앞에** 세운다.
그러자 **네팔어·라오어·미얀마어·크메르어·우크라이나어·키르기스어 여섯 언어에서
영어가 튀어나왔다.** 이 여섯은 처음 넣을 때 영어 블록을 복사해 시작했고,
그 두 항목은 끝내 번역되지 않은 채였다. 페이더 안에서만 쓰일 때는 아무도 못 봤다.

고치는 과정에서 하나 더 걸렸다. 번역을 사전 조립 **앞**에 놓았더니 그대로 지워졌다.
`I18N_X6`가 이 여섯을 `ko + en + X6`로 **처음부터 다시 만들기** 때문이다.
뒤로 옮겨서야 살아남았고, 그 사실을 주석과 검사 항목으로 남겼다.

> 배운 것: 화면에 새로 만든 자리가 **낡은 구멍을 비추는 손전등**이 된다.
> 안 보이던 곳에 있던 값을 눈에 띄는 곳으로 옮기면, 그 값이 부실했다는 사실이
> 같이 드러난다. UI를 바꾸는 일은 그래서 종종 **점검이 된다.**

### ⓓ 검사

`sndtest.js`(36개 항목)를 새로 만들었다. 음량 배율·음원 다섯 곡의 실제 존재와 크기·
한 줄의 조립 규칙·18개 언어의 사전 완성도를 함께 본다. 전체 24개 검사 모두 통과.

---

## v112 — 2026-08-17 · 연출과 조작을 몸에 맞추기

> **4.4(사용성 평가) 기록.** 두 항목 모두 **화면을 보고는 찾을 수 없고,
> 실제로 손을 대 봐야 드러나는 것**이다. 하나는 귀의 문제, 하나는 손의 문제다.

### ⓐ 문이 열리는 동안 소리가 났다

발화 연습에 들어갈 때 한옥 장지문이 열린다. 그런데 **문을 여는 것과 동시에** 첫 발화가
나왔다. 문짝은 애니메이션 3.2초 가운데 **1.92초까지 닫힌 채로 버티다가** 그때부터 열린다.
그러니 학습자에게는 **닫힌 문 뒤에서 말소리가 나는 것**으로 보였다.

- 문이 열린 뒤에 할 일을 예약하는 장치(`doorOnOpen`/`doorFireOpen`)를 만들어,
  첫 발화를 **2.65초 뒤**로 미뤘다. 문틈이 충분히 벌어진 시점이다.
- **화면은 먼저 그린다.** 소리만 미룬다 — 문이 걷혔을 때 이미 준비된 화면이 보여야 한다.
- 문을 눌러 건너뛰면 **그 순간 바로** 들린다. 예약은 반드시 한 번 실행되고,
  두 번은 실행되지 않는다(문을 치울 때도 남은 예약을 흘려보낸다).

> 배운 것: 연출과 소리는 각자 맞는 시점이 있는데, 코드에서는 **한 줄 아래위**에 있어
> 같은 순간에 일어난다. 시간이 걸리는 연출 뒤에 무엇을 붙일 때는 **연출이 끝나는 지점을
> 코드가 알고 있어야** 한다. 안 그러면 "동시에"가 기본값이 된다.

### ⓑ 같은 모양의 버튼이 화면마다 다르게 움직였다

주제 대화·자유 대화의 발화 버튼은 **꾹 누르고 있는 동안만** 말한다.
그런데 발화 연습의 마이크는 **누르면 켜지고 다시 누르면 꺼지는 온·오프**였다.
모양도 색도 거의 같은 버튼이 **다르게 움직인** 것이다. 학습자들이 헷갈렸다.

말하기라는 **하나의 동작에 하나의 방식**만 둔다 — 꾹 누르기.

- 탭 토글(`PR_TAP_MS`·`prTapMode`)을 걷어내고, 대화 화면의 발화 버튼과 같은 구조로 짰다.
  꼬리(끝음절 보존) 350ms, 꼬리 대기 중 다시 누르면 같은 발화를 이어감, 자동 종료 45초.
- 대화 화면 발화 버튼이 가진 **안전장치를 전부 옮겨 왔다** —
  `lostpointercapture`, 창 밖에서 떼기, `blur`, `visibilitychange`, 스페이스바 꾹 누르기.
- 버튼 **속까지 같게** 했다. `[마이크 아이콘] + [막대 5개] + [글자]`.
  예전에는 `textContent` 로 글자를 바꿔 아이콘까지 날려 버렸다. 이제 글자만 갈아 끼운다.
  막대는 같은 마이크 스트림에 분석기를 달아 **실제 목소리 크기**로 움직이고,
  세기 구간(0.015~0.235)도 대화 화면과 똑같이 맞췄다.
- 안내 문구도 동작에 맞췄다. 「다 말했으면 **다시 누르세요**」 → 「다 말하면 **손을 떼세요**」
  (18개 언어). 한국어 발화 버튼은 「꾹 눌러서 말하기」로 분명히 했다.

**꾹 누르기로 바꾸면서 새로 생긴 문제 하나**를 같이 처리했다.
마이크 허락을 처음 받는 순간 브라우저 팝업이 뜨는데, 그 사이 학습자는 손을 뗀다.
그러면 **누른 사람은 없는데 녹음은 도는** 상태가 된다. 시작이 끝난 뒤에
아직 누르고 있는지 다시 보고, 아니면 곧바로 마무리한다.

> **버린 것도 적어 둔다.** 온·오프는 손이 불편한 학습자에게 더 편할 수 있다.
> 그 길을 완전히 막지는 않았다 — 마이크를 못 쓰는 기기용 「✅ 말했어요」 단추는 남겨 두었다.
> 얻은 것: 앱 어디서나 말하는 법이 하나다. **배워야 할 것이 하나 줄었다.**

### ⓒ 또 검사가 옛 결정을 붙들고 있었다

`restest.js`가 「탭·꾹 누르기 둘 다」와 「자동 종료 20초」를 **정답으로 박아 두고** 있었다.
v110의 총평 버그와 같은 모양이다. 세 번째다.

- 검사를 새 결정으로 고치고, **왜 되돌렸는지**를 주석으로 남겼다.
  그래야 다음에 이 검사를 보는 사람이 "옛날엔 둘 다 됐었네"로 오해하지 않는다.
- 발화 연습 항목은 `prtest.js`로 따로 뺐다(29개 항목).

> 배운 것: 검사는 **결정을 지키는 장치**이지 결정 그 자체가 아니다.
> 결정이 바뀌면 검사도 바뀌어야 하고, 그때 **바뀐 이유를 검사에 적어** 두어야
> 검사가 낡은 결정의 화석이 되지 않는다.

---

## v111 — 2026-08-17 · 아이폰 실기기 시험에서 온 화면 고치기

> **4.4(사용성 평가) 기록.** 이 항목은 교육적 판단이 아니라 **실제 기기에서만 드러나는 것**들이다.
> 안드로이드에서 멀쩡하던 것이 아이폰에서 어긋났고, 화면이 좁아 생기는 불편은
> 개발자 화면에서는 끝내 보이지 않았다. **실기기 시험이 아니면 못 찾는 층위**가 따로 있다.

### ⓐ 번역투 — 화면의 말이 챗봇의 말이 된다

「빨리 말해 주세요」 단추를 눌렀더니 호아랑이 **「응, 신속히 이야기할게」**로 답했다.
원인은 챗봇이 아니라 **단추 문장 자체**였다. 격식 단계 문장이 「죄송하지만, 신속히
말씀해 주시겠습니까?」였고, 호아랑은 상대의 어휘를 따라 쓰는 성질이 있어 그대로 받았다.

- 단추 문장에서 「신속히」를 지웠다 → 「조금만 빨리 말씀해 주시겠습니까?」
- 페르소나에 **번역투·문어체 금지** 항목을 넣었다. 금지 목록(신속히·~하도록 하겠습니다·
  ~에 대하여·상기·해당·본인…)과 대체형을 짝으로 준다.
- 더 중요한 한 줄: **학습자가 문어체로 물어도 따라 하지 마라.** 챗봇이 학습자의 말을
  되받는 순간, 학습자가 잘못 쓴 말이 **입력으로 되돌아온다.**

> 배운 것: 학습자에게 보이는 문장은 곧 학습자가 쓰는 문장이고, 챗봇이 되받으므로
> **다시 학습자에게 돌아온다.** 화면의 예시 문장은 교재 문장과 같은 무게로 다루어야 한다.

### ⓑ 빠른 단추 넷을 한 줄로, 짝을 나란히

「천천히 · 빨리」는 **속도라는 한 축의 양끝**인데 화면에서 떨어져 있었다.
휴대폰에서는 2×2로 접혀 더 멀어졌다.

- 차례를 🐢천천히 · 🐇빨리 · 🔁다시 · 💡쉽게로 바꿔 **짝을 붙였다.**
- 이름을 줄이고(다시 한 번→다시) 글자·여백을 줄여 **좁은 화면에서도 한 줄**을 지켰다.

### ⓒ 대화가 시작되면 페이더가 접힌다

말투·도움 정도는 **시작 전에 정하는 것**이고 시작한 뒤에는 바꿀 수 없다(이미 서버로 갔다).
그런데 화면에는 계속 남아 대화 창을 밀어내고 있었다. 시작과 동시에 접는다.
대화 창이 그만큼 길어져 **휴대폰에서 스크롤할 일이 준다.**

### ⓓ 영상 통화에도 대화 종료 단추

예전에는 채팅으로 돌아가야만 끝낼 수 있었다. **학습자는 그 길을 모른다.**
영상 통화 화면 아래에 종료 단추를 두고, 누르면 확인 뒤 곧바로 결과로 간다.

### ⓔ 빈 화면 문구를 '할 일'로

「대화를 시작하면 카카오톡처럼 대화가 표시됩니다」는 **설명**이지 **지시**가 아니다.
처음 여는 학습자에게 필요한 것은 다음에 뭘 누르는지다.
→ 「「대화 시작하기」를 눌러 보세요」 (18개 언어)

### ⓕ 결과 화면 — 점수를 크게

점수·내 발화·대화 시간이 **똑같은 크기**라 무엇이 점수인지 한눈에 안 들어왔다.
점수 칸을 1.5배 넓히고 숫자를 30px로 키웠다.

### ⓖ 지연 표시(ms·KB) 감추기

오류를 잡을 때 필요했던 진단 숫자다. 이제 안정되었으니 학습자 화면에서는 지운다.
다만 **없애지는 않았다** — 주소 뒤에 `?net=1`을 붙이면 다시 나온다.
시험 중에 다시 필요해질 수 있고, 지워 버리면 그때 처음부터 만들어야 한다.

### ⓗ 아닌 것으로 밝혀진 것 — 녹음 안내 팝업

「두 번째 대화부터 녹음 안내가 안 뜬다」는 보고는 **버그가 아니었다.**
동의 여부는 페이지가 열려 있는 동안만 기억하는 값이라, 같은 접속 안에서는 다시 묻지 않는다.
아이폰 홈 화면 앱은 페이지를 오래 살려 두어 **그 성질이 더 눈에 띈 것**이다.

### ⓘ 검사 자체가 낡아 있었다

이번에 검사를 전부 돌려 보니 **아홉 개가 옛 파일 이름(`i.html`)과 죽은 절대 경로**를 보고 있었다.
파일을 못 열어 그대로 죽었으므로, **통과도 실패도 아닌 채로 한동안 지나간 것**이다.

- 아홉 개의 경로를 고쳐 22개 검사가 모두 돈다.
- `langtest.js`는 빈 화면 문구가 **두 줄이라는 옛 전제**(`<br>`이 있어야 한다)를 붙들고 있었다.
  ⓔ에서 한 줄로 바꾸었으니 그 전제부터 고쳤다.

> 배운 것: v110에서 「검사가 버그를 정답으로 박아 두고 있었다」를 겪었는데, 이번에는
> **검사가 아예 돌지 않고 있었다.** 검사도 코드와 같이 낡는다. 통과 개수만 볼 것이 아니라
> **몇 개가 실제로 돌았는지**를 같이 봐야 한다.

---

## v103 — 2026-08-16 · ★ 음성 합성을 Cloud TTS로 옮김 — 한 반이 동시에 써도 안 막히게

> **논문 제4장 집필용 기록.** 이 항목은 4.3(구현) 가운데 *운영 제약이 설계를 되밀어 올린 사례*에
> 해당한다. 교육적 판단이 아니라 **한 교실 30명이라는 조건**이 구현을 바꾸게 한 경우다.

### ⓐ 문제 — 수업에 들어가기도 전에 막히는 한도

주제 대화 도중 429(`RESOURCE_EXHAUSTED`)가 떴다. 원인을 따라가 보니 대화(Live API)가 아니라
**발화 연습·🪜 도움말의 음성 합성**이 병목이었다.

| 경로 | 분당 | 하루 |
|---|---|---|
| Gemini API TTS (`gemini-2.5-flash-preview-tts`) | **10회** | **100회** |
| Google Cloud Text-to-Speech (Chirp 3 HD) | **200회** | 상한 없음 |

한 학습자가 한 차시에 발화 연습을 10회쯤 누른다고 보면, **하루 100회는 학생 10명에서 끝난다.**
45명 대상 실험을 감당할 수 없는 수치다.

### ⓑ 관찰 — 같은 목소리가 두 문에 걸려 있었다

`VOICE_TABLE`이 쓰는 Puck·Leda·Charon·Kore·Aoede·Algenib·Gacrux는 **Chirp 3 HD 프리빌트
보이스**다. 이 목소리는 Gemini API와 Cloud TTS **양쪽에서 같은 이름으로 제공된다.**
즉 *들리는 소리를 하나도 바꾸지 않고* 부르는 문만 바꿀 수 있었다.

- Gemini API : `models.generate_content(response_modalities=["AUDIO"], voice_name="Puck")`
- Cloud TTS  : `POST /v1/text:synthesize`, `voice.name = "ko-KR-Chirp3-HD-Puck"`

### ⓒ 처치

`/tts`를 **Cloud TTS 우선 · Gemini 폴백**으로 바꿨다. 순서가 중요하다 —
새 경로를 기본으로 두되, **실패해도 소리가 끊기지 않는 것**을 먼저 보장했다.

```python
pcm = await cloud_tts_pcm(text, voice, style_on)   # ① Cloud
if pcm: return Response(pcm, media_type="audio/pcm")
…                                                  # ② 안 되면 예전 Gemini 경로
```

되돌아가는 조건을 넷으로 못 박았다. **키 없음 · 목소리 목록 조회 실패(403 등) ·
합성 실패(429·5xx) · 빈 응답.** 연달아 3번 실패하면 5분, 목록 자체를 못 받으면
10분 쉬었다 다시 본다. 매 호출마다 죽은 문을 두드려 지연을 두 배로 만들지 않기 위해서다.

**딸린 문제 셋을 함께 처리했다.**

1. **응답 형식이 다르다.** Cloud TTS의 `LINEAR16`은 WAV 통에 담겨 온다. 화면 쪽
   `playPcm24()`는 알맹이(raw PCM)만 받는다. 44바이트를 고정으로 자르지 않고
   **`data` 청크를 찾아 벗기게** 했다(`_pcm_from_wav`). 앞에 `LIST` 같은 청크가
   끼어 오는 경우가 있어 고정 자르기는 언젠가 깨진다.
2. **말투 지시를 못 받는다.** Gemini TTS는 `"밝고 신나는 아이 목소리로: \"…\""` 같은
   자연어 지시를 받지만 **Chirp 3 HD는 받지 않는다.** 그대로 보내면 지시문을 소리 내어
   읽는다. 그래서 Cloud 경로에는 본문만 보내고, 아이 목소리일 때 `speakingRate`를
   1.08로 올려 「조금 빠르고 가볍게」만 남겼다. 목소리 자체가 이미 아이 목소리라
   손실은 크지 않다.
3. **없는 목소리를 부르면 400이 난다.** 켜질 때 `/v1/voices`로 **그 프로젝트에서
   실제로 쓸 수 있는 이름을 한 번 받아 두고**, 목록에 없으면 기본 목소리로 바꿔 부른다.
   추측으로 이름을 짓지 않는다.

### ⓓ 확인 경로를 남겼다

배포가 됐는지 **로그를 안 열고도** 알 수 있어야 한다. `/version`에 붙였다.

```json
"tts": { "key": true, "path": "cloud", "voices": 30, "used": 12, "err": "" }
```

`path`가 `"gemini"`면 키가 안 들어갔거나 거부된 것이고, 이유는 `err`에 찍힌다.
`/voicetest` 맨 아래에도 같은 내용을 한 줄로 적었다.

> **교훈.** v95에서 「올라간 줄 알았는데 안 올라간」 사고를 겪고 `/version`을 만들었다.
> 이번에도 **새 경로를 만들면 그 경로가 살아 있는지 보는 창을 같이 만든다**는 규칙을 지켰다.
> 새 기능보다 *새 기능이 죽었는지 아는 법*이 먼저다.

### ⓔ 비용

Cloud TTS Chirp 3 HD는 100만 자당 $30이다. 45명 × 한 차시 발화 연습 40문장 ×
평균 30자 ≈ 5.4만 자 → **약 $1.6.** 실험 전체를 통틀어도 무시할 만하다.
다만 **Cloud TTS는 AI Studio의 선불 크레딧과 청구가 별개**이므로 결제 계정을 따로 본다.

### ⓕ 교육적 개입을 〈표 33〉의 하위 내용에 맞춰 채웠다

v95에서 여덟 요소를 **덮기는** 했으나, 세어 보니 쏠려 있었다.

```
전:  대화이동 4 · 전략 3 · 화제 3 · 수정 2 · 듣기 1 · 차례 1     (14개)
후:  대화이동 4 · 전략 4 · 화제 4 · 수정 3 · 듣기 3 · 차례 4     (22개)
```

**차례 관리와 상호작용적 듣기가 각각 하나뿐**이었다. 둘 다 미시층의 핵심인데
개입할 거리가 하나밖에 없으니, 그 요소는 사실상 개입 대상에서 빠져 있던 셈이다.

논문 〈표 33〉이 각 요소에 둔 **하위 내용을 기준선으로 삼아** 빈 자리를 채웠다.
임의로 늘린 것이 아니라, 표에 이미 적혀 있으나 코드에 없던 것을 옮긴 것이다.

| 요소 | 〈표 33〉 하위 내용 | 있던 것 | 채운 것 |
|---|---|---|---|
| 차례 관리 | 획득 · 유지 · 양보 · 되찾기 | 양보 | `qTakeTurn`(획득) `qKeepTurn`(유지) `qRegain`(되찾기) |
| 상호작용적 듣기 | 이해 확인 · 반응 표시 | 이해 확인 | `qEmpathy`(공감) `qContinuer`(말 이어가기 신호) |
| 의사소통 전략 | 우회 · 모국어 전환 · 따라 말하기 · **지연·회피** | 앞의 셋 | `qFiller`(지연) |
| 화제 관리 | 도입 · 심화·확장 · 전이 · **마무리** | 앞의 셋 | `qCloseTopic`(마무리) |
| 의사소통 단절 수정 | 자기 교정 · 명료화 요구 · 이해 확인 · 도움 요청 | 자기 교정·고쳐 말하기 | `qCheckUnd`(이해 확인) |

**겹치지 않게 두었다.** 명료화 요구와 도움 요청은 화면 쪽 `qClarify`·`qWord`가
이미 세고 있으므로 LLM 판정으로 다시 만들지 않았다. 논문 3.3.2 말미가
「두 요소의 세부 요소가 중복되지 않도록 정리한다」고 못 박은 대로다.

**두 요소에는 일부러 개입을 두지 않았다.**

- **기능 단계(stage)** — 진행 바와 단계 칩이 이미 비계 노릇을 한다. 개입을 더하면
  같은 것을 두 번 미는 꼴이 된다.
- **맥락·정체성 인식(context)** — 이름 그대로 *인식*이다. 존댓말과 역할 수행을
  제대로 하는지만 보면 되고, 혼동하면 호아랑이 **대화 안에서 곧바로 바로잡고**
  사후 피드백에서 다루는 편이 맞다. 대화를 멈추고 「지금 존댓말 하세요」라고
  알리는 것은 맥락 인식을 가르치는 방식이 아니다.

### ⓕ-2 〈표 34〉를 세부 요소 단위로 훑으니 ◎인데 빈 자리가 셋 더 있었다

ⓕ에서 〈표 33〉의 **요소** 단위로 맞췄으나, 〈표 34〉의 **세부 요소** 단위로 다시
훑으니 ◎(AI 담당) 판정인데도 대응하는 항목이 아예 없는 자리가 셋 남아 있었다.

| 세부 요소 | 판정 | 근거(표 34) | 있었나 |
|---|---|---|---|
| 대화이동 **시작** | ◎ | 이남호·이찬규(2025) | **없음** → `qInitiate` |
| 대화이동 **역시작** | ◎ | 이남호·이찬규(2025) | **없음** → `qCounter` |
| 화제 **전환·전이** | ◎ | 이남호·이찬규(2025) 유형 C | `qReturn`은 *돌아오기*라 전환을 못 잼 → `qShiftTopic` |

대화이동 관리는 항목이 넷(`qRefuse`·`qAlt`·`qCond`·`qHold`)이나 있어 넉넉해 보였는데,
**전부 반응·수정·고수 쪽**이었다. 정작 연쇄를 여는 **시작**과 되받는 **역시작**이
비어 있었다. 개수를 세는 것과 무엇을 재는지 보는 것은 다르다는 사례다.

**△·✕는 그대로 두었다.** 표 34가 교실 몫으로 배분한 것을 챗봇으로 끌어오면
매체 배분표가 무너진다.

| 세부 요소 | 판정 | 표 34의 배분 | 코드에서 |
|---|---|---|---|
| 차례 시작·요구 | △ | 경쟁적 차례 요구는 교실 | 두지 않음 |
| 발화 공동 완성 | △ | 따라 말하기의 대안적 형태로 AI | `qEcho`가 그 자리 |
| 맞장구·반응 발화 | △ | 턴 사이 반응은 AI | `qBackch`·`qEmpathy` |
| 말 이어가기 신호 | △ | 추가 정보 요구의 형태로 AI | `qContinuer` |
| 끼어들기·이어말하기 | △ | AI는 **턴 되찾기의 대안적 형태**로 | `qRegain` |
| 비언어적 행위 | ✕ | 교실 전담 | 두지 않음(화면에 안내문) |

### ⓕ-3 ★ 끼어들기가 △인 이유를 거꾸로 읽었던 것 — 되찾기 퀘스트를 걷어냄

처음에 `qRegain`(뺏긴 차례 되찾기)을 넣으면서 이렇게 적었다.
「표 34는 텍스트 대화를 놓고 △로 판정했는데, 이 챗봇은 음성이라 끼어들기가
실제로 감지된다(`sc.interrupted`)」. **방향을 거꾸로 본 것이다.**

끼어들기가 △인 것은 **학습자가 못 해서가 아니라 챗봇이 못 해서다.**
경쟁적 차례는 양쪽이 겨루어야 성립한다. 코드를 다시 보면 분명하다.

```python
automatic_activity_detection=types.AutomaticActivityDetection(disabled=True)
```

**자동 음성 감지를 꺼 두었다.** 학습자가 버튼을 눌렀다 뗄 때만 차례가 넘어간다.
그러므로 호아랑은 **일부러도, 침묵 감지로도** 학습자의 말을 자르고 들어오지 못한다.
`sc.interrupted`는 반대 방향 — 학습자가 호아랑을 끊는 신호다.

그 결과는 이렇다.

| 차례 관리 세부 | 학습자 | 호아랑 | 판정 |
|---|---|---|---|
| 시작·요구 | 할 수 있음 | **겨루지 않음** | △ → 교실 |
| 유지·발언권 갖기 | 할 수 있음 | 방해하지 않음 | ◎ (`qKeepTurn`) |
| 양보·끝맺기 | 할 수 있음 | 받음 | ◎ (`qEndTurn`) |
| 끼어들기 | **할 수 있음** | **못 함** | △ (`qTakeTurn` — 학습자 쪽 반쪽만) |
| 되찾기 | — | **뺏지 않음** | **일어날 수 없음** |

`qRegain`은 **일어날 수 없는 일을 재는 퀘스트**였다. 뺏긴 차례가 없으니 되찾을
차례도 없다. 서버·화면·18개 언어 이름에서 모두 걷어냈다(25 → **24개**).

> **제4장에 쓸 것.** 이 챗봇은 **협력적으로만 행동하는 대화 상대**다. 절대 바닥을
> 두고 겨루지 않는다. 그래서 차례 관리 가운데 **경쟁적 국면(요구·방어·되찾기)은
> 구조적으로 연습되지 않고**, 학습자 쪽 반쪽(끼어들기)만 남는다. 이것이 〈표 34〉가
> 이 세부 요소를 교실에 배분한 이유이며, 매체의 한계를 모형의 결핍으로 두지 않고
> 교실과 나눈다는 3.3.4의 논지를 **구현이 그대로 뒷받침하는** 사례다.
>
> 뒤집어 말하면, 챗봇에 침묵 감지를 켜서 호아랑이 끼어들게 만들면 경쟁적 차례를
> 연습시킬 수는 있다. 다만 그것은 **초급 학습자에게 말할 시간을 빼앗는 일**이라
> 누르고 말하기를 택한 설계 판단(v?? 참조)과 정면으로 충돌한다. 무엇을 얻고
> 무엇을 잃는지가 분명한 갈림길이므로, 4장에서 선택의 근거로 기술한다.

최종 분포 — **24개**.

```
대화이동 6 · 화제 5 · 전략 4 · 차례 3 · 듣기 3 · 수정 3
```

### ⓕ-4 ★★ 발화 연습이 늘 '가운데'에만 서 있던 이유 — 코드가 프롬프트를 뭉갰다

「① 듣고 따라 말하기 → ② 대답해 보기」의 **'대답'이라는 말부터 틀렸다.** 학습자의
연습 발화는 대화이동 연속체 안에서 **시작·반응·의사확인 어디에도** 설 수 있다.
가운데에만 세우면 묻는 말에 답하는 연습만 하게 되어, 정작 기르려는 대화이동 관리와
차례 관리를 못 기른다. 이름을 **「② 말해 보기」**로 고쳤다(18개 언어).

그런데 이름만 문제가 아니었다. 프롬프트는 이미 이렇게 시키고 있었다.

> cue — text 바로 앞에 올 상대의 발화. **학습자가 먼저 말을 여는 자리면 빈 문자열로 둔다.**

**그리고 그 뒤에서 코드가 도로 채웠다.**

```python
e["cue"] = prev["text"] if (prev and prev["speaker"] == "ai") else ""
```

대화문은 늘 상대(`ai`)로 시작해 번갈아 가므로 **학습자 줄의 앞줄은 거의 항상 상대의
발화**다. 모델이 「여는 자리」로 비워 둔 `cue`를 이 한 줄이 전부 메웠다.
지시를 아무리 정교하게 써도 소용이 없었던 것이다.

- `if e.get("cue"):` — **비어 있는 cue 는 채우지 않는다.** 여기서 할 일은
  *맞춰 주기*이지 *만들어 내기*가 아니다.
- `_balance_expr_positions()` 신설 — 만들어진 결과를 보고 한쪽으로 몰렸으면 편다.
  여는 자리가 하나도 없으면 가장 짧은 표현을 여는 자리로 돌리고, 3항이 없으면
  자리만 열어 둔다(문구는 대화문이 채운다). **지시로 안 되면 결과를 고친다.**
- 대화문도 「첫 줄은 상황에 맞는 쪽이 연다」로 바꿨다. 늘 `ai`로 시작하면 학습자는
  대화를 여는 연습을 한 번도 못 한다.

> **교훈.** 프롬프트를 고쳐도 안 바뀌면 **프롬프트 뒤에서 결과를 만지는 코드**를
> 의심해야 한다. 이번 것은 v?? 부터 계속 있었고, 그동안 프롬프트만 다듬고 있었다.

**말해 보기에서는 연속체 전체를 들려준다.** 한 마디만 들려주면 앞뒤가 잘려
연쇄를 배울 수 없다. `prSeqPlay()` 가 [상대] → [나] → [상대] 를 이어 재생하고,
지금 나오는 대목을 화면에서 테두리로 표시한다(누구의 차례인지 보이게).
이를 위해 `playPcm24()` 가 끝나는 시점을 `Promise` 로 알려 주게 했다.

**연습 화면의 「화제 관리」 배지도 뺐다.** 연습하는 사람이 알아야 할 것은
*무엇을 말하나*이지 *이것이 무슨 능력인가*가 아니다. 요소별 결과는 끝난 뒤에 본다.
같은 이유로 🪜 도움말 제목의 「· 의사소통 단절 수정」도 뺐다.

### ⓕ-5 ★ 비계가 대화를 끊고 있었다

🪜 도움말이 「아니, 없어.」 「음, 없어.」를 내놓았다. **틀린 답이 아니라 닫는 답이다.**
막혀서 도움을 부른 학습자에게 대화를 끝내는 말을 주면 도움이 아니라 마침표가 된다.

프롬프트가 「상대가 물었으면 답이 되어야 한다」까지만 시키고 있었다. 답이 되는 것과
**대화를 이어 가는 것**은 다르다. 대화이동 연속체로 보면, 부정응답만 하고 연속체를
끝낸 자리다. 이어 가려면 **이유·되묻기·새 화제·조건** 가운데 하나가 붙어야 한다.

- 프롬프트에 금지 목록과 바꿔 쓰기 예시를 넣었다.
  「아니, 없어.」 → 「아, 그럼 이번엔 내가 하나 물어봐도 돼?」
- **두 제안은 서로 다른 방식으로 이어야 한다.** 둘 다 같은 방식이면 학습자는
  대화를 여는 길이 하나뿐이라고 배운다.
- 그래도 새어 나올 때를 대비해 `_is_dead_end()` 로 걸러 낸다. 지시는 지켜지지 않을
  수 있으므로 **마지막 문을 코드로 잠근다.** (시험 18건 통과 — 닫는 말 10개를 걸러
  내고 이어 가는 말 8개는 살린다)

### ⓕ-6 넛지 — 횟수보다 '자리'

4분 21초·19차례 대화에서 4회는 적었다. 다만 **횟수만 늘리면 잔소리가 된다.**
두 가지를 함께 바꿨다.

- 상한을 올렸다: `{{1:2, 2:4, 3:6}}` → `{{1:3, 2:6, 3:10}}`. 개입할 거리가 8개에서
  24개로 늘었으니 상한이 병목이 되어서는 안 된다. 「많이」에서 최대 4회 → **8회**.
- **고르는 기준을 바꿨다.** 예전에는 목록에서 「학습자가 할 수 있는 것」을 골랐다.
  이제 **[상대가 방금 한 말]을 따로 떼어 주고, 거기에 이어 붙는 대화이동**을 고르게 한다.
    · 상대가 제안했다 → 거절·조건 달기·대안
    · 상대가 난색을 보였다 → 고수·대안
    · 학습자가 짧게만 답한다 → 이어 말하기·먼저 꺼내기
  흐름과 상관없는 지시는 돕는 것이 아니라 방해다.

### ⓕ-7 대화문 속도를 되돌렸다

Cloud TTS로 옮기며 아이 목소리에 `speakingRate 1.08` 을 붙였는데, 그것이
**「먼저 들어보기」의 모델 대화문까지 빠르게** 만들었다. 초급 학습자에게 들려주는
대화문은 느린 편이 낫다. 속도로 어린 톤을 흉내 내는 것보다 **알아들을 수 있는 것이
먼저다.** 기본값을 1.0으로 되돌렸다(환경변수로 올릴 수는 있다).

### ⓕ-8 총평이 안 나오는 이유를 볼 수 있게

「이번에는 총평을 만들지 못했어요」가 자유·주제 양쪽에서 계속 떴는데 **이유를 알 길이
없었다.** 프롬프트는 지시문 1,300자에 대화 30턴이고 `thinking_budget=0`이므로
1~3초면 끝날 분량이다. 느린 것이 아니라 **실패하는 것**이다.

- `/version` 에 `review: {{ok, fail, err, model, timeout}}` 을 붙였다.
- `/reviewtest` 를 새로 만들었다. 대화를 끝내지 않고도 **같은 모델·같은 설정**으로
  한 번 불러 보고 첫 글자까지 걸린 시간과 오류를 돌려준다.
  ok=true 면 모델·키·한도는 멀쩡한 것이고, 그러면 총평 쪽 로직 문제다.
- 제한 시간 12초 → 25초. 12초는 빡빡했다.
- 예외 없이 빈 응답이 오는 경우(안전 필터·빈 후보)도 따로 기록한다.

> **교훈.** 「왜 안 되지」를 세 번 물었으면 **묻는 대신 보이게 만들어야 한다.**
> 진단을 먼저 붙였으면 추측으로 두 번 고칠 일이 없었다.

### ⓕ-9 ★★★ 총평이 안 나오던 진짜 이유 — 빠르게 만든 것이 사라지게 했다

며칠을 추측으로 헤맸다. 모델이 느린가, 한도에 걸렸나, 키가 문제인가.
`/reviewtest` 를 만들어 재 보니 **0.49초에 첫 글자, 0.83초에 123자**였다. 멀쩡했다.
그다음 `/version` 의 누적을 보고 답이 나왔다.

```json
"review": { "ok": 1, "fail": 0, "chars": 308, "err": "" }
```

**서버는 308자를 만들어 보냈다.** 화면이 그것을 버리고 있었던 것이다.

원인은 **우리가 고친 두 가지가 겹친 결과**다.

1. v96 — 종료 신호를 받자마자 **총평부터** 시작하게 했다(점수보다 먼저).
2. v102 — `thinking_budget=0` 으로 첫 글자가 **0.8초**에 나오게 했다.

그런데 화면은 이렇게 받고 있었다.

```js
if (rpFinal) rpFinal.review = (rpFinal.review || "") + (msg.text || "");
```

`rpFinal`(점수)은 최종 분석과 요소 프로파일을 **다 마친 뒤에야** 온다.
총평이 그보다 먼저 도착하면 `rpFinal` 이 아직 `null` 이므로 **조각이 통째로 버려진다.**

v102까지는 총평이 느려서 점수 뒤에 도착했고, 그래서 우연히 동작했다.
**총평을 빠르게 만든 순간 총평이 사라졌다.** 최적화가 기능을 껐다.

- `reviewBuf` 를 따로 두어 **점수와 무관하게** 담는다. 화면은 이 값을 본다.
- 결과 화면 두 곳(주제·자유) 모두 `reviewBuf` 를 먼저 보게 했다.
- 새 대화가 시작되면 비운다.

> **교훈 하나.** 「A를 빠르게 하기」와 「B가 A보다 먼저 오기」는 각각 옳았는데
> 둘이 만나 순서가 뒤집혔다. **비동기로 나란히 돌리는 것을 늘릴 때는
> 도착 순서에 기대는 코드가 있는지 먼저 봐야 한다.**
>
> **교훈 둘.** `ttstest2.js` 가 이 버그를 **정답으로 박아 두고** 있었다.
> ```js
> ok("조각을 이어 붙인다", /rpFinal\.review = \(rpFinal\.review \|\| ""\) \+ …/.test(html));
> ```
> 검사가 *코드가 이렇게 생겼는지*를 재면 버그도 함께 굳는다. *무엇을 보장해야
> 하는지*를 재야 한다. 새 검사 넷을 넣어 이 버그가 되살아나면 걸리게 했다.
>
> **교훈 셋.** 추측으로 세 번 고치는 동안 아무것도 못 잡았고, **진단을 붙이자
> 두 번 만에 끝났다.** 「왜 안 되지」를 두 번 물으면 그때는 고치지 말고
> **보이게 만들어야 한다.**

### ⓖ 대화 마무리 영상 — 총평 만드는 시간을 번다

종료를 누르면 총평이 오기까지 몇 초가 빈다. 여기를 「점수 계산 중…」으로 두면
학습자는 **기다림을 의식한다.** 호아랑이 대화를 마무리하는 영상으로 덮었다.

- 원본 12.8 MB(1080×1920, 6.2초) → **412 KB**(720×1280, CRF 28, faststart).
  **교실에서 20명이 한꺼번에 받는다**는 조건에서 13 MB는 그 자체가 병목이다.
  받는 동안 총평이 먼저 도착하면 영상을 넣은 뜻이 없어진다.
- `preload="none"`으로 두고 **대화가 시작될 때** 받는다(`outroWarm()`).
  홈에서 미리 받으면 수업 시작 순간에 20명이 동시에 내려받는다.
- 영상이 도는 **뒤에서 결과 화면을 만든다.** 끝나면 곧바로 결과가 보인다.
- **붙잡아 두지 않는다.** 아무 데나 누르면 건너뛰고, 건너뛰기 단추도 두었다.
  영상이 멈춰 서도 9초면 넘어간다.
- 소리를 켠 채로 재생을 시도하고, 브라우저가 막으면 **음소거로 한 번 더** 해 본다
  (자동재생 정책은 기기마다 다르다).

### ⓗ 총평이 안 올 때 화면이 멈추던 문제

소켓이 끊긴 채 대화가 끝나면 `review_done`이 영영 못 온다. 그런데 화면에는
「총평을 쓰는 중이에요…」가 **시간 제한 없이** 남아 있었다. 교실 와이파이가
20명을 감당하다 한 번 끊기면 그 학습자는 오지 않을 것을 계속 기다린다.

서버 쪽은 `_review_job`이 실패해도 빈 총평을 보내게 잘 짜여 있었다.
**못 보내는 경우**가 빠져 있었던 것이다.

처음에 25초 제한을 생각했으나 **그건 학습자를 모르는 설계다.** 10초만 되어도
학습자는 지쳐 홈으로 나가 버린다. 그리고 애초에 **기다릴 이유가 없다** —
소켓이 죽은 것은 `onclose`로 그 자리에서 알 수 있다.

- `ws.onclose` · `ws.onerror` → **즉시** `revGiveUp()`. 기다리지 않는다.
- 소켓은 살아 있는데 늦는 경우만 10초 backstop.
- 흘러오다 끊긴 총평은 **건드리지 않는다**(부분이라도 없는 것보다 낫다).
  아직 한 글자도 못 받아 `revWait` 그대로인 자리만 바꾼다.

> **교훈.** 「얼마나 기다리게 할까」를 묻기 전에 **「기다릴 필요가 있나」**를 먼저
> 물어야 했다. 신호가 이미 와 있는데 타이머를 붙이려 한 것이 설계 실수였다.

### ⓗ 창을 닫을 때 묻는다

홈 버튼에는 종료 확인이 있었으나 **창 닫기·새로고침·뒤로 가기에는 없었다.**
대화 도중에 나가면 녹음 올리기와 총평이 통째로 날아간다.

- `beforeunload` — 대화 중일 때만 브라우저 확인창을 띄운다.
- `popstate` — 설치한 앱에서는 안드로이드 뒤로 가기가 곧 창 닫기다.
  자리를 도로 채워 두고(`pushState`) 우리 확인창을 띄운 뒤 정상 종료로 넘긴다.

### ⓘ 함께 정리한 것

- 쓰이지 않는 `freeFbBody`를 20개 사전에서 걷어냈다. 「상호작용 능력 피드백은
  곧 추가될 예정」이라는 낡은 문구가 6개 언어에 남아 있었는데, 그 기능은 이미
  들어갔고 **화면에 뿌려지지도 않는 죽은 데이터**였다. 언어마다 문장이 서로
  달라 나중에 누가 되살려 쓰면 앞뒤가 안 맞는다.
- 동시 접속 상한 24 → **32**. 20명한테 24석은 빠듯하다. 학습자가 새로고침하면
  끊긴 것을 서버가 알아채는 데 40초쯤 걸리고(uvicorn ping 20초 × 2) 그동안
  옛 자리가 잡혀 있다. **인원수 = 좌석수로 잡으면 수업 초반 재접속 몇 번에
  벽에 닿는다.**

### ⓙ 함께 정리한 것 (Cloud TTS)

- `requirements.txt`에 `httpx>=0.28.1` 명시. `google-genai`가 이미 끌고 오지만,
  **의존성이 딸려 오는 것에 기대면 상위 패키지가 바뀔 때 조용히 깨진다.**
- HTTP 연결을 모듈 하나가 재사용하도록 했다(`_hc()`). 매번 새로 열면 TLS 악수에만
  수백 ms가 샌다. 발화 연습은 **누르고 나서 들리기까지의 시간**이 곧 체감 품질이다.

---

## v102 — 2026-08-16 · ★ 총평이 늦던 진짜 원인 · 단계가 안 차오르던 원인 · 데스크톱 홈 좌우 배치

### ⓐ ★★ 총평 — 「종료 즉시 시작」인데도 오래 걸린 이유

시작 시점은 v96부터 이미 최적이었다. 문제는 **시작한 뒤 모델이 생각만 하고 있었다**는 것이다.

```python
config=types.GenerateContentConfig(temperature=0.7)   # ← thinking 을 끄지 않았다
```

`_gen_json`도, 라이브 세션도 전부 `thinking_budget=0`으로 끄고 있는데 **총평만 빠져 있었다.**
개발일지에 이미 적어 둔 함정이다 — 「2.5 모델은 동적 사고가 기본 활성화라 응답 전에
수십 초씩 생각한다」(v?? `_gen_json` 주석). 스트리밍을 넣어도 **첫 조각 자체가 안 나오니**
소용이 없었다. 학습자는 결과 네 장을 다 넘기고도 「총평을 쓰는 중이에요…」를 본다.

- `thinking_config=types.ThinkingConfig(thinking_budget=0)` 추가. 이제 첫 글자가 1~2초에 나온다.

> **교훈.** 같은 모델을 부르는 자리가 넷인데 설정이 셋만 같았다. 새 호출을 만들 때
> **기존 호출의 config를 그대로 베끼는 편이** 안전하다.

### ⓑ 기능 단계가 안 차오르던 원인 — 표시선이 판정까지 좁혔다

마무리 인사를 주고받고도 진행률이 50%에 멈춰 있었다. v100에서 **한 덩어리 안에 표시선만
그은 것**이 원인이다. 「(1)(4)(5)는 표시선과 무관하게 전체를 보라」고 지시했으나
모델이 따르지 않았다.

**지시로 막지 말고 블록을 갈랐다.**

```
[대화 기록(전체) — (1)(4)(5)는 여기서 본다]
…

[새로 늘어난 부분 — (2)(3)은 여기서만 본다]
…
```

무엇을 어디서 보는지가 구조로 드러나므로 헷갈릴 여지가 없다. 입력이 조금 늘지만,
단계가 안 차오르는 것에 비하면 싼 값이다.

### ⓒ 개입이 왜 안 나오는지 로그로 보인다

v100의 「2분 대화에 개입 0회」는 원인(기기에 남은 부풀린 누적)을 찾는 데 오래 걸렸다.
**로그가 없어서다.** 이제 걸러질 때마다 이유를 찍는다.

```
[개입] 거름 — qRefuse: move 이미 자율(누적 7회)
[개입] 거름 — qEcho: 차례 간격(1/3턴)
[개입] LLM도 서버도 고를 자리가 없음 (수준 {…})
```

### ⓓ 데스크톱 홈 — 세로로 세우지 말고 좌우로 나눈다

v98·v101에서 **폰 모양 한 기둥의 비율만** 손봤다. 그러나 화면이 넓을수록 위아래만 길어지고
가운데가 비는 성질 자체는 그대로였다. 대화 화면은 이미 좌우 두 기둥인데 홈만 한 기둥이었다.

`@media (min-width: 1000px)`에서 **영상은 왼쪽(34%), 조작은 오른쪽 기둥**으로 나눴다.

- `.hb-main` — 화면 높이에 맞춘 9:16, 둥근 모서리와 그림자로 폰 모양 패널처럼.
- `.home-screen` — 폭 `min(400px, 32vw)`, **세로 가운데 정렬.** 높이를 화면에 맞추지 않으므로
  늘어질 일이 없다.
- `.home-header { margin-bottom: auto }` 를 푼다. 카드를 아래로 밀던 규칙은
  **한 기둥일 때만 필요한 것**이었다.

821~999px(태블릿)는 v101의 9:16 한 기둥을 그대로 쓴다.

### 검증
- 검사 24종 전부 통과.
- 버전 세 곳 + `templates/` 두 벌 같은 해시(b19ef7b229).
- **캐시 무효화**: `?v=101 → ?v=102`(35곳), 서비스워커 `hoarang-v101 → v102`.

> ⚠️ **데스크톱 배치는 눈으로 확인하지 못했다.** CSS는 렌더링해야 알 수 있는데 계산으로만
> 잡았다. 올린 뒤 노트북·큰 화면 두 곳에서 봐 주셔야 한다.

## v101 — 2026-08-16 · ★ 개입이 한 번도 안 나오던 원인 · 총평 쉬운 말 · 데스크톱 홈 비율

> v100 배포 후 첫 시험에서 **2분 대화(학습자 발화 10회)에 교육적 개입이 0회**였다.
> 「보통」이면 2~3회 나와야 한다.

### ⓐ ★★ 원인 — 숫자 세는 법만 고치고 기기에 남은 옛 숫자를 안 지웠다

v100에서 **부풀림을 고쳤지만**, 학습자 기기(localStorage)의 `idcCounts`에는
**부풀려진 값이 그대로 남아 있었다.** 그 값은 접속할 때 `?idc=` 로 서버에 올라간다.

```
기기에 저장된 값   context:11, turn:9, stage:8 …   ← v100 이전에 부풀려진 것
        ↓
서버가 접속하자마자  모든 요소 = 자율(SOLO)
        ↓
send_teach_intervention:  자율 요소는 건드리지 않는다 → return
        ↓
개입 0회. 유발 지시도 "이제 멈추고 자연스러운 대화 상대로만 있어라"로 시작.
```

**세는 법을 고쳤으면 이미 잘못 센 것도 지웠어야 했다.** 데이터 이전을 빠뜨린 배포 실수다.

- `idcCountsVer` 를 두어 `"2"` 가 아니면 `idcCounts` 를 **한 번 지운다.**
  기존 사용자도 다음 접속 때 한 번만 초기화되고, 그 뒤로는 v100의 정직한 값이 쌓인다.

> **교훈.** 저장된 값의 *의미*가 바뀌면 값 자체도 함께 옮겨야 한다.
> 코드만 고치면 옛 데이터가 새 코드를 고장 낸다.

### ⓑ 총평이 중급 학습자에게 어려웠다

금지 용어 목록은 v96부터 있었으나 **부족했다.** 어휘 급수 제한이 없었고, 문장 길이도
풀려 있었다.

- **1~4급 어휘·문법**으로 묶었다(챗봇 발화에 적용하는 `LEVEL_RULES` 와 같은 기준).
- **한 문장에 한 가지만.** 쉼표로 길게 잇지 못하게 했다.
- 금지 용어에 상호작용·주도성·전략적·유창성·정확성을 더했다.
- **바꿔 쓰기 예시**를 다섯 줄 넣었다. 목록만 주면 모델이 다른 어려운 말로 바꿔 쓴다.
  「대화를 주도적으로 이끌었습니다」 → 「먼저 말을 걸어서 좋았어요」

### ⓒ 총평 대기 — 시작 시점은 이미 최적이다

「별점을 매기는 순간부터 시작해야 한다」는 지적. **확인해 보니 그보다 더 이르다.**
총평은 `send_final_score` 맨 앞에서 시작하는데(v96), 그 시점은 **종료 버튼을 누른 직후**다.
별점 화면은 그 뒤 `final_score` 가 도착해야 뜨므로, 총평은 이미 한 발 앞서 돌고 있다.

남은 것은 생성 시간 자체다. 그래서 **입력과 분량을 줄였다** — 대화 기록 40턴 → 30턴,
분량 200~320자 → 160~240자. 그래도 모자라면 3초짜리 영상을 넣는 편이 맞다.

### ⓓ 데스크톱 홈이 위아래로 길었다

v98에서 **폭만 9:16으로 잡고 높이는 `100dvh`** 로 두었다. 노트북처럼 세로가 짧은 화면에서는
그 둘이 어긋나 칸이 비율보다 길어 보였다.

```
width  : min(420px, calc((100dvh - 32px) * 9 / 16))
height : min(calc(100dvh - 32px), calc(420px * 16 / 9))
```

폭과 높이를 함께 정해 **화면 어디서나 폰과 같은 비율**이 되게 했다. 위아래 16px씩 띄운다.

### 검증
- 검사 24종 통과. 다만 **둘이 제가 일부러 바꾼 값을 옛것으로 단정하고 있어 고쳤다.**
  - `restest.js` 「총평도 어려운 말 금지」 — 특정 문구를 찾고 있었다. 이제 **금지 목록·급수
    제한·바꿔 쓰기 예시가 있는지**를 본다(세 항목으로 늘렸다).
  - `ttstest2.js` 「입력을 40턴으로 줄였다」 — 숫자 40을 박아 두었다. 이제 **프로파일(80턴)보다
    짧기만 하면** 통과한다. v86·v95에서 겪은 것과 같은 유형이다.
- **캐시 무효화**: `?v=100 → ?v=101`(35곳), 서비스워커 `hoarang-v100 → v101`.

## v100 — 2026-08-16 · ★ 실현 횟수가 부풀려져 비계가 30초 만에 걷히던 결함

> v99를 되짚다가 찾았다. v59부터 있던 것이고, 비계 페이딩이라는 **설계의 핵심**이
> 사실상 작동하지 않고 있었다.

### ⓐ 무엇이 잘못되었나 — 같은 것을 여러 번 셌다

분석은 6초마다 도는데 **매번 대화 전체를 다시 본다.** 판정 지시에 「새로」라는 한정이
없었으므로, 3번째 차례에 한 번 한 일이 분석마다 다시 잡혔다.

```
차례 3   학습자가 새 화제를 꺼냄
         분석 ①  → topic +1  (1점)
         분석 ②  → 같은 발화를 또 셈  (2점)
         분석 ③  → 또  (3점)  → 자율 도달, 유발 중지
```

임계가 `{모델링 1회, 촉진 3회}`이므로 **분석 세 번이면 자율**이다. 3분 대화에서 분석이
열 번쯤 도니, **한 번이라도 해낸 요소는 30초 안에 「다 배웠음」 처리**되었다.

**규칙(3회면 자율)은 옳다.** 진짜로 세 번 했으면 손을 떼는 게 맞다. 문제는 **숫자가
가짜**라는 것뿐이었다. 챗봇은 아래 둘을 구분하지 못했다.

| | 차례 3 | 차례 7 | 차례 10 | 점수 |
|---|---|---|---|---|
| 진짜 세 번 | 화제 개시 | 화제 전환 | 화제 복귀 | 3점 ✓ |
| 실제로 일어난 일 | 화제 개시 | — | — | 3점 ✗ |

### ⓑ 여덟 요소 전부, 그중에서도 자주 하는 것이 먼저 망가진다

`_idc_absorb`는 요소를 가리지 않는다. 그런데 **분석마다 잡힐 확률은 요소마다 다르다.**

- **맥락·정체성 인식**(존댓말) — 말할 때마다 잡힌다. 첫 차례부터 부풀어 곧바로 자율.
- **차례 관리**·**기능 단계** — 주고받고 인사만 해도 잡힌다.
- 대화이동 관리·의사소통 전략 — 드물어서 부풀 일이 없다.

**쉬운 요소일수록 비계가 먼저 걷혔다.** 잘해서가 아니라 자주 나와서 걷힌 것이다.
존댓말을 틀리게 쓰는 학습자도 똑같이 자율 처리되었다. 그리고 `idcCounts`는 세션을 넘어
이월되므로(v84) **한 번 해 본 요소가 영구히 자율로 굳었다.**

> 같은 코드 안에 정답이 이미 있었다. 퀘스트는 `rp_progress["quests"].add(q)` — **집합**이라
> 중복이 안 쌓인다. 같은 판정 결과를 받는데 한쪽만 누적되고 있었다.

### ⓒ 고침 — 대화 기록에 경계선을 긋는다

기능 단계·대화 유형은 전체 맥락이 필요하므로 **기록은 그대로 두고 표시만 넣었다.**

```
상대: 안녕하세요
학습자: 안녕하세요
───────── 여기서부터 새로 늘어난 부분 ─────────
상대: 이건 어때요?
학습자: 좀 비싼데요
```

- 어디까지 세었는지는 `rp_progress["last_len"]`에 **이미 저장돼 있었다.** 줄만 그으면 됐다.
- (2) idc 판정에 「표시선 **아래에서만** 고르라. 위쪽은 이미 세어 둔 것이다」를 명시.
- (3) quest 는 「했나 안 했나」의 누적이므로 **전체에서 확인해도 된다**고 따로 밝혔다.
- 종료 시 로그에 「그중 지시받고 해낸 것」을 함께 찍는다.

### ⓓ 재점검에서 더 잡은 둘 — 구조 검사로는 안 잡히는 것

**① 표시선이 기능 단계 판정까지 좁힐 뻔했다.** (2)(3)에는 적용 범위를 적었는데
(1) done·(4) abc·(5) chains에는 「위 대화에서」라고만 되어 있었다. 모델이 표시선을 보고
기능 단계까지 새 부분만 판정하면 **진행률이 멈춘다.** 판정 목록 맨 앞에
「표시선은 (2)(3)에만 해당한다. (1)(4)(5)는 대화 전체를 보라」를 못박았다.

**② 지시받은 실현이 과소 집계되고 있었다.** 창이 두 차례였는데, `turn_now`는
**분석이 도는 시점**의 차례 수다. 개입 5차례 → 학습자가 6차례에 해냄 → 분석이 8차례에
돌면 차이가 3이라 창을 벗어나 **자발로 잡힌다.** 실현 시점은 알 수 없으므로 창을
**네 차례**로 넓혔다. 한 번 셈하면 표시를 지우므로 개입 하나에 실현 하나만 귀속된다.

### 검증
- 경계선 로직을 떼어 내 단위 시험 — 1차(전부 새것) · 2차(중간에 선) · 3차(새것 없음)
  세 경우 모두 **표시선이 정확히 한 번, 새 발화 앞에** 놓이는 것을 확인했다.
- 검사 **24종 전부 통과.**
- 버전 세 곳 + `templates/` 두 벌 같은 해시(29baa7427c).
- **캐시 무효화**: `?v=99 → ?v=100`(35곳), 서비스워커 `hoarang-v99 → v100`.

> **논문에 남길 것.** 이 결함은 「만들어 보지 않았으면 몰랐을 것」의 사례다.
> 〈표 34〉의 매체 배분도, 비계 페이딩의 임계값도 종이 위에서는 옳았다. 실제로 돌려 보니
> **판정을 누적하는 방식** 때문에 설계가 의도대로 작동하지 않았다. 4.4.3의 「구현하며
> 알게 되어 3장으로 되먹인 것」에 쓸 자리다.

## v99 — 2026-08-16 · ★ 개입의 간격을 초에서 '학습자의 차례'로

> **논문 제4장 2절 3항(피드백과 비계설정) 집필용 기록.** v95에서 개입을 넣으면서
> 간격을 초로 잡았다. 근거 없이 고른 값이었고, 세 가지가 어긋나 있었다.

### ⓐ 왜 초가 아니라 턴인가

- **재려는 것이 턴 단위다.** IDC의 차례 관리·대화이동 연쇄가 모두 차례를 단위로 정의된다.
  이남호(2025)의 대표 수치도 발화 순서 평균이다. **분석 단위가 턴인데 개입만 초로 재면
  결이 어긋난다.**
- **초는 학습자 속도에 휘둘린다.** 한 문장에 20초 걸리는 학습자와 3초에 받아치는 학습자가
  같은 대화에서 전혀 다른 횟수를 받는다.
- **무엇보다, 초로 재면 개입이 '말을 고르는 도중'에 튀어나온다.** MKO 원칙의
  「학습자가 말을 고르느라 침묵하면 채우지 말고 3초쯤 기다려라」와 정면으로 부딪힌다.
  침묵이 길수록 개입이 가까워지는 구조였다.

### ⓑ ★ 값의 근거 — 선행 연구 두 값에서 갈라 취했다

| | 도구·환경 | 대상 | 발화 순서 평균 | 학습자 차례 |
|---|---|---|---|---|
| 이남호·이찬규(2025:403) | 범용 GPT-4 (프롬프트만) | 중급 10인 | **12.12** | 약 6회 |
| 이남호(2025) | 맞춤형 GPT「한국어 말하기 튜터」 | 초급 26인 | **23.84** | 약 12회 |

발화 순서는 「대화 참가자 **한 명이** 주어진 발화 기회에서 수행하는 발화의 총체」이므로
두 화자를 합친 차례 수다. 학습자의 차례는 그 절반이다.

- **간격은 앞의 값(6회)에** 맞춘다 — 짧은 대화에서도 개입이 한 번은 닿아야 한다.
- **상한은 뒤의 값(12회)에** 맞춘다 — 대화가 길어져도 넘치지 않아야 한다.

> **★ 상한을 눈금으로 쓰지 않는 이유.** 두 수치는 모두 **「물건 사기」 한 과제**에서 나왔다.
> 구매 대화는 탐색-결정-결제로 빨리 닫히는 짧은 과제다(교재 대화의 A형을 논문은
> 「핵심 과업을 빠르게 종결하는 단순성」이라 했다). 기능 단계가 많은 주제라면 턴이 훨씬 는다.
> **절대 횟수로 정하면 어떤 주제에서든 어긋나므로, 간격이 눈금을 정하고 상한은 폭주만 막는다.**
> 그러면 주제에 따라 저절로 맞춰진다.

```
간격  조금 5턴 · 보통 3턴 · 많이 2턴   ← 눈금
상한  조금 2회 · 보통 4회 · 많이 6회   ← 폭주 방지선
바닥  최소 20초                        ← 「네.」「네.」처럼 짧은 차례가 몰릴 때
준비  학습자 2번째 차례부터            ← 유발이 작동할 기회를 먼저 준다
```

| 페이더 | 학습자 6회 | 12회 | 20회 |
|---|---|---|---|
| 끔 | 0 | 0 | 0 |
| 조금 | 1 | 2 | 2 |
| 보통 | 2 | 4 | 4 |
| 많이 | 3 | 6 | 6 |

### ⓒ 준비 구간 — 첫 차례에 개입하지 않는다

시뮬레이션에서 개입이 **학습자의 첫 차례**에 나올 수 있음을 발견했다. 개입은
「유발이 먹히지 않았을 때」 메우는 층인데, 유발이 작동할 기회도 주기 전에 끼어들면
**순서가 뒤집힌다** — 스스로 해낼 수 있었던 것까지 지시받은 것으로 기록된다.
`INTV_WARMUP = 2`로 학습자가 두 번은 말해 본 뒤에 개입한다.

### ⓓ ★ 서버 보충 — 3분 대화에서 개입이 0이 되던 문제

개입은 LLM이 「지금 거절할 자리」를 골라 줘야 나간다. 그런데 **3분 남짓한 대화에서는
LLM이 그 자리를 만날 여유가 없다.** 열댓 번 주고받는 동안 거절할 국면이 안 나오면
그 세션의 개입은 통째로 0이다. 페이더를 어디에 두든 결과가 같아진다.

퀘스트는 성격이 둘로 갈린다.

- **맥락이 필요한 것** — 거절·대안 제안·조건 협상·다시 부탁·화제 복귀·바꿔 말하기.
  상대가 제안·거절을 해야 성립하므로 **LLM이 대화를 읽어야만** 안다.
- **아무 때나 되는 것** — 이해 확인·따라 쓰기·말 끝맺기·새 화제·이야기 얹기.
  대화가 흐르고 있으면 언제든 할 수 있으므로 **서버가 혼자 판단해도 된다**(`INTV_ANYTIME`).

LLM이 조용하면 서버가 뒤 갈래에서 **가장 덜 실현된 요소**로 채운다. 단 **페이더가 「보통」
이상일 때만.** 그래야 눈금이 실제로 돈다 — 상한만 조이면 원래 안 나오는 것에 뚜껑만
하나 더 씌우는 셈이다. 분석 프롬프트에도 세기를 실어, 「많이」면 적극적으로 고르고
「조금」이면 확실할 때만 고르게 했다.

### 검증
- 개입 시뮬레이션으로 세 대화 길이 × 네 페이더를 전수 확인(위 표).
  빠른 턴(5초/턴)에서 초 바닥이 몰아치기를 막는 것도 확인했다.
- 검사 **24종 전부 통과.**
- 버전 세 곳 + `templates/` 두 벌 같은 해시(6d43225b44).
- **캐시 무효화**: `?v=98 → ?v=99`(35곳), 서비스워커 `hoarang-v98 → v99`.

## v98 — 2026-08-16 · 데스크톱 홈이 깨져 보이던 원인

> 데스크톱에서 홈을 열면 제목이 「호 / 아 / 랑」 세 줄로 깨지고 칸이 폰보다도 좁았다.
> 어쩔 수 없는 것이 아니라 **자국이 남은 것**이었다.

### ⓐ ★ 홈 칸이 폰보다도 좁아지던 원인 — 나누기만 남았다

`.home-screen`은 폭과 높이를 `--ui-zoom`으로 **나누고** 있다.

```
max-width: min(480px, calc(100vh * 9/16 / var(--ui-zoom) - 24px));
min-height: calc(100dvh / var(--ui-zoom));
```

그런데 **이 요소에는 `zoom`이 걸려 있지 않다.** 지금 `zoom: var(--ui-zoom)`이 붙은 것은
영상통화의 `.hv-top`·`.hv-caption`·`.hv-controls`·`.hv-stages`뿐이다.
v77~79에서 「`.home-screen`에 걸린 `zoom`이 `fixed` 요소의 기준을 바꾼다」는 사고를 겪고
zoom을 떼어 냈는데, **나누기는 그대로 남았다.** 곱하기 없는 나누기만 남은 셈이다.

- 1400px 화면에서 `--ui-zoom`은 1.34다. 세로의 9:16을 다시 1.34로 나누니 칸이 폰보다 좁아진다.
- 그 좁은 칸에 다섯 버튼이 **글자까지 펴고** 들어앉았다. 글자를 숨기는 조건이
  `@media (max-width: 560px)`, 곧 **화면 폭**을 재는데 정작 좁은 것은 **칸**이라 어긋난 것이다.
  화면이 넓으니 글자를 펴라고 판단했고, 남은 자리가 없어진 제목이 세 줄로 깨졌다.

### ⓑ 고침 — 데스크톱 전용 블록 하나로

`@media (min-width: 821px)` 안에만 뒀다. **폰은 이 블록을 한 줄도 읽지 않는다.**
🪜 도움말이 왼쪽 기둥으로 옮겨 가는 경계와 같은 값을 써서 기준을 하나로 유지했다.

- `max-width: min(560px, calc(100dvh * 9/16 - 24px))` — 나누기를 걷어냈다.
- `min-height: 100dvh` — 카드가 화면 아래에 제대로 붙는다.
- 헤더는 **아이콘만.** 대신 마우스를 올리면 이름이 뜨도록 `title`을 붙였다
  (「지난 대화」·「이름 바꾸기」는 글자가 숨겨지면 무엇인지 알 길이 없었다).
- `.hb-main`에 둥근 모서리와 그림자 — **잘린 화면이 아니라 「폰 모양 패널」로 읽히게** 했다.
  이 앱은 설치해서 폰으로 쓰는 물건이므로, 데스크톱에서는 그 형태를 숨기지 않고 드러내는 편이 낫다.
- 카드 폭 86% → 92%, 제목 26px, 로고 42px.

### 검증
- 검사 **24종 전부 통과.**
- 버전을 세 곳 모두 올렸다(`main.py`·`const APP_VERSION`·서비스워커).
  `templates/` 두 벌까지 같은 해시(c085c62be7).
- **캐시 무효화**: `?v=97 → ?v=98`(35곳), 서비스워커 `hoarang-v97 → v98`.

## v97 — 2026-08-16 · 🪜가 상대의 말에 대답하게 · 장소 예시 무작위 · 버전은 한 곳에서만

> ★ **먼저 알려 드릴 것.** `/version`을 확인하니 서버가 아직 **v91**이었다.
> v92~v96이 **하나도 올라가지 않았다.** 「도와주기 버튼은 생겼는데 개입이 안 튀어나온다」는
> 지적은 코드 결함이 아니라 그 탓이다 — 서버에 그 코드가 아예 없다.

### ⓐ ★ 🪜 도움말이 상대의 말과 상관없는 문장을 내던 원인 — 경합이었다

「호아랑의 발화에 맞추지 못하고 기능 단계에서 필요한 발화만 보인다」는 지적. 원인은 둘이었다.

- **원인 1 · 시간 경합.** 서버는 최대 **12초** 생성하는데 클라이언트는 **10초**만 기다리고
  `renderScaffoldStatic()`으로 되돌아갔다. 그 폴백이 바로 **그 단계의 연습 표현**이다.
  생성이 10~12초 걸리면 학습자는 **맥락과 무관한 연습 표현만** 보게 된다.
  → 서버 9초 · 클라이언트 13초로 벌렸다. 폴백은 진짜 실패일 때만 나온다.
- **원인 2 · 프롬프트의 무게중심.** 「연습했던 표현과 같거나 유사하게 하라」가 앞에 있어,
  상대가 방금 무슨 말을 했든 그 표현이 나왔다.
  → **「상대가 방금 한 말」을 따로 뽑아 프롬프트에 별도 블록으로 올렸다.**
  연습 표현은 「참고일 뿐이고, 상대의 말과 맞지 않으면 쓰지 말라」로 내렸다.
  두 제안이 서로 달라야 한다는 조건도 넣었다.

### ⓑ 대화 장소 예시를 무작위로

`rpPlacePh`가 「예) 동네 옷 가게」 하나로 굳어 있었다. **예시가 하나면 학습자가 그 범위 안에서만
상상한다.** 18개 언어 각각에 **접두사(`rpExPre`)와 예시 여섯(`rpPlaceEx`)**을 두고,
설정 창을 열 때마다 새로 뽑는다 — 카페 · 병원 접수처 · 옷 가게 · 시장 · 회사 면접장 · 친구 집.
사전이 없으면 예전 문구로 돌아가므로 깨지지 않는다.

### ⓒ 버전은 「만드는 데 쓴 것들」에서만

- 대화 화면 상태줄에서 버전을 뺐다. 학습자가 읽을 것이 아닌 정보가 자리를 차지하고 있었다.
- 홈 왼쪽 호아랑 아이콘 → 「🧾 만드는 데 쓴 것들」에 **버전·날짜** 줄을 넣었다.
  창을 열 때마다 현재 값을 채우므로 배포 뒤 `/version`과 눈으로 대조할 수 있다.

### ⓓ 🪜 도와주기 페이더에 색

손잡이가 회색이라 D(뇌록)·P(삼청)와 달리 눌러야 할 것으로 보이지 않았다. **단청의 주홍(#D9741F)**을
줬다. 세 페이더가 색으로 구분된다.

### 검증
- 검사 **24종 전부 통과.** (v95·v96에서 실패하던 `idctest`·`scripttest`·`loadtest`도 통과.)
- `i18nhtml.js`로 새 라벨이 한국어로 박히지 않았는지 확인 — `scaf` 관련 경고 0건.
- **버전을 세 곳 모두 올렸다** — `main.py`·`app.html`의 `const APP_VERSION`·서비스워커.
  v95에서 `app.html`만 빠뜨려 `/version`이 `match:false`였던 일을 되풀이하지 않는다.
  `templates/app.html`·`templates/index.html`까지 같은 해시(dfc1786f81)로 맞췄다.
- **캐시 무효화**: `?v=96 → ?v=97`(35곳), 서비스워커 `hoarang-v96 → v97`.

## v96 — 2026-08-16 · ★ 총평이 오지 않던 결함 · 쓰이는 대로 흘려보내기

> 「총평이 나오는 데 너무 오래 걸린다」는 지적을 파 보니, **오래 걸리는 게 아니라 아예 도착하지 못하고 있었다.** v94에서 제가 만든 결함이다.

### ⓐ ★ 총평이 영영 오지 않던 결함
v94에서 「결과를 먼저 띄우고 총평은 뒤따라 보낸다」로 바꿨다. 그런데 **결과가 도착하면 클라이언트가 `stopSession()`을 부르고, 그 함수는 웹소켓을 닫는다.** 서버가 뒤이어 총평을 보내려 해도 **보낼 곳이 없다.** 화면에는 「총평을 쓰는 중이에요…」가 영원히 남는다.

- **고침** — `reviewPending` 동안에는 소켓을 닫지 않는다. `ws`는 `null`로 두어 나머지 코드가 「끊김」으로 알게 하되, `reviewWs`에 옮겨 두고 `onmessage`만 살려 총평 조각을 계속 받는다. `review_done`을 받으면 정리하고, 30초가 지나거나 홈으로 나가면 강제로 정리한다.
- v93에서 「`stopSession()`이 소켓을 닫으니 종료 시점에 부르면 안 된다」를 이미 겪었는데, **v94에서 같은 함수의 같은 성질에 또 걸렸다.** 부수효과가 큰 함수는 이름만 봐서는 안 된다.

### ⓑ 총평을 맨 먼저 시작한다
도착하더라도 느렸다. 예전 순서는 ①진행 중 분석 대기(≤8초) → ②최종 분석(≤12초) → ③요소 프로파일(≤18초) → **그제야** ④총평 시작(≤18초). 첫 글자까지 **10~25초**.
- 총평은 프로파일 결과를 쓰지 않는다(v92에서 실시간 누적 횟수를 참고하도록 바꿨다). **기다릴 이유가 없다** → `send_final_score` 맨 앞에서 시작해 ①②③과 나란히 돌린다.
- 입력도 줄였다. 대화 전문 **80턴 → 40턴**. 총평은 흐름만 보면 되고, 80턴은 입력이 두 배라 첫 글자가 그만큼 늦다.

### ⓒ ★ 쓰이는 대로 흘려보낸다 (스트리밍)
가장 큰 변화. `generate_content_stream`으로 받아 **조각마다 `review_chunk`로 내보낸다.** 다 쓰일 때까지 기다리지 않으므로 **첫 글자가 1~2초 안에** 뜬다. 끝나면 `review_done`으로 전문을 한 번 더 보내 기록에 얹는다.
- 화면에서는 글이 **붓끝(▍)이 깜빡이며 차오르듯** 나타난다. 「기다리는 중」이 아니라 「쓰고 있는 중」이 된다.
- 학습자가 창을 닫으면 조각 전송이 실패하고 **생성도 그 자리에서 멈춘다** — 아무도 읽지 않을 글을 계속 만들지 않는다.

> **영상은 다시 볼 것.** 첫 글자가 1~2초에 뜨면 3초짜리 「쓰는 중」 반복 영상은 오히려 글을 가린다. 두루마리를 펼치는 한 편(B)만, 그것도 짧게 쓰는 편이 맞다. `총평영상_프롬프트.md`는 그대로 두되 A는 보류.

### 검증
- **`ttstest2.js` 신설**(18항목) — 총평을 맨 먼저 시작하는지, 스트리밍으로 나가는지, **총평이 오는 중에 소켓을 닫지 않는지**, 다 받으면 정리하는지.
- `ttstest`·`restest`의 v94 단정(한 번에 보내기)을 스트리밍 구조로 교체.
- 검사 **24종 전부 통과**.
- **캐시 무효화**: `?v=95 → ?v=96`, 서비스워커 `hoarang-v95 → v96`.

> ⚠️ **v95 배포에 어긋남이 있었다.** `main.py`·서비스워커는 v95였는데 `app.html`의 `APP_VERSION`만 v94로 남아 `/version`이 `match:false`였다. `templates/app.html`·`templates/index.html`도 v94 내용 그대로였다. v96에서 세 파일을 모두 같은 것으로 맞췄다(해시 1종 확인). **`/version`은 `server`만 보지 말고 `screen`과 `match`까지 볼 것.**

## v95 — 2026-08-15 · 교육적 개입 · 퀘스트 14종 · 자유 대화에도 🪜

> **논문 제4장 2절 3항(피드백과 비계설정) 집필용 기록.** 지금까지 챗봇의 비계는
> 모델링(시범) → 촉진(자리 만들기) → 자율의 세 수준이었다. 그런데 **유발이 먹히지 않았을 때
> 그 자리에서 붙잡아 줄 층이 없었다** — 다음 개입이 대화가 끝난 뒤의 사후 피드백뿐이었다.
> 넛지가 그 사이를 메운다. 교실이 요소를 지식으로 가르치고(〈표 37〉 도입), 챗봇은
> 그것이 필요해지는 순간에 상기시킨다. 둘은 다른 행위다.

### ⓐ 교육적 개입 — 호아랑이 잠깐 극 밖으로

호아랑은 배역이 아니라 **배역을 맡은 배우**다(`build_roleplay_prompt`: "너는 호아랑인 채로
'{ai_role}' 역할을 연기한다"). 그러니 무대 옆으로 비켜서는 것은 극을 깨지 않는다. 다만
비켜섬이 분명해야 하므로 배역의 발화가 아니라 **화면 층(빼꼼)**으로 보낸다. 3.1.3이 문제 삼은
것은 개입 자체가 아니라 AI가 **극 안에서** 슬그머니 선생이 되는 모호함이었다.

- **[교육] ★ 기능만 알린다. 표현은 주지 않는다.** 「지금이에요! 한 번 거절해 보기」가 전부다.
  타이밍과 형식을 모두 주면 학습자는 알아채지도 고르지도 않게 되어 받아쓰기가 된다.
  > 처음에는 2단(1단으로 안 되면 예시를 붙임)을 두었다가 **뺐다.** 2단은 「LLM이 같은 퀘스트를
  > 또 고를 때」만 뜨는데, 7분 대화에서 거절할 자리가 두 번 오는 일은 드물다 — **거의 안 도는
  > 경로는 검증이 안 되고, 검증 안 된 경로가 나중에 터진다**(v62·v93이 그랬다). 무엇보다
  > 형식이 필요하면 🪜 도움말이 이미 그 일을 한다. 그편이 맥락에 맞고, 비계를 가져가는
  > 주체가 학습자가 된다(옛 설계 원칙 「비계는 요청 시」).

  | | 누가 띄우나 | 무엇을 |
  |---|---|---|
  | 교육적 개입 | 챗봇이 | 기능만 — "지금 거절할 자리" |
  | 🪜 도움말 | 학습자가 | 형식 — 맥락에 맞는 문장 2개 |
- **[발동] 서버가 맥락을 보고 고른다.** 턴마다 도는 단계 분석에 판정 `(6) nudge`를 **합쳐서**
  넣었다 — 추가 호출이 없으므로 비용·지연이 늘지 않는다. 「지금 학습자가 바로 다음 차례에
  자연스럽게 해 볼 수 있는 것 하나」를 고르게 하고, 자리가 없으면 빈 문자열을 받는다.
- **[용어] 「넛지」가 아니라 「교육적 개입」으로 부른다.** 행동경제학 용어를 그대로 쓰면
  교육 행위의 성격이 흐려진다. 코드도 `send_teach_intervention`·`INTV_*`·`intv_*`,
  메시지 형식 `intervene`, 기록 항목 `interventions`로 맞췄다.
  > ⚠️ 논문에서는 DBR의 intervention(개입 = 교육 모형과 챗봇 전체)과 층위가 겹친다.
  > 4.2.3에서 쓸 때는 「대화 중 교육적 개입」처럼 한정어를 붙이는 편이 안전하다.
- **[절제] 네 겹으로 막는다.** ① 이미 자율에 도달한 요소는 건드리지 않는다(페이딩을 스스로
  되돌리는 셈이다) ② 퀘스트 하나에 세션당 한 번 ③ 총량 상한 ④ 최소 간격.
  v72~74에서 「퀘스트를 띄워 두면 학습자가 목록을 좇게 되어 주도성(RQ2)을 해친다」고 판단해
  대화 중에는 감췄다. 그 판단은 지금도 유효하다 — **문제는 목록의 상시 노출이었지, 한 번에
  하나씩 때맞춰 나오는 것은 다른 물건이다.**
- **[통제] 🪜 도와주기 페이더** — 홈의 D/P 아래에 셋째 페이더를 뒀다(0 안 도와주기 ~ 3 많이).
  학습자가 **상한**을 정하고, 그 아래에서는 실현 여부에 따른 자동 페이딩이 그대로 돈다.
  최대치로 두고 안 내리면 내면화가 안 되기 때문이다. 기기에 기억한다.
  > ⚠️ 제5장 실험에서는 학습자마다 값이 다르면 교란 변인이 된다. 고정하거나, 최소한
  > 기록에 남은 `scaf` 값으로 통제해야 한다.

### ⓑ ★ 지시받은 실현을 따로 센다 — 구인 오염 방지

`_idc_absorb`가 세는 값이 페이딩과 최종 프로파일을 만들고, 제5장에서 과정 자료가 된다.
**시켜서 한 거절과 스스로 한 거절이 같은 무게로 쌓이면** 페이딩이 잘못 내려가고, 이 로그가
무엇을 재는 자료인지 말할 수 없게 된다.

- 넛지를 받고 **두 차례 안에** 나온 실현은 `prompted`로 따로 센다.
- 종료 시 `idcPrompted`·`nudges`·`scaf`를 함께 내려보내고, 기기 기록과 서버 업로드 메타에 남긴다.
- **세션이 갈수록 지시받고 한 것이 줄고 스스로 한 것이 늘면, 그것이 내면화의 증거가 된다.**

### ⓒ 퀘스트 8종 → 14종 · 여섯 요소를 덮는다

기존 여덟은 대화이동(4)·화제(2)·전략(1)·수정(1)에 몰려 있어 **듣기와 차례에는 LLM이 읽어야만
알 수 있는 퀘스트가 하나도 없었다.**

| 새 퀘스트 | 요소 | 무엇을 |
|---|---|---|
| `qParaphrase` | 상호작용적 듣기 | 상대 말을 자기 말로 바꾸어 확인 |
| `qRephrase` | 의사소통 단절 수정 | 못 알아들었을 때 다른 표현으로 |
| `qNative` | 의사소통 전략 | 모국어로 묻고 한국어로 받아 다시 |
| `qEcho` | 의사소통 전략 | 상대가 쓴 표현 가져다 쓰기 |
| `qEndTurn` | 차례 관리 | 말끝 흐리지 않고 끝까지 맺기 |
| `qExpand` | 화제 관리 | 상대 이야기에 자기 얘기 얹기 |

- **`qSelfFix`는 넛지에서 뺐다.** 자기 수정은 시켜서 하면 자기 수정이 아니다.
- **기능 단계·맥락·정체성에는 넛지를 두지 않았다.** 기능 단계는 진행률·단계 칩으로 이미
  명시돼 있고, 맥락은 「높임말 쓰세요」가 비계가 아니라 훈계가 되기 때문이다.
- 새 퀘스트는 `mode:"both"` — v84에서 자유 대화에도 최종 판정을 붙였으므로 두 모드 모두에서
  판정된다. 오히려 과업이 없는 자유 대화에서 화제·차례·듣기가 순수하게 드러난다.
- **18개 언어 전부에 문구를 넣었다.** 앞쪽 12개(`I18N_QZ3`)와 뒤쪽 여섯(`I18N_X6_QZ3`).
  빠뜨리면 그 학습자만 퀘스트 목록이 영어와 제 나라 말로 섞여 보인다.


### ⓓ 🪜 도움말을 자유 대화에도

v94까지 🪜는 주제 대화 전용이었다(`send_hints`가 `rp_plan is None`이면 그냥 돌아갔고, 화면도
`scfDock`을 숨겼다). 그런데 개입에서 형식을 빼면서 **자유 대화 학습자만 막혔을 때 아무 손길도
못 받는** 자리가 생겼다.

- **두 모드의 축이 다르므로 근거도 다르게 잡는다.**
  · 주제 대화 — 아직 못 밟은 기능 단계와 그 단계에서 연습한 표현
  · 자유 대화 — 최근 흐름과 **지금 이 학습자에게 가장 필요한 요소**(실현 횟수가 가장 적고 아직
    자율에 이르지 않은 것 둘)
- 자유 대화에는 폴백으로 쓸 연습 표현이 없으므로, 생성이 실패하면 빈 목록을 보낸다.
  억지로 채우면 상황에 맞지 않는 문장이 나온다.
- 화면 — 채팅·영상통화 모두에서 🪜를 보인다. **기능 단계 표시만** 주제 대화 전용으로 남는다.
### 검증
- **`questtest.js`의 낡은 단정을 고쳤다.** 「LLM 퀘스트는 주제 대화 전용」이 **개수 8을 박아 두어**,
  퀘스트를 늘리는 옳은 변경인데도 검사가 깨졌다. 이제 **서버 `QUEST_LLM`과 화면 `llm:1` 정의를
  서로 대조**한다(양쪽에만 있는 유령도 잡는다). v86에서 고친 것과 같은 유형이다.
- 검사 20종 통과(csscheck·domcheck·i18nhtml 포함). **v94에서도 이미 실패하던 둘은 그대로다** —
  `scripttest.js`의 「🪜가 천천히·다시 버튼 바로 위에 있다」(v93에서 PC 자리를 옮긴 뒤 낡음),
  `idctest.js`의 「제외 안내」(v80에서 일부러 지운 것을 아직 찾고 있어 크래시). 다음에 손볼 것.
- **`i18nhtml.js`가 잡아 준 것** — 새로 만든 페이더 라벨 셋(`#scafLabel`·`#scafLowLbl`·
  `#scafHighLbl`)이 한국어로 박힌 채 JS 갱신 대상에서 빠져 있었다. 언어를 바꿔도 그 줄만
  한국어로 남았을 것이다. `applyI18n`에 붙였다.
- **캐시 무효화**: `?v=94 → ?v=95`(35곳), 서비스워커 `hoarang-v94 → v95`.

## v94 — 2026-08-15 · 기다리게 하지 않는다 · 발화 연습 음성 되살리기

### ⓐ ★ 「🧮 점수 계산 중…」을 없앴다 — 총평을 기다리지 않는다
v92·v93에서 **기다리는 시간을 늘리는 쪽**으로 고쳤는데, 그것부터가 틀린 방향이었다. 학습자는 「어? 왜 결과 창으로 안 넘어가지?」 하게 된다. **기다림을 늘릴 게 아니라 없애야 했다.**

- **서버** — 종료 신호를 받으면 ①단계 분석 ②요소 프로파일까지만 하고 **결과를 곧바로 보낸다.** 총평은 뒤이어 `{"type":"review"}` 로 따로 보낸다.
- **클라이언트** — 총평 자리에 「총평을 쓰는 중이에요… 잠시만요」(18개 언어)를 두고, 도착하면 그 자리에서 바뀐다. 주제 대화·자유 대화 모두. **기록(TXT)에도 그때 얹는다.**
- 무응답 폴백도 25초 → **13초**로 되돌렸다(요소 프로파일만 기다리면 되므로).

> **교훈.** 오래 걸리는 것을 붙잡고 기다리게 하지 말고, **먼저 줄 수 있는 것부터 주고 나머지는 뒤따라 보낸다.** v92에서 9→25초로 늘린 것은 증상만 미룬 것이었다.

### ⓑ ★ 발화 연습 음성이 기계 소리로 들리던 문제
서버 음성(Gemini TTS)이 **한 번만 실패해도 `geminiTtsDead = true` 로 그 접속 내내 브라우저 내장 음성**을 썼다. 렌더 서버가 깨어나는 첫 요청 한 번이 늦었을 뿐인데 **발화 연습 전체가 기계 소리**가 됐다. 학습자가 따라 말할 본이 되어야 하는 자리인데 본이 망가진 셈이다.

- **한 번 더 해 본다** — 실패하면 0.6초 뒤 재시도. 서버가 깨어나는 중이면 두 번째는 대개 된다.
- **영구 포기 → 45초 쉼** — 두 번 다 실패해도 45초 뒤에는 다시 서버 음성을 시도한다. 성공하면 곧바로 쉼을 푼다.
- 서버 `/tts` 는 이미 3회 재시도 + 모델 교체를 하고 있어 그대로 뒀다.

### 검증
- **`ttstest.js` 신설**(14항목) — 영구 포기 코드가 남아 있지 않은지, 재시도·쉼·복귀가 들어갔는지, 총평이 별도 메시지로 오가는지, 「쓰는 중」 문구가 18개 언어인지.
- `restest`·`btntest` 의 v92·v93 단정(동시 실행·25초 대기)을 새 구조로 교체.
- 검사 21종 전부 통과. **캐시 무효화**: `?v=93 → ?v=94`, 서비스워커 `hoarang-v93 → v94`.

> ⚠️ **배포 확인.** 이 시점에 서버는 아직 **v91**이었다(`/version`). v92·v93이 올라가지 않은 채 v91로 시험하신 것이라, 그때 보신 「총평 없음」은 v92에서 이미 고쳐진 것이다. **올린 뒤 반드시 `/version`의 `match: true`와 버전 번호를 확인할 것.**

## v93 — 2026-08-15 · 자유 대화 결과도 세 장 · PC 배치 · 별점 기록

- **[화면] 자유 대화 결과를 세 장으로.** 한 장에 별점·요소 8개·총평을 다 쏟아 스크롤이 길었다. 기능 단계가 없으므로 **⓪ 자기 성찰 → ① 상호작용 대화 능력 → ② 총평** 세 장이다(주제 대화는 네 장). 쪽 표시 점과 이전·다음 단추는 주제 대화와 같은 모양을 쓴다.
- **[PC] 🪜 도움말을 왼쪽 기둥으로.** 넓은 화면(≥821px)에서는 「뭐라고 말하지? 도와줘」가 **「대화 종료하기」 바로 아래**로 옮겨 간다. 휴대폰에서는 손이 가는 대화창 아래 그대로다. 마크업을 복사하지 않고 **자리만 옮긴다**(영상통화 무대와 같은 방식). PC에서는 왼쪽 기둥이 통째로 비어 있고 대화창은 아래로 길어, 펴 둔 채로 대화할 수 있는 자리가 낫다.
- **[PC] 펴 둔 도움말은 말차례마다 새로 받는다.** 예전 단계의 표현이 남아 있으면 오히려 헷갈린다. 호아랑의 말이 끝나고 0.7초 뒤에 갱신한다.
- **[반응] 종료를 누르면 곧바로 문이 닫힌다.** 지금까지는 서버 응답(총평까지)을 **다 받은 뒤에야** 문이 닫히기 시작해, 누르고 나서 한참 아무 일도 안 일어나는 것처럼 보였다. 이제 누른 즉시 소리를 끊고 문을 닫아 **기다림을 연출 뒤로 숨긴다.** 결과가 오면 `doorSwap()`이 문이 닫힌 만큼만 더 기다렸다가(최대 1.35초) 바꾼다.
  > ⚠️ 처음엔 `stopSession()`을 곧바로 부르려 했으나, **그 함수가 웹소켓까지 닫아** 총평이 영영 오지 않는다. 소리(`stopAllAudio`)와 단추만 잠근다. 검사 전에 코드를 다시 읽어 발견했다.
- **[기록] ★ 자기 평가 별점을 대화 기록에 남긴다.** 기록은 대화가 끝나는 순간 저장되고 별점은 그 뒤에 매기므로, 별을 누르면 **방금 저장한 기록을 다시 써서** 별점·상호작용 점수·총평을 머리말에 얹는다. 서버에도 `self_rating`으로 보낸다. **자기 평가와 실제 수행의 차이 자체가 5장의 자료**가 되므로 총점에는 넣지 않는다.

### 검증
- **`pctest.js` 신설**(20항목) — 1400px과 420px 두 폭으로 각각 띄워 도움말이 제자리에 가는지, 자유 대화 세 장을 **실제로 넘겨** 단추가 맞게 나오는지, 별을 눌러 칠해지는지 확인.
- **★ 하마터면 앱을 통째로 멈출 뻔했다.** 새로 넣은 `dockScaffold()`가 `window.matchMedia`를 그냥 불렀는데, **그 함수가 없는 환경에서는 예외가 나고 그 아래 스크립트가 전부 죽는다**(v62 클릭 먹통 사고와 같은 꼴). 검사가 `RES_LAST` 초기화 실패로 잡아냈다. 전체를 `try`로 감싸고 `matchMedia`가 없으면 창 너비로 대신 재게 고쳤다. **최상위에서 도는 코드는 예외를 밖으로 내보내면 안 된다.**
- 검사 20종 전부 통과. **캐시 무효화**: `?v=92 → ?v=93`, 서비스워커 `hoarang-v92 → v93`.

## v92 — 2026-08-15 · v88의 세 결함 교정 (총평 실종 · 단추 · 말투)

> 예비 적용에서 나온 지적 여섯이 **실은 세 가지 원인**이었다. 셋 다 v88에서 제가 들여온 것이다. 4.4절 자료.

### ⓐ ★ 총평과 상호작용 능력 칸이 통째로 비던 문제 — 원인은 시간
「자유 대화에서도, 주제 대화에서도 총평을 못 만든다」 + 「상호작용 대화 능력 피드백이 빠져 있다」 — 셋 다 **같은 원인**이었다.

- **원인** — 대화를 끝내면 서버는 ①단계 분석(≤15초) ②상호작용 프로파일(≤25초) ③총평(≤25초)을 **차례로** 만든다. 그런데 클라이언트는 **9초**만 기다리고 마지막 진행률로 화면을 띄운다(v59부터 있던 무응답 폴백). v87까지는 ①②만 있어 대개 9초 안에 왔지만, **v88에서 ③총평을 더하면서 넘어갔다.** 화면은 떴는데 알맹이가 없으니 학습자는 그런 기능이 없는 줄 안다.
- **고침 셋**
  1. **②와 ③을 나란히 돌린다**(`asyncio.gather`). 총평이 프로파일 결과를 기다리지 않도록, 참고 자료를 등급표 대신 **대화 중 쌓인 실시간 실현 횟수**로 바꿨다. 각 제한 시간도 25→18초.
  2. **클라이언트 대기 9초 → 25초.** 그동안 「🧮 점수 계산 중…」이 뜬다.
  3. **늦게 도착해도 채운다.** 기다리다 지쳐 화면을 먼저 띄운 뒤 결과가 오면 그 자리에서 다시 그린다. 두 모드 모두.
- **덤** — 프로파일 생성이 실패하면 `return blank`으로 되돌아가 요소 칸이 통째로 비었다. 이제 실패해도 **실시간 누적 횟수로 등급을 채워** 내려보낸다. 원래 그러라고 만들어 둔 대비책인데 예외 처리가 그 앞에서 가로채고 있었다.

### ⓑ ★ 결과 화면 단추가 제각각이던 문제 — `.hidden` 이 애초에 없었다
「이전·다음·홈으로가 다 같이 뜬다」, 「마지막 장인데 다음 단추가 있다」.

- **원인** — `classList.add("hidden")` 을 43곳에서 쓰는데 **전역 `.hidden` 규칙이 없었다.** `.res-page.hidden`, `.lang-overlay.hidden` 처럼 **컨테이너별로만** 정의돼 있어, 그 목록에 없는 단추류는 `hidden` 을 붙여도 그대로 보였다. v85에서 결과를 두 장으로 나눌 때부터 있던 결함인데, 네 장이 되면서 눈에 띄었다.
- **고침** — `.hidden { display: none !important; }` 전역 규칙 추가. 이걸로 **여섯 곳이 한꺼번에 고쳐졌다**(결과 3단추 · 지난 대화 내보내기 · 자유 대화 닫기).
- **또 하나** — `#histExportBtn { display: inline-block !important; }` 라는 v84의 ID 규칙이 `.hidden` 보다 세서 내보내기 단추는 **영영 숨지 않았다.** `:not(.hidden)` 을 붙여 숨겨야 할 때는 비켜 주게 했다.

### ⓒ ★ 대화문에서 손님이 존댓말 하다 반말 — 검사가 헐거웠다
v88에서 넣은 `_style_mixed()` 는 「한 화자가 두 말투를 섞었는가」만 봤다. 그래서 ① 판정된 줄이 3줄 미만이면 넘어가고 ② 한 번 다시 만들어 또 어긋나면 그냥 썼다. 무엇보다 **`auto` 일 때 어떤 말투를 써야 하는지 코드가 몰랐다** — 모델에게 맡겨 두고 결과만 봤다.

- **말투를 코드가 정한다.** 페이더에서 화자별로 못 박는다 — 반말은 **가까운 사이**(D≥60)에서만 나오고, 지위(P)가 위아래를 갈라 비대칭을 만든다.

  | 페이더 | 학습자 | 상대 |
  |---|---|---|
  | 서먹·대등 (손님↔점원) | 존댓말 | 존댓말 |
  | 절친·대등 (친구) | 반말 | 반말 |
  | 가깝고 학습자가 아랫사람 | 존댓말 | 반말 |
  | 가깝고 학습자가 윗사람 | 반말 | 존댓말 |

  정한 값을 프롬프트에 「▶ 손님은 존댓말만 쓴다」처럼 화자 이름과 함께 박아 준다.
- **검사도 바뀐다.** `_style_offenders()` 가 **줄마다 정해진 말투와 대조**해 어긴 줄 번호를 돌려준다. 한쪽으로 통일됐지만 설정과 어긋난 경우까지 잡힌다.
- **통째로 다시 만들지 않고 그 줄만 고친다.** `_fix_style()` 이 어긴 줄만 뽑아 「뜻은 그대로, 말투만」 다시 쓰게 한다(15초). 20초짜리 재생성보다 빠르고, 나머지 줄이 흔들리지 않는다. 고친 뒤 **발화 연습의 cue·follow도 다시 잇는다.**
- 스크린샷의 그 대화문(손님이 4번째 줄에서 반말)을 그대로 넣어 잡히는지 확인했다.

### 검증
- **`btntest.js` 신설** — 쪽마다 어느 단추가 보여야 하는지를 표로 두고 **실제로 넘겨 가며** 확인. `.hidden` 이 다섯 단추에 먹는지도 각각 검사.
- `restest.js` 의 말투 단정을 새 구조에 맞게 교체, 동시 실행·대비책 확인 항목 추가.
- 검사 19종 전부 통과. **캐시 무효화**: `?v=91 → ?v=92`, 서비스워커 `hoarang-v91 → v92`.

## v91 — 2026-08-15 · 학생 국적에 맞춰 여섯 언어 추가 (12 → 18)

> **학습자 모국어가 없으면 설치 안내부터 막힌다.** 예비 적용 대상 학생의 국적을 챗봇이 지원하는 언어와 대조한 결과, **16개국 중 절반이 자기 말로 앱을 쓸 수 없었다.**

### 대조 결과
학생 국적 — 네팔·라오스·러시아·몽골·미얀마·베트남·에콰도르·영국·우즈베키스탄·우크라이나·이집트·캄보디아·키르기즈·타이·파키스탄·한국계중국인.

| 이미 있던 것 | 없던 것 |
|---|---|
| 러시아어·몽골어·베트남어·태국어·우즈베크어·스페인어·영어·중국어 | **네팔어·라오어·미얀마어·크메르어·우크라이나어·키르기스어** / 아랍어·우르두어 |

### 이번에 넣은 여섯
`ne 네팔어 · lo 라오어 · my 미얀마어 · km 크메르어 · uk 우크라이나어 · ky 키르기스어` — **272개 문구 전부** 번역. 화면 사전 26개(`I18N`·`I18N_NAV`·`I18N_IG`…)에 흩어져 있던 열쇠를 한데 모아 언어당 하나의 블록으로 넣었다.

- **구현** — 26곳을 각각 고치는 대신, 모든 병합이 끝난 **뒤에** `I18N_X6`을 한 번 얹는다. 기준을 `ko → en` 순으로 깔고 그 위에 덮으므로 **혹시 빠진 열쇠가 있어도 한국어가 아니라 영어가 나온다.** 유지 지점이 한 곳뿐이라 다음 언어를 더하기도 쉽다.
- **언어 고르기 단추 세 곳**(첫 화면·설치 안내·인앱 안내)에 모두 추가 — 12개 → 18개.
- **`main.py`의 `LANG_NAMES`** 에도 추가. 코드 전환(모국어로 말해 보라고 권할 때) 지시문에 실제 언어 이름이 박힌다.
- **글꼴** — 지마켓 산스에는 데바나가리·크메르·미얀마·라오 글자가 없어 그대로 두면 **네모(두부)로 보인다.** `--font-ui`에 Noto Sans Devanagari/Khmer/Myanmar/Lao와 iOS의 Sangam MN 계열을 대체 글꼴로 넣었다.
- **번역하지 않고 남긴 것** — `vcSample`(호아랑 목소리 미리듣기 예문)과 `obChipEx`(온보딩 예시 칩)는 **학습 대상인 한국어 자체**라 그대로 둔다. `sgPolite`·`sgBanmal`은 '존댓말/반말'이라는 한국어 용어를 남기고 뜻만 괄호로 풀었다.

### 아직 없는 둘 — 아랍어·우르두어
이집트·파키스탄 학생용. **글이 오른쪽에서 왼쪽으로 흐르므로**(RTL) 낱말만 번역해 넣으면 화면이 깨진다. 뒤로 가기, 진행률 막대, 페이더 슬라이더, 말풍선 방향까지 좌우를 뒤집어야 한다(`dir="rtl"`). 다음 판에서 따로 다룬다.

### 검증
- **`langtest.js` 신설**(19항목) — 여섯 언어 각각에 **272개 열쇠가 하나도 빠지지 않았는지**, 한국어가 그대로 남은 곳이 없는지, `\n`과 `<br>`이 살아 있는지, 세 곳의 단추에 다 들어갔는지, 그리고 **크메르어로 실제 실행해 홈 문구가 크메르어로 나오는지**까지 확인.
- **검사 다섯 개의 「12개 언어」 단정을 고쳤다.** 숫자를 박아 두면 언어를 더할 때마다 검사가 깨진다 — 이제 언어 고르기 단추 수를 세어 비교한다(`questtest`·`phototest`·`seqtest`·`idcfulltest`·`uxtest`).
- 검사 18종 전부 통과. **캐시 무효화**: `?v=90 → ?v=91`, 서비스워커 `hoarang-v90 → v91`.

> **검수 부탁.** 번역은 기계가 아니라 제가 직접 만든 것이지만, 학생에게 보이기 전에 **해당 국가 학생 한 명씩에게 홈 화면과 설치 안내만이라도 확인**받는 편이 좋다. 특히 높임 표현과 「존댓말/반말」을 푼 대목.

## v90 — 2026-08-15 · 언어를 먼저 · 안드로이드 설치 경로 정리

- **[★ 순서] 인앱 브라우저 관문도 언어부터 묻는다.** 설치 관문은 v72부터 언어를 먼저 물었으나 **카카오톡 관문은 `navigator.language`로 짐작**하고 있었다. 기기 언어가 한국어인 베트남 학생은 한국어 안내를 받고 그냥 닫는다 — 설치 실패가 여기서 났을 수 있다. 인앱 관문에도 12개 언어 선택(`iaStep1`)을 두고, 고른 뒤에 안내(`iaStep2`)를 보인다. 이미 고른 적이 있으면 건너뛴다.
- **[안드로이드] 설치 버튼이 안 오는 브라우저의 안내를 고쳤다.** 삼성 인터넷 등은 `beforeinstallprompt`를 주지 않는데, 그때 **「지금 브라우저에서는 설치할 수 없어요. 크롬에서 열어 주세요」**로 빠지고 있었다. 그러나 삼성 인터넷도 ⋮ 메뉴에 '홈 화면에 추가'가 있다. **브라우저를 바꾸라고 하기 전에 지금 브라우저에서 담는 법을 먼저 알려 준다** — `igStepsAos` 3단계 카드 + 오른쪽 위 ⋮ 를 가리키는 ↑ 화살표. 크롬으로 여는 길은 보조 버튼으로 남겼다.
- **[버그] 따옴표를 못 알아보던 문제.** 공유 창 그림에 강조할 줄의 이름을 안내 문구의 따옴표에서 꺼내는데, 곡선 따옴표와 `「」`만 보고 **곧은 작은따옴표(`'`)를 빠뜨려** 베트남어·태국어 등에서 강조가 안 됐다. 곧은/곡선 따옴표와 `「」`·`『』`를 모두 본다.
- **[검증] `gatetest2.js` 확장**(19 → 28항목, 6가지 환경) — 아이폰 사파리 / 안드로이드 크롬(설치 버튼 있음) / 안드로이드 설치 버튼 없음 / 카카오톡 아이폰 / 카카오톡 안드로이드 / 언어를 이미 고른 재방문. 각 환경에서 **언어를 고르고 → 카드가 3장 뜨고 → 화살표가 맞는 쪽을 가리키는지**를 실제로 눌러 확인. 베트남어·태국어·러시아어로 골라 문구가 실제로 바뀌는지도 본다.
- 검사 17종 전부 통과. **캐시 무효화**: `?v=89 → ?v=90`, 서비스워커 `hoarang-v89 → v90`.

## v89 — 2026-08-15 · 설치 안내를 눈으로 읽히게

> 링크를 처음 연 학습자가 가장 먼저 보는 화면이다. **여기서 막히면 수업 자체가 시작되지 않는다.** 특히 아이폰은 애플이 한 번에 설치되는 버튼을 허용하지 않아 학습자가 직접 공유 → '홈 화면에 추가'를 밟아야 하는데, 지금까지 그 안내가 **줄글 세 줄**이었다.

- **[안내] 줄글 → 단계 카드.** `① …\n② …\n③ …` 문자열을 `renderStepCards()`가 **번호 동그라미 + 그림 + 한 줄**의 카드로 그린다. **12개 언어 문구를 그대로 쓰므로 번역을 새로 만들지 않았다** — 앞의 ①②③·`1.`·`1)` 만 떼어 내고 우리가 그린 번호로 대신한다. 설치 게이트와 인앱 브라우저 게이트에 함께 적용.
- **[안내] 화면 가장자리를 짚어 준다.** 「아래 공유 버튼」이라고 글로만 말하지 않고, **화면 아래 가운데에 말풍선 + ↓ 화살표**를 띄워 사파리의 공유 버튼 자리를 가리킨다(위아래로 까딱이는 애니메이션). 안드로이드에서 손으로 담아야 할 때는 오른쪽 위 ⋮ 를 ↑ 로 가리킨다. 문구가 길어도 화면을 넘지 않게 폭을 74vw로 제한.
- **[안내] '설치하는 법 보기'가 실제로 무언가를 보여 준다.** 지금까지는 이미 보이는 글자를 **깜빡이기만** 해서 누를 이유가 없었다. 이제 **공유 창을 흉내 낸 그림**을 펼쳐, 찾아야 할 줄('홈 화면에 추가')을 노랗게 강조해 보여 준다. 찾을 줄의 이름은 **안내 문구의 따옴표 안에서 꺼내 쓴다** — 언어마다 따옴표가 다르므로(`' '` · `「 」` · `" "`) 그것까지 함께 본다. 앞 카드 두 장에도 테두리가 들어온다.
- **[아이콘] 3종 추가** — `ic-share`(공유), `ic-addhome`(홈 화면에 추가), `ic-dots`(⋮).
- **[구현 메모] 화살표를 HTML에 두고 켜고 끈다.** 처음엔 `createElement`로 만들어 붙였는데 `domcheck.js`가 「HTML에 없는 id」로 잡아냈다. **검사를 느슨하게 고치는 대신 코드를 고쳤다** — 동적 생성은 오탈자를 검사로 못 잡는다.
- **[검증] `gatetest2.js` 신설**(19항목) — 아이폰·카카오톡 UA로 각각 띄워 언어 고르기 → 카드 3장 → 화살표 방향 → '설치하는 법' → 강조된 줄까지 **실제로 눌러** 확인. 검사 17종 전부 통과.
- **캐시 무효화**: `?v=88 → ?v=89`, 서비스워커 `hoarang-v88 → v89`.

> **아이폰 배포에 대한 결론(4장 각주용).** 애플은 웹 앱의 한 번에 설치되는 버튼을 허용하지 않는다. TestFlight를 쓰면 링크 하나로 되지만 **연 99달러 + 맥/Xcode + 90일마다 새 빌드**가 필요하다. 한 학급 규모의 예비 적용에는 과하므로 **PWA(공유 → 홈 화면에 추가)를 유지하고 안내의 질을 높이는 쪽**을 택했다. iOS 17부터 크롬·엣지에서도, iOS 26부터는 홈 화면에 추가한 것이 기본으로 앱처럼 열린다.

## v88 — 2026-08-14 · 말투 일관성 · 마이크 · 결과 네 장(자기 성찰·총평)

> 예비 적용에서 나온 지적 여섯을 처리했다. 앞의 셋은 **기능이 없던 것이 아니라 이어져 있지 않던 것**이다. 4.4절(예비 적용 및 점검)의 자료로 쓸 것.

### ⓐ ★ 대화문이 페이더의 말투를 따르지 않던 문제
학습자가 말투를 고르고 페이더를 맞춰도 **'먼저 듣기'의 대화문은 그것을 몰랐다.** 한 화자가 「무슨 일이야?」(반말)라고 했다가 세 줄 뒤에 「무슨 일이에요?」(존댓말)로 바뀌는 대화문이 학습자에게 본보기로 제시됐다.

- **원인** — 클라이언트는 `style`을 `/roleplay-setup`에 보내고 서버도 그것을 변수에 받았지만, **생성 프롬프트에 한 번도 넘기지 않았다.** 페이더 좌표(D·P)는 아예 보내지도 않았다. 말투 규칙은 대화 세션(`_STYLE_RULES`)에만 걸려 있었고, 대화문·표현을 만드는 자리에는 없었다.
- **고침** — 클라이언트가 `d`·`p`를 함께 보내고, 서버가 `style_line`을 만들어 프롬프트 **맨 앞**에 세운다. `polite`/`banmal`은 두 사람 모두 고정, `auto`는 D·P로 관계(가까운/서먹한 × 학습자가 윗사람/아랫사람)를 읽어 **화자마다 말투를 하나로 정하게** 한다.
- **코드로도 막는다** — `_speech_level()`이 종결어미로 발화의 말투를 재고, `_style_mixed()`가 한 화자가 존댓말·반말을 함께 쓰면 그 대화문을 무효로 본다(판정된 줄 3줄 이상일 때). 어긋나면 **한 번 다시 생성**한다. 두 번째도 어긋나면 그냥 쓴다 — 연습 자체를 막을 일은 아니다. 비대칭(상대는 반말·학습자는 존댓말)은 정상이므로 화자별로 따로 센다.

### ⓑ 발화 연습이 대화문과 이어지지 않던 문제
들은 대화문과 연습하는 문장이 달라 두 활동이 따로 놀았다. `_link_expr_to_script()`가 표현의 `text`와 가장 비슷한 학습자 줄을 대화문에서 찾아(유사도 0.45 이상, 같은 단계 가산) **그 앞줄을 `cue`로, 뒷줄을 `follow`로 끌어온다.** 대화문에 없는 표현은 건드리지 않는다.

### ⓒ ★ 발화 연습 마이크가 매번 "목소리가 안 들렸어요"
학습자가 정확히 말해도 인식이 안 됐다. **원인은 인식기가 아니라 조작 방식의 어긋남이었다.** 화면 안내는 「다 말했으면 **다시 누르세요**」(탭 방식)인데 코드는 **꾹 누르고 있어야** 녹음되는 방식만 받고 있었다. 안내대로 톡 누르면 0.4초짜리 녹음이 서버로 가서 매번 실패했다.
- 이제 **둘 다 받는다.** 450ms 미만이면 탭으로 보고 계속 듣다가 한 번 더 누르면 끝난다. 꾹 누르면 손 떼는 순간 끝난다.
- 0.5초도 안 되는 녹음은 「안 들림」이 아니라 **「너무 짧아요」**로 갈라 안내한다(`prTooShort`, 12개 언어). 안전 자동 종료 12초 → 20초.

### ⓓ 발화 연습에 '기능 단계' 태그가 뜨던 문제
발화 연습은 **한 차례 주고받기(대화이동)를 익히는 자리**다. 대화 전체의 조직인 기능 단계는 한 문장 연습으로 기를 수 없는데 태그로 붙어 학습자에게 거짓 정보를 줬다.
- `EXPR_IDC_KEYS`(= `IDC_SCORED_KEYS` − `stage`)를 새로 두고, 표현의 `idc`를 이 목록으로 제한. 비면 **기본값 `move`(대화이동)**. 생성 지시에서도 `stage`를 뺐다.

### ⓔ 결과를 네 장으로 — 순서가 곧 주장이다
두 장(흐름·요소)에서 **네 장**으로. 순서에 뜻이 있다 — **남이 매기기 전에 스스로 매긴다.**

| 쪽 | 내용 |
|---|---|
| ⓪ | **자기 성찰** — 「이번 대화, 스스로 어땠어요?」 별 다섯 개로 자기 평가 |
| ① | 대화 흐름 — 점수·발화 수·시간 + 기능 단계 + 유형 A/B/C |
| ② | 상호작용 대화 능력 — 〈표 42〉 요소별 상·중·하 |
| ③ | **총평** — 한국어 선생님의 줄글 |

- **자기 성찰**은 총점에 넣지 않는다. **자기 평가와 실제 수행의 차이 자체가 5장의 자료**가 되므로 기록(`selfRating`)에만 남긴다. 안 매기고 넘어가도 된다.
- **총평**(`run_review`)은 등급표가 못 담는 것을 맡는다. 200~320자 줄글, **학습자가 실제로 한 말을 따옴표로 인용**하게 하고(인용 없는 칭찬은 빈말이다), 고칠 것은 **하나만** 고르게 했다. 「○○보다 △△가 더 자연스러워요」처럼 대안을 함께 준다. 연구 용어 금지 목록은 사후 피드백과 같다.
- **이전·다음을 한 줄 두 칸으로**(`.res-actions` 2열 그리드). 첫 장은 「다음」이 폭을 다 쓴다. 세로로 쌓이던 것을 없앴다.
- 자유 대화 피드백에도 별점과 총평을 넣었다(기능 단계가 없으므로 한 장 그대로).

### 검증
- **`restest.js` 신설**(28항목) — 쪽을 실제로 넘겨 보고, 별을 눌러 칠해지는지·줄어드는지, 첫 장 「다음」이 폭을 다 쓰는지를 클릭으로 확인. 말투 판정은 스크린샷의 그 대화문을 그대로 넣어 걸러지는지 시험.
- `uxtest.js`의 「결과 두 장」 단정을 네 장으로 갱신. `idcfulltest.js`가 v87의 이름 변경(자유 수다→자유 대화) 때문에 조용히 실패하던 것도 고쳤다 — **통과하지만 낡은 검사는 없느니만 못하다.**
- 검사 16종 전부 통과.
- **캐시 무효화**: `?v=87 → ?v=88`, 서비스워커 `hoarang-v87 → v88`.

## v87 — 2026-08-14 · '자유 대화'로 이름 고침 · 지난 대화 되돌리기 · 노들섬 배경

- **[용어] '자유 수다' → '자유 대화'.** 화면·프롬프트·기록 전부(화면 27곳, 서버 8곳). '수다'는 목적 없는 잡담이라는 함의가 있어, IDC를 재는 활동의 이름으로는 어긋난다. 두 모드가 **주제 대화 / 자유 대화**로 나란해져 대비도 분명해졌다.
- **[화면] 홈 카드 가로를 86%로.** 배경 영상이 좌우로 더 보인다.
- **[화면] 대화 화면 맨 아래 크레딧 삭제.** 대화 중에 제작자 정보가 있을 자리가 아니다. 홈 아래와 호아랑 로고의 출처 창에는 그대로 있다.
- **[배경] 노들섬 영상 추가.** `static/home_loop_nodeul.mp4`(201KB). 원본 720×1280에서 **아래 132px을 잘라 생성 워터마크를 제거**하고 가운데 9:16으로 맞춰 460×818, 2패스 150kbps로 인코딩(기존 두 편과 같은 규격). 홈 배경은 이제 **마루·경회루·노들섬** 세 편이다.
- **[배경] ★ 홈에 돌아올 때마다 새로 뽑는다.** 지금까지는 앱을 켤 때 한 번만 뽑아, 다시 켜기 전에는 같은 풍경만 보였다. `setScreen("home")`에서 재추첨하며, 연달아 같은 편이 나오면 한 번 더 굴린다. 파일만 넣으면 서버가 `home_loop*.mp4`를 훑어 알려 주므로 코드는 그대로다.
- **[화면] 지난 대화 — v85의 3개 제한을 풀었다.** 저장된 대화를 **전부**(최대 30개) 보인다. 목록 높이도 34vh→52vh로 되돌렸다.
- **[화면] 지난 대화 — 자리에서 펼치고 접는다.** 항목을 누르면 그 자리에서 기록이 펼쳐지고 다시 누르면 접힌다(한 번에 하나만). 예전처럼 목록을 떠나 상세 화면으로 넘어가지 않으므로 **앞뒤 대화를 견주어 보기 쉽다.** 펼친 대화가 TXT 내보내기 대상이 되고, 접으면 버튼도 사라진다.
- **[검증] `histtest.js` 신설**(jsdom, 12항목) — 7개를 넣어 두고 전부 렌더되는지, 눌러 펼치고 다시 눌러 접히는지, 하나만 열리는지, 내보내기 대상이 따라오는지를 실제로 클릭해 확인. 검사 15종 전부 통과.
- **캐시 무효화**: `?v=86 → ?v=87`, 서비스워커 `hoarang-v86 → v87`.

## v86 — 2026-08-14 · 챗봇이 가르칠 수 있는 IDC 하위 요소 마저 넣기

> 「챗봇에서 수행할 수 있는 IDC 요소가 다 들어가 있는가」를 재점검한 결과. 〈표 34〉에서 ✕(교실)로 배분된 것(차례 시작·요구, 끼어들기, 비언어적 행위)은 그대로 두되, **AI가 할 수 있는데 빠져 있던 세 가지**를 넣었다. 논문 4.2.1의 요소별 구현 대응표에 반영할 것.

### ⓐ 발화 공동 완성 — 「의사소통 단절 수정」에 편입
지금까지 이 요소의 하위 항목에서 **명료화 응답·발화 공동 완성을 '교실 보완'으로 빼 두었다.** 그러나 챗봇도 할 수 있다. 학습자의 말이 조각으로 끊길 때(「바나… 먹어…」) 챗봇이 뜻을 이어 완성해 주고 **학습자가 그 문장을 스스로 말하게** 하면, 대신 말해 준 것이 아니라 함께 만든 것이 된다(비고츠키의 공동 구성).
- `sub` — "자기 수정·명료화 요구 (…교실 보완)" → **"자기 수정·명료화 요구·명료화 응답·발화 공동 완성"**
- `model`에 완성 절차를 명시: 틀렸다고 하지 말 것 → 뜻을 이어 완성 → 학습자가 따라 말하면 통과.
- `criteria`(〈표 42〉 사후 평가)에도 "상대가 되물으면 알아듣게 다시 말할 수 있고, 상대가 이어 준 문장을 받아 스스로 완성할 수 있다"를 더해, **가르치기만 하고 재지 않는 상태**를 없앴다.

### ⓑ 모국어 전환(code-switching) · 따라 말하기 — 「의사소통 전략」에 편입
`sub`에는 "모국어 전환·따라 말하기"가 적혀 있었으나 **지시문에는 없었다.** 이름만 있고 동작이 없던 항목이다.
- 우회 표현으로도 두 번 넘게 막히면 답을 주지 말고 **학습자의 설정 언어로 말해 보라고 권한다.** 학습자가 모국어로 말하면 그 뜻을 한국어 한 문장으로 바꿔 주고 따라 말하게 한다. **모국어 사용은 회피가 아니라 대화를 잇는 전략**이므로 나무라지 않는다.
- 이를 위해 `{native}` 자리표를 두고 `idc_focus_block(native=…)` → `build_mko_block(native=…)` 경로로 **`LANG_NAMES`의 실제 언어 이름**(베트남어·몽골어…)이 지시문에 박히게 했다. 미설정이면 "학습자의 모국어".
- 따라 말하기 — 챗봇이 쓸 만한 표현을 자기 발화에 미리 흘려 두고, 학습자가 가져다 쓰면 짧게 짚어 준다.

### ⓒ 차례 넘기기(끝맺기) — 「차례 관리」에 편입
기존 지시는 **챗봇이 차례를 넘기라**는 것뿐이었다. 학습자가 자기 말을 매듭짓는 훈련은 없었다. L–L 대화의 **불완전 종결 69.2%**(이남호 2025)에 대한 처방이 설계 원칙 P8인데, 종결 훈련이 대화 전체 수준에만 있고 발화 수준에는 없었던 셈이다.
- 학습자가 할 말을 다 하고도 문장을 흐리면(「음… 그래서…」) **이어받지 말고 한 박자 기다려 스스로 맺게** 한다.
- `criteria`에 "자기 말을 끝까지 맺고 상대에게 차례를 넘길 수 있다"를 추가.

### ⓓ 자유 수다에서 A/B/C 유형 판정 제외 — v84의 오류 교정
v84에서 분석을 자유 수다까지 넓히며 **대화 유형 판정까지 함께 넘겼다.** A/B/C는 이남호·이찬규(2025)의 **기능 단계 유형론**이다. 기능 단계가 없는 자유 대화에 매기면 재려는 것과 다른 것을 재게 된다(구인 타당도 훼손).
- 분석 프롬프트에 "기능단계가 없는 자유 대화라면 빈 문자열로 두라"를 명시하고, 코드에서도 `rp_plan`이 있을 때만 유형을 반영하도록 이중으로 막았다.

### 검증
- 검사 14종 전부 통과. 다만 **낡은 단정 두 개를 고쳤다** — 통과하지만 틀린 검사는 없느니만 못하다.
  - `i18ntest` — `t("st_" + k)`처럼 **이어붙이는 호출의 접두사**를 고정 키로 오해해 "사전에 없는 키"로 잡고 있었다. 접두사로 시작하는 키가 있는지 보도록 고침.
  - `questtest` — v85에서 저자 요청으로 뺀 메달 줄(🏅 n/8)을 여전히 있어야 한다고 단정하고 있었다. **없어야 한다**로 뒤집음.
- `idc_focus_block`을 실제로 실행해 ① `{native}`가 언어별로 치환되는지 ② 새 지시 네 개가 프롬프트에 실리는지 ③ 비언어적 행위가 여전히 채점 8요소에서 빠져 있는지 ④ 페이딩 임계(0→모델링, 1→촉진, 3→자율)를 확인.
- **캐시 무효화**: `?v=85 → ?v=86`, 서비스워커 `hoarang-v62 → v86`.

> **사고 기록(4.4 예비 적용용).** v85 작업 중 프로젝트 폴더(시놀로지 드라이브 동기화)에 `main.py`를 쓰다가 **파일 끝 21줄이 잘린 채 저장**됐다. 문법 오류라 서버가 뜨지 못했고, 렌더는 직전 v84를 계속 돌려 **배포가 실패한 줄 모른 채 하루가 지났다.** `/version`이 v85를 보고할 것으로 기대했으나 v84를 보고한 것이 단서였다. 깃헙 커밋 이력에서 v84 원본을 받아 복원했다. 이후 **모든 쓰기 뒤에 바이트 수를 대조하고 다르면 다시 쓰는 절차**를 넣었다. 교훈 — 배포 성공은 업로드가 아니라 `/version`의 `match: true`로 확인해야 한다.

## v85 — 2026-08-13 · 결과 화면 두 장으로 · 학습자 언어 낮추기

예비 적용에서 결과 화면의 정보량이 한꺼번에 너무 많고, 용어가 학습자에게 어렵다는 지적을 받아 정리했다.

- **[화면] 결과를 두 장으로 나눴다.** ① 점수·내 발화·대화 시간 + 기능 단계 + 대화 유형(A/B/C) ② 상호작용 대화 능력. 쪽 표시 점과 「다음 →」·「← 이전」으로 넘긴다. 한 화면에 9요소를 다 쏟으면 읽히지 않는다.
- **[언어] ★ 피드백 문구의 어휘 수준을 낮췄다.** 평가 프롬프트에 **금지 용어**를 명시 — 화행·레지스터·담화·대화이동·기능 단계·의사소통 전략·명료화·구인·발화 순서·연속체. 학습자가 읽는 화면에 연구 용어가 그대로 나가고 있었다("적절한 화행을 시작하지 못했습니다"). 또 **'~하지 못했습니다'가 아니라 '~해 보세요'**로 쓰게 해, 결핍 진술을 다음 목표로 바꿨다(P7 심리적 안전).
- **[언어] 설정 화면의 '화계(말투)' → '말투'.** 화계는 연구 용어다.
- **[화면] 자유 수다 결과** — 제목의 💬 이모지와 "상호작용 능력 피드백은 곧 추가될 예정이에요" 줄 삭제(v84에서 실제로 붙었으므로 낡은 안내였다).
- **[화면] 지난 대화** — 목록을 **최근 3개**만 보이고 창 높이를 줄였다(46vh→34vh, 52vh→38vh). 창이 길어져 「내보내기」·「닫기」가 화면 밖으로 밀렸다.
- **[화면] 오늘의 퀘스트** — 버튼을 **다시 누르면 닫힌다.** 말풍선의 도장 요약(🏅 n/8)은 뺐다 — 퀘스트 목록과 성격이 다른 정보라 한 자리에 섞이면 읽는 부담만 늘었다.
- **[검증] `uxtest.js`** 16개 항목. 기존 13종 포함 전부 통과.
- **캐시 무효화**: `?v=84 → ?v=85`, 서비스워커 `hoarang-v61 → v62`.

## v84 — 2026-08-13 · 3장 확정에 따른 IDC 전면 보강

> 3장(260813본)이 확정되어, 4장의 챗봇이 3장의 구인을 빠짐없이 구현하도록 다섯 단계로 보강했다. **논문 제4장 집필용 기록.**

### 1단계 — 자유 수다에도 IDC (즉각 + 사후 피드백)
지금까지 MKO 블록·비계 페이딩·요소 판정은 **주제 대화 전용**이었다. 자유 수다는 그냥 잡담이었다. 그러나 IDC는 상황극 전용 능력이 아니다 — 과업이 없는 자유 대화야말로 화제·차례 관리가 순수하게 드러난다.
- 자유 수다 프롬프트에 MKO 블록 결합(즉각 피드백·유발·시범이 두 모드 공통이 됨).
- 주기 분석·비계 갱신 주입·종료 시 IDC 프로파일(〈표 42〉 기준 사후 피드백)을 자유 수다에도 적용. 기능단계 판정만 상황극 전용으로 남는다.
- 자유 수다 종료가 `end_session` → `final_score` 를 기다리도록 바꿔, 결과 화면에 요소별 상·중·하 프로파일이 뜬다.

### 2단계 — A/B/C 유형 판정 + 대화이동 연쇄 (ⓑⓒ)
논문의 대표 수치가 "GenAI–L 확장형 69.2%"인데 챗봇은 자기 대화의 유형을 몰랐고, 분석 단위인 대화이동 **연속체**도 세지 않았다(요소별 횟수만).
- 주기 분석에 (4) **abc** — 단순형 A / 반복형 B / 확장형 C (이남호·이찬규 2025 유형론), (5) **chains** — 학습자의 시작·역시작·수정·고수 연쇄 횟수를 추가. **같은 호출**이라 비용 증가 없음.
- 결과 화면에 유형 배지(A 회색·B 황·C 뇌록)와 설명(12개 언어). 기록·서버 업로드에 abc·chains 포함 — 5장에서 사전·사후 유형 분포를 자동 비교할 수 있다.

### 3단계 — 비계 페이딩이 세션을 넘는다 (ⓓ)
페이딩이 한 세션 안에서만 작동해, 어제 자율 수준까지 간 학습자가 오늘 다시 모델링부터 시작했다. **근접발달영역은 세션을 넘어 이동한다.**
- 종료 시 서버가 요소별 실현 누적(`idcCounts`)을 내려 주고 기기가 보관 → 다음 세션 시작 때 `?idc=stage:2,...` 로 올려 보낸다. 서버는 그 누적에서 수준(모델링→촉진→자율)을 계산해 출발한다.

### 4단계 — 미시 요소 보강 (②①③)
- **② 명료화 응답 △→◎**: 〈표 34〉가 "빈도가 과제 설정에 따라 가변적"이라 △로 판정한 요소. MKO의 **변하지 않는 원칙**에 "두 어절 이하·뜻이 흐리면 반드시 되물어라"를 넣어 페이딩과 무관하게 상시 작동시켰다.
- **① 발화 연습 요소 태그**: 표현마다 `idc` 요소를 지정하게 하고("한 단계 안에서 요소가 겹치지 않게 안배하라"), 연습 화면에 작은 배지로만 표시(수업하듯 설명하지 않는다).
- **③ 채움말**: 침묵 시 답 대신 채움말("음…", "그러니까…")을 권하는 원칙 추가 — 박소연(2024)의 명시적 교육 항목 처치.

### 5단계 — 교실 연계·화면
- **지난 대화 → TXT 내보내기 복구.** v70에서 `.export-btn` 전체를 숨길 때 같이 죽어 있었다. 지난 대화의 버튼만 되살림 — 수업 B 활동물 공유 통로.
- **홈 영상 줌 완화**: 좌우를 잘라 채우던 방식(v83)이 너무 확대돼 보여, 폭만 채우고 아래 남는 자리는 막의 그러데이션이 한지 바탕색(#F4F0E6)으로 완전히 스며들게 했다 — '여백'이 아니라 '바탕'으로 읽힌다.
- **버튼 축소**: 카드 패딩 12→10px·제목 17px·설명 12px·아이콘 44px, 퀘스트 버튼 56→46px, 홈 간격 10→8px.
- **[검증] `idcfulltest.js`** — 위 다섯 단계 26개 항목 전수 검사. 기존 12종 포함 전부 통과.
- **캐시 무효화**: `?v=83 → ?v=84`, 서비스워커 `hoarang-v60 → v61`.

## v77~v81 — 2026-08-11 · 교재 사진으로 상황 만들기 + 홈 정리

### ★ 📷 교재 사진으로 상황 채우기 (v81) — 교실과 앱을 잇는 고리

**왜 만들었나.** 설계 원칙 P9는 "대체가 아닌 외연 확장"인데, 실제로는 학습자가 앱에서 아무 상황이나 만들어 연습하니 **그날 교실에서 배운 내용과 따로 놀 수 있었다.** 설계문서의 수업 순환(수업 A → 개별 → 수업 B)이 앱 쪽에서 끊겨 있던 셈이다. 교재를 찍어 넣으면 교실에서 배운 그 대화가 그대로 확장 연습이 된다. FGI가 동의했던 QR 프리셋보다 낫다 — QR은 교사가 미리 만들어야 하지만 사진은 학습자가 그 자리에서 찍는다.

- **[서버] `POST /roleplay-from-photo`** — 교재 말하기 페이지 사진에서 **장소·목적·내 역할·상대 역할·말투·주제**를 뽑는다. 모국어 병기도 함께.
  - 프롬프트에 **"사진에 없는 내용을 지어내지 마라, 판단이 안 되는 항목은 빈 문자열"**을 명시. 없으면 그럴듯한 상황을 만들어 낸다.
  - **"한국어 교재가 아니거나 말하기 활동이 아니면 모든 값을 빈 문자열"** — 엉뚱한 사진을 넣었을 때 대비.
  - 형식(jpeg/png/webp/heic)·용량(8MB) 제한.
- **[안전] ★ 사진은 저장하지 않는다.** 교재는 저작물이므로 설정만 뽑고 `finally`에서 즉시 버린다. 서버 어디에도 남지 않는다.
- **[안전] 반드시 학습자가 확인한다.** 뽑은 값을 칸에 **채워만 넣고** 대화 만들기는 여전히 학습자가 눌러야 시작된다. 잘못 읽힌 채로 대화가 시작되면 엉뚱한 연습이 되기 때문. 이미 적어 둔 값은 덮지 않고, 채워진 칸은 테두리로 표시해 확인을 유도한다.
- **[화면] 말투는 페이더로 옮긴다** — 교재 대화문이 존댓말이면 친밀도를 낮은 쪽(20), 반말이면 높은 쪽(85)으로. 별도 항목을 만들지 않고 기존 축을 쓴다.
- **[검증] `phototest.js`** — 서버 안전장치(형식·용량·미저장·지어내기 금지)와 화면 동작(카메라 바로 찍기, 자동 시작 안 함, 기존 값 보존, 확인 안내), 12개 언어까지 17개 항목.

### 오늘의 퀘스트 정리 (v80)

- **[수정] 홈과 결과의 퀘스트가 서로 달랐다.** 홈은 `"rp"`, 자유 수다 결과는 `"free"`로 뽑아 다른 목록이 나왔다 → **목록을 하나로 고정**.
- **[변경] 결과 화면에서 퀘스트를 뺐다.** 대화가 끝나면 조용히 기록만 하고, **홈의 퀘스트 버튼**에서 `☐`/`✓`로 확인한다. 결과 화면은 대화 자체를 돌아보는 자리이므로. 같은 날 여러 번 대화해도 한 번 깬 것은 계속 인정(`qzDone_<날짜>`).
- **[교육] ★ 비언어적 행위를 IDC 점수 화면에서 완전히 제거.** 서버가 「몸짓·표정으로 반응하기」를 회색 줄(`grade: na`)로 보내고 있었는데, **회색으로라도 두면 학습자가 '못 한 항목'으로 읽는다.** 〈표 34〉의 ✕ 판정 취지대로 아예 보내지 않는다. 하단 안내 문구도 함께 삭제.

### 홈 화면 (v77~v79)

- **[수정] ★ `position: fixed`가 안 먹던 이유** — 퀘스트 버튼이 `.home-screen` 안에 있었고, 이 요소에 걸린 **`zoom`이 fixed 요소의 기준을 바꿔** `top: 50%`가 화면이 아니라 패널 기준으로 잡혔다. 배경 영상에서 같은 문제를 겪고 밖으로 뺐었는데 버튼은 안에 둔 것이 실수. **버튼을 `.home-screen` 밖으로 옮겨** 해결.
- **[화면] 카드를 화면 맨 아래에 고정.** 여백(`vh`)으로 밀던 방식은 화면 크기마다 결과가 달라 두 번이나 반대로 움직였다 → **헤더에 `margin-bottom: auto`**를 줘서 남는 공간을 전부 헤더가 먹게 했다. 화면 크기와 무관하게 카드는 아래에 붙는다.
- **[화면] 홈 아래 출처 표기 삭제 → 왼쪽 위 호아랑 아이콘으로.** 누르면 「🧾 만드는 데 쓴 것들」 창(글꼴·배경음·효과음·문양·아이콘·음성 API·만든이). 12개 언어.
- **[화면] 위아래 남던 여백 제거** — `100dvh` + 홈에서 바깥 패딩 0.
- **[화면] 퀘스트 버튼 표정 회전** — 인사·신남·큭큭·당황·황당 5종을 6초마다. 원본 5.7MB짜리를 160px로 줄여 5개 합계 142KB.
- **[콘텐츠] 홈 배경 영상에 경회루 추가** — 9:16 원본에서 워터마크가 있는 아래쪽을 잘라내고(지우개는 자국이 남았다) 6초 왕복 루프로 12초, 235KB. 서버가 `home_loop*.mp4`를 훑으므로 마루/경회루가 무작위로 나온다.
- **캐시 무효화**: `?v=76 → ?v=81`, 서비스워커 `hoarang-v53 → v58`.

## v72~v74 — 2026-08-09 · 오늘의 퀘스트 — IDC 요소를 학습자가 '해내게' 만들다

> **논문 제4장 집필용 기록.** 이 항목은 "교육 모형의 어느 부분이 챗봇의 어느 기능이 되었는가"를 추적하기 위한 것이다. 4.2(설계)·4.3(구현)의 근거 자료로 그대로 쓴다.

**설계 근거.** v59에서 IDC 9요소를 챗봇에 심었으나, 그것은 **챗봇이 유발하고 시범을 보이는 쪽**(MKO 역할)이었다. 학습자가 그 요소를 **스스로 목표로 삼아 수행하는 장치**는 없었다. 오늘의 퀘스트는 그 자리를 메운다 — 논문 〈표 33〉의 구성 요소를 학습자가 읽을 수 있는 행동 목표로 번역한 것이다.

- **[교육] 퀘스트 21종 = IDC 6요소** (〈표 33〉 대응)
  - 기능 단계의 조직과 흐름 — 단계 빠짐없이 · 150점 · 200점
  - 화제 관리 — 먼저 말 걸기 · 새 이야기 꺼내기 · 딴 얘기 했다가 돌아오기
  - 대화이동 관리 — 거절하기 · 다른 방법 제안 · 조건 걸고 협상 · 물러서지 않고 다시 부탁
  - 차례 관리 — 열 번/스무 번 주고받기 · 한 번에 길게 말하기
  - 의사소통 단절 수정 — 다시 말해 달라기 · 천천히 부탁 · 뜻 물어보기 · 스스로 고쳐 말하기
  - 의사소통 전략 — 모르는 말 돌려 말하기
  - 상호작용적 듣기 — 맞장구 세 번 · 세 번 질문하기
  - 맥락·정체성 인식 — 끝까지 존댓말
- **[교육] ★ 비언어적 행위(✕ 판정)는 퀘스트에서 제외했다.** 〈표 34〉가 교실 담당으로 판정한 요소를 AI 연습의 성과로 잡으면 **구인이 오염된다.** 〈표 42〉가 비언어를 총점에서 뺀 처리와 같은 원칙.
- **[교육] 대화 중에는 퀘스트를 감춘다.** 띄워 두면 학습자가 목록을 좇게 되어 **주도성(RQ2)** 을 해친다. 시작 전에 한 번 보여 주고, 끝난 뒤 총평에서 달성 여부를 알린다.
- **[교육] 난이도를 섞어 하루 3개만.** 쉬움 1 + ★ 1 + ★★ 1. 불안 평균 3.0(RQ4)인 집단에 어려운 것만 주면 역효과다. 날짜 시드 추첨이라 새로고침해도 그날 퀘스트는 고정된다.
- **[판정] 두 갈래**
  - **로그로 세는 것 13종** — 턴 수, 진행률, 명료화 요구, 맞장구, 질문, 존댓말 일관성, 평균 발화 길이. 정규식·집계로 판정하므로 편차가 없다. 빠른 요청 버튼(천천히·다시)도 그 전략의 실행으로 인정한다.
  - **LLM이 읽는 것 8종** — 거절·조건·대안·고수·우회 표현·화제 복귀·새 화제·자기 수정. **진행률 판정과 같은 호출**에서 처리해 비용·지연을 늘리지 않았다. 프롬프트에 **"상대(챗봇)가 한 것은 세지 마라"** 를 명시 — 없으면 챗봇의 수행을 학습자 성과로 잡는다.
- **[자료] ★ 퀘스트 달성을 기록으로 남긴다** (5장 효과 검증 자료). 지금까지 챗봇이 남기는 것은 대화 전문과 진행률뿐이었다. 이제 세션마다 **어떤 IDC 요소를 실제로 실행했는가**가 함께 쌓인다.
  - 기기 `지난 대화` — 퀘스트 달성 여부 + 집계값
  - 서버 업로드(GDrive 수합) — 세션 정보에 `quests`·`idcStats`(턴 수·낱말 수·명료화·맞장구·질문·반말 횟수·최고 진행률·LLM 판정 목록)
- **[화면] v74 — 홈에서는 작은 호아랑 버튼 하나만.** 큰 카드가 자리를 너무 차지했다. 누르면 **호아랑이 오른쪽에서 빼꼼 나와 읽어 주고 들어간다**(기존 `hamPeek` 재사용). 그날 처음 홈에 닿으면 한 번만 저절로 나오고, 본 뒤에는 빨간 점이 사라진다.
- **[화면] v74 — 맨 아래 출처 표기** 글자 10 → 9px, 행간 1.5 → 1.18. 「공동체에 대한 공헌」 줄은 그대로.
- **[검증] `questtest.js`** — 빼꼼 안내에 퀘스트 3개가 뜨는지, 난이도가 섞였는지, 비언어 퀘스트가 없는지, IDC 6요소가 모두 연결됐는지, 서버 판정을 흡수하는지, 기록·업로드에 저장되는지. 26개 항목 통과.
- **캐시 무효화**: `?v=71 → ?v=74`, 서비스워커 `hoarang-v48 → v51`.

## v65~v71 — 2026-08-07 · 배포 경로 확보와 오디오 정비

**배포 사고의 전말(4장 4.4 예비 적용의 기록).** `main.py`는 깃헙에 올라가는데 `templates/index.html`만 계속 옛 버전(v60)에 머물렀다. `/healthz`가 `main.py` 버전만 보여 주어 배포가 된 것처럼 보였고, 그동안 앱은 클릭도 소리도 죽어 있었다. 세 단계로 뚫었다.

- **v65 — `GET /version` 신설.** `main.py`와 화면 파일의 버전을 **함께** 돌려주고 `match`로 일치 여부를 알린다. 이 한 줄로 "코드는 갔는데 화면은 안 갔다"가 즉시 판별된다.
- **v66 — 응급 런타임 패치.** 확실히 올라가는 `main.py` 쪽에서 화면 파일을 시작할 때 읽어 치명 버그(goHome TDZ)를 때우고 그 사본을 서빙한다. 화면 파일이 낡아도 앱이 살아난다. 최신이면 아무 일도 하지 않는다.
- **v67 — 화면 파일을 루트로.** `templates/` 하위가 안 올라가는 것을 확인하고, 루트의 `app.html`을 쓰도록 했다.
- **v69 — gzip 배포.** 루트에 둬도 526KB 파일만 실패했다(전송 중 끊김 추정). **156KB로 압축**해 올리고 서버가 시작할 때 푼다. 이후 배포가 안정됐다.
- **v68 — 대화 중 「대화 내보내기」 숨김** → v70에서 앱·웹 모두 완전 제거.
- **v70 — 화면 정리**: 웹 홈에서 카드가 배경 영상보다 넓어 캐릭터를 가리던 것(카드 폭을 영상 폭 안으로), 문 글자가 어절 중간에서 잘리던 것(`word-break: keep-all`), **문 뒤 버튼이 눌리던 것**(문이 뒤쪽을 막고, 누르면 바로 걷힘), 왼쪽 패널이 화면보다 길 때 페이더가 잘리던 것(패널 내부 스크롤), 채팅 화면에서 캐릭터·제목 숨김.
- **v71 — 오디오 두 건**
  - **배경음이 통화 음량을 쓰던 문제.** 호아랑 목소리는 Web Audio로 나가는데 배경음만 `<audio>` 그대로여서, 마이크가 열리면 안드로이드가 `<audio>`를 통화 스트림으로 돌렸다. 배경음도 같은 AudioContext를 거치게 해 둘이 같은 음량을 쓴다.
  - **목소리 갈라짐.** ① 기기가 24kHz가 아니면 브라우저가 조각마다 따로 리샘플링해 이음매에 잡음이 생긴다 → 우리가 먼저 기기 속도로 맞춘다. ② 스케줄 여유 10ms → 60ms. ③ 그래도 구멍이 나면 그 조각만 3ms 페이드인.
- **v67 — 창 하나만 마이크를 쓴다.** 브라우저 탭을 열어 둔 채 앱을 켜면 마이크를 뺏겨 앱이 먹통이었다(웹 탭을 닫아야 됐던 문제). `BroadcastChannel`로 창끼리 알아보고 **앞으로 나온 창이 가져가고 숨은 창은 물러난다**. 물러난 창에는 이유를 띠로 알린다(12개 언어).
- **v67 — 교사용 `?web=1` 우회 제거.** 관문에 「웹에서 그냥 볼게요」를 되살려 누구나 웹으로 쓸 수 있게 했으므로 숨은 길이 필요 없어졌다.

## v61~v64 — 2026-08-06~07 · 클릭 먹통 사고와 그 원인

### ★ 사고 — 앱 전체가 먹통 (클릭·소리 모두 죽음)

**증상**: 홈 화면은 정상으로 보이는데 「주제 대화」·「자유 수다」를 눌러도 아무 반응이 없고 소리도 안 났다. 폰·웹 모두.

**원인 ①(코드)**: `goHome()`이 아직 선언 전인 `const rpScriptOverlay`를 참조했다(v60에서 유입).
```
ReferenceError: Cannot access 'rpScriptOverlay' before initialization  at goHome
```
`skipEntryGuide`인 **재방문 사용자는 로드 직후 `goHome()`이 돌기 때문에** 거기서 스크립트가 통째로 멈추고, 그 뒤에 등록되는 이벤트 리스너가 **하나도 안 붙었다**. 오디오 초기화도 그 뒤라 소리도 죽었다. 예전 `vuBars` 사고와 완전히 같은 유형. **처음 쓰는 사람에겐 안 터지고 재방문자에게만 터져서** 기존 `loadtest.js`가 못 잡았다.
→ 변수 대신 **id로 직접 찾도록** 고쳐 선언 순서와 무관하게 만들었다.

**원인 ②(배포)**: 고쳐서 배포한 뒤에도 증상이 그대로였다. 배포본을 직접 받아 보니 **`main.py`는 v63인데 `templates/index.html`은 v60**이었다. 화면 파일만 깃헙에 안 올라간 것. `/healthz`는 `main.py` 버전만 보여 주므로 배포가 된 것처럼 보였다.

### 재발 방지

- **`GET /version` 신설** — `main.py`와 `templates/index.html`의 버전을 **함께** 돌려주고 `match` 로 일치 여부를 알려 준다. 배포 후 이 주소만 열어 `"match": true` 인지 보면 된다.
  ```json
  {"server":"v64","screen":"v64","match":true,"sessions":0,"max_sessions":24}
  ```
- **`returntest.js` 신설** — 재방문 사용자 경로(웹·설치된 앱·웹으로 계속)를 각각 띄워 ① 로드 오류가 없는지 ② **홈 카드가 실제로 눌리는지**를 확인한다. 이번 사고를 잡아낸 검사.

### 함께 처리한 것

- **[진입] 웹에서도 쓸 수 있게 되돌림** — 관문에 「웹에서 그냥 볼게요」를 되살렸다. 숨은 교사용 `?web=1` 우회는 필요 없어져 제거(`TEACHER_WEB` → `WEB_OK`).
- **[운영] 동시 접속 5 → 24명.** 15명 수업이 감당 안 됐던 직접 원인. 서버를 키워도 코드가 6명째부터 거부하고 있었다. Render 환경변수 `MAX_CONCURRENT_SESSIONS`로 조절.
- **[소리] 🎧 대화문 듣기 = 완전 무음.** 모범 대화를 듣는 자리라 배경음이 깔리면 집중이 깨진다. `recomputeAudioScene()`의 `sceneQuiet`에 넣어 화면을 나가면 자동 복귀한다.
- **[소리] 기본 목소리를 남자아이(Puck)로.** 서버 기본은 원래 `boy`였는데 클라이언트가 `auto`로 덮고 있었다.
- **[화면] 홈** — 카드 세로 22 → 12px, 크레딧 행간 1.28, 히어로 자리 34 → 26vh, 배경 영상 `top: -7%`. 화면 높이는 90vh로 되돌렸다(늘리지 않고 카드만 줄인다).
- **[화면] 채팅 중 캐릭터 숨김을 자유 수다까지 확대** — 채팅을 보는 동안에는 캐릭터가 시야 밖이라 세로만 잡아먹는다. 노드는 남겨 영상통화 구조는 그대로.
- **[화면] 채팅 글자** 16 → 14.5px (폰 18 → 15.5px).
- **[진입] 인트로 스플래시 삼중 안전장치** — 남으면 화면 전체가 투명한 벽이 되어 아무것도 안 눌린다. 요소 제거 + `display:none` + 12초 최후 타이머.
- **[버전] `manifest.json`이 혼자 `?v=58`에 멈춰 있던 것**을 찾아 동기화했다.
- **캐시 무효화**: `?v=60 → ?v=64`, 서비스워커 캐시 `hoarang-v37 → v41`.

### 아직 남은 것
배경음이 **통화 음량**을 쓰는 문제(호아랑 음성은 미디어 음량)와 **목소리 갈라짐**. 둘 다 오디오 라우팅이라 함께 손대야 한다.

## v60 — 2026-08-06 · 대화문 듣기 단계 신설 + 화면 정리

주제 대화의 흐름에 **듣기**를 넣었다. 지금까지는 설정을 마치면 곧바로 기능 단계 목록이 나왔는데, 학습자가 그 단계를 **어떤 대화로 채워야 하는지**를 본 적이 없는 상태에서 연습에 들어가고 있었다. 교실 수업의 도입 단계에서 교재 대화문을 보며 흐름을 관찰하는 자리(3.4.2 도입)가 챗봇 쪽에는 비어 있던 셈이다.

**흐름이 바뀌었다**: 설정 → **🎧 먼저 들어 보기** → 🗺️ 대화 단계 → 발화 연습 → 대화.

- **[서버] 모델 대화문 생성** (`/roleplay-setup`의 `script`)
  - 기능 단계를 처음부터 끝까지 밟아 가는 10~14줄의 대화문을 단계·표현과 **같은 호출에서** 만든다. 따로 만들면 대화문이 그 단계를 안 밟는 일이 생긴다.
  - 각 줄에 `speaker`(user/ai) · `text` · `native`(모국어) · `stage`(기능 단계 번호). 단계 구분선을 그 자리에서 그릴 수 있다.
  - 앞서 만든 단계별 연습 표현이 대화문 안에 들어가도록 지시했다 — **들은 것을 그대로 연습**하게 된다.
  - 실제 구어의 특징(담화표지·맞장구·조각문)을 담게 했다. 교재 대화문의 실제성 결여(박석준, 2005; 이소영, 2013)를 그대로 물려받지 않기 위함이다.
  - `_validate_script()`가 방어한다. 어긋난 줄은 그 줄만 버리고, 6줄 미만이거나 학습자 발화가 2개 미만이면 **빈 목록**으로 돌려보내 듣기 단계를 건너뛴다. 대화문 생성이 실패해도 대화 자체는 그대로 할 수 있다.
  - 생성이 길어져 타임아웃을 40초 → 55초로 늘렸다.
- **[화면] 🎧 먼저 들어 보기**
  - **전체 듣기 버튼이 계속 숨 쉬듯 움직인다**(`scPulse`). 학습자가 이 화면에서 무엇을 해야 하는지 한눈에 알아야 한다. 재생이 시작되면 움직임을 멈춘다 — 들을 때 시선이 흔들리면 안 된다.
  - 재생 중인 줄이 강조되며 따라간다. 다음 줄 음성은 미리 받아 두어 줄 사이가 끊기지 않는다.
  - **줄을 누르면 그 줄만 다시 들리고 모국어 번역이 함께 펼쳐진다.** 안 들린 데만 돌려 볼 수 있다.
  - **기능 단계 구분선**을 넣어 어디서 단계가 바뀌는지 보인다. 다음 화면의 대화 단계와 그대로 이어진다.
  - **역할별로 목소리가 다르다.** 상대 역은 배역에 맞춰 서버가 고르고, 학습자 역은 그와 겹치지 않는 목소리로 읽는다. 학습자가 홈에서 고른 목소리가 상대 역에 쓰이므로, 그것과 부딪히지 않도록 `scMyVoice()`가 피해서 고른다 — 안 그러면 두 사람이 같은 목소리로 읽혀 누가 말하는지 귀로 구분이 안 된다(검사에서 잡았다).
  - 학습자가 맡을 줄은 배경색과 테두리로 구분한다. 곧 연습할 발화이기 때문이다.
- **[화면] 🪜 비계 버튼을 손이 가는 자리로** — 진행률 막대 오른쪽 구석의 작은 아이콘이라 막혔을 때 눈에 띄지 않았다. **빠른 요청 버튼(천천히·다시 한 번) 바로 위**로 내리고, 「뭐라고 말하지? 도와줘」라는 글자를 붙인 가로 폭 전체 버튼으로 바꿨다.
- **[화면] 영상통화 겹침 해소** — 🪜 버튼과 추천 발화 카드가 겹쳐 있었다. 카드 높이를 158px로 묶고(넘치면 카드 안에서 스크롤) 버튼을 그 위 여백까지 올려, 제안이 길어져도 카드가 버튼 쪽으로 자라지 못하게 했다.
- **[화면] 영상통화에 기능 단계 표시** — 캐릭터를 크게 보는 동안에는 지금 어느 단계인지 알 수 없었다. 상단 이름줄 아래에 진행률·막대·단계 칩을 채팅 화면과 같은 내용으로 띄운다.
- **[화면] 주제 대화 채팅에서 맨 위 캐릭터를 뺐다** — 화면이 위아래로 길어 채팅을 보는 동안에는 어차피 캐릭터가 시야 밖으로 나간다. 캐릭터·제목·상태줄을 `body.rp-chat`에서 숨겨 세로를 줄였다. 반응을 보고 싶으면 영상통화 버튼으로 크게 본다. **노드는 그대로 두어** 영상통화가 이 무대를 옮겨다 쓰는 구조는 건드리지 않았다(`.character-panel` 안에 있을 때만 숨김 → 옮겨 가면 다시 보인다).
- **[i18n] 12개 언어 × 6키 추가** (`scTitle`·`scGuide`·`scPlayAll`·`scStop`·`scNext`·`doorScript`·`scfBtnLbl`).
- **[검증] `scripttest.js` 추가** — ① 줄·단계 구분선·역할 이름 렌더 ② 줄 탭 시 **그 줄만** 재생되고 모국어가 펼쳐지는지 ③ 홈에서 고른 목소리를 바꿔 가며 두 역할이 항상 다른 목소리인지 ④ 재생 상태 표시(▶/⏸, 흔들림 정지) ⑤ 대화문이 비면 건너뛰고 단계로 가는지 ⑥ 🪜가 빠른 요청 버튼 바로 위에 있고 주제 대화에서만 보이는지, 영상통화 단계 표시가 진행률을 따라가는지. 25개 항목 통과.
- **캐시 무효화**: `?v=59 → ?v=60`, 서비스워커 캐시 `hoarang-v36 → v37`.

## v59 — 2026-08-06 · 상호작용 대화 능력(IDC)을 챗봇에 심다

논문 제3장 3절에서 정의한 **상호작용 대화 능력(IDC, Interactional Dialogue Competence)** 9요소를 챗봇의 교육 기능으로 구현했다. 지금까지 챗봇이 다룬 것은 9요소 가운데 **기능 단계 하나뿐**이었다(진행률 = 단계 충족률). 나머지 여덟은 대화에서 우발적으로 나타나는 데 맡겨져 있었고, 이는 논문 3.1.4가 세 대화 환경 모두에서 지적한 바로 그 결핍이다.

**설계 전제 — 챗봇은 대화 상대가 아니라 '더 유능한 타인'(비고츠키)이다.**
학습자 혼자서는 아직 못 하지만 챗봇과 함께라면 해내는 것, 그 근접발달영역이 챗봇이 일할 자리다. 그래서 챗봇의 임무를 *대화를 잘 굴리는 것*에서 *학습자가 상호작용을 직접 해내게 만드는 것*으로 바꿨다.

- **[근거] IDC 9요소를 서버 상수로 이식** (`main.py` `IDC_ELEMENTS`)
  - 논문 〈표 33〉의 구성 요소·하위 내용, 〈표 34〉의 매체 배분(◎ AI / △ AI+교실 / ✕ 교실), 〈표 42〉의 중급 평가 기준을 요소마다 함께 담았다.
  - 요소별로 **`elicit`(유발 — 그 요소가 필요해지는 자리를 만드는 법)**과 **`model`(시범 — 챗봇이 먼저 보여 주는 법)**을 새로 기술했다. 논문에 없는 부분으로, 매체 배분 판정을 실제 대화 행동으로 옮긴 것이다.
  - ✕ 판정인 **비언어적 행위는 유발하지 않고 총점에서도 뺀다.** 교실 도입이 담당하는 요소를 AI 연습의 효과로 잡으면 구인이 오염되기 때문이다(논문 〈표 42〉 각주와 동일한 처리).
- **[유발] MKO 블록을 교육용 프롬프트에 결합** (`build_mko_block`)
  - 변하지 않는 원칙 다섯: ① 학습자가 할 수 있는 것을 대신하지 않는다 ② 도움은 필요한 만큼만, 점점 줄인다 ③ 어려운 말로 밀어붙이지 말고 그것이 필요해지는 자리를 만든다 ④ 해냈으면 짧게 짚어 준다 ⑤ 배역을 깨고 선생님으로 나오지 않는다.
  - 금지: 요소 이름을 학습자에게 말하기, 일부러 어렵게 말하기, 한 턴에 여러 요소 끌어내기.
- **[페이딩] 근접발달영역을 대화 중에 다시 조인다** (`idc_focus_block`·`send_idc_nudge`)
  - 비계 3수준: **모델링(3) → 촉진(2) → 자율(1)**. 학습자가 한 요소를 실현하면 시범을 거두고, 세 번 실현하면 유발 자체를 멈춘다.
  - 프롬프트에는 **지금 필요한 세 요소만** 싣는다. 아홉을 한꺼번에 지시하면 어느 것도 실현되지 않는다.
  - 갱신 방식: 턴마다 도는 단계 분석에 IDC 판정을 **합쳐서**(추가 호출 없이) 실현 요소를 누적하고, 그 결과로 만든 새 지시를 라이브 세션에 `end_of_turn=False`로 얹는다. 이 주입만으로는 챗봇이 말하지 않고, 학습자의 다음 발화와 함께 읽힌다. 대상이 그대로면 보내지 않으며, 주입에 실패해도 초기 프롬프트가 남아 있어 대화는 그대로 굴러간다.
- **[점검] 결과 화면을 IDC 9요소 프로파일로** — 모형의 '점검·성찰' 국면에 해당한다.
  - 종료 시 1회, 대화 전체를 〈표 42〉의 중급 기준으로 재어 요소별 **상·중·하**와 **근거 한 줄**을 낸다. 판정 원칙은 표현의 정확성이 아니라 **상호작용 기능의 수행 여부**다 — 문법이 틀려도 기능을 해냈으면 인정한다.
  - 총점은 교실 전담 요소를 뺀 **8요소 100점 만점**(상 2·중 1·하 0). 비언어적 행위는 회색 행으로 이름만 두고 제외 이유를 화면에 밝힌다.
  - 기존 진행률(단계 충족률)은 그대로 둔다. 학습자의 동기를 끄는 지표와 교육적 진단을 분리했다.
  - LLM 판정이 비면 실시간 누적 횟수로 메워 화면이 비지 않게 했다.
- **[i18n] 12개 언어 × 13키 추가** — 요소 이름은 학습자가 자기 능력으로 읽을 수 있어야 하므로 술어를 풀어 옮겼다(예: '의사소통 단절 수정' → 「막혔을 때 되살리기」). 키 규칙 `idc_<key>`, `err_`와 같이 변수로 호출한다.
- **[검증] `idctest.js` 추가** — ① 서버 `IDC_ELEMENTS`의 key가 12개 언어 사전에 다 있는가(서버가 요소를 늘렸는데 화면이 못 그리는 사고 방지) ② 서버가 총점에서 빼는 요소를 화면도 등급 없이 그리는가 ③ 실제 페이로드로 9행이 렌더되는가(jsdom) ④ 비계 수준이 내려가는 방향으로 정렬돼 있는가. 페이딩은 Python 시뮬레이션으로 별도 확인(전 요소 3회 실현 → 유발 중지, 8요소 만점 100점).
- **[정비] 검사 스크립트의 세션 경로 하드코딩 제거** — `domcheck`·`i18nhtml`·`i18ntest`·`splashtest`·`zaptest`가 예전 작업 경로를 박아 두고 있어 다른 자리에서 실행하면 전부 죽었다. `process.argv[2]`, 없으면 스크립트 위치 기준으로 잡게 고쳤다.
- **캐시 무효화**: `?v=58 → ?v=59`, 서비스워커 캐시 `hoarang-v35 → v36`.

> **남은 것** — 자유 수다에는 아직 IDC를 적용하지 않았다(`freeFbOverlay` 스텁 유지). 주제 대화는 과업 기반이라 기능 단계가 축이 되지만, 자유 수다는 화제 관리·차례 관리·상호작용적 듣기가 축이 되므로 유발 설계를 따로 세워야 한다. 대화 전 브리핑(`rpBriefOverlay`)에 목표 요소를 명시하는 일도 남았다.

## v58 — 2026-08-02
- **[인트로] ★ 환영 영상을 누르면 뒤의 카드가 같이 눌리던 문제.** 영상을 넘기려고 화면을 탭하면 「주제 대화」나 「자유 수다」가 함께 실행됐다.
  - **원인**: `pointerdown`에서 `.gone`을 붙이는데 그 클래스에 `pointer-events: none`이 들어 있었다. 터치는 **`pointerdown` → `touchend` → `click`** 순서로 오는데, 첫 단계에서 오버레이가 이미 통과 상태가 되어 마지막 `click`이 뒤쪽 카드에 그대로 꽂혔다.
  - **고침 ①**: `.splash.gone`에서 `pointer-events: none`을 뺐다. 0.65초 페이드가 끝나고 요소가 제거될 때까지 계속 뒤쪽을 막는다.
  - **고침 ②(두 번째 방어선)**: `swallowNextClick()` — 오버레이를 닫은 직후 같은 손가락에서 생기는 `click` **한 번만** 캡처 단계에서 가로챈다. 한 번 삼키면 즉시 스스로 풀리고, 아무 클릭도 안 오면 0.8초 뒤 풀려서 다음 조작을 붙잡아 두지 않는다.
  - 같은 패턴을 다른 오버레이에서도 찾아봤으나 `pointerdown`으로 닫는 곳은 인트로뿐이었다(나머지는 버튼 `click`이라 안전).
- **[검증] `splashtest.js` 추가** — jsdom으로 인트로를 탭한 상황을 재현해 ① 인트로가 닫히는지 ② **뒤쪽 카드가 안 눌리는지** ③ 그 클릭이 취소되는지 ④ **다음 클릭은 정상 동작하는지**(삼키기가 한 번뿐인지)까지 확인한다. 8개 항목 통과.
- **[점검] 어플 재점검에서 함께 잡은 것**
  - `main.py`의 `APP_DATE`가 `2026-07-29`에 멈춰 있었다 → `2026-08-02`로 갱신(개발일지 유지 규칙).
  - **`domcheck.js` 추가** — JS가 찾는 `id`가 HTML에 실제로 있는지 검사한다. 예전 `vuBars`·`homeBubble` 사고(참조는 남고 요소는 사라져 스크립트가 통째로 멈춤) 유형을 미리 잡기 위함. `getElementById` 221개 대상 전부 존재 확인. `<use href="#…">`로 쓰는 SVG 심볼은 오탐이 나지 않게 예외 처리.
  - 정적 자산 20개 참조 전부 존재(총 12.4MB), 버전 4개 파일 일치, 추적 파일에 비밀값 없음 확인.
  - ℹ️ 아이콘 스프라이트 24개 중 **12개가 미사용**(ic-back·ic-check·ic-ear·ic-home·ic-mute·ic-phone·ic-plan·ic-target·ic-tone·ic-trophy·ic-user·ic-wave). 동작에는 지장 없어 그대로 둔다 — 상호작용 모형 화면에서 쓸 가능성이 있다.
- **캐시 무효화**: `?v=57 → ?v=58`, 서비스워커 캐시 `hoarang-v34 → v35`.

## v57 — 2026-08-02 · 학습자 모국어 점검
12개 언어 번역이 실제로 다 되어 있는지 자동 검사를 만들어 확인했다. **구멍이 여섯 군데 있었다.**

- **[i18n] 사전에 아예 없던 키** — 한국어만 있어 어느 언어로 봐도 한국어가 나오던 것들. 11개 언어씩 채웠다.
  - `sgTapFill` "눌러 채우기" — 추천 발화 칩의 안내
  - `appDlTitle`·`appDlBody`·`appDlInstall`·`appDlClose` — 홈의 앱 설치 안내창 전체
  - `rpTopicLbl`·`rpTopicPh` — 영어·중국어만 있었다(현재는 화면에서 쓰이지 않지만 사전은 채워 둠)
- **[i18n] HTML에 박혀 있던 한국어**
  - **키보드·리모컨 안내** — "⌨️ 스페이스바(꾹)…"가 HTML에 하드코딩돼 있어 i18n 키조차 없었다. `pttKeyHint` 신설 + 12개 언어.
  - **영상통화 버튼 툴팁** — 라벨은 번역되는데 `title` 속성만 한국어로 남아 있었다.
- **[i18n] 서버가 한국어로 띄우던 오류** — `rp_expired`("대화 계획이 만료되었어요")가 클라이언트에서 코드 분기 없이 서버 문구를 그대로 alert하고 있었다. `err_<code>` 규칙을 만들어, **서버가 새 오류 코드를 보내도 사전에 키만 추가하면 자동 번역**되게 했다.
- **[검증] 번역 검사 두 개 추가**
  - `i18ntest.js` — ① 12개 언어 존재 ② 한국어 209개 키가 다른 11개 언어에 다 있는지 ③ 번역 안 하고 한국어 원문이 남았는지 ④ `t()`가 부르는 키가 사전에 있는지 ⑤ 죽은 키. **`vcSample`(학습자가 들어야 할 한국어 예시 발화)은 일부러 한국어이므로 예외 처리.**
  - `i18nhtml.js` — jsdom으로 HTML을 훑어 **JS가 번역해 덮어쓰지 않는 한국어**를 찾는다. `.innerText`·`data-qkey` 같은 동적 갱신도 인식해 오탐을 걸러낸다. 처음 15건 → 지금 2건(둘 다 의도적: 언어 선택 전 한·영 병기 제목, 앱 이름 「호아랑」).
- **[확인] 정상으로 판명된 것** — 서버 `LANG_NAMES`에 `ko`가 없는 것은 설계대로다(한국어 사용자에겐 모국어 힌트를 붙이지 않는다). 빠른 버튼(천천히·다시·쉽게·빨리)과 「대화 시작하기」는 각각 `data-qkey`·`innerText`로 이미 번역되고 있었다 — 첫 검사의 오탐.
- **캐시 무효화**: `?v=56 → ?v=57`, 서비스워커 캐시 `hoarang-v33 → v34`.

## v56 — 2026-08-02
- **[주제 대화] 100% 초과 구간에서 화면이 계속 흔들리고 맨 위로 튀던 문제** — 원인이 둘이었다.
  - **흔들림이 매 턴 반복**: 서버가 100% 초과 후 턴당 +5%를 주는데, `screenZap()`이 `rpPercent > prevPct`마다 호출됐다. 즉 확장 대화를 이어 갈수록 매 턴 흔들렸다. → **`zapDone` 잠금**을 걸어 100%를 처음 넘는 순간에만 한 번 놀래킨다. 세션이 끝나면 초기화되어 다음 대화에서는 다시 축하한다.
  - **★ 스크롤이 맨 위로 튀던 진짜 이유**: 흔드는 대상이 `<body>`였다. **body에 `transform`을 걸면 `position:fixed` 자식들의 기준이 body로 바뀌고 스크롤 앵커링이 풀려** 브라우저가 스크롤을 맨 위로 되돌린다. → 흔드는 대상을 **현재 보이는 패널(`.app` / `.home-screen`)** 로 옮겼다. 채팅 스크롤 컨테이너는 바깥 요소의 transform에 영향받지 않으므로 위치가 그대로 유지된다.
- **[주제 대화] 버닝 상태를 유지한다.** 흔들림을 한 번으로 줄인 대신, 100%를 넘은 진행 바가 **2.2초 주기로 계속 타오르게**(`@keyframes burn`) 했다. 놀람은 한 번, 상태는 계속 — 확장 중이라는 정보는 남기되 대화를 방해하지 않는다.
- **[접근성] `prefers-reduced-motion`이면 흔들림과 타오름을 모두 끈다.**
- **[검증] `zaptest.js` 추가** — `screenZap()`을 떼어내 여섯 턴 연속 호출하고 **흔들림 1회·소리 1회**인지, body를 흔들지 않는지 자동 확인한다. 8개 항목 통과.
- **캐시 무효화**: `?v=55 → ?v=56`, 서비스워커 캐시 `hoarang-v32 → v33`.

## v55 — 2026-08-02 · 디자인 정비
기존 어학 앱(듀오링고·스픽·Praktika·Teuida 등)의 디자인 강점과 이용자 불만을 조사하고, 호아랑의 CSS를 실측해 손봤다. **기능은 건드리지 않았다** — 논문 내용이 확정되기 전이므로 표현 층만.

- **[색] 면색과 글자색을 분리했다.** 단청 안료를 그대로 글자에 쓰면 한지 바탕(#F4F0E6)에서 대비가 4.2 수준이라 초급 학습자가 읽기 어렵다. 먹을 섞은 글자 전용 색을 신설.
  - `--accent-ink #375D57` (뇌록먹, 대비 4.17 → **6.44**), `--danger-ink #87412F` (장단먹, 4.19 → **6.53**) — 글자로 쓰이던 24곳을 교체.
  - `--muted`도 대화창 바탕(#C7D5C4) 위에서 3.50이라 어둡게 조정(→ 4.13). 이제 본문 색이 모든 바탕에서 WCAG AA 통과.
  - **죽은 토큰 4개 제거** — `--teal`·`--teal-soft`·`--rose`·`--rose-soft`는 선언만 있고 한 번도 안 쓰였다(`--rose`는 `--danger`와 값까지 동일). 남는 토큰마다 역할을 하나씩만 줬다: 뇌록=진행·내 차례 / 장단=교정 / 황토=비계 / 진녹=주제 대화.
- **[색] ★ 팔레트 밖 남색을 찾아 제거했다.** 미디어쿼리 안의 `.ptt-btn:not(:disabled):not(.talking) { background: linear-gradient(#47658F, #3D5A80) }`이 우선순위가 높아, 위에서 정한 장단(자유 수다)과 진녹(주제 대화)을 **폰에서 모두 덮어쓰고 있었다.** v36에서 지적된 "눌러서 말하기 버튼 색이 자꾸 바뀐다"의 실제 원인. 남색 그림자 4곳도 먹빛으로 교체.
- **[글자] 두 겹 크기 체계 도입.** 초급 학습자는 모국어 UI는 훑어보지만 한국어 문장은 글자 하나씩 해독한다 — 인지 부담이 다른데 같은 11~13px 구간에 섞여 있었다(그 구간에만 **69곳**이 몰려 21종의 크기가 난립).
  - `--t-ui-s/m/l = 11/13/15` (라벨·버튼) · `--t-ko-s/m/l = 16/18/21` (한국어 문장)
  - 학습 콘텐츠를 위층으로: 채팅 말풍선 13 → **16**(폰 18), 추천 발화 12 → **16**, 영상통화 자막 15.5 → **18**, 발화 연습 목표 문장 17 → **21**.
- **[턴 신호] ★ 화면 가장자리로 차례를 알린다.** 음성 대화 앱에서 가장 중요한 정보("지금 내가 말할 차례인가")를 **화면에서 제일 작은 것**(12px 글자 + 9px 점)으로 알리고 있었다. 정보 위계가 뒤집혀 있던 셈.
  - 내 차례 → 가장자리에 뇌록빛이 3.4초 주기로 스민다(한지 창에 빛이 드는 정도). 호아랑 차례 → 황토빛으로 옅게.
  - `setStatus`가 `body[data-turn]`을 갱신하고 CSS가 받는 구조. 대화 화면에서만 작동하고, `prefers-reduced-motion`이면 호흡 애니메이션은 끈다.
  - **근거**: 불안 3대 원인 중 '즉답 압박'에 직접 대응(P7). 글자를 안 봐도 주변시로 차례를 알면 화면에 눈을 붙들 이유가 준다.
- **[마감] 여백·선·그림자 — 절제로 세련되게.**
  - 단청 띠를 다섯 칸으로 자르지 않고 **서로 스미게** 바꿔 띠 하나로 읽히게 했다(3px → 2.5px). 장식이 아니라 마감선이 되도록.
  - 그림자를 먹빛 3단(`--shadow-s/m/l`)으로 통일 — 한지 바탕에서 검정 그림자는 탁하게 가라앉는다.
  - 모서리 3단(`--r-s/m/l = 10/16/22`) — 한옥 처마처럼 부드럽되 완전한 원은 아니게.
  - 말풍선 안팎 여백을 넓혔다(패딩 8/12 → 11/15, 묶음 간격 3 → 4, 폭 72% → 76%).
- **[접근성] 키보드 초점 표시 신설** — `:focus-visible`이 아예 없어서 교실 PC에서 탭 키로 조작할 때 위치를 알 수 없었다. 마우스 클릭에는 뜨지 않는다.
- **[검증] `csscheck.js` 추가** — `loadtest.js`는 JS 오류만 잡고 CSS 회귀는 못 잡는다. css-tree로 ① 문법 오류 ② 선언 없이 쓰는 변수 ③ 선언만 하고 안 쓰는 죽은 토큰을 검사한다. 셋 다 통과.
- **캐시 무효화**: `?v=54 → ?v=55`, 서비스워커 캐시 `hoarang-v31 → v32`.

## v54 — 2026-08-01
- **[배포] APK 배포 보류 — v50 상태로 되돌림**: 박사논문 연구용이라 배포 범위가 한 학급이면 충분한데, APK 직접 배포는 학생마다 '출처를 알 수 없는 앱' 경고를 넘겨야 하고 설치 실패 대응 부담이 컸다. **정식 배포는 논문 완성 후 플레이스토어로 미룬다.**
  - `static/hoarang.apk`·`static/assetlinks.json` **삭제**. 안드로이드도 다시 PWA 설치 안내(홈 화면에 추가)를 받는다.
  - **코드는 그대로 두었다.** v51에서 "APK가 없으면 PWA 안내로 되돌아간다"를 설계에 넣어 뒀기 때문에, 파일만 빼면 동작이 완전히 되돌아간다. 나중에 `static/hoarang.apk`를 다시 넣으면 그 즉시 APK 배포가 되살아난다. `/app-info`는 `{"apk":""}`, `/download`·`/.well-known/assetlinks.json`은 404로 조용히 비활성.
  - 서명 키(`signing.keystore`)와 `호아랑.aab`은 `keystore/` 폴더에 그대로 보관. **플레이스토어에 올릴 때 같은 키를 써야 이어진다.**
- **[조사] 배포 방식 비교 기록** (논문 부록용): 안드로이드 개발자 검증이 **2026-09-30**부터 브라질·인도네시아·싱가포르·태국에서 시행되고 2027년 전 세계로 확대된다. 미등록 개발자 앱은 그 뒤 24시간 대기가 붙는 '고급 흐름'을 거쳐야 설치된다 — **APK 직접 배포는 유통기한이 있다.** 선택지는 ① 제한 배포 계정(무료·신분증 불필요·기기 20대, 교사/학생 대상) ② 플레이 내부 테스트($25·테스터 100명·경고 없음) ③ 정식 공개(개인 계정은 테스터 12명×14일 필요). 논문 완성 후 ②→③ 경로로 간다.
- **캐시 무효화**: `?v=53 → ?v=54`, 서비스워커 캐시 `hoarang-v30 → v31`.

## v53 — 2026-08-01
- **[진단] "앱이 설치되지 않음" 대응**: 첫 설치 시도가 실패했다. APK를 직접 뜯어 검사한 결과 **파일과 서버는 정상**이었다 — 서명 v1+v2+v3 모두 존재, `minSdk 23`/`targetSdk 35`, 패키지 `com.hoarang.app`, 4.87MB, 배포 서버의 세 경로 모두 응답 정상. 원인은 기기 쪽(Play 프로텍트·기존 설치 흔적·다운로드 손상)으로 좁혀졌다.
- **[서버] `/app-info`에 크기·지문 추가**: 받은 파일이 도중에 잘렸는지 확인할 수 있도록 `mb`(사람이 읽는 크기)와 `sha256`을 함께 돌려준다.
- **[관문] 버튼에 파일 크기 표시**: 「앱 파일 받기 **(4.9MB)**」처럼 적어, 학생이 내려받은 파일과 눈으로 바로 비교할 수 있게 했다. 안드로이드는 다운로드가 조용히 끊겨도 알려 주지 않기 때문.
- 해결 순서는 `설치안됨_해결.md`에 정리(Play 프로텍트 → 기존 설치 제거 → 파일 크기 → 저장 공간 → 실패 코드 확인 → 플레이스토어 내부 테스트).
- **캐시 무효화**: `?v=52 → ?v=53`, 서비스워커 캐시 `hoarang-v29 → v30`.

## v52 — 2026-08-01
- **[배포] 실제 APK 탑재**: PWABuilder로 만든 패키지를 받아 배치했다. **패키지 이름 `com.hoarang.app`, 버전 1.0.0, 4.9MB.**
  - `static/hoarang.apk` — 서명 확인 완료(META-INF/MY-KEY-A.RSA, 459개 항목). 앱이 여는 주소는 `korean-dic.onrender.com/?src=app`.
  - `static/assetlinks.json` — 지문 `8E:0D:40:…:55:F7`. 이걸로 앱 위 주소창이 사라진다.
  - 서명 키(`signing.keystore`)와 `.aab`은 `keystore/` 폴더에 두고 **`.gitignore`에 등록**했다. 유출되면 남이 같은 이름으로 위조 앱을 만들 수 있고, 잃어버리면 업데이트가 영영 불가능하다.
- **[서버] APK 내려받기에 HEAD 허용**: 일부 안드로이드 다운로드 관리자가 GET 전에 HEAD로 크기를 먼저 물어본다. 없으면 405가 떨어져 받기가 실패한다.
- **[검증] 서버를 실제로 띄워 확인**: `/app-info` → `{"apk":"/download/hoarang.apk","size":5103349}`, `/.well-known/assetlinks.json` → 정상 JSON, 내려받은 파일이 열리는 정상 APK인지까지 확인.
- **캐시 무효화**: `?v=51 → ?v=52`, 서비스워커 캐시 `hoarang-v28 → v29`.

## v51 — 2026-07-30
- **[배포] 안드로이드는 APK로, 아이폰은 PWA로**: PWA 설치는 안드로이드에서 '크롬 바로가기'로 떨어지는 일이 잦아 크롬 흔적이 남고 알림에 주소가 떴다. **진짜 앱 파일(APK)을 직접 나눠 주는 길**을 열었다. 관문이 기기를 판별해 알아서 갈라진다.
  - **안드로이드 + 서버에 APK 있음** → 「앱 파일 받기」. 받기 → 알림에서 '열기' → '이 출처 허용' → '설치' 3단계를 그림처럼 안내하고, "선생님이 만든 수업용 앱이라 안전해요"를 덧붙였다(안드로이드의 '출처를 알 수 없는 앱' 경고에서 학생이 멈추지 않도록).
  - **안드로이드 + APK 없음** → 예전처럼 PWA 설치 안내로 되돌아간다. **APK를 안 올려도 앱은 그대로 동작한다.**
  - **아이폰** → APK가 있어도 무의미하므로 공유 ⬆︎ → '홈 화면에 추가' 안내를 유지.
- **[서버] APK 배포 경로 3개**
  - `GET /app-info` — `static/hoarang.apk`가 있는지 알려 준다. 관문이 이걸 보고 안내를 고른다.
  - `GET /download/hoarang.apk` — MIME 타입을 `application/vnd.android.package-archive`로 정확히 줘야 안드로이드가 '설치'로 이어 간다.
  - `GET /.well-known/assetlinks.json` — APK(TWA)가 이 사이트의 정식 앱임을 증명한다. **이게 맞아야 앱 위에 주소창이 뜨지 않는다.** `static/assetlinks.json` 파일을 그대로 쓰거나, 환경변수 `TWA_FINGERPRINT`·`TWA_PACKAGE`로도 된다.
- **[진입] APK로 실행 중이면 관문을 건너뛴다**: 안드로이드가 남기는 `android-app://` referrer 또는 `?src=twa`로 판별하고, **세션에 기억**한다(앱 안에서 화면을 옮기면 referrer가 사라지기 때문).
- **[검증] `gatetest.js` 확장**: 아홉 상황 24개 항목 자동 검사 — 첫 방문·아이폰·카카오톡·설치됨·교사용 + 안드로이드(APK 있음/없음)·TWA 실행·아이폰(APK 있어도 PWA).
- 제작 순서는 `APK_만들기.md`에 정리(PWABuilder 설정값·키스토어 보관 경고·수업 시간 3분 안내).
- **캐시 무효화**: `?v=50 → ?v=51`, 서비스워커 캐시 `hoarang-v27 → v28`.

## v50 — 2026-07-30
- **[진입] 설치 관문 — 웹으로는 쓸 수 없게 했다**: v47~v49의 설치 권유는 '웹에서 그냥 볼게요'로 빠져나갈 수 있어, 학생 대부분이 브라우저에서 그대로 썼다(마이크 권한이 매번 초기화되고 알림도 오지 않음). **빠져나갈 길을 없앴다.**
  - 주소를 열면 곧바로 관문이 뜨고, **닫는 버튼이 없다.** 설치를 마치고 앱으로 열어야 챗봇에 닿는다.
  - **① 나라(언어) 선택 → ② 그 언어로 설치 안내** 2단계. 12개 언어. 잘못 골랐으면 '언어 다시 고르기'로 돌아간다.
  - 관문에서 고른 언어는 그대로 저장되어, **앱을 처음 열 때 언어를 두 번 묻지 않는다**.
- **[진입] 상황별로 안내가 갈린다**: "설치 버튼이 안 보인다"는 문의를 없애기 위해 네 갈래로 나눴다.
  - **카톡·인스타 인앱 브라우저** → 설치 자체가 막힌 환경. "여기서는 설치할 수 없다"고 알리고 **「크롬에서 열기」** 버튼(카카오 스킴·안드로이드 intent·iOS x-safari)만 준다.
  - **안드로이드·PC 크롬** → `beforeinstallprompt`를 잡아 **버튼 한 번에 설치**. 이벤트가 늦게 와도 버튼 문구가 자동으로 「지금 설치하기」로 바뀐다.
  - **아이폰 사파리** → 공유 ⬆︎ → '홈 화면에 추가' 순서 안내. 버튼을 누르면 그 순서가 깜빡이며 강조된다.
  - **그 밖(파이어폭스 등)** → 「크롬에서 열기」 + **「링크 복사」**(누르면 '복사됐어요!'로 바뀜).
- **[진입] 교사용 우회**: 주소 뒤에 **`?web=1`** 을 붙이면 관문 없이 웹으로 쓸 수 있다(수업 화면 시연·검증용). 세션 동안 유지되고 학생에게는 노출되지 않는다.
- **[검증] `gatetest.js` 추가**: jsdom으로 다섯 상황(첫 방문·아이폰·카카오톡·설치됨·교사용)을 자동으로 확인한다. `node gatetest.js` — 15개 항목 통과.
- **캐시 무효화**: `?v=49 → ?v=50`, 서비스워커 캐시 `hoarang-v26 → v27`.

## v49 — 2026-07-30
- **[홈] 카드가 호아랑을 가리던 문제**: 배경 영상을 전체 화면으로 바꿨더니 「주제 대화」·「자유 수다」 카드가 호아랑의 몸통을 덮었다. 두 가지를 함께 손봤다.
  - **카드를 아래로**: 홈 화면을 `justify-content: center → flex-start`로 바꾸고, 캐릭터 자리(`.home-hero`)가 남는 높이를 모두 먹게 했다(`flex: 1`, 최소 34vh). 카드가 화면 아래쪽으로 약 150px 내려가 호아랑의 몸통까지 드러난다.
  - **영상을 위로**: `object-fit: cover`(화면에 맞춰 좌우를 잘라 냄)를 그만두고, **가로 폭을 꽉 채운 채 화면 위에 붙이는** 방식으로 바꿨다(`width: min(100%, 100vh×9/16)`, `top: 0`). 좌우가 잘리지 않고 얼굴이 화면 위 30% 지점에 온다.
  - **남는 위아래는 흐린 사본으로**: 같은 장면의 정지 이미지를 크게 흐려(`blur(38px)`) 뒤에 깔아 여백을 메웠다. 넓은 화면(교실 PC)에서는 영상이 세로에 맞춰 줄고 좌우를 흐린 배경이 감싼다.
  - 덮개(veil)도 가운데를 더 맑게(0.30 → **0.14**) 조정해 호아랑이 선명하게 보이게 했다.
- **[홈] 배경 영상 무작위 재생**: 아침·낮·밤 여러 벌을 돌려 쓸 수 있게 했다. 서버가 `static/home_loop*.mp4`를 훑어 목록을 만들고(`GET /home-loops`), 앱을 열 때마다 그중 하나를 무작위로 고른다.
  - **영상 파일만 넣으면 자동으로 목록에 들어간다** — 코드 수정 불필요. 같은 이름의 `.jpg`가 있으면 포스터·흐린 배경막으로 함께 쓴다.
  - 목록을 기다리지 않고 기본 영상을 먼저 틀어 첫 화면이 비지 않게 했고, 고른 영상을 못 받으면 기본 영상으로 되돌린다.
  - 새 영상 제작용 제미나이 프롬프트 2종(아침 한지 창가 · 밤 마당 등불)은 `홈영상_프롬프트.md`에 정리. 화면에서 가려지는 영역 도해도 함께 넣어 구도를 잡을 수 있게 했다.
- **캐시 무효화**: `?v=48 → ?v=49`, 서비스워커 캐시 `hoarang-v25 → v26`.

## v48 — 2026-07-29
- **[홈] 배경 영상을 화면 전체로**: 호아랑이 마루에 앉아 기다리는 루프 영상을 캐릭터 자리(작은 카드)에서 **홈 화면 전체 배경**으로 옮겼다. 앱을 열면 호아랑이 있는 공간 안에 들어와 있는 느낌이 먼저 오고, 그 위에 대화 카드 두 장만 놓인다.
  - 인사 말풍선("안녕! 🐯", "오늘 뭐 할까?")과 통통 튀는 애니메이션, 표정 이미지 랜덤 교체를 **모두 제거**했다. 영상이 인사를 대신한다. 홈 부제("같이 대화할까요?")도 홈에서는 숨겼다.
  - 글자·카드가 묻히지 않도록 **위아래에 한지빛 그러데이션 막**을 덮었다(위 86% → 가운데 30% → 아래 97%). 가운데가 가장 맑아 호아랑이 잘 보인다.
  - 배경막은 `.home-screen` **바깥**에 두었다. 안에 두면 교실 화면용 확대(`zoom`)까지 함께 먹어 배경이 화면 밖으로 밀려난다.
  - 자동재생이 막힌 브라우저에서는 **첫 터치에 재생**되고, 그래도 안 되면 포스터 이미지가 그대로 남는다.
- **[설치] 사이트를 열면 곧바로 설치 안내**: 이전에는 이름 입력을 마치고 홈에 닿아야 설치 화면이 떴다. 이제 **주소를 열자마자** 뜬다(기기 언어로 먼저 표시). 웹으로 계속 쓸 길은 남겨 두었다.
- **[설치] manifest 보강**: `id`·`start_url=/?src=app`·`display_override`·`categories`·`prefer_related_applications` 추가. 안드로이드가 **WebAPK**(주소창 없는 진짜 앱)로 설치하도록 조건을 채웠다.
- **캐시 무효화**: `?v=47 → ?v=48`, 서비스워커 캐시 `hoarang-v24 → v25`.

## v47 — 2026-07-29
- **[사운드] 대화 배경음 한 번 더 절반으로**: 음원을 **-6dB 더** 낮췄다(평균 -27.0 → **-33.5dB**). 말할 때 더킹도 **35% → 15%**로 강화 — 호아랑이 말하는 동안에는 배경음이 거의 사라진다.
- **[신규] 홈 화면 무한 루프 영상**: 호아랑이 마루에 걸터앉아 발을 흔들며 기다리는 영상을 홈 캐릭터 자리에 넣었다.
  - **끊김 없는 반복**: 원본은 첫 프레임(눈 감음)과 끝 프레임(눈 뜸)이 달라 그냥 반복하면 툭 튄다. **정방향 + 역방향을 이어 붙여(왕복)** 이음새를 없앴다(12초).
  - 워터마크 제거, 460px·20fps·무음·CRF 30으로 **237KB**까지 줄였다. 화면이 가려지면 재생을 멈춰 배터리를 아끼고, 영상을 못 틀면 기존 정지 이미지가 그대로 보인다(화면이 비지 않음).
- **[진입] 앱으로 쓰라고 먼저 권한다**: 웹 브라우저로 쓰면 마이크·알림이 불편하다는 판단. 홈에 닿으면 **앱 아이콘과 함께 설치를 권하는 화면**을 띄운다.
  - 안드로이드·PC 크롬은 **버튼 한 번으로 설치**(`beforeinstallprompt`), iOS는 '공유 → 홈 화면에 추가' 순서를 그림처럼 안내. 12개 언어.
  - 이미 앱으로 열었거나 인앱 브라우저면 띄우지 않고, 설치가 불가능한 환경을 위해 '웹에서 계속' 길은 남겨 뒀다(그 세션 동안 다시 묻지 않음).
- **[디자인] 말하기 버튼에 한국적 문양 + 파동**: 소슬빗살(한옥 창살) 문양을 버튼 바탕에 얹고, **말하는 동안 안쪽에서 파동이 번져 나가게** 했다. 기존 레벨 막대와 함께 "소리가 들어가고 있다"를 두 겹으로 보여 준다. 발화 연습 마이크도 동일.
- **[영상통화] 겹침 해소**: 추천 발화 카드와 말하기 버튼이 겹쳤다. 캐릭터·말풍선을 위로 올리고(`justify-content: flex-start`), 카드는 `bottom 148 → 190px`, 🪜 버튼은 `252 → 300px`로 올렸다. 아이폰 14 기준으로 계산했을 때 카드–버튼 사이 66px, 🪜–카드 사이 18px 여유.
- **[온보딩] ③ 설정 화면이 잘리던 문제**: 입력칸 다섯 줄이 그림 영역에 다 안 담겼다. 그림 영역을 204 → **250px**로 키우고 제목·설명 크기를 조금 줄여 여유를 만들었다.
- **캐시 무효화**: `?v=46 → ?v=47`, 서비스워커 캐시 `hoarang-v23 → v24`.

## v46 — 2026-07-29
- **[신규] 알림(웹 푸시) — 하루 세 번 호아랑이 부른다**: 듀오링고처럼 학습을 이어 가게 돕는 리마인더. **한국시간 오전 10시 · 오후 2시 · 오후 8시**에 발송.
  - **서버**: `pywebpush` 기반. `/push/key`(공개키)·`/push/subscribe`·`/push/unsubscribe`·`/push/send`(교사용 수동 발송). 1분마다 시계를 보고 한국시간 기준 정해진 시각에 하루 한 번만 보내는 스케줄러(`_push_scheduler`). 서버가 UTC로 돌아도 한국시간으로 판정한다.
  - **문구**: 시간대마다 다르고(아침·점심·저녁), **학습자 화면 언어(12개)로, 이름을 넣어** 개인화한다. 예) "좋은 아침이에요! / 남정, 오늘 한국어 한마디 해볼까요?"
  - **권유 시점**: 앱을 열자마자 묻지 않는다 — 처음부터 물으면 대부분 거절한다. **대화를 한 번 마치고 결과 화면을 본 뒤**에 호아랑 그림과 함께 한 번만 부드럽게 권한다. 거절하면 다시 묻지 않는다(`notifAsked`).
  - **자가 회복**: 구독 목록은 서버 파일에 저장되는데 재배포로 사라질 수 있다. **앱을 열 때마다 조용히 다시 등록**하게 해 목록이 스스로 회복되게 했다.
  - **서비스워커**: `push`·`notificationclick` 처리 추가. 같은 태그로 보내 알림이 쌓이지 않고, 누르면 이미 열린 창으로 포커스한다.
  - **키가 없으면 기능 전체가 조용히 꺼진다** — 환경변수를 넣기 전에도 앱은 그대로 동작한다.
  - 설정 방법은 `알림설정_안내.md`에 정리(환경변수 3개 붙여넣기 → 배포 → 로그 확인).
  - 검증: 스케줄러 시간 판정을 하루치·이틀치로 돌려 정확히 3번씩 발송되는지, UTC 서버에서도 한국시간으로 맞는지 확인.
- **캐시 무효화**: `?v=45 → ?v=46`, 서비스워커 캐시 `hoarang-v22 → v23`.

## v45 — 2026-07-29
- **[디자인] 기본 이모지 → 선 아이콘(Lucide, ISC)**: 이모지가 기기마다 제각각이고 "AI가 대충 만든 것" 같다는 지적. 오픈소스 선 아이콘 24종을 **SVG 스프라이트로 앱 안에 내장**했다(약 5KB, 외부 요청 없음). 색·굵기는 단청 팔레트를 그대로 물려받는다.
  - 특히 마음에 안 든다던 **🎬 → 지도 핀(장소를 정해 연습)**, **☕ → 말풍선 두 개(자유 수다)**로 교체.
  - 그 밖에: 🎤 마이크, 🪜 → 전구(도움말), 🎥 영상, 💬 채팅, 🔊/🔇 볼륨, 🌐 언어, ✏️ 이름, 🗂️ 기록, 🎙️ 목소리, 🎲 → 주사위, 🎧 헤드폰.
  - 다국어 문구 앞에 붙어 있던 이모지는 표시할 때만 떼어 내고(`stripLeadEmoji`) 그 자리에 아이콘을 세운다 — 12개 언어 문구를 건드리지 않고 처리.
  - 홈 하단 출처란에 `아이콘 Lucide(ISC)` 표기 추가.
- **[온보딩] ③④를 실제 화면 그대로 축소**: "주제 대화 설명이 실제 UI처럼 보여야 한다"는 지적. 단순한 라벨 목록이던 것을 **진짜 설정창·브리핑창과 같은 구성**으로 다시 그렸다.
  - ③ 설정창: 장소 입력칸 → 목적 입력칸 + 🎲 추천 버튼 → 내 역할·호아랑 역할 2단 배치 → 말투 페이더(손잡이까지) → '대화 계획 만들기' 버튼. 항목 이름은 실제 앱과 같은 문구를 그대로 쓴다.
  - ④ 브리핑창: 상황 한 줄 아래에 단계 카드가 놓이고, **각 단계 안에 연습 표현 칩**("안녕하세요", "구경 좀 할게요" 등)까지 실제와 같이 보인다. 아래 '…'로 단계가 더 있음을 표시.
  - 그림 영역을 178 → 204px로 키워 축소판이 답답하지 않게 했다.
- **검증**: 로드 검사 통과, 온보딩 6장 전 과정 클릭 검사 통과.
- **캐시 무효화**: `?v=44 → ?v=45`, 서비스워커 캐시 `hoarang-v21 → v22`.

## v44 — 2026-07-29
- **[사운드] 대화 중 배경음이 호아랑 목소리를 덮던 문제 — 숫자만이 아니라 실제로 해결**: 지금까지 목표 음량 값만 20↔30%로 오가며 고쳤는데, 음원 자체가 최대 0dB로 꽉 차 있어 20%로 낮춰도 말소리와 겹쳤다.
  - **음원 자체를 낮췄다**: 대화 배경음 **-12dB**(평균 -14.6 → -27.0dB), 연습 배경음 **-7dB**(-18.8 → -26.3dB). 이제 20%에서 말소리보다 확실히 아래에 깔린다.
  - **목표 음량 20%로 확정**(홈·대화 단계·대화 중 모두).
  - **말할 때 자동으로 더 낮춘다(더킹)**: 호아랑이 말하는 동안 대화 배경음을 목표의 35%까지 내렸다가(0.22초) 말이 끝나면 되돌린다(0.5초). 방송에서 쓰는 방식으로, 음악을 끄지 않으면서 말소리를 확실히 살린다.
- **[연출] 대화 종료 순서 교정**: 결과 화면이 먼저 뜨고 그 위로 문이 닫혀 순서가 거꾸로였다. 이제 **대화 화면에서 문이 먼저 닫히고 → 문이 다 맞물린 뒤(1.35초) 그 뒤에서 결과 화면으로 바뀌고 → 문이 걷히며 결과가 드러난다.**
  - 이를 위해 문 타이머를 다른 연출 타이머와 분리하고(`doorTimers`), 화면 전환 중에도 문이 지워지지 않게 하는 표시(`fxKeepDoor`)를 뒀다. v38에서 넣은 '화면 바뀌면 연출 정리'가 이 경우엔 방금 닫은 문까지 지웠기 때문이다.
- **[온보딩] 앱의 핵심이 드러나게 다시 구성 (5장 → 6장)**: 이전 구성은 "상황을 정하면 계획이 나온다"만 있고 **무엇을 어떻게 정하는지**가 빠져 단계 이름이 뜬금없이 보였다.
  - ① 호아랑 인사 ② **연습할 상황을 내가 직접 만든다**(주제 대화) / 정하지 않고 이야기한다(자유 수다) ③ **장소·목적·내 역할·호아랑 역할·말투를 정하는 설정 화면** ④ 그 설정에서 나온 대화 계획 ⑤ **채팅으로도, 영상통화로도** ⑥ 꾹 누르고 말하기 · 🪜 · 이어폰
  - **③→④가 한 시나리오로 이어진다**: 「옷 가게 · 마음에 드는 옷 사기 · 손님 ↔ 점원 · 존댓말」로 설정한 뒤, 계획 단계도 "인사하고 둘러보기 → 원하는 옷 물어보기 → 입어 보기 → 값 물어보고 사기"로 그 상황에서 자연스럽게 나온다. 계획 위에는 상황 한 줄(🎭)을 붙여 맥락을 보였다.
  - 문구 12개 언어 전면 재작성. 설정 화면의 항목 이름은 실제 앱과 같은 문구를 그대로 쓴다.
- **검증**: 로드 검사 통과, 온보딩 6장 전 과정 브라우저 엔진에서 클릭 검사 통과, 종료 순서(문 닫힘 → 결과)를 상태 시뮬레이션으로 확인.
- **캐시 무효화**: `?v=43 → ?v=44`, 서비스워커 캐시 `hoarang-v20 → v21`.

## v43 — 2026-07-29
- **[진입] 앱 소개를 온보딩 5장으로 교체**: 지금까지는 긴 설명 한 덩어리를 팝업 하나에 담았는데, 그걸 끝까지 읽는 학습자는 없다. 텀블벅 온보딩처럼 **그림 → 짧은 제목 → 한 줄 설명**을 한 장씩 넘겨 보는 방식으로 바꿨다. **언어 선택 다음에 나오므로 학습자 모국어로 보인다.**
  - 다섯 장: ① 호아랑 인사 ② 주제 대화/자유 수다 두 갈래 ③ 상황을 정하면 대화 계획이 나온다 ④ 마이크를 꾹 누르고 말한다(막히면 🪜) ⑤ 이어폰 권장 · 시작
  - **그림은 새 이미지 없이 실제 화면을 작게 옮겨 그렸다** — 홈 카드 두 장, 대화 단계 목록(①②③ + 🎤), 말하기 버튼(움직이는 레벨 막대)까지 진짜와 같은 모양이라, 학습자가 본 그림을 앱에서 그대로 만난다.
  - 점(dot) 표시로 현재 위치를 보여 주고 눌러서 바로 이동 가능. 마지막 장에서 버튼이 '시작하기'로 바뀌고 건너뛰기는 사라진다. **손가락으로 좌우로 밀거나 화살표 키로도** 넘어간다.
  - 문구는 12개 언어 모두 작성(장당 제목·설명 + 버튼·예시 단계명).
  - 온보딩이 떠 있는 중에 언어를 바꾸면 그 자리에서 새 언어로 다시 그린다.
- **검증**: 새로 만든 `loadtest.js`로 로드 오류 없음 확인 + **온보딩 전 과정을 브라우저 엔진에서 실제로 클릭해 검사**(언어 선택 → 5장 전부 제목·설명·그림·점 표시 → 마지막에서 닫히고 이어폰 안내로 연결). 12개 언어 문구 누락 0건.
- **캐시 무효화**: `?v=42 → ?v=43`, 서비스워커 캐시 `hoarang-v19 → v20`.

## v42 — 2026-07-27 · ★ 원인 확정 및 해결
- **[치명] 페이지가 열릴 때 스크립트가 중간에서 죽고 있었다 — v37부터**
  - **원인**: v37에서 넣은 발화 레벨 막대(`buildPttMeter`)가 스크립트 **3473번째 줄**에서 실행되는데, 거기서 쓰는 `vuBars`를 **4127번째 줄**에서 선언해 두었다. `const`는 선언 전에 접근하면 오류(TDZ)이므로 **페이지를 여는 순간 `ReferenceError`가 나고, 그 지점에서 스크립트가 통째로 멈췄다.**
  - **파급**: 그 뒤에 있던 모든 상수(`HAM_EXPR` 등)가 초기화되지 않았고, 뒤쪽에서 등록하는 이벤트도 붙지 않았다. 그래서 대화 시작을 누르면 `setStatus` → `setHamExpr` → `HAM_EXPR` 접근에서 또 오류가 나 **웹소켓을 만들기도 전에 중단** → 화면에는 방금 찍힌 **"서버에 연결 중…"만 남았다.** 서버는 멀쩡한데 연결 중에서 멈추던 증상의 정체다.
  - **"학습 세션 플로우가 불안정하다"던 증상도 상당 부분 이 하나에서 비롯됐을 가능성이 크다** — v38에서 손본 순서 문제와 별개로, 애초에 뒤쪽 코드가 실행되지 않고 있었다.
  - **조치**: 선언을 사용 지점보다 앞으로 옮겼다.
- **[재발 방지] 로드 검사 도구 추가 (`loadtest.js`)**: `index.html`을 브라우저 엔진(jsdom)에 실제로 올려 **페이지를 여는 순간 나는 오류**를 잡는다. 지금까지 쓰던 `node --check`는 문법만 보기 때문에 이런 실행 시점 오류를 전혀 못 잡았다 — 이번 사고가 그 한계를 그대로 보여 줬다.
  - 검사 자체의 신뢰성도 확인: 고치기 전 상태를 재현해 넣었더니 `Cannot access 'vuBars' before initialization`을 정확히 잡아냈다.
  - 사용법: 프로젝트 폴더에서 `npm i jsdom` 후 `node loadtest.js templates/index.html`. **앞으로 배포 전 필수 검사로 삼는다.**
- **[참고] 서버는 처음부터 정상이었다**: `/conn-diag` 실측 — 서버 응답 269ms, 웹소켓 714ms 연결, Gemini 첫 응답 5.5초, 마이크 허용, 활성 세션 0. v39(무료 플랜 콜드 스타트)·v40(네트워크 차단) 추정은 모두 빗나갔고, 원인은 클라이언트 코드였다.
- **캐시 무효화**: `?v=41 → ?v=42`, 서비스워커 캐시 `hoarang-v18 → v19`.

## v41 — 2026-07-27
- **[진단 결과] 서버·네트워크·Gemini는 모두 정상**: `/conn-diag`로 실측한 결과 — 서버 응답 269ms, **웹소켓 핸드셰이크 714ms 성공**, 연결 후 5.5초 만에 Gemini의 첫 응답(`ai_text`) 도착, 마이크 허용. 활성 세션 0개. 즉 '서버에 연결 중…' 멈춤은 **서버 문제가 아니다.** (v39에서 콜드 스타트로 짚은 것도, v40에서 네트워크 차단을 의심한 것도 실측으로 배제됐다.)
- **[조치] 연결 과정을 조용히 넘어가지 않게 함**: 남은 가능성은 앱이 소켓을 아예 만들지 않고 '연결 중' 문구만 남는 경우다. 지금까지 `startSession()`은 조건에 걸리면 **아무 표시 없이 그냥 빠져나갔다.**
  - 시작 요청 때마다 **현재 상태(대화 중 여부·소켓 상태·모드·계획 ID)를 콘솔에 남기고**, 무시하고 빠져나갈 때마다 그 이유를 남긴다("이미 대화 중", "아직 연결 시도 중 N초째", "죽은 연결 정리 후 재시도").
  - 소켓을 실제로 만들 때 **접속 주소를 콘솔에 남긴다** — 만들지도 않았다면 로그가 아예 없으므로 바로 구분된다.
  - 연결 대기 중에는 상태줄에 **"(N초)"**를 함께 표시한다. 초가 올라가면 기다리는 중, 멈춰 있으면 소켓을 만들지 못한 것이다.
- **캐시 무효화**: `?v=40 → ?v=41`, 서비스워커 캐시 `hoarang-v17 → v18`.

## v40 — 2026-07-27
- **[정정] v39의 원인 진단이 틀렸다**: '서버에 연결 중…' 멈춤을 **무료 플랜 콜드 스타트**로 단정했는데, 실제 배포는 유료 플랜이었다. `render.yaml`의 `plan: free`만 보고 판단한 것이 오판이었다 — 이 값은 **블루프린트로 처음 만들 때만** 쓰이고, 이미 만들어진 서비스의 요금제는 대시보드 설정이 우선한다. `render.yaml`에 이 주의 문구를 남겼다.
  - v39에서 넣은 웜업(`/healthz`)·연결 타임아웃·재시도는 원인과 무관하게 **그대로 두는 편이 안전**하므로 유지한다(실제 회귀였던 'CONNECTING 붙박이면 재시도 영영 무시' 수정도 유효).
- **[버그] 연결됐는데도 '연결 중'에 굳던 경로 차단**: 소켓은 열렸지만 `onopen` 안에서 화면을 꾸미다 예외가 나면 상태 문구를 바꾸기 전에 멈춰, **실제로는 연결됐는데 '연결 중'으로 보이는** 상태가 됐다. `onopen` 전체를 감싸 실패해도 세션을 정리하고 사유를 알리도록 했다.
- **[진단] 왜 끊겼는지 보이게 함**: 웹소켓 종료 코드·사유(`close 1006` 등)를 잡아 두고 재시도 안내와 콘솔에 함께 보여 준다. 지금까지는 "연결 실패"만 떠서 원인을 좁힐 수 없었다.
- **[신규] `/conn-diag` 연결 진단 페이지(교사용)**: 문제가 나는 **그 기기·그 네트워크에서** 열면 ① 서버 응답 시간과 현재 세션 수 ② 웹소켓 핸드셰이크 시간 ③ 연결 후 첫 서버 메시지까지 걸린 시간 ④ 마이크 권한을 차례로 재고, 막힌 단계와 원인 후보를 함께 알려 준다. 학생 화면에는 링크가 없다.
  - 단계별로 원인을 구분한다: 핸드셰이크가 안 되면 서버 재시작/네트워크(방화벽·프록시)의 웹소켓 차단, 붙었는데 조용하면 Gemini 연결·API 키·쿼터, 마이크 거부면 권한 문제.
- **캐시 무효화**: `?v=39 → ?v=40`, 서비스워커 캐시 `hoarang-v16 → v17`.

## v39 — 2026-07-27
- **[사운드] 배경음 30%로 조정**: 전 버전에서 20%로 낮춰 두었던 목표 음량을 **기기 음량의 30%**로 되돌렸다(홈·대화 단계·대화 중 세 음원 모두).
- **[버그] '서버에 연결 중…'에서 멈추던 문제 — 두 가지 원인**
  - **원인 ①(환경) — ※ v40에서 정정됨**: 무료 플랜 콜드 스타트로 단정했으나 실제 배포는 유료 플랜이었다(`render.yaml`의 `plan: free`만 보고 오판). 아래 웜업·타임아웃 처리는 원인과 무관하게 유용하므로 유지한다.
    - 서버에 가장 가벼운 응답만 하는 **`/healthz`**를 추가하고, **앱을 켤 때·시나리오 설정을 열 때·자유 수다를 누를 때·대화 단계 화면에서** 미리 찔러 깨워 둔다(1분 안에 깨웠으면 다시 찌르지 않음).
    - 3초가 지나도 안 붙으면 **"🌙 서버를 깨우는 중이에요…"**로 안내를 바꿔, 멈춘 게 아니라는 걸 알린다(12개 언어).
    - **25초 안에 못 붙으면 매달려 있지 않고 끊고**, 홈으로 되돌린 뒤 "다시 시도할까요?"를 묻는다. 예를 누르면 서버를 깨우고 재시도.
  - **원인 ②(v38 회귀)**: v38에서 넣은 중복 세션 방지 가드가 **연결이 `CONNECTING`에 붙박이면 이후 모든 시도를 영영 무시**해, 한 번 막히면 다시 시작할 수 없었다. 이제 25초가 지난 연결은 죽은 것으로 보고 정리한 뒤 새로 연다.
- **참고(수업 운영)**: `MAX_CONCURRENT_SESSIONS`가 **5**로 잡혀 있다. 한 반에서 여섯 명 이상이 동시에 대화하면 그 뒤 학생은 "사용자가 많아요" 안내를 받는다. 학급 규모에 맞춰 Render 환경변수에서 올리거나(유료 플랜 필요) 조를 나눠 진행해야 한다.
- **캐시 무효화**: `?v=38 → ?v=39`, 서비스워커 캐시 `hoarang-v15 → v16`.

## v38 — 2026-07-27 · 학습 세션 플로우 안정화
v28~v37에서 연출(문·호아랑 등장·비계)을 여럿 얹으면서 각자 타이머로 돌게 된 것이 누적돼, 화면을 조금만 빨리 넘겨도 **순서가 엉키거나 빈 화면이 남는** 문제가 생겼다. 개별 증상을 하나씩 막는 대신 전환 구조 자체를 정리했다.

- **[구조] 연출 예약을 한 곳에서 관리**: 흩어져 있던 `setTimeout`을 `fxAfter()`로 모으고, **화면이 바뀌면 `fxReset()`이 예약을 통째로 취소하고 화면을 깨끗이** 한다. 지난 화면의 문·호아랑·비계가 다음 화면까지 따라오던 문제 제거. 문에는 최후의 안전장치(예약이 실패해도 일정 시간 뒤 반드시 걷힘)를 별도로 달았다.
- **[버그] 문이 녹음 동의 팝업보다 먼저 열리던 순서 오류**: '이 설정으로 시작하기'를 누르면 문부터 열리고 그 위로 동의 팝업이 떴다. **동의를 취소하면 브리핑도 닫힌 채 홈에 덩그러니 남았다.** 문을 **실제 세션이 열리는 `startSession()` 시점으로 옮기고**, 동의를 취소하면 **브리핑 화면으로 정확히 되돌아가게** 했다.
- **[버그] 화면 전환이 방금 연 문을 지우던 문제**: 위 `fxReset` 도입으로 `문 열기 → 화면 전환` 순서일 때 문이 곧바로 지워졌다. 자유 수다 진입과 대화 종료(피드백·결과) 세 곳의 **순서를 '화면 전환 먼저, 문 연출 나중'으로 교정**했다.
- **[버그] 대화 단계가 최대 6초간 빈 화면**: 등장 애니메이션이 3.2초 뒤부터 0.56초 간격이라, 단계가 6개면 마지막이 6초 뒤에나 보였다. **시작을 2.4초로 당기고**, 단계가 5개를 넘으면 간격을 0.40초로 줄이며, **정해진 시간이 지나면 무조건 전부 보이도록** 안전장치를 넣었다. 애니메이션이 아예 지원되지 않는 환경에서도 보이도록 `both`로 보정.
- **[버그] 연타로 세션이 두 번 열리던 문제**: 홈 카드·시작 버튼·단계 카드에 **전환 잠금(`flowLock`)**을 걸고, `startSession()`에는 **이미 연결 중/연결됨이면 무시하는 가드**를 넣었다. 연결에 성공하면 잠금을 즉시 푼다.
- **[버그] 연결 실패 시 빈 대화 화면에 남던 문제**: 한 번도 붙지 못하고 끊긴 경우를 구분해(`sessionOpened`) **홈으로 되돌리고 안내**한다. 종료 처리(`stopSession`)에도 재진입 가드를 넣어 녹음이 이중 저장되지 않게 했다.
- **검증**: 정상 진행·동의 취소 후 재시도·시작 버튼 연타·연결 실패·카드 연타·대화 중 끊김·연출 도중 뒤로가기 등 **9개 시나리오를 상태 기계로 모사**해, 끝났을 때 화면·문·잠금이 남지 않는지 모두 확인.
- **캐시 무효화**: `?v=37 → ?v=38`, 서비스워커 캐시 `hoarang-v14 → v15`.

## v37 — 2026-07-27
- **[버그] 눌러서 말하기가 손을 뗀 뒤에도 계속 듣던 문제**: 손을 화면 밖에서 떼거나 앱이 뒤로 밀리면 '뗀' 신호를 놓쳐 녹음이 이어졌다.
  - 눌린 상태(`pttHeld`)를 따로 두고 **`lostpointercapture`·창 전체 `pointerup`·`blur`·탭 숨김**에서도 확실히 끝내도록 했다. 45초 안전 타이머도 추가.
  - 손을 뗀 뒤 흘려보내던 꼬리를 **400ms → 250ms**로 줄이고, 누르기 직전 버퍼도 384ms → **256ms**로 줄여 "누른 동안만" 감각에 가깝게 맞췄다.
- **[신규] 말하는 동안 목소리 세기가 보인다**: 입력이 되고 있는지 알 수 없다는 지적. 버튼 안에 **막대 5개**를 넣고 **실제 마이크 신호(RMS)**로 움직이게 했다. 소리가 크면 크게 뛰고 조용하면 가라앉는다 — 눈으로 입력을 확인할 수 있다.
- **[버그] 발음이 엉망인데 '완벽해요'가 뜨던 문제 — 원인 제거**: `/stt`에 **목표 문장을 힌트로 넘기고 있어서** 모델이 그 문장을 그대로 베껴 적었다(그래서 항상 유사도 1.00). 이제 **목표를 모델에 주지 않고 들린 대로만 전사**한다("문법·맞춤법 고치지 마라, 잘못 발음한 음절은 그대로 적어라"). 채점은 클라이언트가 전사와 목표를 비교해서 한다.
  - 통과 기준도 올림: 완벽 `0.85 → 0.90`, 좋아요 `0.68 → 0.76`. 목표의 60%도 안 되게 말하면(중간에 그만두면) 통과시키지 않는다.
- **[사운드] 화면마다 다른 가야금**: 배경음을 셋으로 나눴다.
  - **대화 진행 중(주제 대화·자유 수다)** → 「How are you 가야금」(신규 `static/chat_bgm.mp3`). 전에는 대화 중 무음이었다.
  - **대화 단계(브리핑)·발화 연습** → 「통통 가야금」. 대화 단계를 보는 동안 무음이던 것을 채웠다.
  - **홈·안내** → 기존 배경음.
  - **모든 배경음 음량을 기기 음량의 20%로 통일**(기본 배경음도 30% → 20%). 서로 넘어갈 때는 페이드 인·아웃.
- **[UI] 주제 대화의 말하기 버튼은 진녹색**: `body[data-mode]`로 모드를 표시해 주제 대화 `#2E5B3E`, 자유 수다 장단 주홍으로 구분. 발화 연습 마이크도 같은 규칙을 따른다.
- **[발화 연습] 문은 처음 한 번만**: 단계를 누를 때마다 문이 열려 번거롭던 것을 **그 계획에서 처음 들어갈 때만** 열도록 했다.
- **[UI] 대화 단계 등장 간격 2배**: 0.28초 → **0.56초**. 너무 빨라 차례로 나타나는 느낌이 없었다.
- **[영상통화] 비계**: 저절로 튀어나오던 자동 노출을 없애고 **🪜를 눌렀을 때만** 나오게 했다(`hvScfWanted`). 상단에 있던 🪜 버튼을 **카드가 나오는 오른쪽 아래로 옮겨** 버튼과 카드가 붙어 보이게 했다.
- **출처 추가**: 「How are you 가야금」 서예지 — 공유마당(자유이용 기증).
- **캐시 무효화**: `?v=36 → ?v=37`, 서비스워커 캐시 `hoarang-v13 → v14`.

## v36 — 2026-07-27
- **[STT] 사용자 발화 인식 강화 (두 가지)**: 전사가 자꾸 어긋나던 문제.
  - **앞머리 보존(신규)**: 학습자는 버튼을 누르면서 동시에 말을 시작하는 일이 잦아 첫 음절이 통째로 날아갔다. 마이크는 항상 돌고 있으므로 **직전 3조각(약 384ms)을 들고 있다가 발화가 시작되면 그것부터 먼저 보낸다.**
  - **끝음절 보존 강화**: 손을 뗀 뒤 흘려보내는 꼬리를 **220ms → 400ms**로 늘렸다(대화·발화 연습 모두).
- **[UI] 눌러서 말하기 버튼**: 배경과 구분이 안 되고 상태마다 색이 널뛰던 문제.
  - 색을 **장단(주홍) `#B5573F`로 통일** — 대화창(옅은 뇌록)·영상통화(한지색) 어느 배경에서도 확실히 뜬다. 영상통화용 별도 색 지정은 제거해 어느 화면이든 같은 버튼으로 보이게 했다.
  - 말하는 중에도 **색상 계열은 유지**하고 조금 진하게(`#8E4231`) + 테두리만 두껍게 — 색이 바뀌는 느낌을 없앴다. 발화 연습 마이크도 동일.
- **[UI] 문구 정리**: '과업 진행률'(`progressLabel`)과 '버튼을 누른 채로 말하세요'(`stPushHint`·`prMicIdle`)를 12개 언어에서 모두 비웠다. 빈 라벨이 자리를 차지하지 않도록 `:empty` 처리.
- **[UI] 채팅 영역 확대**: 대화창 높이 `540 → 600px`(최대 92vh → 96vh), 말풍선 사이 간격 8 → 11px, 위아래 여백 14 → 20px.
- **[영상통화] 비계 위치**: 화면 중앙 오른쪽(top 40%) → **오른쪽 아래, 눌러서 말하기 버튼 위**(`bottom: 148px + 안전영역`).
- **[연출] 문 여닫는 시간 추가 연장**: 학습자가 글자를 읽을 시간을 더 줬다. 열기 **2.4 → 3.2초**(닫힌 상태 유지 52% → 60%), 닫기 **2.0 → 2.8초**.
- **[발화 연습] 흐름 재설계**: 대화 계획을 만든 직후 → **① '대화 흐름' 문이 열리고 → ② 호아랑이 오른쪽에서 나와 "이제 이 대화를 연습해 봐요!" → ③ 대화 단계가 첫 번째부터 0.28초 간격으로 차례로 나타난다.** 그 다음 **단계를 누르면 '발화 연습' 문이 열린다.** (v35에서는 문·호아랑이 연습창 안에서 나와 순서가 어긋나 있었다.)
- **[발화 연습] '가→나' 구조 보장**: 표현에 상대 발화(cue)가 없으면 따라 말하기만 하고 다음 표현으로 넘어가 `a-b-a1-b1`처럼 보였다. 계획 생성 프롬프트를 고쳐 **모든 표현에 cue를 반드시 채우게** 했다(학습자가 먼저 여는 발화라도 그 직전에 올 법한 상대의 말을 만들어 넣도록). 이제 표현마다 `따라 말하기 → 말차례 교환`이 짝으로 돈다.
- **[발화 연습] 호아랑 발화 지연 제거**: 말차례 교환에서 상대 발화가 나올 때까지 기다려야 했다. **앞 차례를 하는 동안 다음 음성을 미리 받아 두는 선(先)요청**을 넣어(클라이언트 캐시 40개) 넘어가는 즉시 재생되게 했다.
- **캐시 무효화**: `?v=35 → ?v=36`, 서비스워커 캐시 `hoarang-v12 → v13`.

## v35 — 2026-07-26
- **[버그] 휴대폰 뒤로가기 버튼에 앱이 그냥 닫히던 문제**: 웹앱이라 첫 뒤로가기에 브라우저/앱이 종료돼 대화가 통째로 날아갔다.
  - 히스토리에 여분 항목을 하나 심어 두고, 뒤로가기마다 **지금 화면을 직접 보고** 한 단계 뒤로 보낸 뒤 항목을 다시 채운다. 상태를 따로 쌓아 두지 않아 어긋날 일이 없다.
  - 단계: 인트로 영상 → 건너뛰기 / 팝업이 떠 있으면 → 그 팝업의 ✕와 같은 동작(기존 로직 재사용) / 영상통화 → 채팅 / 대화 중 → 종료 확인 후 홈 / 결과 화면 → 홈 / **홈에서는 아무 일도 하지 않음**(수업 중 실수로 앱이 꺼지지 않게).
  - 검증: 여덟 가지 상황을 가짜 DOM으로 시뮬레이션해 각각 무엇이 닫히는지 확인.
- **[연출] 문 열기를 더 크고 길게**: 주제 대화·자유 수다에 들어갈 때 모드 이름을 **큰 볼드체**(27px 고정 → `clamp(30px, 8.4vw, 52px)`, weight 800→900)로 키우고, 문이 열리는 시간을 **1.7초 → 2.4초**로 늘렸다. 닫히는 문(2.0초)은 그대로. 징 소리는 기존과 같이 문이 움직일 때 울린다.
- **[사운드] 배경음악 페이드 인·아웃**: 툭 켜지고 툭 꺼져 거슬리던 것을 **0.5초 페이드인 / 0.42초 페이드아웃**으로 바꿨다. 홈에 들어설 때, 대화를 시작·종료할 때, 발화 연습의 가야금으로 넘어갈 때 모두 서로 부드럽게 교차한다. 같은 음원에 페이드가 겹쳐 불려도 직전 것을 취소하고 현재 음량에서 이어받는다.
- **[발화 연습] 진입 연출**: 연습 단계에 들어가면 문이 열리고(‘발화 연습’ 표시), 1.5초 뒤 **호아랑이 화면 오른쪽에서 빼꼼 나와 “이제 이 대화를 연습해 봐요!”**라고 말을 건다(12개 언어, 4.2초 뒤 스스로 들어감). 연습창을 닫으면 함께 사라진다.
- **[발화 연습] 마이크를 '눌러서 말하기'로 통일**: 탭해서 시작 / 탭해서 종료 방식이 대화 화면(꾹 누르기)과 달라 학습자가 헷갈렸다. 대화 화면과 **같은 알약형 버튼 + 같은 문구**(`pttHold`/`pttTalking`)로 바꾸고 포인터 이벤트로 꾹 누르기 처리. 손을 떼도 **220ms 더 듣고** 끝내 문장 끝음절이 잘리지 않게 했다(대화 화면과 동일한 처리).
- **캐시 무효화**: `?v=34 → ?v=35`, 서비스워커 캐시 `hoarang-v11 → v12`.

## v34 — 2026-07-26
- **[연출] 장지문을 '대화의 문턱'으로 재배치**: 기능 단계마다 문이 여닫혀 대화 흐름을 자주 끊던 것을 정리했다.
  - **기능 단계 달성 → 문 연출 제거.** 짧은 딩동(`playDing`)만 남겼다. 징은 이제 문이 움직일 때만 울린다.
  - **문 열기(신규)**: 주제 대화에서 브리핑을 마치고 '이 설정으로 시작하기'를 누를 때, 그리고 자유 수다 카드를 누를 때 — 닫힌 문이 좌우로 열리며 대화 자리로 들어간다. 문 위에는 주제명(또는 '자유 수다')이 뜬다.
  - **문 닫기(신규)**: 대화를 마칠 때(주제 대화 결과·자유 수다 피드백 직전) 열린 문이 가운데로 닫히고 '수고했어요'가 뜬 뒤 스르륵 사라진다.
  - 구현: 한 함수(`playDoor`)에 `opening`/`closing` 두 모드. 기존의 '닫혔다 열림' 한 가지 애니메이션을 두 방향으로 분리했다.
  - **문 위 글씨 색 변경**: 갈색(`#6B4A2A`) → **창호지 위의 먹빛(`#2E3338`)**. 흰 그림자도 옅게 줄였다.
- **[시작 영상] 화면 아무 데나 눌러도 넘어감**: 작은 버튼을 찾아 눌러야 했던 것을 화면 전체 탭으로 바꿨다. 오른쪽 아래 표시는 버튼이 아니라 **안내 문구**("화면을 누르면 넘어가요", 12개 언어)로 바뀌고 1.1초 뒤 부드럽게 나타난다.
- **[영상통화] 말풍선을 머리 바로 위로**: 말풍선이 화면 위쪽에 따로 떠 있어 캐릭터와 멀었다. 말풍선을 캐릭터와 **한 덩어리(`.hv-stage-slot` 세로 정렬)**로 묶어 머리 바로 위에 붙였다.
- **[영상통화] 비계가 안 보이던 문제**: 🪜 버튼이 아이콘만 있어 눈에 띄지 않았다.
  - 버튼에 **글자 라벨**을 붙여 키우고(다국어), 빼꼼 카드의 `z-index`를 6 → 40으로 올렸다.
  - **주제 대화로 영상통화를 처음 열면 0.9초 뒤 비계가 한 번 자동으로 빼꼼** 나온다 — 학생이 기능의 존재를 알 수 있게. (세션당 한 번)
- **캐시 무효화**: `?v=33 → ?v=34`, 서비스워커 캐시 `hoarang-v10 → v11`.

## v33 — 2026-07-26
- **[연출] 앱 시작 영상 추가**: 한옥 장지문이 열리며 호아랑이 나오는 6.5초 인트로(`호아랑 캐릭터/호아랑 앱시작.mp4`)를 진입 화면으로 넣었다. 앱의 장지문 연출·단청 팔레트와 결이 맞는다.
  - **영상 처리**: 1.7MB → **648KB**로 재인코딩(720p→640p, CRF 28, `+faststart`로 즉시 재생 시작). 오른쪽 아래 **생성 워터마크(✦)는 `delogo`로 제거**. 첫 프레임을 포스터(`intro-poster.jpg`, 56KB)로 뽑아 로딩 중 검은 화면이 안 보이게 했다.
  - **재생 규칙**: 같은 세션에 한 번만(`sessionStorage`) — 새로고침으로는 다시 안 뜨고, 앱을 닫았다 열면 다시 나온다. '건너뛰기'로 언제든 넘길 수 있고(12개 언어), 끝나면 스스로 사라진다.
  - **소리**: 소리를 켜고 재생을 시도한 뒤 브라우저가 막으면 **음소거로 자동 전환**. 인트로가 도는 동안에는 배경음악을 멈춰 소리가 겹치지 않게 했다(`splashActive`).
  - **안전장치(수업 중 멈춤 방지)**: ① 4초 안에 재생이 시작되지 않으면 넘어감 ② 영상 오류 시 즉시 넘어감 ③ 어떤 이유로든 10초를 넘기면 강제 종료 ④ 동작 최소화 설정(`prefers-reduced-motion`)이면 아예 재생하지 않음.
  - 넓은 화면에서는 세로 영상이 화면을 다 채우지 않고 가운데에 서도록(`max-width: 46vw`) 처리.
  - 서비스워커 캐시 규칙에 `mp4` 추가 — 두 번째 접속부터는 다시 내려받지 않는다.
  - 검증: 재생 성공/자동재생 차단/영상 오류/느린 회선/이미 본 세션/동작 최소화 여섯 경우를 가짜 DOM으로 시뮬레이션해 상태 전이를 확인.
- **캐시 무효화**: `?v=32 → ?v=33`, 서비스워커 캐시 `hoarang-v9 → v10`.

## v32 — 2026-07-26
- **[디자인] 버튼 무늬를 실제 전통 문양으로**: v31의 단순 격자는 한국적이지 않다는 지적에 따라 **소슬빗살**(한옥 창호의 빗살 — 대각선 살대 두 방향 + 세로살)로 교체. 국립문화유산연구원 「한국의 전통문양」의 문양 유형을 참고해 **타일링 가능한 SVG로 직접 작도**했다(문양 DB의 이미지 파일을 쓰지 않아 색·크기를 자유롭게 조절할 수 있고 용량은 0.4KB). 흰색 16% 농도로 버튼·홈 카드·말하기 버튼에 얹었고, 고대비 설정에서는 그대로 꺼진다.
  - 함께 작도해 둔 문양(교체용): 뇌문(번개무늬)·칠보문·귀갑문. `outputs/patterns.py`에 함수로 정리.
  - 홈 하단 출처란에 「전통 문양(소슬빗살) 참고: 국립문화유산연구원 한국의 전통문양」 표기 추가.
- **[영상통화] 어두운 화면을 한지빛으로**: 남색 그라데이션이 답답하다는 지적. 배경을 `#FFFEFA → 한지색 → #E9E1CE`로 바꾸고 그 위의 모든 요소를 밝은 배경에 맞게 다시 잡았다.
  - 이름 흰색 → 삼청(`#2A4A55`), 채팅·🪜 버튼은 반투명 흰색 → 미색 카드 + 뇌록 글자, 말하기 버튼은 밝은 틸 → 뇌록 + 흰 글자, 빠른요청 버튼도 미색 카드로.
  - 흰 말풍선이 한지 배경에 묻히지 않도록 **뇌록 테두리(`#A9C4BB`)**를 두르고, 꼬리도 테두리색·흰색 두 겹으로 다시 그렸다.
  - 캐릭터 뒤 글로우도 하늘색 → 뇌록빛으로.
  - 대비 재검증: 이름 8.35:1, 버튼 글자 4.67:1, 말하기 버튼 4.74:1, 비활성 버튼 4.20:1 — 모두 AA 이상.
- **캐시 무효화**: `?v=31 → ?v=32`, 서비스워커 캐시 `hoarang-v8 → v9`.

## v31 — 2026-07-26
- **[대화창] 내 말풍선을 흰 바탕으로**: 카카오톡식 황색 말풍선 → 흰색. 상대(호아랑)와는 좌우 위치·프로필·뇌록 테두리(`#B9CFC7`)로 구분한다. 흰 말풍선이 묻히지 않도록 대화창 바탕도 `#E3E9DF → #D8E2D6`으로 살짝 진하게.
- **[사운드] 주제 대화 설정 화면은 배경음악 유지**: v28에서 설정창까지 무음으로 만들었는데, 아직 연습 전이라 음악이 있어도 방해되지 않는다는 판단. **무음 구간을 브리핑(대화 단계 보기)부터**로 좁혔다. 발화 연습에서 가야금 20%만 까는 규칙은 그대로.
- **[진입] 안내 팝업 3회 후 기억**: 같은 기기에서 여러 번 들어오는 학습자에게 언어 선택·앱 소개·이어폰 안내를 매번 다시 묻던 문제. 안내를 끝까지 본 횟수를 세어(`entryDoneCount`) **3회를 채우고 언어·이름이 남아 있으면 네 팝업을 모두 건너뛰고 곧장 홈**으로 들어간다. 언어는 홈 우상단 🌐, 이름은 ✏️로 언제든 다시 바꿀 수 있다.
- **[디자인] 버튼에 한옥 창살 무늬**: 주요 버튼·홈 카드·말하기 버튼에 격자(문살) 무늬를 **흰색 7.5% 투명도**로만 얹었다. 무늬로 보이기보다 결로만 느껴지는 정도. 배경색을 새로 지정하는 `:hover`·`:disabled` 규칙보다 뒤에 두어 상태가 바뀌어도 무늬가 유지되게 했고, 고대비 설정(`prefers-contrast: more`)에서는 자동으로 꺼진다.
- **[영상통화] 말풍선을 호아랑 머리 위로**: 캐릭터 아래에 있던 말풍선을 **위로 옮기고 꼬리 방향도 아래(캐릭터 쪽)로** 뒤집었다. 말이 머리 위에서 나오는 형태.
- **[영상통화] 비계(🪜)를 옆에서 빼꼼**: 지금까지 영상통화 화면에서는 비계를 쓸 수 없었다(채팅 패널에만 있었음). 상단에 🪜 버튼을 추가하고, 누르면 **오른쪽 가장자리에서 카드가 슬며시 나와 표현을 알려주고 스스로 사라진다**(응답 있으면 9초, 없으면 4.5초, 로딩 중 11초). 표현을 탭하면 들려주고 표시 시간이 연장된다. 주제 대화일 때만 버튼이 보이고, 영상통화를 닫으면 카드도 함께 들어간다. 채팅 화면의 접이식 비계 패널과 같은 내용을 화면에 맞는 방식으로 보여줄 뿐이라 서버 요청 경로는 그대로.
- **캐시 무효화**: `?v=30 → ?v=31`, 서비스워커 캐시 `hoarang-v7 → v8`.

## v30 — 2026-07-26
- **[디자인] 단청 리디자인 (은은하게)**: 슬레이트·틸 팔레트를 **한옥 단청 안료색**으로 전면 교체. 색을 진하게 쓰지 않고 채도만 낮춰 옮겨, 한국적이되 어지럽지 않게 했다.
  - 팔레트: 배경 한지색 `#F4F0E6` · 종이 `#FFFDF8` · 먹 `#2E3338` · **뇌록(주색) `#4A7C74`** · 삼청(제목) `#2A4A55` · **장단/주홍(강조) `#B5573F`** · 단청 황 `#C9922E` · 황토 `#D6C6A2`. 대화창 바탕은 카카오풍 하늘색 → 옅은 뇌록빛 `#E3E9DF`.
  - 홈 카드: 주제 대화 = 삼청→뇌록, 자유 수다 = 적갈→황토 그라데이션.
  - **단청 오방색 띠**: 팝업(`.lang-box`)과 왼쪽 패널 맨 위에 청·황·적·백·흑 3px 한 줄. 스크롤해도 제자리에 있도록 배경으로 그렸다.
  - **한지 결**: 배경에 씨실·날실 패턴을 투명도 3% 이하로 깔아 색이 아니라 질감으로 한국적인 느낌을 준다.
  - 하드코딩돼 있던 회청색 계열(`#5A6B7B`·`#41586B`·`#34507a` 등)과 그림자 색까지 모두 단청 계열로 정리. PWA `theme_color`·`background_color`도 함께 교체.
  - **가독성 검증**: 주요 글자–배경 조합의 명도 대비를 계산해 전부 WCAG AA(4.5:1) 이상으로 맞춤. 이 과정에서 보조 글자색(3.4:1)과 자유 수다 카드(3.0:1)가 기준 미달이라 각각 `#726A5C`, 적갈 그라데이션으로 조정.
- **[목소리] 호아랑 톤을 더 어리게**: 목소리를 남자아이로 바꿨는데도 어른스럽게 들리던 문제.
  - **시스템 프롬프트에 나이 규칙 추가**: "초등학생 또래 남자아이처럼, 밝고 가볍고 조금 빠르게. 뉴스 앵커·상담원 같은 어른 톤 금지." 감탄사 예시("우와!", "헐 진짜?")도 함께. 단, **주제 대화에서 배역을 맡으면 그 배역 나이에 맞춘다**(어른 역에 아이 목소리 흉내 금지).
  - **TTS 스타일 지시**: 아이 목소리(`Puck`·`Leda`·`Fenrir`·`Sadachbia`)일 때만 "밝고 신나는 초등학생 아이 목소리로…" 지시를 앞에 붙여 같은 목소리도 더 어리게. 어른 배역엔 붙이지 않는다. 환경변수 `TTS_CHILD_STYLE`로 문구 교체 가능.
- **[버그] 정체성 문구가 아직 햄스터였음**: 시스템 프롬프트의 `BASE_PERSONA`가 v26 개명 이후에도 "회색 후드티를 입은 햄스터"로 남아 있었다 → "갓을 쓴 하얀 아기 호랑이"로 수정. 좋아하는 것도 해바라기씨 → 곶감으로.
- **[교사용] `/voice-lab` 목소리 비교 페이지 신설**: 후보 목소리 12종을 같은 문장으로 나란히 들어보고 기본 목소리를 정하는 숨은 페이지(학생 화면에는 링크 없음). 목소리마다 **'어린 톤 지시 있음/없음'을 A/B로 비교**할 수 있어, 스타일 지시가 음성으로 새어 나오는지도 바로 확인 가능.
- **캐시 무효화**: `?v=29 → ?v=30`, 서비스워커 캐시 `hoarang-v6 → v7`.

## v29 — 2026-07-26
- **[정체성] 호아랑 목소리 = 남자아이**: 호아랑이 '갓 쓴 아기 호랑이'인데 성인 여성 목소리(`Kore`)로 말하던 불일치 해소. 기본 목소리를 밝은 남자아이 톤(`Puck`)으로 교체. 대화(Live API)와 발화 연습(TTS)이 같은 목소리를 쓰도록 통일.
- **[주제 대화] 배역에 맞춘 목소리 자동 전환**: 호아랑이 맡은 배역에 어울리는 목소리를 서버가 골라 세션을 연다(`pick_voice`).
  - 배역에 성별·나이가 **드러나 있을 때만** 반영: 아주머니·언니 → 성인 여성, 아저씨·형 → 성인 남성, 할머니/할아버지 → 어르신 남/여, 친구·학생 등 또래 → 아이 목소리.
  - 직업 이름만 있어 성별을 알 수 없는 배역(점원·선생님·의사·간호사 등)은 **직업으로 성별을 넘겨짚지 않는다.** 역할 이름을 해시해 성인 남/여 중 하나를 고정 배정 — 같은 배역이면 언제나 같은 목소리라 수업 중 목소리가 널뛰지 않는다.
  - 목소리 표(Chirp 3 HD): boy `Puck` · girl `Leda` · man `Charon` · woman `Kore` · elder_m `Algenib` · elder_f `Gacrux`.
- **[선택] 홈 우상단 🎙️ 목소리 버튼**: 자동(기본) 외에 남자아이·여자아이·성인 남성·성인 여성·어르신을 직접 고를 수 있고, 각 항목에서 🔊 **미리 듣기** 가능. 고른 값은 기기에 저장되며 배역 자동 선택보다 우선한다. 12개 언어 지원.
  - 전달 경로: 대화는 `ws/live?voice=`, 발화 연습은 `POST /tts {voice, role}`. TTS 캐시 키도 `(문장, 목소리)`로 바꿔 목소리별로 따로 캐싱.
- **[디자인] 홈 좌상단 제목 정리**: `🐯 호아랑과 한국어 수다` → **앱 아이콘 이미지 + 「호아랑」**. 이모지를 없애고 실제 앱 아이콘(단청 프레임)을 로고로 사용. 캐릭터 이름을 12개 언어로 분리(`charName`: 한국어 호아랑 / 일본어 ホアラン / 그 외 Hoarang) — 영상통화 이름표에 한국어가 그대로 나오던 것도 함께 수정.
- **캐시 무효화**: `?v=28 → ?v=29`, 서비스워커 캐시 `hoarang-v5 → v6`.
- *(남은 작업: 단청 계열 한국적 리디자인, 호아랑 추가 표정·포즈 이미지)*

## v28 — 2026-07-26
- **[수업 배포] 인앱 브라우저 게이트**: 카톡·인스타 DM으로 링크를 받으면 메신저 안의 작은 브라우저에서 열려 **마이크 권한과 홈 화면 설치(PWA)가 막히던** 문제. 이제 진입 즉시 안내 화면을 띄우고 바깥 브라우저로 나가게 한다.
  - 감지 대상: 카카오톡·인스타그램·스레드·페이스북·라인·네이버앱·다음·밴드·위챗·에브리타임·스냅챗 + 안드로이드 일반 웹뷰(`; wv)`).
  - 탈출 방법(환경별): 카카오톡 `kakaotalk://web/openExternal`, 라인 `?openExternalBrowser=1`, 안드로이드 `intent://…package=com.android.chrome`, iOS `x-safari-` 시도 후 실패하면 **주소 자동 복사**. 수동 안내(⋯ → 다른 브라우저로 열기)도 함께 표시.
  - 안내 문구 12개 언어 지원. 언어를 아직 안 고른 첫 진입에는 **기기 언어**로 자동 표시. '괜찮아요' 선택 시 그 세션 동안 다시 뜨지 않음.
- **[디자인] 지마켓 산스 적용**: 본문 글꼴을 시스템 기본 → **지마켓 산스**(Medium/Bold)로 교체. TTF를 woff2로 변환해 `static/fonts/`에 포함(한글 11,172자 전부 포함, Medium 500KB·Bold 578KB). `font-display: swap`으로 글자가 늦게 뜨지 않게 하고, 폼 요소(`input·button`)도 같은 글꼴을 쓰도록 통일. 서비스워커는 글꼴·사운드만 **캐시 우선**으로 바꿔 접속할 때마다 1MB를 다시 받지 않도록 함.
- **[수업 환경] 교실 스크린 글자 확대**: 폰에서는 괜찮지만 수업용 PC·프로젝터에서 글자가 작아 안 보이던 문제. 화면 크기에 따라 UI 전체 배율을 자동 조절(`--ui-zoom`: 1000px↑ 1.18배 / 1400px↑ 1.34배 / 1800px↑ 1.5배). 폰(좁은 화면)은 1.0배 그대로.
  - 배율은 내용 블록(`.app`·`.home-screen`·`.lang-box`·`.inapp-card` 등)에만 걸고, 화면을 덮는 배경막(`position:fixed`)은 건드리지 않아 어느 브라우저에서도 화면 밖으로 삐져나가지 않는다. 배율이 걸린 블록 안의 `vh/vw` 값은 모두 `/ var(--ui-zoom)`로 보정.
- **[사운드] 대화 계획·발화 연습 구간 배경음 정리**: 대화 계획을 짜거나 발화 연습을 하는 동안에도 메인 BGM이 계속 흘러 방해되던 문제.
  - **대화 계획 설정·브리핑 중 → 완전 무음.**
  - **발화 연습 중 → 메인 BGM 정지, 「통통(가야금)」을 20% 음량으로만 은은하게** (`static/practice_bgm.mp3`).
  - 구현: 창의 열림/닫힘을 `MutationObserver`로 직접 감시해 배경음 장면을 자동 재계산 — 창을 여닫는 코드를 일일이 고치지 않아도 반영됨. 🔇 음소거는 모든 소리에 함께 적용.
- **[연출] 단계 달성 = 징 + 한옥 장지문**: 주제 대화에서 한 단계를 충족하면 기존의 전자음(딩동) 대신 **전통악기 징**(`static/gong.mp3`, 3.2초로 다듬음)이 울리고, **창호지 장지문 두 짝이 스르륵 닫혔다가 다시 열리며** 달성한 단계 이름을 보여 준다. 별도 이미지 없이 창살은 CSS 그라데이션으로 그렸고, 문은 클릭을 막지 않는다(`pointer-events:none`). 동작 최소화 설정(`prefers-reduced-motion`)에서는 짧게 재생.
- **출처 표기**: 홈 하단에 글꼴·배경음·효과음 출처를 표시(징 효과음이 공유마당 KOGL 제1유형 = 출처 표시 조건).
- **캐시 무효화**: `?v=27 → ?v=28`(21곳), 서비스워커 캐시 `hoarang-v4 → v5`.
- *(남은 작업: 단청 계열 한국적 리디자인, 호아랑 추가 표정·포즈 이미지 — v29 예정)*

## v27 — 2026-07-26
- **호아랑 정적 이미지 복구 + 표정 세트 완성**: 로컬 `static/`에 캐릭터·아이콘 PNG가 비어 있던 것을 확인(코드는 호아랑 참조 중) → `호아랑 캐릭터/` 소스로 재생성. 흰 배경을 테두리 flood-fill로 제거해 **흰 몸통은 보존**. 표정 4종: idle(호아랑 인사)·smile(큭큭 웃음)·surprised(당황)·shy(곤란) + 기본 아바타 `hamster.png`. → v26에서 "표정 1종, 세트는 별도 아트 필요"라던 부분 해결.
- **호아랑 앱 디자인(아이콘)**: `호아랑 어플 이미지.png`(단청 프레임 아트)를 정사각 크롭해 `icon-192·512·maskable-512·apple-touch-icon` 생성(manifest·apple-touch 링크 연결).
- **PWA 테마색 정리**: 옛 주황(theme #FF8C5A / bg #FFF8F0) → 현재 팔레트에 맞춰 theme `#5A7285`, background `#F3F1EB`.
- **캐시 무효화**: 이미지 교체 반영을 위해 `?v=26 → ?v=27`(21곳), 서비스워커 캐시 `hoarang-v3 → v4`.

## v26 — 2026-07-24
- **캐릭터 개명: 마사마사 → 호아랑**(성 호·이름 아랑). 코드·i18n(12개 언어)·제목·PWA manifest·Capacitor 설정까지 전면 교체. 한국어 조사도 받침에 맞게 교정(호아랑과/이/은/을/이라는). 로마자 Masamasa→Hoarang, 일본어 マサマサ→ホアラン. 캐릭터 이모지 🐹→🐯.
- **캐릭터 이미지 교체**: 새 호아랑(갓 쓴 백호) 이미지를 배경 제거(누끼)해 무대·홈·결과·플로팅 캐릭터에 적용. PWA 아이콘(192·512·maskable·apple-touch)도 호아랑 단청 아트로 교체. *(현재 표정 1종 — 미소/놀람/당황 등 표정 세트는 별도 아트 필요.)*
- **[#1 버그] 기능단계 마무리 처리**: 중간 단계(예: 12345 중 4)를 건너뛰어도 **마지막(마무리) 단계에 도달하면 100% 완료**로 인정. 기존엔 모든 단계 충족을 요구해 마무리가 안 되던 문제 해결.
- **[#2] 소음 대응 말투**: 사용자 발화가 안 들리면 되묻고, 같은 상황이 3번 이상 반복되면 곤란해하는 솔직한 말투로 응대(대화 프롬프트에 규칙 추가). 안 들린 말을 지어내지 않음.
- **[#3] 첫 단계 박스 강조**: 브리핑에서 첫 연습 단계 박스가 살짝 흔들려 눌러볼 수 있음을 유도(호버 시 멈춤, reduced-motion 존중).
- **[#4] 발화 연습 판정 강화**: STT가 부정확한 발음도 목표 문장으로 고쳐 적어 통과되던 문제 완화 — 실제 들린 대로 전사하도록 조정, 통과 임계값 상향(great 0.85·ok 0.68).
- **[#5] 대답해 보기 개선**: 호아랑 발화 말풍선 글씨·프로필 확대, 위아래 간격 확대.
- **[#6] 마이크 버튼 통일**: 발화 연습 마이크 버튼을 '눌러서 말하기'와 동일한 알약형 UI로 통일.
- **[#10] 장소·목적 추천 표시**: 추천 예시가 플레이스홀더로 잘려 안 보이던 문제 — 입력창 아래에 전체 예시를 표시하고, 탭하면 채워지도록 변경.
- **[#13] 앱 다운로드**: 홈 맨 아래 다운로드 버튼 + 플랫폼별(iOS/Android/PC) 설치 안내창 추가. 네이티브 설치 가능 시 '지금 설치' 버튼 노출.
- **[#9] 통화 음량 문제**: 마이크 사용 시 볼륨 버튼이 통화 음량을 조절하는 현상 — 네이티브 해결책(Android `setVolumeControlStream(STREAM_MUSIC)`)을 `capacitor-app/README-APP.md`에 문서화(순수 웹은 브라우저 제약으로 코드 해결 불가, 이어폰 권장).

## v25 — 2026-07-15
- **BGM 기본 음량 30%**로 하향(사용자 볼륨의 30%).
- **홈 화면 여백 확대**: UI 요소들이 붙어 보이던 문제 → 위아래 간격을 넉넉히(gap 30px, 상하 패딩) 재배치.
- **홈 마사마사 랜덤 표정**: 홈에서 마사마사 이미지가 2.6초마다 표정 4종 중 랜덤으로 부드럽게(페이드) 계속 바뀜.

## v24 — 2026-07-15
- **마사마사 표정 세트**: 제공된 캐릭터 이미지 4종을 누끼(배경 제거, 눈은 보존)해 상태별로 교체 — 기본(idle)·미소(말할 때)·놀람(끼어들기)·당황(meme). `static/ham_idle·smile·surprised·shy.png`. 무대·영상통화 캐릭터가 상황에 따라 표정이 바뀜.
- **영상통화 말풍선**: 영상통화 뷰에서 마사마사의 말을 흰 말풍선(꼬리 위)으로 표시. 사용자 차례엔 숨김.
- **홈 화면 리디자인**: 마사마사를 크게(원 프레임 제거), 가끔 통통 튀며 말풍선으로 인사(12개 언어). 제목 '마사마사와 한국어 수다'를 좌상단으로, 부제를 '같이 대화할까요?'로 변경.
- **배경음악(BGM)**: 대화 연습 중이 아닌 장면(홈·안내·설정·피드백 등)에서 배경음악 재생(v25에서 30% 음량으로 조정), 대화 시작 시 자동 정지·종료 시 재개. 홈 우상단 🔊/🔇 토글(브라우저 자동재생 정책상 첫 탭 이후 시작).
- **STT(발화 인식) 개선**: push-to-talk에서 버튼을 떼는 즉시 끊겨 끝음절이 잘리던 문제 완화 — 버튼을 떼도 220ms 더 오디오를 흘려보낸 뒤 발화 종료를 보내 문장 끝 인식률을 높임. (마이크는 이미 에코 제거·노이즈 억제·오토게인 적용 중)

## v23 — 2026-07-15
- **영상통화 뷰(마사마사 크게 보기)**: 주제 대화·자유 수다 대화 화면에서 채팅 헤더의 🎥 버튼을 누르면, 오른쪽에서 횡으로 슬라이드되며 마사마사를 크게 보는 '영상통화'형 화면이 열림(듀오링고 스피킹 연습 참고). 화면을 보며 말하고 싶은 학습자를 위한 옵션 — 위아래 채팅 레이아웃 대신 캐릭터에 집중.
  - 구현: 실제 캐릭터 무대(`#stage`)·발화 버튼(`#pttBtn`)·빠른요청을 뷰로 노드 이동해 애니메이션·핸들러를 그대로 유지(별도 미러링/중복 없음). 최근 발화를 자막으로 표시. 💬 버튼으로 채팅으로 복귀(슬라이드 아웃 뒤 원위치 복원). 대화 중에만 활성, 종료 시 자동 복귀. 버튼 라벨은 12개 언어 지원.

## v22 — 2026-07-15
- **모든 팝업에 닫기(✕) 추가**: 언어 선택·이어폰·이름·녹음 동의·자유수다 피드백 팝업에도 ✕ 버튼을 달아 어디서든 닫을 수 있게 함(기존 소개·설정·브리핑·결과·연습·히스토리 팝업과 통일). 언어 팝업의 ✕는 첫 방문(언어 미선택)에는 숨기고, 재오픈 시에만 표시. ✕ 색상도 새 팔레트로 정리.
- **대화 화면 홈 버튼(🏠)**: 주제 대화·자유 수다 대화 화면 좌상단에 홈 버튼 배치(대화 중이면 종료 확인 후 홈). 기존 뒤로가기(←)가 언어 버튼과 같은 우측 좌표에 겹쳐 있던 문제도 함께 해결(좌상단으로 이동).

## v21 — 2026-07-15
- **학교 랭킹 전면 롤백**: 교육상 필요성이 낮아 v20의 학교 대항 랭킹(백엔드 `/schools`·`/score`·`/ranking`, 구글 시트 저장, 학교 선택 드롭다운, 결과 화면 랭킹 카드, 관련 CSS/JS/i18n)을 모두 제거. `get_gdrive_token.py`·`GDRIVE_SETUP.md`도 원복(재인증 불필요).
- **UI 리디자인 (세련된 팔레트)**: 슬레이트(#5A7285)·틸(#60B5C2)·샌드(#D3CBBB)·딥네이비(#394063)·로즈(#B97B77) 팔레트로 전면 교체. 홈 배경 그라데이션, 카드 하이라이트·그림자·이모지 배지 등 폴리시.
- **다국어 버그 수정**: 홈 카드(주제 대화/자유 수다 등) 문구가 ko/en 외 언어에서 한국어로 남던 문제 해결 — 홈·네비·히스토리 문구를 12개 언어 전체에 병합(`I18N_NAV`).
- **홈 카드 이모지 교체**: 중복(🎭🎭·💬💬)을 없애고 주제 대화 🎬 / 자유 수다 ☕ 로 구분.
- **홈 우상단 버튼 추가**: ✏️ 이름 바꾸기(이름 입력 재오픈), 🗂️ 지난 대화(이 기기에 저장된 대화록 열람·내보내기 — 대화 종료 시 localStorage에 자동 저장, 최근 30개).
- **플로팅 마사마사 제거**: 대화 화면 우상단 미니 캐릭터 삭제(주제 대화 포함).
- **자유 수다 말투 배치 복원**: 접이식 숨김 대신 '대화 시작하기' 위(캐릭터 패널)에 페이더를 펼쳐 배치.
- **대화 시작 연출**: 대화 시작 시 대화창이 스르륵 슬라이드 다운.

## v20 — 2026-07-15
- **네비게이션 재구성 (방안 A: 홈 + 대화 분리)**: 한 화면에 모드선택·페이더·채팅이 모두 떠 있던 과밀함 해소. 라우터 없이 `body[data-screen="home|chat|feedback"]` 하나로 화면 블록만 토글.
  - **홈 화면 신설**: 진입 안내(언어·인트로·이어폰·이름·동의) 완료 후 착지. 작은 캐릭터 + 큰 카드 2개(🎭 주제 대화 / 💬 자유 수다)만. 기존 `.mode-toggle`(대화 영역의 모드 버튼)은 이 카드가 대체 → 대화 화면에서 숨김.
  - **주제 대화**: 홈 카드 → 시나리오 설정(`#rpSetupOverlay`, D/P 페이더 그대로) → 브리핑 → 대화 전용 화면.
  - **자유 수다**: 홈 카드 → 곧장 대화 전용 화면. D/P 페이더는 대화 화면 상단의 접이식 "⚙️ 대화 스타일"로 이동(기본값 즉시 시작, 필요 시 펼침).
  - **대화 전용 화면**: 캐릭터·채팅·눌러서 말하기·빠른요청만. 진행률·🪜 비계는 주제 대화일 때만.
  - **뒤로 가기(←)**: 좌상단 버튼. 대화 중이면 종료 확인 후, 아니면 바로 홈.
  - **피드백 화면**: 대화 종료 → 곧장 홈이 아니라 피드백 화면. 주제 대화는 기존 결과(점수·단계), 자유 수다는 스텁(상호작용 능력 평가는 `<한국어 대화 상호작용 교육 모형>` 문서로 채울 예정 — 임의 기준 미기재). 확인 후 🏠 홈.
- **지브리 색감 적용**: 초원 크림 배경 + 노을 테라코타 강조 + 하늘/잎 보조색으로 팔레트 전면 조정(`:root` CSS 변수). 홈 카드는 테라코타/잎 그라데이션.
- 기존 기능(음성 대화, 발화 연습, 드라이브 업로드, `?remote=1` 키보드/리모컨 발화)은 그대로 유지.

## v19 — 2026-07-14
- **추천 값 모국어화**: 🎲 자동 추천(목적·장소·역할)이 학습자의 화면 언어(모국어)로 생성되도록 변경 — 초급·중급 학습자가 추천을 읽고 고를 수 있게. '예)' 접두어도 언어별 현지화(e.g./例）/VD) 등). 제출 시 서버가 한국어로 정규화하는 기존 구조는 유지.
- **화계 예시 칩 제거**: 페이더 아래 "예) 존댓말 — ..." 표시 삭제(페이더 자동 설정으로 충분). 목적 입력창 기본 안내문을 "원하는 대화 목적을 자유롭게 넣어 보세요!"로 변경(12개 언어).
- **소개 팝업 생동감**: 마사마사가 통통 튀는 바운스 애니메이션 + 이모지 표정(😊🤩🎉 등)이 주변에 팡팡 터짐.
- **대화창 가독성**: 말풍선 폰트 14→16px.

## v18 — 2026-07-14
- **키보드·무선 프레젠터 리모컨으로 말하기(수업용 한정)**: 교탁 PC 화면의 '눌러서 말하기' 버튼을 마우스로만 눌러야 했던 문제 → 자리에 앉은 학습자가 발화 참여를 못 하는 교실 상황 해결.
  - **수업용 접속에서만 활성화**: 주소 뒤에 `?remote=1`을 붙여 접속했을 때만 켜짐(`remoteInputMode`). 학생·교사의 일반 접속(폰 포함)에는 영향 없음 — 실수로 스페이스바/화살표 눌러 발화되는 일 방지. 안내 문구도 이때만 표시.
  - **스페이스바**: 누르는 동안 발화, 떼면 종료(화면 버튼과 동일한 '꾹 누르기').
  - **PageUp/PageDown/좌우 화살표**: 한 번 누르면 시작, 다시 누르면 종료(**토글**). 무선 프레젠터 리모컨의 앞/뒤 버튼이 보내는 키. 리모컨은 '꾹 누르기'가 불가능(순간 탭)하므로 토글로 처리.
  - 입력창(이름 등)에 포커스가 있거나 발화 버튼이 비활성일 때는 무시. 키 반복 이벤트 무시.

## v18 — 2026-07-14
- **앱 소개 팝업**: 언어 선택 직후, 선택한 언어로 앱 취지("원하는 상황에서 진짜 한국어 말하기 실력을 키운다")와 사용법(주제 대화·자유 수다·말투 페이더·비계)을 소개하는 팝업 추가. 흐름: 언어 → 소개 → 이어폰 권장 → 이름.
- **모드 버튼 크기 차등**: 핵심 과업인 '주제 대화' 버튼을 크게, '자유 수다'는 작게 — 과업 중심 사용 유도.
- **오버레이 ✕ 버튼**: 소개·대화 계획 설정·대화 단계(브리핑)·발화 연습·결과 화면 우상단에 닫기(✕) 버튼 추가 — 어디서든 한 번에 돌아가기.

## v17 — 2026-07-12
- **비계(스캐폴딩) 고도화**: 🪜 버튼이 정적 연습 표현 대신, **현재 대화 맥락(최근 12발화)과 미충족 기능단계를 반영해 학습자의 '다음 턴'에 쓸 발화 2개**를 실시간 생성해 제시. 연습했던 표현과 같거나 유사하도록 유도(전이). 생성 실패 시 연습 표현으로 폴백. 탭하면 TTS로 들려줌.
- **플로팅 마사마사**: 모바일 한 열 레이아웃에서 대화 중 캐릭터가 안 보이는 문제 해결 — 대화 시작 시 우상단에 미니 마사마사가 떠서 본 무대와 동일하게 반응(말하기 바운스·듣기 기울임·놀람 흔들림). 캐릭터-학습자 상호작용 상시 유지.
- **대화 종료 시 과업 진행률·기능 단계 표시 리셋** (채팅 리셋은 v15).
- **모드 버튼 순서 교체**: 주제 대화(과업 중심)가 앞, 자유 수다가 뒤.
- **버전에 수정 일자 병기** + 이 개발일지 신설.
- **제작자 크레딧 추가**: 화면 하단.

## v16 — 2026-07-12
- **연습 STT 관대화(힌트 기반 인식)**: 외국인 학습자 발화 전사가 부정확한 문제 →
  연습에서는 목표 문장을 알고 있으므로 이를 힌트로 함께 전송, "목표와 비슷하게 들리면 목표 표기로, 명백히 다르면 들리는 대로" 전사하도록 지시. 온도 0 고정.

## v15 — 2026-07-12
- **발화 연습 음성 Gemini화**: 듣기(TTS)를 기기 내장 → Gemini TTS(자연 음성, 서버 캐시)로, 말하기 평가를 브라우저 인식 → 녹음 후 Gemini 멀티모달 인식으로 교체. 실패 시 내장 TTS/브라우저 인식/자가확인 버튼 순 폴백.
- **학습자 먼저 말걸기**: 접속 직후 마사마사가 곧장 말하지 않고 대기(기본 4초, `FIRST_SPEAK_WAIT_S`). 학습자 발화가 없을 때만 첫인사 — '말 걸기(대화 개시)' 연습 기회 제공. 근거: 이남호(2025) 학습자 주도성.
- **🎲 추천 → 친밀도(D)·지위(P) 페이더 자동 설정**: 역할 관계에 맞는 화계·관계 좌표 자동화.
- **듀오링고식 완료 화면**: 마사마사 등장 애니메이션 + 점수·내 발화 수·대화 시간 카드 (정의적 요인·동기 설계).
- **대화 종료 시 채팅창 리셋**.
- **🪜 비계 버튼 신설**(v17에서 맥락 기반으로 고도화).
- **구어성(입말) 지침 추가**: 정선화(2009)·김현지(2015)·김주연 외(2021)의 구어 문법 요소 반영 — 담화표지·간투사, 맞장구, 조각문, 생략, 반복, 어순 전위, 대용어, 머리말·꼬리말(-거든요/-잖아요/-더라고요), 덩어리 표현. 음운 변이 표기·의도적 끼어들기는 구현 제외.
- **녹음 동의 팝업을 앱 실행마다 표시**(공용 기기에서 참여자별 동의 보장).

## v14 — 2026-07-11
- **한국어 중급 수준 제약**: '2017 국제 통용 한국어 표준 교육과정(4단계) 어휘·문법 등급 목록'에서 **1~4급 문법 224항 전체**를 추출해 시스템 프롬프트에 내장. 모든 발화를 중급(4급 이하) 어휘·문법으로 제한, 어려운 단어는 즉시 풀어 말하기. 계획 생성·추천 표현에도 동일 기준 적용.
- **대화 중 주기 저장**: 60초마다 녹음+대화기록+메타를 서버로 보내 같은 파일 갱신(Google Drive 파일 교체) — 앱 강제 종료에도 데이터 보존(연구 자료 무결성).
- **일시정지 버튼 제거**, '화계(말투)'→'말투' 라벨 단순화, UI 전반 폴리시(터치 타깃·가독성·모바일 여백).

## v13 — 2026-07-11
- **분석 모델 자동 전환 체계**: gemini-2.5-flash-lite가 신규 사용자 제공 종료(404)로 판명 → 기본 모델 gemini-2.5-flash 복귀 + 모델 단종 시 후보 목록 → API 모델 목록 순 자동 전환. `/rp-diag?models=1`로 사용 가능 모델 확인.

## v12 — 2026-07-11
- **429(쿼터 소진) 대응**: 원인 확인 — Gemini 무료 등급 일일 한도 소진. 쿼터 오류 시 재시도 생략(소모 2배 방지), 실패 사유 한국어화. (이후 운영 계정에 결제 연결로 근본 해결)

## v11 — 2026-07-11
- **실패 원인 가시화(진단 체계)**: 계획 만들기 실패의 원인을 추적하기 위해 서버가 마지막 생성 오류를 기록하고 실패 응답(detail)과 팝업에 노출. 진단 페이지 `/rp-diag`(?test=1: 실호출 점검) 신설. → 이 장치로 429 쿼터 문제를 즉시 특정.

## v10 — 2026-07-11
- **설정 흐름 개편**: '주제(필수)' 필드 삭제, 대화 장소를 맨 위로(장소→목적 순). 🎲 상시 활성 — 누르면 목적(교재형→일상형→엉뚱형 순환)과 장소·역할·말투 전체 추천, 한 바퀴 돌면 새 추천 세트.
- **생성 호출에 thinking_budget=0**(2.5 모델 기본 '사고'로 인한 타임아웃 방지) + 타임아웃 확대.
- **게임화 피드백**: 기능단계 달성 시 딩동 효과음, 100% 초과 '확장 중!' 시 화면 전기충격 셰이크+지지직 사운드 (WebAudio 합성).
- **연습 TTS 장문 중단 수정**(문장 분할+주기적 resume), 대화 시작/종료 버튼 패널 하단 이동, 푸터를 앱 다운로드(PWA 설치) 버튼으로 교체.
- **언어패치**: ja·vi·th·id·mn·uz·ru·es·fr 9개 언어에 주제대화·발화연습 UI 전체 키 번역 추가(12개 언어 완비).

## v9 — 2026-07-10
- **자동 추천**: 주제 입력 시 달성 목적(3단계: 교재형·일상형·엉뚱형)·장소·역할·말투를 '예)'로 자동 추천 (`/roleplay-suggest`).
- **발화 연습 신설**: '이 대화의 기능단계' → '대화 단계'로 개칭, 각 단계 탭 → 듀오링고식 연습: ① 듣고 따라 말하기 → ② 상대 발화(cue)에 대답하기(말차례 교환). 자모 분해 편집거리 유사도로 관대 채점, 연습 완료 배지. 계획 스키마에 상대 발화(cue) 추가.
- 근거: 이남호·이찬규(2024) 대화연습 모형의 '연습' 단계를 상황 적용 전 비계로 도입.

> 아래 v8 이전 항목은 개발일지 신설(v17, 2026-07-12) 이후 과거 작업 대화 기록을 역추적해 상세화한 것이다. 날짜는 대화·파일 수정 시각 기준의 근사값.

## v8 — 2026-07-10
- **주제 대화(상황극) 모드 신설**: 왼쪽 패널에 🎭 주제 대화 모드 추가. 설정 화면에서 주제·달성 목적·장소·내 역할·마사마사 역할을 **모국어로 입력(서버가 한국어로 정규화)** + 화계(존댓말/반말/자동) 선택, 친밀도·지위 페이더 적용.
- **대화 계획 생성**: "대화 계획 만들기"를 누르면 목적에 맞는 **기능단계 4~6개 + 단계별 필요 표현**을 Gemini로 생성해 브리핑 화면에 표시(모국어 번역 병기).
- **선형 진행 바 + 단계 자동 감지**: 채팅창 상단 진행 바가 단계 충족을 자동 판정해 차오르고(단계 칩 ✓), 전 단계 충족 시 100%, 이후 학습자 턴당 +5%로 100% 초과. 종료 시 퍼센트를 점수로 치환한 결과 화면 + 대화 자동 저장(녹음·GDrive 파이프라인에 주제·단계·점수 메타 포함).
- **설계 원칙 P3(코드 통제)**: 세션 종료·단계 전환을 전부 코드가 관장 — AI의 임의 종료 원천 차단. 단계 충족 판정은 턴 완료마다 Gemini REST 일회성 호출을 백그라운드로 돌려 음성 릴레이를 막지 않음(기본 `gemini-2.5-flash`, env `ANALYSIS_MODEL`로 변경 가능). 상황극 프롬프트에 "단계 이름 발화 금지, 목적 달성 후 확장 1회 시도, 마무리 인사 대응, 짧은 교정 후 극 복귀" 규칙 내장.

## v7 — 2026-07-09
- **발화 끊김 완화(VAD 튜닝)**: 스피커에서 나온 마사마사 목소리·주변 소음이 마이크로 들어가 Gemini가 사용자 끼어들기로 오판, 대화가 중간에 끊기는 문제 → **발화 시작 감지 민감도를 LOW로 낮추고 prefix 300ms 적용**. 진짜 목소리엔 여전히 끼어들기 반응, 에코·잡음엔 무반응.
- **STT 왜곡 원인 규명·안내**: 마사마사 발화 중 겹쳐 말할 때 에코 캔슬러가 사용자 음성까지 깎아 음절이 뭉개지는 문제는 코드로 완전 해결 불가 → **이어폰 사용이 근본 해결**임을 앱 내 안내로 유지.
- **지연(RTT) 진단**: RTT 209~260ms로 서버가 미국(오리건)에 있음을 확정, 싱가포르 리전 이전(재생성) 권고. 가끔의 500ms 스파이크는 무료 플랜 CPU 경합으로 진단(필요 시 Starter 유료 전환 카드).

## 푸시투톡 도입 — 2026-07-09
- **STT 30~60초 지연 해결**: 진단 결과 업로드·응답은 정상인데 STT만 느린 원인이 Gemini 자동 발화감지 지연으로 확인됨 → 자동 발화감지를 끄고(`disabled=True`), 클라이언트의 **activity_start / activity_end 신호로 발화 시작·끝을 명시 처리**. 채팅창 아래 **🎤 눌러서 말하기** 버튼 추가(누르는 동안만 녹음·전송, 떼는 순간 턴 확정). 리전·CPU와 무관하게 반응 속도 확보, 오탐·업로드 낭비 제거, 발화 중 누르면 자연스러운 끼어들기.

## 배포·인프라 결정 — 2026-07-08~10
- **배포 방식 확정(앱스토어 → PWA)**: 통제된 소규모 학습자 대상이므로 앱스토어 정식 배포 대신 **Render + ACCESS_CODE + PWA "홈 화면에 추가"** 방식 채택. `capacitor-app/`(iOS/Android 래퍼)는 보류. PWA 설계 절차(manifest, 아이콘 192/512·apple-touch-icon, iOS 메타태그, 서비스워커, HTTPS) 정리·적용.
- **GitHub 배포 파이프라인**: 커밋 대상 파일 관리 체계 확립 — `main.py`, `templates/`, `static/`, `render.yaml`, `requirements.txt`, `Procfile`, `.gitignore` 업로드, `.env`(API 키)는 `.gitignore`로 제외. 커밋 시 Render 자동 재배포(2~5분).
- **Gemini API 유료 전환**: 429 쿼터 문제의 근본 해결로 Google AI Studio에 결제 연결(종량제). 예상 비용 월 $1~3(수업 규모 30명 기준 ~$22.5/월)로 실질적 무제한 확보. 기존 API 키·코드 수정 불필요.

## 초기 구축(기본 골격) — ~2026-07-03
- **프로토타입 → 서비스 전환**: Jupyter 노트북 프로토타입을 **FastAPI + WebSocket 서버로 이식**, Gemini Live API(`gemini-2.5-flash-native-audio-latest`)와 실시간 오디오 스트리밍 연동.
- **캐릭터·화계 엔진**: "마사마사"(회색 후드티 햄스터) 정체성 고정 + **친밀도(D)·사용자 지위(P) 2축 페이더(0~100)**로 말투·격식·대화 주도권을 실시간 조절하는 프롬프트 엔지니어링 로직(설계안 문서를 코드화). 자유 수다/주제 대화 모드, 기능단계 자동 판정·진행률, 즉시 교정·모국어 대응·천천히/다시/쉽게 요청 대응.
- **프론트엔드**: 마이크 녹음/음성 재생, 채팅 로그, D/P 슬라이더 UI, 다국어 오버레이(12개 언어), 대화 내보내기(.txt).
- **녹음·연구 데이터**: 대화 녹음·기록을 서버로 업로드(Google Drive 연동)해 연구 자료로 축적.
- **PWA**: `manifest.json`, service worker, 아이콘 세트(`static/`)로 홈 화면 설치·앱형 실행 지원.
- **보안·비용 방어**: API 키를 `.env`로 분리(노트북 평문 키·체크포인트 제거), 접속 코드(`ACCESS_CODE`) 인증 + 동시 세션 제한(`MAX_CONCURRENT_SESSIONS`)으로 공개 배포 시 비용 남용 방지.
- **배포 준비물**: `render.yaml`·`Procfile`·`requirements.txt`(Render 무료 티어, WebSocket 지원), `README.md`(로컬 실행·Render 배포 절차·비용 주의), `capacitor-app/`(iOS/Android 래퍼 스캐폴딩, 이후 PWA로 방향 전환하며 보류).
