# 알림(푸시) 설정 — 5분이면 끝납니다

호아랑이 **하루 세 번(한국시간 오전 10시 · 오후 2시 · 오후 8시)** 학습자에게 짧게 인사합니다.

---

## 1단계 · Render에 환경변수 3개 붙여넣기

Render 대시보드 → 해당 서비스 → **Environment** → *Add Environment Variable* 로 아래 셋을 그대로 넣으세요.

| Key | Value |
|---|---|
| `VAPID_PUBLIC_KEY` | `BIX_Qc2MCkAPhzkWg2xhU4driCdctQkadg12fE5DDk3kwe9nBS0XT7G-449eUwEE2fLMKoLiBkn0xZU9y_V5Orc` |
| `VAPID_PRIVATE_KEY` | `aNOYABi4KPixWpRQiPhdACy70IIWKX03rPfnaVVDT88` |
| `VAPID_SUBJECT` | `mailto:namho1210@naver.com` |

> 이 키쌍은 이 앱 전용으로 새로 만든 것입니다. **개인키(`VAPID_PRIVATE_KEY`)는 공개된 곳에 올리지 마세요** — 깃헙 저장소에도 넣지 말고 Render 환경변수로만 두면 됩니다.

## 2단계 · 배포

바뀐 파일을 깃헙에 올리면 Render가 자동으로 다시 배포합니다. `requirements.txt`에 `pywebpush`가 추가돼 있어 설치가 조금 더 걸립니다(1~2분).

## 3단계 · 확인

서버 로그에 이 줄이 보이면 정상입니다.

```
[알림] 스케줄러 시작 — 매일 10시, 14시, 20시 (한국시간)
```

키를 안 넣었으면 대신 이렇게 나옵니다(앱은 정상 동작, 알림만 꺼짐).

```
[알림] VAPID 키가 없어 알림 기능은 꺼짐
```

---

## 학습자에게는 이렇게 보입니다

1. 앱을 열고 **대화를 한 번 마치면** 호아랑이 "불러도 될까요?" 하고 한 번 묻습니다.
   (처음부터 물으면 대부분 거절하기 때문에 일부러 뒤로 미뤘습니다. 거절하면 다시 묻지 않습니다.)
2. 허락하면 하루 세 번 알림이 갑니다. 문구는 시간대마다 다르고, **학습자 화면 언어로, 이름을 넣어** 갑니다.
   - 오전 10시 — "좋은 아침이에요! / 남정, 오늘 한국어 한마디 해볼까요?"
   - 오후 2시 — "잠깐 쉬어 갈까요? / 남정, 호아랑이랑 5분만 이야기해요"
   - 오후 8시 — "오늘 하루 어땠어요? / 남정, 호아랑이 기다리고 있어요"
3. 알림을 누르면 앱이 열립니다.

**아이폰은 홈 화면에 추가해 설치한 경우에만** 알림을 받을 수 있습니다(애플 정책). 안드로이드·PC 크롬은 설치하지 않아도 됩니다.

---

## 수업 시작 알림을 직접 보내고 싶을 때

브라우저에서 아래 주소를 열면 그 자리에서 전체에게 발송됩니다.

```
https://korean-dic.onrender.com/push/send?key=<ADMIN_KEY>
```

`ADMIN_KEY`는 이미 Render에 넣어 두신 값입니다. 문구를 바꾸려면 `slot`을 붙이세요 — `&slot=0`(아침) `&slot=1`(점심) `&slot=2`(저녁, 기본).

---

## 알아 두실 점

- **구독 목록은 서버 파일(`push_subs.json`)에 저장**됩니다. 재배포하면 이 파일이 사라질 수 있는데, **학습자가 앱을 열 때마다 자동으로 다시 등록**되므로 며칠 안에 스스로 회복됩니다.
- 발송 시각을 바꾸려면 `main.py`의 `PUSH_HOURS = [10, 14, 20]` 숫자만 고치면 됩니다.
- 학습자가 알림을 끄려면 브라우저·시스템 설정에서 이 사이트의 알림을 차단하면 됩니다.
