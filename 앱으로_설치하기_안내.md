# 왜 알림에 `.com`이 뜨고, 화면 위에 크롬이 보이는가

## 한 줄 요약

지금 학생 폰에 깔린 건 **"진짜 앱"이 아니라 "크롬 바로가기"**일 가능성이 큽니다.
같은 '홈 화면에 추가'인데 안드로이드는 두 가지로 갈라집니다.

| | **바로가기 (Shortcut)** | **WebAPK (진짜 앱)** |
|---|---|---|
| 화면 위 크롬 표시 | **보임** (주소·↩︎ 버튼) | 없음 |
| 알림 발신자 이름 | **`korean-dic.onrender.com`** | **호아랑** |
| 앱 서랍 / 설정 > 앱 | 안 뜸 | 「호아랑」으로 뜸 |
| 아이콘 | 크롬 배지가 붙음 | 배지 없음 |

**둘 다 되돌릴 수 있습니다.** 아래 순서대로 보시면 됩니다.

---

## 1단계 — WebAPK 조건 채우기 (v48에서 완료)

안드로이드가 바로가기 대신 진짜 앱으로 설치하려면 조건이 있습니다. v48에서 다 채웠습니다.

- [x] HTTPS — Render 기본
- [x] `manifest.json`에 `name`·`short_name`·`start_url`·`id`
- [x] 192px·512px 아이콘 + maskable 아이콘
- [x] `display: standalone` + `display_override`
- [x] `fetch` 처리하는 서비스워커 (`sw.js`)
- [x] `prefer_related_applications: false`

## 2단계 — 다시 설치 (★ 이게 핵심)

**기존에 깔린 아이콘은 조건이 안 맞던 시절에 만들어진 바로가기라, 지우고 다시 깔아야 바뀝니다.**

학생들에게 이렇게 안내하세요.

> 1. 홈 화면의 호아랑 아이콘을 **길게 눌러 삭제**
> 2. **크롬을 직접 열고**(카톡·인스타에서 링크 누르지 말고) 주소창에 `korean-dic.onrender.com` 입력
> 3. 뜨는 **「앱 설치」 버튼**을 누르기
> 4. "앱 서랍에도 호아랑이 생겼으면" 성공

**카톡·인스타에서 링크를 눌러 들어가면 절대 WebAPK가 안 됩니다.** 인앱 브라우저는 설치 권한이 없어 무조건 바로가기가 됩니다. (앱 안에 "크롬으로 열기" 버튼을 넣어 둔 이유입니다.)

**확인 방법**: 폰 설정 → 앱 → 목록에 **「호아랑」**이 있으면 WebAPK 성공. 없으면 아직 바로가기입니다.

## 3단계 — 아이폰은?

아이폰은 WebAPK가 없습니다. 대신 **공유 → 홈 화면에 추가**를 하면
사파리 UI 없이 전체 화면으로 뜨고, 알림에도 앱 이름이 나옵니다.
**단, 아이폰은 홈 화면에 추가하지 않으면 알림 자체가 아예 안 옵니다** (iOS 16.4+ 제약).

---

## 그래도 주소가 신경 쓰인다면 — 두 가지 선택지

### 선택지 A. 도메인 사기 (쉬움 · 연 1~2만원)

`korean-dic.onrender.com` → `hoarang.kr` 같은 짧은 주소로 바꿉니다.
Render 대시보드 → Settings → Custom Domain 에 등록하고, 도메인 산 곳에서 CNAME 한 줄 넣으면 끝입니다. 30분이면 됩니다.

- 장점: 학생에게 불러 주기 쉽고, 논문·수업자료에 적기 좋음
- 한계: **바로가기로 깔면 여전히 주소가 보입니다.** 2단계를 먼저 하세요.

### 선택지 B. 진짜 APK 파일 만들기 (확실함 · 반나절)

[PWABuilder](https://www.pwabuilder.com)에 주소를 넣으면, 브라우저에서 **안드로이드 설치 파일(.apk / .aab)을 만들어 줍니다.** 코딩 없이 됩니다. 내부적으로는 구글의 Bubblewrap으로 TWA(신뢰된 웹 액티비티) 앱을 만드는 방식입니다.

1. pwabuilder.com → `https://korean-dic.onrender.com` 입력 → Start
2. Package for stores → **Android** → Generate package
3. 나온 zip 안의 `.apk`를 학생에게 배포 (`.aab`은 나중에 플레이스토어용)
4. zip 안의 `assetlinks.json`을 서버 `/.well-known/assetlinks.json`에 올려야 주소창이 완전히 사라집니다 — 이건 제가 넣어 드릴 수 있습니다

- 장점: 크롬 흔적 완전 제거, 앱 이름·아이콘 완전 제어, 카톡으로 파일 전달 가능
- 단점: APK 직접 배포는 "출처를 알 수 없는 앱" 경고를 넘겨야 함 → 수업에서 한 번 같이 하면 됩니다. 플레이스토어에 올리면 이 경고도 사라집니다(등록비 $25, 심사 며칠)

---

## 제 권고

수업이 곧이라면 **2단계(다시 설치)만 해 보세요.** v48 배포 후 지우고 다시 깔면 대부분 해결됩니다.
그래도 남거나, 논문 부록에 "앱"으로 적어야 한다면 **선택지 B**로 가는 게 깔끔합니다. 말씀만 주시면 `assetlinks.json` 자동 제공까지 붙여 드리겠습니다.

---

Sources: [WebAPKs on Android — web.dev](https://web.dev/articles/webapks) · [pwabuilder-google-play](https://github.com/pwa-builder/pwabuilder-google-play)
